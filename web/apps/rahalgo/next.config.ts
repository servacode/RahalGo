import type { NextConfig } from "next";

/**
 * ══════════════════════════════════════════════════════════════════════
 * **ولا هويّةَ بيئةٍ تُخبَز في الحزمة** — دورةُ ٧١و
 * ══════════════════════════════════════════════════════════════════════
 *
 * # ما كان
 *
 * **وكان هذا الملفّ يحقن `NEXT_PUBLIC_API_URL` و`NEXT_PUBLIC_SITE_URL`
 * في `env:`** — **فتُستبدَل نصّاً في الحزمة وقتَ البناء.**
 *
 * **وأثرُه مقيسٌ لا مُستنتَج**: **حزمةُ التجهيز فيها `staging` مرّةً
 * و`api.rahalgo.com` صفراً، وحزمةُ الإنتاج بالعكس** — **فصورتان من
 * التزامٍ واحدٍ ليستا أثراً واحداً**، **وما يُختبَر هنا لا يُرقَّى
 * هناك.** وهو نقضُ قاعدة «ابنِ مرّةً ورقِّ الأثرَ عينَه».
 *
 * # وما صار
 *
 * **والبناءُ لا يعرف البيئةَ أصلاً.** **والتهيئةُ تصل عند التشغيل**:
 * `/config.js` يكتبها من بيئة الحاوية، **ويقرؤها عقدٌ مركزيٌّ واحد**
 * (`lib/config.ts` و`ui/runtimeconfig.ts`).
 *
 * # وأين ذهب فحصُ «عنوانٌ فارغٌ يُسقط البناء»
 *
 * **كان هنا، ولم يعد له موضعٌ**: **البناءُ لا يملك القيمةَ ليفحصها.**
 * **فانتقل الحارسُ إلى وقت التشغيل** — `/config.js` يردّ ٥٠٠ ويُعلن
 * النقصَ في التهيئة نفسِها، **ولا يخترع عنواناً ولا يرتدّ إلى
 * `localhost`.**
 *
 * **والعطبُ الذي عُولج سنةَ ٢٠٢٦-٠٨-١٠ لم يُفتَح بابُه ثانيةً**:
 * **حينها خُبز عنوانٌ فارغٌ فصار كلُّ نداءٍ مساراً نسبيّاً في الويب
 * نفسِه، والشاشةُ تقول «حدث خطأٌ غير متوقّع» ولا شيءَ يدلّ.**
 * **واليومَ لا يُخبَز شيءٌ أصلاً**، **والنقصُ يظهر في حال الردّ وفي
 * سجلّ البوّابة قبل أن يفتح أحدٌ شاشة.**
 */
const nextConfig: NextConfig = {
  transpilePackages: ["@rahalgo/ui", "@rahalgo/i18n"],
};

export default nextConfig;
