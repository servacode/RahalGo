"use client";

/**
 * **تقييماتُ السائق — نجومُه وما قيل فيه.**
 *
 * # لماذا صفحةٌ لا رقمٌ في الشريط
 *
 * الشريطُ يعرض المعدّلَ رقماً واحداً — **ورقمٌ بلا سببٍ لا يُصلَح**: من رأى
 * ٣٫٨ لا يعرف أيَّ توصيلةٍ خفضتها، **فيقرؤه حكماً عامّاً على نفسه** أو
 * يتجاهله. **والتعليقاتُ هي ما يُقرأ ويُتعلَّم منه.**
 *
 * # وهو الطرفُ الوحيد الذي يُقيَّم بعينه
 *
 * المتجرُ لا نجومَ له — الزبونُ لا يرى اسمَه أصلاً. **والسائقُ يقرع البابَ
 * ويُرى ويُحكَم عليه**، فتقييمُه شأنُه هو.
 */

import { ReputationReviews } from "@rahalgo/ui";
import { api } from "@/lib/api";

export default function DriverReviewsPage() {
  return <ReputationReviews api={api} />;
}
