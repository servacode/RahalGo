/**
 * **الصفحةُ الرئيسيّة — عرضٌ يقول ما هذه المنصّة.**
 *
 * # ما كانت
 *
 * **كانت `/` هي صفحةَ التسوّق نفسَها**: أقسامٌ وبحثٌ وسلايدر — فلا وجودَ
 * لصفحةٍ رئيسيّةٍ أصلاً. **ثمّ أُفرغت بقرار المالك** (٢٠٢٦-٠٨-٠٦: «اترك الصفحة
 * الرئيسية فارغة — فقط توب بار وفوتر والمنتصف فارغ») ريثما يُقرَّر ما فيها.
 *
 * # وما صارت
 *
 * (طلبُ المالك ٢٠٢٦-٠٨-٠٩: «الصفحة الرئيسيّة لازم نقدر نرفع صورة تكون هي عرض
 *  الصفحة… وراح يكون فوق الصورة على اليمين نصّ نقدر نبدّله، مع زرّ حمّله
 *  الآن».)
 *
 * **وعرضُها من الإعدادات لا من الشيفرة**: صورةٌ ونصٌّ وزرّ، **تُبدَّل من لوحة
 * الإدارة بلا نشر.** وعدّةُ عروضٍ تتبدّل تلقائيّاً.
 *
 * # ولماذا يُجلب في المتصفّح لا في الخادم
 *
 * **الشريطُ والتذييلُ يُقدَّمان من الخادم** — وهما ما يراه الزائرُ في أوّل
 * رسم. **والعرضُ صورةٌ تحتاج تحميلاً على كلّ حال**، فجلبُه بعد الرسم لا
 * يؤخّر شيئاً يُرى، **ويُبقي الصفحةَ ساكنةً في الذاكرة بين الزيارات.**
 *
 * **ورئيسيّةٌ بلا عرضٍ تبقى فارغةً كما كانت** — لا خطأَ ولا مربّعَ ينتظر.
 *
 * # ولا غلافَ حولَها
 *
 * (تصحيحُ المالك ٢٠٢٦-٠٨-٠٩: «طلبتُ الصورة تكون خلفيّة الشاشة بشكلٍ كامل
 *  وأنت تضعها بهذا الشكل» — صورةٌ في صندوقٍ مستديرٍ وسطَ الصفحة.)
 *
 * **كان هنا `max-w-6xl` وحشوةٌ** لم يطلبهما أحد — **والخلفيّةُ الكاملةُ لا
 * تُحدَّد بعرضٍ ولا تُبعَّد بحشوة.** */

import HeroSection from "./HeroSection";

export default function HomePage() {
  return <HeroSection />;
}
