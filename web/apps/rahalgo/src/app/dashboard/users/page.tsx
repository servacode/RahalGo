"use client";

/**
 * **«الحسابات» — بابٌ واحدٌ لكلّ من في المنصة.**
 *
 * # لماذا اجتمعت
 *
 * كانت خمسةَ أبوابٍ للشيء الواحد: «الحسابات» فيها الجميع، **و«الزبائن»
 * و«السائقون» و«المندوبون» و«المتاجر» كلٌّ يعيد عرضَ فئةٍ منهم.** فمن أراد أن
 * يعرف إنساناً **فتح بابين ولم يجد صورتَه في أيّهما كاملة**، ومن أراد أن يبحث
 * لم يعرف في أيّ بابٍ يبحث.
 *
 * قرارُ المالك (٢٠٢٦-٠٨-٠٣): «يمكننا حذفُ أقسام المندوب والسائقين والزبائن
 * والمتاجر بما أنّها مجموعةٌ كلُّها بقسم الحسابات، فلا داعي أن تكون بمكانين».
 *
 * # ولا ميزةَ تُخسَر — **وهذا شرطُه**
 *
 * «**ولا نريد خسارةَ أيّ ميزة عند حذف الأقسام المنفصلة**».
 *
 * **فحُذفت الأربعةُ الأولى (٢٠٢٦-٠٨-١٥) بعد أن استوعب ملفُّ الحساب أعمدتَها**:
 * دوامُ السائق ونقدُه · إنفاقُ الزبون وآخرُ طلبٍ له · كودُ المندوب وعمولاتُه.
 *
 * # وتبويبُ المتاجر آخرُ ما سقط
 *
 * (قرارُ المالك ٢٠٢٦-٠٨-١٦: «الآن نستطيع نحذف».)
 *
 * **وكان الاستثناءَ المكتوبَ هنا**: «المتجرُ كيانٌ لا حساب — له قائمةٌ وساعاتٌ
 * وعمولةٌ ومخالفات، **وصاحبُه حسابٌ آخر**». **والاستثناءُ سقط حين صار كلُّ ما
 * فيه يُبلَغ من ملفّ صاحبه**: طلباتُ متجره وإنذاراتُه ونزاعاتُه وتقييماتُ
 * السائقين له، **وأفعالُه السبعةُ في سطره** (`StoreActions`) — والقائمةُ
 * والساعاتُ خلف زرّ «ملفّ المتجر».
 *
 * **ولا شريطَ بتبويبٍ واحد**: عنصرٌ لا يفعل شيئاً يزاحم ما يفعل، **ويَعِد
 * بخيارٍ ثانٍ لا وجودَ له.**
 */

import AllAccountsTable from "@/components/admin/accounts/all";

export default function AccountsPage() {
  return <AllAccountsTable />;
}
