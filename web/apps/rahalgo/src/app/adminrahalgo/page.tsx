/**
 * ══════════════════════════════════════════════════════════════════════
 * **‏/adminrahalgo — بابُ الإدارة، والوحيدُ في الموقع**
 * ══════════════════════════════════════════════════════════════════════
 *
 * (قرارُ المالك ٢٠٢٦-٠٨-٢٦: «رابط تسجيل الدخول للأدمن لازم يتغيّر ويصير
 *  مثلاً adminrahalgo مشان ما حدا يوصله برابط خارجي».)
 *
 * # وما الذي يمنعه اسمٌ غيرُ مشهور
 *
 * **لا يمنع من عرف الاسم** — الحمايةُ في كلمة المرور والأدوار لا في
 * المسار. **إنّما يمنع ما يجري بلا هدف**: زحّافاتُ البحث، وقوائمُ
 * المسارات المعروفة (`/login` و`/admin` و`/wp-login`)، **وكلُّ تجربةٍ
 * آليّةٍ تطرق أبواباً بأسمائها الشائعة.**
 *
 * **وسجلُّ المحاولات يهدأ** — فمن ظهر فيه بعد اليوم ظهر عن قصد.
 */

import AuthScreen from "@/components/AuthScreen";

export default function Page() {
  return <AuthScreen mode="password" />;
}
