"use client";

/**
 * شريط موقع الزبون — نفس الشريط العلوي المركزي المستعمل في اللوحات.
 *
 * # ما في الشريط وما في القائمة
 *
 * (قرارُ المالك ٢٠٢٦-٠٨-٠٦: «نزيل هذه العناصر من التوب بار ليكون توب بار
 *  احترافيّاً بعناصر قليلة واضحة».)
 *
 *   الشريطُ  → ما يُفتح كلَّ يوم: التسوّقُ والجرسُ والمحفظةُ والطلبات
 *   القائمةُ → ما يُفتح مرّةً في الشهر: العروضُ والدعوةُ والمفضّلةُ والشكاوى
 *
 * **وكانت قائمةٌ منسدلةٌ قد رُفضت من قبل** — لأنّها كانت تُخفي خلف نقرةٍ ما
 * هو معروضٌ بجانبها أصلاً. **وهذه عكسُها**: ما فيها ليس في الشريط، وما في
 * الشريط ليس فيها. **فلا بابَ له بابان.**
 *
 * **وأربعُ أيقوناتٍ صامتةٍ في صفٍّ واحدٍ لا تُقرأ**: قلبٌ وسلسلةٌ وبوقٌ ودعمٌ
 * بلا أسماء — **والقائمةُ تكتب أسماءَها.**
 */

import Link from "next/link";
import { useCallback, useEffect, useState } from "react";
import { usePathname, useRouter } from "next/navigation";
import { getMessages, defaultLocale } from "@rahalgo/i18n";
import {
  TopBar,
  BrandMark,
  usePlatform,
  TopBarLink,
  AppDownloadChip,
  TopBarChip,
  TopBarActions,
  TOPBAR_ICON,
  LiveNotifications,
  useLiveRefresh,
  IconOrder,
  IconPromos,
  IconHeart,
  IconLink,
  IconSupport,
  IconWallet,
  IconUser,
  IconStore,
  IconOverview,
} from "@rahalgo/ui";
import { homeFor, portalFor, goTo } from "@rahalgo/auth";
import { api, mediaUrl, tokenStore } from "@/lib/api";
import { useAuth, isLoggedIn } from "@/lib/auth";

const m = getMessages(defaultLocale);
const N = m.site.nav;

/**
 * **أبوابُ التعريف** — (قرارُ المالك ٢٠٢٦-٠٨-١٦).
 *
 * **و«ميزاتنا» قسمٌ في الرئيسيّة لا صفحة**: الموقعُ صفحةٌ واحدةٌ طويلة،
 * **والرابطُ يمرّر إليها** (`#features`). ومن فتحه من صفحةٍ أخرى عاد إلى
 * الرئيسيّة ثمّ نزل.
 */
const MARKETING = [
  /* **ولا «ميزاتنا»** — (قرارُ المالك ٢٠٢٦-٠٨-١٧: «احذف قسم ميزاتنا…»).
     **ورابطٌ إلى قسمٍ حُذف يمرّر الصفحةَ إلى لا شيء** — والزائرُ يظنّ
     صفحتَه معطوبةً لا فارغة. */
  /* **و«من نحن» ليست في الشريط** — (قرارُ المالك ٢٠٢٦-٠٨-١٧: «احذفه
     أيضاً من هنا»). **وموضعُها التذييل**: صفحةٌ تُقرأ مرّةً لا كلَّ يوم،
     **والشريطُ لِما يُضغط كلَّ يوم.**

     **ولم تُترك بلا رابط**: صفحةٌ لا يقود إليها شيءٌ تسقط من فهرسة
     غوغل، **وهي التي طلبها للنشر على غوغل بلاي.** */
  { href: "/contact", label: N.contact },
];
const WS_URL =
  (process.env.NEXT_PUBLIC_API_URL ?? "http://localhost:8080").replace(/^http/, "ws") +
  "/api/v1/ws";

interface Summary {
  full_name: string;
  avatar_thumb_url: string | null;
  balance: number;
  /**
   * **كم عرضاً حيّاً الآن** — (قرارُ المالك ٢٠٢٦-٠٨-١٢: «صفحة العروض
   * مخفيّة، تظهر فقط إذا نزّلت المنصّةُ أو المتاجرُ عروضا»).
   *
   * **وكان يُقرأ ثمّ تُرك** حين صارت العروضُ بنداً ثابتا — **وعاد
   * يُقرأ**: صفحةٌ فارغةٌ أكثرَ العام تُعلّم صاحبَها ألّا يفتحها،
   * **فلا يفتحها يومَ تمتلئ.**
   */
  live_offers?: number;
}

/**
 * **بنودُ قائمة الحساب — ثابتةٌ لا تظهر وتغيب.**
 *
 * كانت العروضُ والشكاوى تُخفيان حين يفرغان (`live_offers` و`open_tickets`)
 * — **وكان ذلك صحيحاً في شريطٍ ضيّق**: أيقونةٌ تُفتح على فراغٍ تُعلّم صاحبَها
 * ألّا يضغطها.
 *
 * **وفي قائمةٍ ينقلب الحكم**: القائمةُ هي حيث يُبحث عمّا لا يُرى، **وبندٌ
 * يختفي يجعل صاحبَه يظنّ الميزةَ غيرَ موجودة** — فيسأل «أين شكواي؟» ولا
 * يجد باباً.
 */
const MENU = [
  { href: "/favorites", label: m.customer.favorites.title, icon: IconHeart },
  { href: "/invite", label: m.customer.invite.title, icon: IconLink },
  /* **والاسمُ واحدٌ في الخمس** — كان «شكاواي» هنا و«الشكاوى والبلاغات» في
     اللوحات الأربع. **والصفحةُ تحمل الاثنين**: ما رفعتَه وما رُفع عليك.
     (قرارُ المالك ٢٠٢٦-٠٨-٠٧: «لازم بدل شكاوي تكون شكاوى وبلاغات، مثل باقي
     اللوحات».) */
  { href: "/complaints", label: m.terms.complaints, icon: IconSupport },
  /* ══════════════════════════════════════════════════════════════════
     **ولا سجلَّ محادثاتٍ في القائمة**
     ══════════════════════════════════════════════════════════════════

     (قرارُ المالك ٢٠٢٦-٠٨-١٢: «دردشاتي السابقة ما تلزم بالقائمة أساسا،
      هي فقط موجودة بسجلّ الطلبات».)

     **والمحادثةُ لا تُقرأ وحدَها**: من يفتحها يبحث عن طلبٍ بعينه —
     «ماذا اتّفقنا في طلب الثلاثاء؟» — **وموضعُها بطاقتُه**، حيث الرقمُ
     والمبلغُ والتاريخُ حولها.

     **وبابٌ ثانٍ إلى الشيء نفسِه يُقرأ شيئين**: يظنّ صاحبُه أنّ في
     القائمة أرشيفاً غيرَ الذي في الطلبات. */
] as const;

export default function Header({
  /** **اسمُ المنصة من الإعدادات** — وفارغٌ يعني «خذ من المعجم». */
  name = "",
  /** **شعارُها** — وفارغٌ يعني أنّ حرفَ العلامة يبقى. */
  logo = null,
}: {
  name?: string;
  logo?: string | null;
} = {}) {
  const { user, logout } = useAuth();
  /* **وبابا الموقع يُقرآن من الهويّة** — (طلبُ المالك ٢٠٢٦-٠٨-١٧).
     **وهي مقروءةٌ في الخادم ومُمرَّرةٌ قيمةً مبدئيّة**، فلا يظهر الزرُّ
     ثمّ يختفي أمام عين الزائر — **وومضةٌ كهذه أسوأُ من بقائه.** */
  const { showLogin, showShop } = usePlatform();
  const router = useRouter();
  const pathname = usePathname();
  const logged = isLoggedIn(user);
  const [summary, setSummary] = useState<Summary | null>(null);

  const loadSummary = useCallback(() => {
    if (logged) api<Summary>("/api/v1/me/summary").then(setSummary).catch(() => undefined);
  }, [logged]);

  useEffect(() => {
    loadSummary();
  }, [loadSummary, pathname]);

  // الرصيد والصورة يتحدّثان لحظياً — بلا إعادة تحميل
  // ("profile" حدث محلي يبثّه AccountSettings عند تغيير الصورة أو الرقم)
  useLiveRefresh(["wallet", "profile"], loadSummary);

  const portal = user ? portalFor(user.roles) : null;

  async function backToDashboard() {
    if (!user || !portal) return;
    try {
      await goTo(homeFor(user.roles));
    } catch {
      /* يبقى في الموقع */
    }
  }

  /**
   * **العلامةُ ومعها بابُ التسوّق.**
   *
   * (قرارُ المالك ٢٠٢٦-٠٨-٠٦: «التسوّق يجب أن يكون بجانب لوغو المنصة».)
   *
   * # ولماذا هنا لا في جهة الأدوات
   *
   * كان في صفّ الأدوات يساراً مع الإشعارات والمحفظة والخروج — **وتلك أدواتُ
   * حسابٍ يفتحها من يعرف ما يريد.** والتسوّقُ **تنقّلٌ لا أداة**: هو الطريقُ
   * الذي يمشي فيه الزائرُ أوّلَ مرّة.
   *
   * **والعينُ تبدأ من العلامة** — في صفحةٍ عربيّةٍ تقع في أقصى اليمين، فأوّلُ
   * ما بعدها أوّلُ ما يُقرأ. **وطرفُ الشريط الآخرُ آخرُ ما يُنظر إليه.**
   *
   * **ويُرى قبل الدخول وبعده**: بقيّةُ الروابط داخل فرع الداخلين، **والتسوّقُ
   * سببُ وجود الموقع** — ومن لم يدخل بعد هو أحوجُ الناس إليه.
   */
  const brand = (
    <>
      <Link href="/" className="flex items-center gap-2">
        {/* **والشعارُ يحلّ محلّ الحرف ولا يُلغيه.**

            (قرارُ المالك ٢٠٢٦-٠٨-٠٦: هويّةُ المنصة — الاسمُ واللوغو.)

            **ومنصّةٌ لم تَرفع شعاراً يجب أن تبقى تعمل**: مربّعٌ فارغٌ في
            الشريط العلويّ **أسوأُ من حرف.** */}
        {/* ══════════════════════════════════════════════════════════
            **اللوغو وحدَه — ولا اسمَ بجانبه**
            ══════════════════════════════════════════════════════════

            (قرارُ المالك ٢٠٢٦-٠٨-٠٦: «اسم المنصة لا داعي له في التوب بار،
             فقط اترك لوغو المنصة».)

            **والاسمُ كان يقول ما يقوله الشعار** — والشعارُ يُقرأ بلمحةٍ
            والاسمُ يُقرأ بحرف. **وشريطٌ فيه علامةٌ واسمُها يكرّر نفسَه في
            أضيق مكانٍ في الشاشة.**

            **ويبقى للقارئ الصوتيّ**: `alt` الشعار اسمُ المنصة **فمن لا
            يرى يسمعه**، ومن لا شعارَ عنده يقرأ حرفَ الاسم. */}
        {/* ══════════════════════════════════════════════════════════
            **ونصفُ الشعار يتدلّى من الشريط**
            ══════════════════════════════════════════════════════════

            (قرارُ المالك ٢٠٢٦-٠٨-٠٩: «اللوغو خلّيه نصّ بالتوب بار والنصف
             الآخر وكأنّه يتدلّى من التوب بار، ورجّع التوب بار لحجمه
             الطبيعيّ — لا نريد توب بار عريض».)

            **والشريطُ يطول بأطول ما فيه** — فشعارٌ بواحدٍ وثمانين بكسلاً
            يجعله تسعين. **والحلُّ ألّا يُحسب نصفُه**: هامشٌ سالبٌ بمقدار
            النصف **يُخرجه من حساب الارتفاع ويُبقيه مرسوماً.**

            **فيعود الشريطُ إلى مقاسه** ويتدلّى النصفُ فوق ما تحته.

            **و`overflow-visible` شرطٌ** — شريطٌ يقصّ ما يفيض عنه يبتر
            المتدلّي. */}
        {/* ══════════════════════════════════════════════════════════
            **ولا يتدلّى على الجوّال**
            ══════════════════════════════════════════════════════════

            (كشفه المالك ٢٠٢٦-٠٨-٠٩ بلقطة: الشعارُ يلامس بطاقةَ الدخول —
             «لا يجب أن يلامس الفورم بالجوّال».)

            **والتدلّي يحتاج فراغاً تحته**، وشاشةُ الحاسوب تعطيه: البطاقةُ
            في وسطها. **وعلى هاتفٍ البطاقةُ تبدأ تحت الشريط مباشرةً** —
            فينزل الشعارُ عليها.

            # وشيئان لا واحد

            **العلّةُ فسحةٌ ناقصة**: ما تحت الشريط يبدأ حيث ينتهي الشريطُ لا
            حيث ينتهي المتدلّي. **فالفسحةُ في `<main>`** — وهي الإصلاح.

            **والمقاسُ مسألةٌ ثانية**: شعارٌ بمئةٍ واثنَي عشرَ في شريطِ هاتفٍ
            عرضُه ثلاثُمئةٍ وستّون يأكل نصفَ الشريط. **فأربعةٌ وستّون على
            الجوّال ومئةٌ واثنا عشرَ فوقه** (قرارُ المالك ٢٠٢٦-٠٨-٠٩).

            **وصغّرتُه أوّلاً بلا فسحةٍ فردّه** — بحقّ: **كان يعالج العرَضَ
            وحدَه**، والشعارُ يبقى ملاصقاً وإن صغُر.

            **والهامشُ السالبُ نصفُ الارتفاع بالضبط** — أكبرُ منه يرفع
            الشعارَ داخلَ الشريط، وأصغرُ يُطيله. */}
        {/* ══════════════════════════════════════════════════════════
            **والشعارُ صغُر على الجوّال — لأنّه كان يزاحم ما بعده**
            ══════════════════════════════════════════════════════════

            (شهده المالك ٢٠٢٦-٠٨-١١: «بنسخة الجوّال كان اللوغو ماخذ
             بادينك، لهذا السبب الجرسُ يبين مقصوص».)

            **وقِيس على عرض ٣٩٠**: الشعارُ ٦٤ والحشوةُ ٢٤ والعناصرُ ٢٨٠
            والفجواتُ ٤٨ — **المجموع ٤١٦، والفائضُ ٢٦ بكسلاً.** فيتزاحم
            آخرُ ما في الصفّ، **والجرسُ يُقصّ لأنّه في طرفه.**

            **وأربعون تُخلي ما يكفي** — والشعارُ يبقى مقروءاً، **والهامشُ
            السالبُ نصفُ الارتفاع كما كان** (أكبرُ منه يرفعه داخلَ الشريط
            وأصغرُ يُطيله). */}
        <BrandMark
          size={64}
          /* **وأصغرُ ممّا كان** — (طلبُ المالك ٢٠٢٦-٠٨-١٧).
             **وثلاثةُ أرقامٍ مرتبطة**: الشعارُ، ونصفُه المتدلّي تحت
             الشريط، وفسحةُ الصفحة تحته — **ومن بدّل واحداً بدّل
             الثلاثة**، وإلّا وقع الشعارُ على ما تحته أو بقي فراغٌ
             لمقاسٍ لم يعد موجوداً. */
          /* **ولا تدلٍّ بعد اليوم** — (طلبُ المالك ٢٠٢٦-٠٨-١٧:
             «ألغِ تدلّي اللوغو واجعله بالتوب بار بشكلٍ جيّد»).

             **وكان ينزل نصفَه تحت الشريط**، فيلزم الغلافَ فسحةٌ تحته
             لئلّا يقع على ما بعده — **وثلاثةُ أرقامٍ مرتبطةٍ تُضبط
             معاً.** **والآن يسكن داخلَه** فتذهب الفسحةُ ويذهب معها
             نصفُ الفراغ فوق السلايدر. */
          /* **والقرصُ الأبيضُ عاد** — (طلبُ المالك ٢٠٢٦-٠٨-١٧). **وسقط
             سهواً حين أُلغي التدلّي**: بُدّل السطرُ الذي يحمله معه. */
          disc
          heightClass="h-9 sm:h-12"
        />
      </Link>

      {/* **وفرجةٌ بين العلامة وأوّل باب.**

          (قرارُ المالك ٢٠٢٦-٠٨-٠٩: «بعّد التسوّق عن اللوغو قليلاً».)

          **والشعارُ كبُر مرّتين وربعاً** فالتصق به ما بعدَه — **والفجوةُ
          العامّةُ في الشريط مقاسٌ واحدٌ لكلّ ما فيه**، وهي تكفي بين بابين
          ولا تكفي بين علامةٍ وباب.

          **والفرجةُ منطقيّةٌ لا يمينٌ ولا يسار** — تنقلب مع اللغة. */}
      {showShop && (
      <TopBarLink
        Link={Link}
        href="/shop"
        title={N.shop}
        aria-label={N.shop}
        tone={pathname.startsWith("/shop") ? "active" : "primary"}
        /* ══════════════════════════════════════════════════════════
           **ويُخفى على الجوّال لمن دخل وحدَه**
           ══════════════════════════════════════════════════════════

           **الشريطُ السفليُّ يحمل التسوّقَ بتسميته** — **وبابان لشيءٍ واحدٍ
           في شاشةٍ بثلاثمئةٍ وستّين يزاحمان ما لا بديلَ له.** (قرارُ المالك
           ٢٠٢٦-٠٨-٠٦.)

           **والزائرُ لا شريطَ سفليَّ له** — فلو أُخفي عنه لم يبقَ في شاشته
           بابٌ إلى السوق أصلاً: **يفتح الموقعَ فيرى صورةً وزرَّ دخولٍ ولا
           يعرف أين البضاعة.** (قرارُ المالك ٢٠٢٦-٠٨-٠٩: «زرّ تسوّق للزوّار
           برأيي».) */
        className={`ms-2 ${isLoggedIn(user) ? "hidden md:flex" : "flex"}`}
      >
        <IconStore size={TOPBAR_ICON} />
        <span className="hidden sm:inline">{N.shop}</span>
      </TopBarLink>
      )}
      {/* ══════════════════════════════════════════════════════════════
          **وأبوابُ التعريف على الحاسوب وحدَه**
          ══════════════════════════════════════════════════════════════

          (قرارُ المالك ٢٠٢٦-٠٨-١٦: موقعٌ فيه الرئيسيّة وتسوّق ومن نحن
           وتواصل وميزاتنا.)

          **ولا تُزاحم شريطَ الجوّال**: فيه السلّةُ والإشعاراتُ والحساب،
          **وثلاثةُ أبوابٍ نصّيّةٍ فوقها تدفع ما لا بديلَ له خارجَ الشاشة.**
          **وزائرُ الجوّال يجدها بالتمرير وفي التذييل** — وهي صفحةٌ واحدةٌ
          طويلة. */}
      {MARKETING.map((l) => (
        <TopBarLink
          key={l.href}
          Link={Link}
          href={l.href}
          title={l.label}
          tone={pathname === l.href ? "active" : "plain"}
          className="ms-1 hidden lg:flex"
        >
          <span>{l.label}</span>
        </TopBarLink>
      ))}
    </>
  );

  return (
    <TopBar start={brand} sticky>
      {logged ? (
        <TopBarActions
          Link={Link}
          notifications={
            <LiveNotifications
              api={api}
              wsUrl={WS_URL}
              token={tokenStore.access}
              Link={Link}
              allHref="/notifications"
            />
          }
          walletHref="/wallet"
          balance={summary?.balance ?? 0}
          walletIcon={<IconWallet size={TOPBAR_ICON} />}
          accountHref="/account"
          accountLabel={m.terms.account}
          avatarUrl={mediaUrl(summary?.avatar_thumb_url)}
          name={summary?.full_name || user?.phone || ""}
          onLogout={() => {
            logout();
            router.push("/");
          }}
          logoutLabel={m.auth.logout}
          active={pathname}
          /* ══════════════════════════════════════════════════════════
             **ولا «لوحتي» في القائمة — هي في الشريط**
             ══════════════════════════════════════════════════════════

             (قرارُ المالك ٢٠٢٦-٠٨-١٢: «لوحتي موجودة بالتوب بار، شو إلها
              لازمة بالقائمة أيضا؟».)

             **وكانت هنا بقرارِ ٢٠٢٦-٠٨-١٠** حين كان الشريطُ يُخفي كلمتَها
             على الجوّال — **فصارت أيقونةً تُقرأ بالتخمين.** وقد صارت لها
             كلمةٌ ظاهرة، **فبقي البابان.**

             **وبابان متجاوران لشيءٍ واحد** يجعلان صاحبَهما يسأل: أيُّهما؟

             ── والعروضُ تظهر حين تكون ──

             (قرارُ المالك ٢٠٢٦-٠٨-١٢: «صفحة العروض مخفيّة، تظهر فقط إذا
              نزّلت المنصّةُ أو المتاجرُ عروضا».)

             **وكان الحكمُ عكسَه** (٢٠٢٦-٠٨-٠٧): بندٌ يختفي يجعل صاحبَه
             يظنّ الميزةَ غيرَ موجودة. **وهو صحيحٌ لبابٍ يُنتظر أن يمتلئ
             اليوم** — كالشكاوى.

             **والعروضُ غيرُها**: تكون أو لا تكون شهوراً. **وصفحةٌ فارغةٌ
             أكثرَ العام تُعلّم صاحبَها ألّا يفتحها**، فلا يفتحها يومَ
             تمتلئ. */
          menu={[
            ...(summary?.live_offers
              ? [{ href: "/offers", label: m.customer.offers.title, icon: IconPromos }]
              : []),
            ...MENU,
          ]}
          extras={
            <>
              {/* **لا سلّة في الشريط**: صارت عائمةً أسفل الصفحة (`FloatingCart`)
                  لأن الشريط يمضي مع التمرير، فتغيب السلّة في اللحظة التي
                  تُستعمل فيها. وذهابُها يحسم كذلك التباسها بـ«الطلبات»
                  المجاورة — R-88. */}
              {/* ══════════════════════════════════════════════════════
                  **وأربعةٌ نزلت إلى القائمة**
                  ══════════════════════════════════════════════════════

                  (قرارُ المالك ٢٠٢٦-٠٨-٠٦.)

                  كانت هنا: الشكاوى · العروضُ · المفضّلةُ · الدعوة —
                  **أربعُ أيقوناتٍ صامتةٍ في صفٍّ واحد** قلبٌ وسلسلةٌ وبوقٌ
                  ودعمٌ، **يُقرأ الواحدُ منها بالتخمين لا بالنظر.**

                  **واثنتان منها كانتا تظهران وتغيبان** بحسب العدد — فيتبدّل
                  ترتيبُ الشريط بين زيارةٍ وأخرى، **ومن حفظ موضعَ زرٍّ وجده
                  مكانَ غيره.**

                  **والطلباتُ وحدَها بقيت**: تُفتح كلَّ يومٍ لا كلَّ شهر. */}
              <TopBarLink
                Link={Link}
                href="/orders"
                title={m.terms.orders}
                aria-label={m.terms.orders}
                tone={pathname.startsWith("/orders") ? "active" : "plain"}
                className="hidden md:flex"
              >
                <IconOrder size={TOPBAR_ICON} />
              </TopBarLink>
              {/* ══════════════════════════════════════════════════════════
                  **ولا بابَ ثالثاً للطلب الخاصّ**
                  ══════════════════════════════════════════════════════════

                  (قرارُ المالك ٢٠٢٦-٠٨-١٢: «ليش أيقونة خاصّة؟ ما إلها
                   داعي هون».)

                  **وثلاثةُ أبوابٍ لصفحةٍ واحدة**: زرٌّ في صفحة الطلبات
                  (كلُّ الأحجام)، والشريطُ السفليُّ (الجوّال)، وأيقونةٌ
                  هنا (الحاسوب).

                  **وقرارُه ٢٠٢٦-٠٨-١١ كان «خلص واحدٌ يكفي»** — فرُفعت من
                  الجوّال **وبقيت هنا بحجّة أن لا شريطَ سفليَّ في
                  الحاسوب**، **وزرُّ صفحة الطلبات يعمل في الأحجام
                  كلِّها** — فلم تكن الحجّةُ صحيحةً أصلا.

                  **وبابٌ زائدٌ يزاحم ما لا بديلَ له** في شريطٍ كلُّ
                  أيقونةٍ فيه تُقاس. */}
              {/* ══════════════════════════════════════════════════════
                  **وزرُّ التطبيق للداخل كما هو للزائر**
                  ══════════════════════════════════════════════════════

                  (كشفه فحصٌ يدويٌّ ٢٠٢٦-٠٨-٠٨: يظهر للزائر ويختفي فورَ
                   الدخول.)

                  وُضع بجانب «تسجيل الدخول» حرفيّاً — **ففُقد بفقدِ جاره.**
                  **والداخلُ أحوجُ إليه**: من طلب مرّةً هو من يريد التطبيق،
                  ومن لم يقرّر الدخولَ بعد قد لا يعود.

                  ولا يظهر إن لم يُضبط رابطٌ ولا رُفع ملفّ — يخفي نفسَه. */}
              <AppDownloadChip label={m.auth.getApp} />
              {/* لوحتي لمن له لوحة فقط — الزبون لا لوحة له وعناصره كلها هنا */}
              {/* **وتختفي من الشريط على الجوّال** — (قرارُ المالك
                  ٢٠٢٦-٠٨-١١): **نزلت إلى الشريط السفليّ مكانَ «الرئيسيّة»**،
                  **وبابان لشيءٍ واحدٍ في ثلاثمئةٍ وستّين يزاحمان ما لا
                  بديلَ له.** وتبقى فوق الحاسوب — لا شريطَ سفليَّ هناك. */}
              {portal && (
                <TopBarChip tone="accent" onClick={backToDashboard} title={m.shared.backToDashboard} className="hidden md:flex">
                  <IconOverview size={TOPBAR_ICON} />
                  <span className="hidden md:inline">{m.shared.backToDashboard}</span>
                </TopBarChip>
              )}
            </>
          }
        />
      ) : (
        <>
          {/* **واسمُه يُخفى على الجوّال لا أيقونتُه.**

              (قرارُ المالك ٢٠٢٦-٠٨-٠٩: «أيقونة لتسجيل الدخول ليكون أيضاً
               واضح».)

              **وأيقونةُ الشخص تُقرأ «حسابي» في كلّ تطبيق** — والاسمُ يأخذ
              ثلاثةَ أضعافِ عرضِها **في شريطٍ صار فيه بابُ التسوّق أيضاً.**

              **ويبقى للقارئ الصوتيّ** — `aria-label` يحمله. */}
          {showLogin && (
          <TopBarLink
            Link={Link}
            href="/login"
            title={N.login}
            aria-label={N.login}
            className="border border-line"
          >
            <IconUser size={TOPBAR_ICON} />
            <span className="hidden sm:inline">{N.login}</span>
          </TopBarLink>
          )}
          {/* **وبجانبه زرُّ التطبيق** — لمن لم يقرّر الدخولَ بعد.
              (طلبُ المالك ٢٠٢٦-٠٨-٠٨: «لازم يكون بالتوب بار».) */}
          <AppDownloadChip label={m.auth.getApp} />
        </>
      )}
    </TopBar>
  );
}
