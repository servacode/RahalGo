/**
 * **صفحةُ التسوّق — فارغةٌ عمداً.**
 *
 * (قرارُ المالك ٢٠٢٦-٠٨-٠٦: «أوّلاً ابنِ الصفحة فارغة، ثمّ نحكم ماذا نضع
 * فيها».)
 *
 * # لماذا وُجدت
 *
 * **كانت `/` هي صفحةَ التسوّق نفسَها** — فلا رئيسيّةَ للموقع أصلاً. وقد
 * أُفرغت `/` لتصير صفحةً رئيسيّةً حقيقيّة، **فبقي التسوّقُ بلا بيت.**
 *
 * # وما ينتظر أن ينتقل إليها
 *
 * **`HomeClient.tsx` باقٍ كما هو ولم يُمسّ**: فيه شبكةُ الأقسام بصورها،
 * والبحثُ في الأصناف (لا في المتاجر)، وسلايدرُ اللافتات. **وهو منطقٌ عاملٌ
 * مجرَّبٌ لا يُعاد كتابتُه** — ينتقل حين يُقرَّر شكلُ الصفحة.
 *
 * # ولا رابطَ إليها بعد
 *
 * **لا شيءَ في الموقع يشير إلى `/shop` الآن** — لا الشريطُ العلويُّ ولا
 * الشريطُ السفليُّ ولا الفوتر. **والربطُ قرارُ تنقّلٍ يُتَّخذ مع محتواها**،
 * لا قبله: **زرٌّ يقود إلى صفحةٍ بيضاءَ أسوأُ من غياب الزرّ.**
 */

export default function ShopPage() {
  return <div />;
}
