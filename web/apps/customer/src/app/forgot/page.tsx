/** استعادةُ كلمة المرور — **والمنطقُ كلُّه في `AuthScreen`**، وهذه بابُه. */

import AuthScreen from "@/components/AuthScreen";

export default function Page() {
  return <AuthScreen mode="reset" />;
}
