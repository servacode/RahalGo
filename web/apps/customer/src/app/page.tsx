/**
 * **الصفحةُ الرئيسيّة — فارغةٌ عمداً.**
 *
 * (قرارُ المالك ٢٠٢٦-٠٨-٠٦: «اترك الصفحة الرئيسية فارغة — فقط توب بار وفوتر
 * والمنتصف فارغ».)
 *
 * # ما كانت
 *
 * **كانت `/` هي صفحةَ التسوّق نفسَها**: أقسامٌ وبحثٌ وسلايدر. **فلا وجودَ
 * لصفحةٍ رئيسيّةٍ أصلاً** — واللوغو في الشريط يشير إليها، فيقع الزائرُ الأوّلُ
 * على شبكةِ أقسامٍ **بلا أن يعرف ما هذه المنصّة ولا أين توصّل ولا كيف يُدفع.**
 *
 * # وما لم يُحذف
 *
 * **`HomeClient.tsx` باقٍ كما هو ولم يُمسّ** — فيه منطقُ التصفّح والبحث
 * والسلايدر، **جاهزاً لينتقل إلى صفحة التسوّق حين يُقرَّر مسارُها.**
 *
 * **ولا يُحذف كودٌ عاملٌ لأنّ شاشةً أُفرغت** — الإفراغُ قرارُ عرضٍ، والحذفُ
 * قرارٌ ثانٍ لم يُتَّخذ.
 *
 * # ولماذا لم يبقَ جلبُ البيانات
 *
 * كانت تُنادي `/public/home` عند كلّ فتحة. **وصفحةٌ لا تعرض شيئاً لا تنادي
 * الخادم** — نداءٌ لا يُقرأ ردُّه تأخيرٌ خالصٌ في أوّل ما يفتحه الزائر.
 */

export default function HomePage() {
  return <div />;
}
