"use client";

/**
 * **الشكاوى على السائق — يراها بنفسه.**
 *
 * # لماذا يراها
 *
 * كانت تصل غرفةَ العمليات وحدَها. **ومن اشتُكي عليه ولا يعلم لا يُصلح شيئاً**
 * — يُخصم منه يوماً فيُفاجأ، **ويظنّ الظلمَ حيث كان خبرٌ.**
 *
 * **والشكوى تُقرأ لا تُبلَّغ فحسب**: «تأخّر ساعةً» و«لم يردّ على الهاتف»
 * سببان مختلفان، **وأحدُهما يُصلَح بترتيب اليوم والآخر بشحن الهاتف.**
 *
 * **ولا تُعرض أسماءُ الزبائن** — ما يخصّه ما وقع لا من قاله.
 */

import { ReputationComplaints } from "@rahalgo/ui";
import { api } from "@/lib/api";

export default function DriverComplaintsPage() {
  return <ReputationComplaints api={api} />;
}
