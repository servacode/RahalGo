/**
 * **توكنز التصميم — والألوانُ ليست هنا.**
 *
 * # لماذا خرجت الألوان
 *
 * كانت لوحةٌ كاملةٌ هنا **ومثلُها في `theme.css`** — والتعليقُ يقول «يُعدَّل
 * هناك ثمّ يُعكس هنا». **وهذا لا يقع أبداً**: بُدِّل الثيمُ إلى الداكن وبقيت
 * هذه على الفاتح، **فصار `primary` هنا `#14708F` وهناك `#6fd8f5`.**
 *
 * **ولم يكن ذلك ورقاً ميتاً**: الخريطةُ تقرأ منها فترسم بأزرقَ لا وجودَ له في
 * الشاشة — **خطُّ مسارٍ من منصّةٍ أخرى**، ولا يظهر خطأً ولا يشتكي أحد.
 *
 * **فصار الثيمُ مصدراً واحداً** (`theme.css`)، ومن احتاج اللونَ قيمةً لا صنفاً
 * يقرؤه بـ`themeColor` من `cssvar.ts`.
 *
 * **وما بقي هنا لا مرآةَ له**: مقاساتٌ وحدودُ شاشاتٍ لا تُكتب في الثيم.
 */

export const fontFamily = {
  /** المتن — **مفاصلُ مفتوحةٌ وأرقامٌ ثابتةُ العرض** (ملفّاته في `fonts.css`). */
  sans: `"IBM Plex Sans Arabic", system-ui, sans-serif`,
  /** العناوين — **كوفيٌّ هندسيّ**، خطُّ وادي الفرات. */
  display: `"Reem Kufi", "IBM Plex Sans Arabic", system-ui, sans-serif`,
} as const;

export const radius = {
  control: "10px",
  card: "20px",
  badge: "9999px",
} as const;

/** نقاط القياس الموحدة للتجاوب (GROUND-RULES §2) */
export const breakpoints = {
  sm: "640px",
  md: "768px",
  lg: "1024px",
  xl: "1280px",
} as const;
