package com.rahalgo.ui

/**
 * ══════════════════════════════════════════════════════════════════════
 * **مضيفا المنصّة — يُكتبان مرّةً**
 * ══════════════════════════════════════════════════════════════════════
 *
 * **كانا مكتوبين في أربعة ملفّاتِ `Backend.kt`** (قِيس ٢٠٢٦-٠٨-٢٦)،
 * **فتبديلُ المضيف أربعةُ تعديلاتٍ ومن نسي واحداً لم يعرف**: التطبيقُ
 * يُبنى ويعمل، **ويكلّم خادماً قديماً.**
 *
 * # ولماذا السائقُ ليس هنا
 *
 * **تطبيقُ السائق يقرؤهما من `BuildConfig`** — يضعهما `build.gradle.kts`
 * بحسب نوع البناء، **فالتجريبيّ وحدَه يقبل تجاوزاً** ويحرسه
 * `ProductionEndpointGuardTest`. **وذاك قرارٌ مقصودٌ لا تكرار**، فيبقى.
 */
object Hosts {

    /** **عنوان المحرّك — الإنتاج وحده.** */
    const val API = "https://api.rahalgo.com"

    /**
     * **أصلُ آثار الخرائط.**
     *
     * **ولا يُكتب مضيفٌ في منطق واجهة** — منه وحدَه تُشتقّ عناوينُ
     * الفهرس والأرشيف والموارد، **وتبديلُ المزوّد سطرٌ هنا.**
     */
    const val MAPS = "https://maps.rahalgo.com"
}
