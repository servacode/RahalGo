<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- ══════════════════════════════════════════════════════════════
         **الموقعُ لتسجيل متجرٍ وحدَه — لا تتبّع**
         ══════════════════════════════════════════════════════════════

         **والمندوبُ لا يُتتبَّع**: موقعُه يُقرأ مرّةً ليبدأ منها منتقي
         النقطة، **والنقطةُ التي تُحفظ نقطةُ المتجر لا نقطتُه.**

         **ولا `ACCESS_BACKGROUND_LOCATION`** — إذنٌ بلا حاجةٍ ظاهرةٍ
         يُرفض من المستخدم ومن غوغل معا. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- **وأيقونةُ المنصّة من الوحدة** — (قرارُ المالك ٢٠٢٦-٠٨-١٤). -->
    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RahalGoRep">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
