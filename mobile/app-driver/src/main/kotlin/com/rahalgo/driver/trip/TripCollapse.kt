package com.rahalgo.driver.trip

import androidx.compose.runtime.getValue
import androidx.compose.runtime.mutableStateOf
import androidx.compose.runtime.setValue

/**
 * ══════════════════════════════════════════════════════════════════════
 * **مطويٌّ أم مفتوح — حالٌ واحدةٌ للوحة والأزرار**
 * ══════════════════════════════════════════════════════════════════════
 *
 * **(بلاغُ المالك ٢٠٢٦-٠٨-٣١:** «وقتَ نزّل تبعَ الطلب، لازم حتّى
 * الأزرارُ تنزل تختفي مشان يصير مجالٌ أوسعُ للخريطة».)
 *
 * **وكان المقبضُ يُخفي شريطَ المراحل وحدَه** — والأزرارُ تبقى، **فيسحب
 * السائقُ اللوحةَ ليرى الطريقَ فلا يربح إلّا سطرا.**
 *
 * # ولماذا حالٌ خارجَ الشاشتين
 *
 * **اللوحةُ والأزرارُ في دالّتين لا تعرف إحداهما الأخرى** (`TripPanel`
 * و`TripCard`) — **ورفعُ الحال إلى أمّهما يمرّ بستّة توقيعات.**
 *
 * **وهي حالُ عرضٍ لا حالُ عمل** — لا تُحفظ ولا تُرسل، **وتموت مع
 * الرحلة.** فبقاؤها هنا لا يخبّئ شيئاً عن أحد.
 */
internal object TripCollapse {
    /** **مفتوحٌ افتراضاً** — من فتح رحلةً يريد أن يرى ما أمامه. */
    var open by mutableStateOf(true)
}
