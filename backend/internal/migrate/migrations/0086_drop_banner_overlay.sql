-- **يُرفع ما أضفتُه للّافتات قبل ساعة — الرئيسيّةُ ليست لافتة.**
--
-- (تصحيحُ المالك ٢٠٢٦-٠٨-٠٩: «الصفحة الرئيسيّة مو بانر، هو صورةٌ كاملةٌ
--  للصفحة… ما بدّي البانر بالصفحة الرئيسيّة».)
--
-- **بُنيت اللافتةُ لتخدم الصفحتين**: نصٌّ وزرٌّ وموضع. **والرئيسيّةُ ليست
-- صفحةً فيها لافتة** — هي صورةٌ واحدةٌ تملأها، وضبطُها إعدادٌ لا صفٌّ في جدول.
--
-- **وعمودٌ لا يكتبه أحدٌ ولا يقرؤه أحدٌ يبقى فخّاً**: يقرؤه من يأتي بعدُ
-- فيظنّ أنّ للّافتة موضعين. **وقد أُضيفت قبل ساعةٍ ولا صفَّ فيها** — فالحذفُ
-- بلا فقدِ بيانات.
ALTER TABLE banners
  DROP COLUMN IF EXISTS subtitle,
  DROP COLUMN IF EXISTS cta_label,
  DROP COLUMN IF EXISTS placement;
