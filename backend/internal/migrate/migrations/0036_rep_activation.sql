-- عتبة تفعيل العميل قبل أن تُصرف عمولةٌ عنه.
--
-- كانت العمولة تُقيَّد من **الطلب الأول**، فمندوبٌ يسجّل خمسين متجراً ينتج كلٌّ
-- منها طلباً واحداً يقبض عن الخمسين: **الكمّ يُكافأ كما يُكافأ الإنتاج**.
--
-- والممارسة العالمية تربط المكافأة بعتبة إنجاز — DoorDash لا تدفع مكافأة
-- الإحالة حتى يُنجز المتجر المُحال 15 طلباً خلال 60 يوماً. وعتبتنا أصغر لأن
-- نموذجنا مختلف: عمولةٌ مستمرة لا مكافأة مقطوعة، فالعتبة إثباتُ حياةٍ لا شرطُ
-- استحقاق كامل.
--
-- **ولا أثر رجعي**: الطلبات التي تسبق بلوغ العتبة لا تُدفع عنها عمولة ولو بلغها
-- المتجر لاحقاً. العتبة بابٌ يُفتح مرّة، لا حسابٌ يُعاد.
INSERT INTO app_settings (key, value) VALUES ('sales.activation_orders', '5'::jsonb)
ON CONFLICT (key) DO NOTHING;
