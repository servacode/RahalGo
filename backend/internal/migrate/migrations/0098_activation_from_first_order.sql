-- **عتبةُ تفعيل المتجر تصير طلباً واحداً.**
--
-- (قرارُ المالك ٢٠٢٦-٠٨-١١: «عتبةُ المتاجر يجب أن يكون من أوّل طلبٍ يقبض
--  عمولة».)
--
-- # ولماذا ترحيلٌ لا تبديلُ افتراض
--
-- **الافتراضُ في الفهرس لا يبدّل صفّاً مكتوباً.** ومن ضبط القيمةَ من اللوحة
-- — أو أقلعت منصّتُه فكُتب الافتراضُ القديم — **يبقى عنده خمسة**، ويقرأ في
-- الشيفرة واحداً. **وقيمتان للشيء الواحد أسوأُ من قيمةٍ خاطئة.**
--
-- # ولا يُدهَس ما اختاره المالك
--
-- **يُبدَّل الخمسةُ وحدَه** — وهو الافتراضُ القديم. **ومن وضع ثلاثةً أو
-- عشرةً بيده أرادها**، ولا يُلغى قرارٌ بترحيل.
UPDATE app_settings
SET value = '1'
WHERE key = 'sales.activation_orders' AND value = '5';
