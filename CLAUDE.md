# اتّفاقُ العمل — رحّال غو

> **يُقرأ في كلّ جلسةٍ تلقائيّاً.** وهو المكانُ الوحيدُ الذي يحكم طريقةَ عملي،
> **وأنت تقرؤه وتصحّحه** — فما لا تراه لا يحكم عملي.

---

## لماذا وُجد هذا الملفّ

**كان ما يحكم عملي مبعثراً في عشرين مستنداً وستّةِ ملفّاتِ ذاكرةٍ خارج
المستودع.** والذاكرةُ خصوصاً **لا تفتحها ولا تصحّحها ولا تعرف ما فيها** —
تُحقَن في كلّ جلسةٍ وتوجّه قراراتي في الخفاء.

**وهذا بعينه «فريقٌ كلُّ شخصٍ يعمل منفصلاً»**: أقرأ من كلّ مستندٍ طرفاً فأبني
شيئاً لا يشبه ما أراد صاحبُ المنصة. **ووثيقتان تتناقضان صامتتين أسوأُ من
لا وثيقة**: `BRAND.md` كانت تقول «الخطُّ IBM Plex Sans Arabic» و`theme.css`
تقول «تجوال» — **ولا أحدَ يعلم أيُّهما الحقّ.**

**فمُحيت الذاكرةُ كلُّها وستّةَ عشرَ مستنداً** (٢٠٢٦-٠٨-٠٦، بقرار المالك)،
**وصار ما يحكم العملَ أربعةَ ملفّاتٍ في المستودع لا غير.**

---

## المستنداتُ الأربعة — ولا خامسَ لها

| الملفّ | ما هو | متى يُقرأ |
|---|---|---|
| `docs/TRUTH.md` | **مصدرُ الحقيقة الواحد.** نصفُه مولَّدٌ من الشيفرة فلا يكذب، وحارسٌ (`TestTruthDocIsCurrent`) يُسقط البناءَ إن شاخ | **قبل أيّ قرار** |
| `docs/GROUND-RULES.md` | القواعدُ الهندسيّةُ المُلزمة — المركزيّةُ وRTL والخطُّ والتوكنز | قبل أيّ كودٍ جديد |
| `docs/WORKLOG.md` | **سجلُّ ما وقع** — تاريخٌ يُكتب ولا يُستشار في قرار | بعد العمل |
| `README.md` | كيف يُشغَّل المشروع | عند الإقلاع |

**ولا يُنشأ مستندٌ جديدٌ إلّا بطلب المالك صراحةً.** كثرةُ المستندات هي ما شتّت
المشروع — **وكلُّ مستندٍ جديدٍ مصدرُ تناقضٍ محتمل.**

---

## طريقةُ العمل

**١ · لا تنفيذَ قبل تحليلٍ مكتوب.**
لأيّ تغييرٍ غيرِ تافه: **ما الحالُ الآن مقيساً · ما المطلوب · ما الذي سينكسر ·
كيف يُتحقَّق.** يقرؤه المالكُ ويوافق، **ثمّ** يُنفَّذ.

**٢ · دفعةٌ واحدةٌ في كلّ مرّة.**
**سبعةُ التزاماتٍ تصميميّةٍ في يومٍ واحد (٢٠٢٦-٠٨-٠٦) انتهت بإرجاعها كلِّها** —
كلُّ واحدٍ منها كان يصلح شيئاً ويكشف عيباً جديداً. **وتلك مطاردةٌ لا هندسة.**

**٣ · قرارُ المالك يسبق أيَّ قاعدةٍ عامّةٍ أعرفها.**
**قال «لا حشوةَ يميناً ويساراً» أربعَ مرّاتٍ وأعدتُها ثلاثاً** — لأنّي كنتُ
أُطبّق ما أعرفه عن «طولِ السطر المريح». **وهو صحيحٌ في مقالٍ وخطأٌ في سوق.**

**٤ · لا يُدّعى إنجازٌ بلا دليل.**
«يعمل» تعني: اختبارٌ رآه المُصلحُ ساقطاً قبل الإصلاح وناجحاً بعده، **أو نداءٌ
حيٌّ يُظهر النتيجة.** والقراءةُ ليست إثباتاً.

---

## قواعدُ تشغيليّةٌ مُلزمة

**إعادةُ التشغيل بعد كلّ تعديل** — الخادمُ والويبُ معاً، ثمّ يُتحقَّق أنّ
المنافذ ترد: `8080` (المحرّك) و`3001`–`3005` (الإدارة · المتجر · الزبون ·
المندوب · السائق).

**والطرفيّاتُ تُغلق قبل أن تُفتح** — وإلّا تراكمت عشراتُ النوافذ:

```powershell
Get-CimInstance Win32_Process -Filter "Name='cmd.exe'" |
  Where-Object { $_.CommandLine -like '*RahalGo*' } |
  ForEach-Object { Stop-Process -Id $_.ProcessId -Force }
```

ثمّ **اثنتان لا أكثر**. ولا تُقتل `conhost.exe` — أكثرُها لبوستغرس ودوكر
وWSL وأدوات المالك.

**والقتلُ بمالك المنفذ لا باسم العملية** (`Get-NetTCPConnection -LocalPort`)
— فـ`node.exe` و`cmd.exe` يعمّان مشاريعَ أخرى على الجهاز.

**ولا تُمسّ حاويات مشاريعَ أخرى** — رحّال غو يستعمل `rahalgo-postgres` (5434)
و`rahalgo-redis` (6380) وحدَهما.

---

## مزالقُ هذا الجهاز

**العربيّةُ لا تمرّ في سطر أوامر.** لا تُمرَّر نصوصاً عربيّةً داخل أوامر
الصدفة — تُكتب بأداة `Write`/`Edit` أو ببايثون بترميز `utf-8` صريح.
**و`Get-Content`/`Set-Content` في PowerShell تُفسد أيَّ ملفٍّ فيه عربيّة.**

**والعلامةُ المائلةُ الخلفيّة تُنهي النصَّ الخام في Go** — تعليقٌ فيه
`` `شيء` `` داخل `` `...` `` يكسر البناء.

**والتحقّقُ بالفحوص النصّيّة لا بالصور** — لا لقطاتِ شاشة.

---

## الاختبارُ والتنسيق

```
go test -count=1 -p 1 ./...          # إلزاميّ قبل كلّ التزام
gofmt -l internal/ cmd/              # لا ملفَّ خارجَ التنسيق
node web/scripts/check-central.mjs   # حارسُ المركزيّة
pnpm typecheck                       # ثمانيةُ تحقّقات
```

`TEST_DATABASE_URL=postgres://rahalgo:rahalgo_dev@localhost:5434/rahalgo_test?sslmode=disable`

**ولا يُمسّ دفترُ المال** (`ledger`) إلّا بقرارٍ صريحٍ من المالك.
