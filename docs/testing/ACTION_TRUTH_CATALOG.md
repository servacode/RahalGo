# رحّال غو — دفترُ حقيقةِ الأفعال
## ACTION TRUTH CATALOG

> **أُنشئ بطلب المالك** (٢٠٢٦-٠٩-٠٤): «أريد أن أستطيع اختيار أيّ زرّ أو
> فعل… وأسأل: *إذا فعلت هذا، ماذا يحصل؟* وتستطيع الوثائق أن تعطيني
> السلسلة كاملة حتى قاعدة البيانات وباقي التطبيقات، **في النجاح
> والفشل**».
>
> **نقطةُ القياس**: `26f93c5d` · فرع `claude/project-discussion-planning-sgxyjn`
>
> **الفهرسُ الرئيسيّ**: [`DISCOVERY_COMPLETENESS_AUDIT.md`](DISCOVERY_COMPLETENESS_AUDIT.md)

---

## علاماتُ الإثبات

| | |
|---|---|
| **✅** | قُرئ من الشيفرة أو قِيس من الإنتاج |
| **⚠️** | استُدلّ عليه — **لا يُعامَل حقيقة** |
| **❓** | لم يثبت |
| **NONE** | فُحص فلم يوجد |

---

# ٠ · حالُ الدفتر

| | |
|---|---|
| **المقام** | **١٥٧ مساراً مُطفِراً** |
| **الموصوفُ هنا** | **١٢** |
| **النسبة** | **~٨٪** |

**والترتيبُ خطراً أوّلاً**: المالُ ثمّ الحالة ثمّ الباقي.

---

# ١ · إثباتاتٌ تسبق الأفعال

## ١-١ · مُشغِّلاتُ القاعدة ودوالُّها — **مُغلَق** ✅

**استُعلمت `pg_proc` مع استبعاد ما تملكه الامتدادات (`pg_depend.deptype='e'`):**

| | |
|---|---|
| **دوالُّ رحّال غو المخصَّصة** | **واحدةٌ فقط** |
| اسمُها | `public.wallet_for_new_user()` |
| نوعُها | `trigger` · `plpgsql` |
| **جسمُها كاملاً** | `INSERT INTO wallets (user_id) VALUES (NEW.id) ON CONFLICT (user_id) DO NOTHING; RETURN NEW;` |
| تقرأ | **NONE** |
| تكتب | `wallets` — **صفٌّ فارغٌ برصيدٍ افتراضيّ** |
| **أتعدّل مالاً؟** | **لا** — لا `balance` ولا `wallet_transactions` |
| **أتعدّل حالةً أو صلاحية؟** | **لا** |
| سلوكُ الفشل | `ON CONFLICT DO NOTHING` — **لا تُسقط إنشاءَ المستخدم** ✅ |
| المُشغِّلُ الذي يستدعيها | `users_wallet_trigger` · `users` · `AFTER INSERT` |

**والـ٨٦٦ الباقيةُ تملكها الامتدادات** (PostGIS أساساً).

> **`CUSTOM DB FUNCTIONS: 1/1 MAPPED`** ✅
> **`DB TRIGGERS: 1/1 MAPPED`** ✅

## ١-٢ · الكتاباتُ المباشرةُ على الحالة — **مُحصاة** ✅

**بُحث عن `SET … status = '…'` في المحرّك كلِّه:**

### على `orders` — **اثنتان تتجاوزان البوّابة**

| # | الموضع | ما تكتب | الشرط |
|---|---|---|---|
| **DSW-1** | `internal/orders/merchant_blocked.go` | `status='accepted'` + `driver_id=NULL` + `offered_driver_id=NULL` | متجرٌ حُظر فيُعاد الطلبُ إلى ما قبل الإرسال |
| **DSW-2** | `internal/orders/rotation.go` | `status='dispatching'` + `driver_id=NULL` + `accepted_at=NULL` | فكُّ إسنادٍ من `assigned` |

**وكلتاهما لا تمرّان بـ`canTransition`** ✅ مُثبَت.

### وتاريخُ الطلب — **U13 حُلّ** ✅

**بُحث عن `INSERT INTO order_events` في المحرّك كلِّه، فوُجد في أربعة
ملفّاتٍ فقط**: `transitions.go` · `service.go` · `order_transfer.go` ·
`merchant_blocked.go`.

| الكتابة | أتكتب التاريخ؟ | الأثر |
|---|---|---|
| **DSW-1** `merchant_blocked.go` | **✅ نعم** — `INSERT INTO order_events (order_id, from_status, to_status, actor_id, note)` | **التاريخُ سليم** |
| **DSW-2** `rotation.go` | **❌ لا** — **لا ذكرَ لـ`order_events` في الملفّ كلِّه** | **⚠️ ثغرةٌ مُثبَتة** |

**فأثرُ `DSW-2` خطيرٌ ومُثبَت**: **فكُّ الإسناد** (`assigned → dispatching`
عند سحب الطلب من سائقٍ) **يقع بلا سطرٍ في تاريخ الطلب.**

**ومن قرأ `order_events` بعد شهرٍ رأى الطلبَ يقفز من `assigned` إلى
`at_pickup` مع سائقٍ آخرَ بلا تفسير** — ولا يعرف أنّ الأوّلَ سُحب منه،
ولا متى، ولا بأمرِ من.

**وهذا يمسّ محاسبةَ السائقين**: من يُشتكى عليه أنّه «أخذ الطلبَ وتركه»
لا دليلَ في السجلّ.

### على كياناتٍ أخرى — مساراتُ حياةٍ مستقلّة

| الموضع | الكيان | الحالة |
|---|---|---|
| `internal/support/support.go` ×٢ | `tickets` | `in_progress` · `resolved` |
| `internal/orders/violations.go` | `merchants` | `suspended` |
| `internal/server/emergency_handlers.go` | `driver_emergencies` | `resolved` |
| `internal/server/leads_handlers.go` | `merchant_leads` | `converted` |

> **`DIRECT STATUS WRITES (orders): 2/2 MAPPED`** ✅
> **`UNMAPPED STATUS WRITES = 0`** ✅

## ١-٣ · مساراتُ المال — **محصاةٌ بالملفّ** ✅

**٣٠ نداءَ `ApplyTx` خارجَ حزمة المحفظة**، في **١١ ملفّاً**:

| الملفّ | العدد | المجال |
|---|---|---|
| `internal/orders/transitions.go` | **٨** | تسويةُ التسليم والإلغاء والفشل |
| `internal/referrals/referrals.go` | ٤ | مكافآتُ الإحالة |
| `internal/orders/treasury.go` | ٣ | نصيبُ المنصّة وإعادةُ حسابه |
| `internal/incentives/{target,incentives}.go` | ٤ | أهدافٌ ومنحٌ يدويّة |
| `internal/orders/{custom,goods}.go` | ٤ | الطلبُ المخصَّص وتسويةُ البضاعة |
| `internal/server/failure_aftermath.go` | ٢ | ما بعد الفشل |
| `internal/orders/service.go` · `support.go` · `disputes.go` · `driver_return.go` | ٤ | الإنشاء · الشكاوى · النزاعات · الإرجاع |

> **`FINANCIAL MUTATION SITES: 30/30 LOCATED`** ✅ · **موصوفةٌ بالبنود: ٦**

---

# ٢ · الأفعالُ الماليّةُ والحالة

---

## A-001 · الزبون يُنشئ طلباً

| البند | القيمة |
|---|---|
| **Actor** | `customer` |
| **التطبيق/الشاشة** | تطبيقُ الزبون — السلّة ثمّ التأكيد |
| **الزرّ** | «أكّد الطلب» |
| **الشروطُ المسبقة** | سلّةٌ غيرُ فارغة · عنوانٌ بإحداثيّات · **الدبّوسُ داخلَ منطقةِ توصيلٍ فعّالة** |
| **تحقّقُ الواجهة** | ❓ لم يُقرأ |
| **التخويل** | `RequireAuth` — **بلا شرطِ دور** ✅ |
| **المسار** | `POST /api/v1/orders` — **مفتاحُ تكرار** ✅ |
| **المُعالِج** | `s.idempotent(s.handleCreateOrder)` → `orders.Service.Create` |
| **قراءات** | `menu_items` · `merchants` · `delivery_zones` · `cities` · `promo_codes` (**`FOR UPDATE`**) · `wallets` · `app_settings` |
| **كتابات** | `orders` · `order_items` · `promo_redemptions` · `wallet_transactions` · `wallets.balance` |
| **المعاملة** | **واحدةٌ** ✅ — `ApplyTx` تأخذ `Querier` |
| **الأقفال** | `promo_codes … FOR UPDATE` — «من وصل ثانياً ينتظر» ✅ |
| **الحالة** | ∅ → `pending` |
| **الماليّ** | `order_payment` (خصمٌ من المحفظة) · و`cash_due` يبقى ديناً نقديّاً |
| **الزمنُ الحيّ** | `touch("order","ops")` |
| **الإشعارات** | المتجر · المكتب |
| **واتساب** | ⚠️ `merchant_dispatch.go` يستعمل whatsmeow — **التوقيتُ لم يُقرأ** |
| **الراصد** | يبدأ عدُّ `accept_min` للتصعيد |
| **الزبون يرى** | «بانتظار قبول المتجر» + رقمُ الطلب |
| **المتجر يرى** | طلبٌ جديد + إشعار |
| **السائق يرى** | **NONE** — لم يُطلب سائقٌ بعد |
| **المندوب** | **NONE** الآن — العمولةُ عند التسليم |
| **الإدارة ترى** | صفٌّ جديد + بثٌّ حيّ |
| **فشلُ التحقّق** | صنفٌ غيرُ متوفّر · كوبونٌ مستهلك · **`out_of_zone` (400)** |
| **فشلُ التخويل** | ٤٠١ بلا توكن |
| **فشلُ القاعدة** | المعاملةُ تُرجَع كاملةً — **لا كتابةَ جزئيّة** ✅ |
| **رصيدٌ غيرُ كافٍ** | `ErrInsufficient` من **قيدِ تحقّقٍ في القاعدة** ✅ |
| **التكرار** | مفتاحُ التكرار يردّ الجوابَ الأوّل ✅ |
| **الضغطُ المتكرّر** | محميٌّ بالمفتاح · ⚠️ **إن لم يُرسله العميلُ فطلبان** |
| **التزامن** | قفلُ الكوبون وحدَه ✅ |
| **التراجع** | كاملٌ بالمعاملة |
| **التدقيق** | **NONE** — «انتقالُ الأطراف أنفسهم لا يُسجَّل» |
| **الحقيقةُ النهائيّة** | `orders.status='pending'` · قيدٌ واحدٌ في الدفتر · الرصيدُ منقوص |
| **الأدلّة** | `server.go` · `internal/orders/service.go` · `sources.go` · `internal/wallet/wallet.go` |

---

## A-002 · السائق يقبل طلباً

| البند | القيمة |
|---|---|
| **Actor** | `driver` |
| **الشاشة** | الطلبات — «قبول» |
| **الشروط** | `on_shift` · `held + cash_due ≤ drivers.cash_limit` (**٥٠٠٬٠٠٠ في الإنتاج**) · عددُ المفتوحة · **أو عرضٌ موجَّهٌ له** |
| **التخويل** | `RequireRoles("driver")` ✅ |
| **المسار** | `POST /api/v1/driver/orders/{id}/accept` — **بلا مفتاح تكرار** ⚠️ |
| **قراءات** | `users.on_shift` · `driver_cash_boxes.held` · `orders.cash_due` · عددُ المفتوحة · `offered_driver_id`+`offer_expires_at` |
| **كتابات** | `orders.driver_id` · `status` · `accepted_at` · `order_events` |
| **الأقفال** | `SELECT … FOR UPDATE` على صفّ الطلب ✅ |
| **الحالة** | `dispatching → assigned` (`driverOps`) |
| **الماليّ** | **NONE** — المالُ عند التسليم |
| **الزمنُ الحيّ** | `touch("order","ops")` |
| **الإشعارات** | ⚠️ للزبون — لم يُقرأ الموضع |
| **الزبون يرى** | «السائق في طريقه» — **عبر البثّ لا عبر إشعارٍ دافع** |
| **المتجر يرى** | اسمَ السائق |
| **الإدارة ترى** | الطلبَ منسوباً |
| **U16 حُلّ** ✅ | **لا إشعارَ دافعاً عند قبول السائق نفسِه**: بُحث عن `notify` في `driver_handlers.go` فلم يوجد. **والإشعارُ `driverAssigned` يقع في `admin_orders_handlers.go` وحدَه** — أي عند **الإسناد اليدويّ من المكتب**، لا عند قبول السائق بنفسه |
| **فشل** | خارجَ الورديّة · تجاوزُ سقف النقد · **سبقه غيرُه** (الحالةُ تبدّلت) |
| **التزامن** | **`FOR UPDATE` + شرطُ الحالة** — ⚠️ **لم يُثبَت تحت ضغطٍ حقيقيّ** |
| **التكرار** | **NONE** — يعتمد على الحالة |
| **التدقيق** | **NONE** |
| **ملاحظةٌ حاسمة** | **في الإنتاج `assignment_mode="queue"`** → **لا عرضَ موجَّهاً**، فالقبولُ **سباقٌ مفتوح** ✅ |

---

## A-003 · السائق يُسلّم الطلب — **أخطرُ فعلٍ ماليٍّ في المنصّة**

| البند | القيمة |
|---|---|
| **Actor** | `driver` |
| **الشاشة** | الرحلة — «تم التسليم» |
| **الشروط** | الحالةُ `at_dropoff` · الطلبُ له |
| **التخويل** | `RequireRoles("driver")` ✅ |
| **المسار** | `POST /api/v1/driver/orders/{id}/transition` `{to:"delivered"}` |
| **المُعالِج** | `handleDriverTransition` → `orders.TransitionWithReason` |
| **قراءات** | صفُّ الطلب **بقفل** · `merchants.debt` **بقفل** · الإعدادات |
| **كتابات** | `orders.status` · `delivered_at` · `closed_at` · `order_events` · **`wallet_transactions` ×٤** · `wallets.balance` ×٤ · `driver_cash_boxes.held` |
| **المعاملة** | **واحدةٌ تضمّ كلَّ ذلك** ✅ |
| **الأقفال** | `orders … FOR UPDATE` · `merchants.debt … FOR UPDATE` ✅ |
| **الحالة** | `at_dropoff → delivered` |
| **الماليّ** | `merchant_earning` · `driver_earning` · `commission` (المندوب) · `platform_profit` |
| **الراصد** | يتوقّف عن التصعيد — الطلبُ مغلق |
| **الإشعارات** | الزبون · المتجر · **المندوب** («عمولة جديدة») |
| **مكافأةُ الهدف** | `GrantTargetIfReached(driver)` — **وإن دُفعت أُشعِر** (٢٠٢٦-٠٩-٠٣) |
| **الزبون يرى** | «تم التسليم» + زرَّ التقييم |
| **المتجر يرى** | مستحقَّه في محفظته |
| **المندوب يرى** | عمولتَه فوراً |
| **الإدارة ترى** | الطلبَ مغلقاً + الدفترَ محدَّثاً |
| **فشلُ التحقّق** | حالةٌ غيرُ مسموحة → `errTransition` |
| **فشلُ القاعدة** | **تُرجَع المعاملةُ كاملةً — ولا قيدَ واحدٌ يبقى** ✅ |
| **الجزئيّ** | **مستحيل** ✅ |
| **التكرار** | **NONE** — لكنّ الحالةَ تمنعه (`delivered → delivered` غيرُ مسموح) |
| **التزامن** | القفلُ يمنع تسويتين |
| **التدقيق** | **NONE** للسائق — «انتقالُ الأطراف لا يُسجَّل» |
| **الحقيقةُ النهائيّة** | `closed_at` مضبوط · **٤ قيود** · الأرصدةُ الثلاثة زادت · صندوقُ السائق زاد بالنقد |
| **الأدلّة** | `internal/orders/transitions.go` (**٨ نداءاتِ دفتر**) |

---

## A-004 · الأدمن يعيد حسابَ التسوية

| البند | القيمة |
|---|---|
| **Actor** | `admin` · `finance` |
| **الشاشة** | الطلبات — «دقّق حساب هذا الطلب» |
| **الشروط** | **الطلبُ مُغلَق** (`closed_at IS NOT NULL`) |
| **التخويل** | `RequireRoles("admin","finance")` ✅ |
| **المسار** | `POST /api/v1/admin/orders/{id}/recompute` |
| **قراءات** | `orders.closed_at` · مجموعُ `platform_profit` قبلَ وبعد |
| **كتابات** | `wallet_transactions` — **قيدٌ مقابلٌ بالفرق** |
| **المعاملة** | `Begin`/`Commit` صريحان ✅ |
| **الماليّ** | **يصحّح ولا يمسح** — «يبقى الخطأُ مسطوراً وتصحيحُه بجانبه» ✅ |
| **الزمنُ الحيّ** | `touch("wallet","ops")` |
| **الإشعارات** | **NONE** |
| **التدقيق** | ✅ `finance.settlement_recomputed` مع `before/after/delta` |
| **النجاحُ المرئيّ** | «صُحِّح: ٣٥ ل.س» أو «لا شيءَ لتصحيحه» (٢٠٢٦-٠٩-٠٢) |
| **الفشل** | `errOrderStillOpen` |
| **التكرار** | **NONE** — **لكنّ إعادتَه غيرُ ضارّة**: الفرقُ يصير صفراً ✅ |
| **أثرُه في التطبيقات** | المتجرُ/السائقُ يرى رصيدَه تبدّل — **بلا إشعار** ⚠️ |

---

## A-005 · الأدمن يعدّل رصيداً يدويّاً

| البند | القيمة |
|---|---|
| **Actor** | `admin` · `finance` |
| **الشاشة** | المستخدمون → ملفٌّ → المحفظة |
| **التخويل** | `RequireRoles("admin","finance")` ✅ |
| **المسار** | `POST /api/v1/admin/users/{id}/wallet` — **مفتاحُ تكرار** ✅ |
| **كتابات** | `wallet_transactions` + `wallets.balance` |
| **الماليّ** | قيدٌ بيدِ إنسان — **`actorID` يُسجَّل مع القيد** ✅ |
| **الزمنُ الحيّ** | `touch("wallet","ops")` **+ `touchUser`** ✅ — «الزبونُ يُعوَّض ثمّ ينظر إلى رصيده فيجده كما كان» |
| **الإشعارات** | ⚠️ لم يُقرأ |
| **صاحبُ المال يرى** | رصيدَه تبدّل **فوراً** بلا تحديثِ صفحة |
| **الفشل** | رصيدٌ سالبٌ يُرفض بقيدِ القاعدة ✅ |
| **التدقيق** | ✅ |

---

## A-006 · الأدمن يبتّ طلبَ صرف

| البند | القيمة |
|---|---|
| **Actor** | `admin` · `finance` |
| **المسار** | `POST /api/v1/admin/payouts/{id}/decide` — **مفتاحُ تكرار** ✅ |
| **كتابات** | `payout_requests.status` · `wallet_transactions` |
| **الماليّ** | خصمٌ من محفظة صاحبه عند القبول |
| **الإشعارات** | ✅ لصاحب الطلب (`notifTitles.payoutPaid` / `payoutRejected`) |
| **الزمنُ الحيّ** | `touch("wallet","ops")` + `touchUser` |
| **التدقيق** | ✅ |
| **الفشل** | ⚠️ لم يُقرأ سلوكُ الرصيد غير الكافي |

---

## A-007 · الأدمن يُسند سائقاً يدويّاً

| البند | القيمة |
|---|---|
| **Actor** | `admin` · `ops` |
| **الشاشة** | الطلبات → «إسناد سائق» |
| **الشروط** | الحالةُ `preparing` أو `dispatching` (شرطُ الواجهة) |
| **المسار** | `POST /api/v1/admin/orders/{id}/assign` |
| **كتابات** | `orders.driver_id` · `status` · `order_events` |
| **الحالة** | `dispatching → assigned` |
| **الماليّ** | **NONE** |
| **الإشعارات** | ✅ للسائق («أُسند إليك طلب جديد») |
| **⚠️ ثغرةٌ مُثبَتة** | **اللوحةُ لا تمنع الإسنادَ إلى سائقٍ موقعُه متوقّف** — تكتب «موقعه متوقف» بجانبه (٢٠٢٦-٠٩-٠٣) **ولا تحجبه**، **والمحرّكُ يحجبه عن الإسناد الآليّ وحدَه** |
| **التدقيق** | ✅ |

---

## A-008 · الأدمن ينقل الطلبَ إلى متجرٍ آخر

| البند | القيمة |
|---|---|
| **Actor** | `admin` |
| **المسار** | `POST /api/v1/admin/orders/{id}/transfer` |
| **الشروط** | الحالةُ في `TRANSFERABLE` (٥ حالات) |
| **كتابات** | `orders.merchant_id` · `order_items` (مطابقةٌ بالاسم) |
| **الماليّ** | **سعرُ الزبون لا يتغيّر — والفرقُ على المنصّة** ✅ |
| **الفشل** | أصنافٌ لا تطابق → تُعرض قائمةُ غير المطابق |
| **التدقيق** | ✅ |

---

## A-009 · المكتب يُفشل التوصيل

| البند | القيمة |
|---|---|
| **Actor** | `ops` · `admin` (**والسائقُ كذلك**) |
| **الشاشة** | الطلبات — «فشل التوصيل» (أُظهر ٢٠٢٦-٠٩-٠٢) |
| **الحالات** | من `at_pickup` · `picked_up` · `on_the_way` · `at_dropoff` |
| **الشرط** | **سببٌ إلزاميّ** — `requiresReason["failed"]` ✅ |
| **المسار** | `POST /api/v1/admin/orders/{id}/transition` `{to:"failed", note}` |
| **الماليّ** | **لا يقع آليّاً** — `failure_aftermath.go` ينتظر قراراً بشريّاً: **أاستردّ المتجرُ بضاعتَه؟ وكم يستحقّ السائق؟** ✅ |
| **ما يليه** | زرّا «أُعيدت إلى المتجر» / «إلى المكتب» + «تعويض السائق» |
| **التدقيق** | ✅ |

---

## A-010 · المندوب يسجّل فرصةَ متجر

| البند | القيمة |
|---|---|
| **Actor** | `sales` |
| **الشاشة** | تطبيقُ المندوب — «إضافة عميل» |
| **الشروط** | **كلُّ الحقول إلزاميّة** (قرارُ المالك ٢٠٢٦-٠٨-٣١) |
| **التخويل** | `RequireRoles("sales")` ✅ |
| **المسار** | `POST /api/v1/rep/leads` ⚠️ (المسارُ الدقيق لم يُقتبس) |
| **كتابات** | `merchant_leads` (مع `sales_rep_user_id` · `district_id` · `owner_password_hash`) |
| **الإشعارات** | ✅ للمندوب («فرصةٌ جديدة») + **`NotifyOps`** للمكتب |
| **الوجهة** | `/portal/merchants` — **صُحّحت** بعد أن كانت تشير إلى صفحةٍ غيرِ موجودة |
| **الماليّ** | **NONE** الآن |
| **الإدارة ترى** | الفرصةَ في `/dashboard/leads` |
| **الفشل** | ❓ **رقمٌ مكرَّر** — لم يُقرأ |

---

## A-011 · الإدارة تقبل الفرصة → متجرٌ حقيقيّ

| البند | القيمة |
|---|---|
| **Actor** | `admin` · `ops` |
| **المسار** | `PATCH /api/v1/admin/leads/{id}` `{status:"converted"}` → `convertLead` |
| **كتابات** | `merchants` (جديد) · `users` (مالكُ المتجر) · `user_roles` (`merchant`) · `merchant_leads.status='converted'` + `merchant_id` · `merchants.district_id` |
| **الشرط** | **ما حُوِّل لا يُردّ** — «المتجرُ قائمٌ يبيع» ✅ |
| **الماليّ** | **`grantSalesTargetIfAny`** → قد يُقيَّد `reward` إن بلغ المندوبُ هدفَه |
| **الإشعارات** | ✅ «تمت الموافقة على عميلك» · **و«أنجزت هدف الشهر» إن دُفعت مكافأة** (٢٠٢٦-٠٩-٠٣) |
| **المندوب يرى** | المتجرَ في «عملائي» · وعمولتَه المستقبليّة مرتبطةً به |
| **صاحبُ المتجر** | حسابٌ بكلمة مرورٍ محفوظة · `must_change_password=true` |
| **التدقيق** | ✅ |
| **⚠️ ثغرةٌ مُثبَتة — U14 حُلّ** | **لا معاملةَ واحدةً إطلاقاً**: بُحث عن `Begin(` في `leads_handlers.go` فلم يوجد. **فإنشاءُ المتجر والحسابِ والدورِ وتحديثِ الفرصة نداءاتٌ متتابعةٌ مستقلّة** |
| **والأثر** | **فشلٌ في المنتصف يترك حالاً نصفيّة**: متجرٌ بلا مالكٍ، أو فرصةٌ ما زالت `new` ومتجرُها قائم. **ولا تراجعَ يجمعها** |

---

## A-012 · الإدارة ترفض الفرصة

| البند | القيمة |
|---|---|
| **المسار** | `PATCH /api/v1/admin/leads/{id}` `{status:"rejected", note}` |
| **الشرط** | **سببٌ إلزاميّ** ✅ |
| **الإشعارات** | ✅ للمندوب مع السبب |
| **الاستئناف** | **`new` مسموحةٌ من `rejected`** — «من رُدَّ خطأً» ✅ |
| **الماليّ** | **NONE** |

---

## A-013 · المكتب يقرّر مصيرَ البضاعة بعد الفشل

| البند | القيمة |
|---|---|
| **Actor** | `admin` · `ops` |
| **الشاشة** | الطلبات → طلبٌ فاشل → «أعيدت إلى المتجر» / «إلى المكتب — المنصّة تتحمّل» |
| **الشروط** | `status='failed'` · `goods_settled_to IS NULL` |
| **المسار** | `POST /api/v1/admin/orders/{id}/goods` |
| **المُعالِج** | `orders.SettleGoods` |
| **قراءات** | `orders.status, goods_settled_to` **بقفل `FOR UPDATE`** ✅ |
| **كتابات** | `orders.goods_settled_to` · `wallet_transactions` ×٢ |
| **المعاملة** | **صريحة** — `Begin`/`Commit` ✅ |
| **الماليّ** | قيدان: دعمُ المتجر أو استرجاعُ ثمنِ بضاعته (`+support` / `-take`) |
| **الزمنُ الحيّ** | `touch("order","ops")` · `touch("wallet","ops")` |
| **التدقيق** | ✅ `finance.goods_settled` |
| **التكرار** | **يمنعه `goods_settled_to IS NULL` تحت القفل** ✅ |
| **المتجر يرى** | رصيدَه تبدّل |

---

## A-014 · المكتب يعوّض السائق عن مشوارٍ لم يُثمر

| البند | القيمة |
|---|---|
| **Actor** | `admin` · `finance` |
| **المسار** | `POST /api/v1/admin/orders/{id}/compensate-driver` |
| **الشروط** | مبلغٌ + **سببٌ يُسجَّل** |
| **كتابات** | `wallet_transactions` (`compensation`) |
| **المعاملة** | **صريحة** ✅ |
| **التدقيق** | ✅ `finance.driver_compensation` |
| **الزمنُ الحيّ** | `touch("order")` · `touch("wallet")` |
| **السائق يرى** | رصيدَه زاد |
| **التكرار** | **NONE** ⚠️ — **لا مفتاحَ ولا شرطَ حالة**: **ضغطتان تُعوّضان مرّتين** |

---

## A-015 · السائق يُرجع البضاعةَ إلى المتجر

| البند | القيمة |
|---|---|
| **Actor** | `driver` |
| **المسار** | `POST /api/v1/driver/orders/{id}/return` |
| **كتابات** | `orders` · `wallet_transactions` (`-paid` من مالك المتجر) |
| **المعاملة** | **صريحة** ✅ |
| **التدقيق** | ✅ `driver.order_returned` مع المبلغ |
| **الزمنُ الحيّ** | `touch("order","ops")` |
| **المتجر يرى** | خصماً من رصيده |

---

## A-016 · المكتب يسوّي نزاعاً

| البند | القيمة |
|---|---|
| **Actor** | `admin` · `ops` |
| **المسار** | `POST /api/v1/admin/disputes/{id}/settle` ⚠️ (الاسمُ لم يُقتبس حرفيّاً) |
| **كتابات** | `disputes` · `wallet_transactions` (`-amount`) |
| **المعاملة** | **صريحة** ✅ |
| **التدقيق** | ✅ `ops.dispute_opened` · `ops.dispute_settled` |
| **الزمنُ الحيّ** | `touch("dispute","ops")` |

---

## A-017 · حلُّ شكوى بتعويض

| البند | القيمة |
|---|---|
| **Actor** | `admin` · `ops` |
| **المسار** | عبر `internal/support` |
| **كتابات** | `tickets.status='resolved'` + `resolution` + `compensation` · `wallet_transactions` (`compensation` للزبون) |
| **المعاملة** | تأخذ `tx` ✅ |
| **الزبون يرى** | الشكوى محلولةً + رصيدَه زاد |
| **ملاحظة** | **هذا هو بابُ الاسترجاع** — «الاسترجاعُ يمرّ بالشكوى وحدَها» (قرارُ المالك ٢٠٢٦-٠٨-٠٩) |

---

# ٣ · ما لم يُوصف بعد — بالأرقام

| المجموعة | موصوف | المقام |
|---|---|---|
| **أفعالٌ ماليّة/حالة** | **١٧** | ~٤٠ |
| باقي الطفرات | **٠** | ~١١٧ |
| **المجموع** | **١٧** | **١٥٧** |

## الأفعالُ الماليّةُ الباقيةُ المعروفة (لم تُوصف)

`referral rewards` (٤ مواضع) · `custom order agree/buy` (٢) ·
`manual incentive grant` (٢) · `cash box settle` ·
`customer cancel refund` · `merchant reject refund` · `treasury credit` (٣)

---

# ٣-ب · ثغراتٌ مُثبَتةٌ ظهرت من هذا الوصف

| # | الثغرة | الدليل |
|---|---|---|
| **G1** | **فكُّ الإسناد بلا سطرٍ في تاريخ الطلب** | `rotation.go` بلا `order_events` |
| **G2** | **`convertLead` بلا معاملةٍ واحدة** — إنشاءُ متجرٍ وحسابٍ ودورٍ نداءاتٌ مستقلّة | `leads_handlers.go` بلا `Begin(` |
| **G3** | **تعويضُ السائق بلا منعِ تكرار** — لا مفتاحَ ولا شرطَ حالة | `failure_aftermath.go` |
| **G4** | **قبولُ السائق بلا إشعارٍ للزبون** — الإشعارُ للإسناد اليدويّ وحدَه | `driver_handlers.go` بلا `notify` |
| **G5** | **اللوحةُ تُسند إلى سائقٍ موقعُه متوقّف** — تكتبها ولا تحجبها | `OrdersScreen.tsx` |
| **G6** | **`accept` بلا مفتاح تكرار** | `server.go` |

---

# ٤ · مجهولاتٌ ظهرت أثناء هذا العمل

| # | المجهول | الحال |
|---|---|---|
| **U13** | أتكتب الكتابتان المباشرتان في `order_events`؟ | **✅ حُلّ** — `merchant_blocked` نعم · `rotation` **لا** |
| **U14** | `convertLead` أفي معاملةٍ واحدة؟ | **✅ حُلّ** — **لا معاملةَ إطلاقاً** |
| **U16** | إشعارُ الزبون عند قبول السائق | **✅ حُلّ** — **لا إشعارَ دافعاً؛ البثُّ وحدَه** |
| **U15** | مسارُ `POST /rep/leads` الدقيق · ورقمُ متجرٍ مكرَّر | ❓ |
| **U17** | توقيتُ رسالة واتساب إلى المتجر | ❓ |
| **U18** | **هل يُقفل `disputes` قبل التسوية؟** — رأيتُ المعاملةَ ولا القفل | ❓ |
| **U19** | **`treasury.go` ثلاثةُ قيود** — لم تُقرأ شروطُها | ❓ |

---

**آخرُ تحديث**: ٢٠٢٦-٠٩-٠٤ · `26f93c5d`
