# رحّال غو — دفترُ حقيقةِ الأفعال
## ACTION TRUTH CATALOG

> **أُنشئ بطلب المالك** (٢٠٢٦-٠٩-٠٤): «أريد أن أستطيع اختيار أيّ زرّ أو
> فعل… وأسأل: *إذا فعلت هذا، ماذا يحصل؟* وتستطيع الوثائق أن تعطيني
> السلسلة كاملة حتى قاعدة البيانات وباقي التطبيقات، **في النجاح
> والفشل**».
>
> **نقطةُ القياس**: `26f93c5d` · فرع `claude/project-discussion-planning-sgxyjn`
>
> **الفهرسُ الرئيسيّ**: [`DISCOVERY_COMPLETENESS_AUDIT.md`](DISCOVERY_COMPLETENESS_AUDIT.md)

---

## علاماتُ الإثبات

| | |
|---|---|
| **✅** | قُرئ من الشيفرة أو قِيس من الإنتاج |
| **⚠️** | استُدلّ عليه — **لا يُعامَل حقيقة** |
| **❓** | لم يثبت |
| **NONE** | فُحص فلم يوجد |

---

# ٠ · حالُ الدفتر — **CANONICAL**

| | |
|---|---|
| **CODE TRUTH BASELINE** | **`26f93c5d`** — لا تغييرَ تشغيليٍّ بعده |
| **الحال** | **STATIC DISCOVERY COMPLETE — PROVEN** |

## المقاماتُ النهائيّة

```
SERVER MUTATIONS TABULATED                      157/157   ✅
SERVER MUTATIONS EFFECT-CLASSIFIED              157/157   ✅
DEEP 30-FIELD RECORDS                            26/157
FINANCIAL MUTATION SITES LOCATED                  30/30   ✅
FINANCIAL MUTATION SITES OPERATIONALLY DESCRIBED  30/30   ✅
```

**⚠️ والفرقُ بين السطرين الثاني والثالث مقصودٌ ولا يُخفى**:
**الطفراتُ السبعُ والخمسون بعد المئة كلُّها مجدولةٌ ومصنَّفةٌ بالأثر**
(الجدولُ الكامل في [`SERVER_MUTATIONS_157.md`](SERVER_MUTATIONS_157.md))،
**وستٌّ وعشرون منها وحدَها موصوفةٌ بالبنود الثلاثين هنا** — **وهي
الحسّاسةُ التي تمسّ مالاً أو حالةً أو صلاحيّةً أو تزامناً.**

**ولا يُدَّعى أنّ ١٥٧ لها سجلٌّ بثلاثين بنداً.**

**والترتيبُ خطراً أوّلاً**: المالُ ثمّ الحالة ثمّ الباقي.

## تصنيفُ الاكتشافات — **مُلزَم**

| الصنف | المعنى |
|---|---|
| **PROVEN DEFECT** | يكسر ثابتاً أو سلامةَ مالٍ أو بياناتٍ — **والنتيجةُ مُثبَتة** |
| **PROVEN RISK** | السلوكُ مُثبَت · **والنتيجةُ لم تُثبت** |
| **PROVEN BEHAVIOR** | مُثبَتٌ وقد يكون مقصوداً — **يحتاج قرارَ مالك** |

---

# ١ · إثباتاتٌ تسبق الأفعال

## ١-١ · مُشغِّلاتُ القاعدة ودوالُّها — **مُغلَق** ✅

**استُعلمت `pg_proc` مع استبعاد ما تملكه الامتدادات (`pg_depend.deptype='e'`):**

| | |
|---|---|
| **دوالُّ رحّال غو المخصَّصة** | **واحدةٌ فقط** |
| اسمُها | `public.wallet_for_new_user()` |
| نوعُها | `trigger` · `plpgsql` |
| **جسمُها كاملاً** | `INSERT INTO wallets (user_id) VALUES (NEW.id) ON CONFLICT (user_id) DO NOTHING; RETURN NEW;` |
| تقرأ | **NONE** |
| تكتب | `wallets` — **صفٌّ فارغٌ برصيدٍ افتراضيّ** |
| **أتعدّل مالاً؟** | **لا** — لا `balance` ولا `wallet_transactions` |
| **أتعدّل حالةً أو صلاحية؟** | **لا** |
| سلوكُ الفشل | `ON CONFLICT DO NOTHING` — **لا تُسقط إنشاءَ المستخدم** ✅ |
| المُشغِّلُ الذي يستدعيها | `users_wallet_trigger` · `users` · `AFTER INSERT` |

**والـ٨٦٦ الباقيةُ تملكها الامتدادات** (PostGIS أساساً).

> **`CUSTOM DB FUNCTIONS: 1/1 MAPPED`** ✅
> **`DB TRIGGERS: 1/1 MAPPED`** ✅

## ١-٢ · الكتاباتُ المباشرةُ على الحالة — **مُحصاة** ✅

**بُحث عن `SET … status = '…'` في المحرّك كلِّه:**

### على `orders` — **اثنتان · وواحدةٌ منهما ليست تجاوزاً**

| # | الموضع | ما تكتب | الشرط |
|---|---|---|---|
| **DSW-1** | `internal/orders/merchant_blocked.go` | `status='accepted'` + `driver_id=NULL` + `offered_driver_id=NULL` | متجرٌ حُظر فيُعاد الطلبُ إلى ما قبل الإرسال |
| **DSW-2** | `internal/orders/rotation.go` | `status='dispatching'` + `driver_id=NULL` + `accepted_at=NULL` | فكُّ إسنادٍ من `assigned` |

### تصحيحٌ — **U29 حُلّ** ✅

**قلتُ إنّ كلتيهما «تتجاوزان البوّابة». والقياسُ نقض نصفَ ذلك:**

**`merchantBlocked` تُنادى من `transitions.go` نفسِه**:

```go
return s.merchantBlocked(ctx, tx, orderID, actorID, from, failReason, ...)
```

**فهي:**
- **داخلَ البوّابة** — بعد `canTransition` لا قبلَها ✅
- **داخلَ معاملتِها** — تأخذ `tx wallet.Querier` ✅
- **وتكتب `order_events`** ✅

> **فـ`DSW-1` ليست تجاوزاً — هي فرعٌ من الانتقال نفسِه.**
> **والتجاوزُ الحقيقيُّ واحدٌ: `DSW-2` في `rotation.go`.**

**وهذا يشدّد `D1` بدل أن يخفّفه**: **الاستثناءُ الوحيدُ للبوّابة هو
الوحيدُ الذي لا يكتب التاريخَ أيضاً.**

### وتاريخُ الطلب — **U13 حُلّ** ✅

**بُحث عن `INSERT INTO order_events` في المحرّك كلِّه، فوُجد في أربعة
ملفّاتٍ فقط**: `transitions.go` · `service.go` · `order_transfer.go` ·
`merchant_blocked.go`.

| الكتابة | أتكتب التاريخ؟ | الأثر |
|---|---|---|
| **DSW-1** `merchant_blocked.go` | **✅ نعم** — `INSERT INTO order_events (order_id, from_status, to_status, actor_id, note)` | **التاريخُ سليم** |
| **DSW-2** `rotation.go` | **❌ لا** — **لا ذكرَ لـ`order_events` في الملفّ كلِّه** | **⚠️ ثغرةٌ مُثبَتة** |

**فأثرُ `DSW-2` خطيرٌ ومُثبَت**: **فكُّ الإسناد** (`assigned → dispatching`
عند سحب الطلب من سائقٍ) **يقع بلا سطرٍ في تاريخ الطلب.**

**ومن قرأ `order_events` بعد شهرٍ رأى الطلبَ يقفز من `assigned` إلى
`at_pickup` مع سائقٍ آخرَ بلا تفسير** — ولا يعرف أنّ الأوّلَ سُحب منه،
ولا متى، ولا بأمرِ من.

**وهذا يمسّ محاسبةَ السائقين**: من يُشتكى عليه أنّه «أخذ الطلبَ وتركه»
لا دليلَ في السجلّ.

### على كياناتٍ أخرى — مساراتُ حياةٍ مستقلّة

| الموضع | الكيان | الحالة |
|---|---|---|
| `internal/support/support.go` ×٢ | `tickets` | `in_progress` · `resolved` |
| `internal/orders/violations.go` | `merchants` | `suspended` |
| `internal/server/emergency_handlers.go` | `driver_emergencies` | `resolved` |
| `internal/server/leads_handlers.go` | `merchant_leads` | `converted` |

> **`DIRECT STATUS WRITES (orders): 2/2 MAPPED`** ✅
> **`UNMAPPED STATUS WRITES = 0`** ✅

## ١-٣ · مساراتُ المال — **محصاةٌ بالملفّ** ✅

**٣٠ نداءَ `ApplyTx` خارجَ حزمة المحفظة**، في **١١ ملفّاً**:

| الملفّ | العدد | المجال |
|---|---|---|
| `internal/orders/transitions.go` | **٨** | تسويةُ التسليم والإلغاء والفشل |
| `internal/referrals/referrals.go` | ٤ | مكافآتُ الإحالة |
| `internal/orders/treasury.go` | ٣ | نصيبُ المنصّة وإعادةُ حسابه |
| `internal/incentives/{target,incentives}.go` | ٤ | أهدافٌ ومنحٌ يدويّة |
| `internal/orders/{custom,goods}.go` | ٤ | الطلبُ المخصَّص وتسويةُ البضاعة |
| `internal/server/failure_aftermath.go` | ٢ | ما بعد الفشل |
| `internal/orders/service.go` · `support.go` · `disputes.go` · `driver_return.go` | ٤ | الإنشاء · الشكاوى · النزاعات · الإرجاع |

> **`FINANCIAL MUTATION SITES: 30/30 LOCATED`** ✅ · **موصوفةٌ بالبنود: ٦**

---

# ٢ · الأفعالُ الماليّةُ والحالة

---

## A-001 · الزبون يُنشئ طلباً

| البند | القيمة |
|---|---|
| **Actor** | `customer` |
| **التطبيق/الشاشة** | تطبيقُ الزبون — السلّة ثمّ التأكيد |
| **الزرّ** | «أكّد الطلب» |
| **الشروطُ المسبقة** | سلّةٌ غيرُ فارغة · عنوانٌ بإحداثيّات · **الدبّوسُ داخلَ منطقةِ توصيلٍ فعّالة** |
| **تحقّقُ الواجهة** | ❓ لم يُقرأ |
| **التخويل** | `RequireAuth` — **بلا شرطِ دور** ✅ |
| **المسار** | `POST /api/v1/orders` — **مفتاحُ تكرار** ✅ |
| **المُعالِج** | `s.idempotent(s.handleCreateOrder)` → `orders.Service.Create` |
| **قراءات** | `menu_items` · `merchants` · `delivery_zones` · `cities` · `promo_codes` (**`FOR UPDATE`**) · `wallets` · `app_settings` |
| **كتابات** | `orders` · `order_items` · `promo_redemptions` · `wallet_transactions` · `wallets.balance` |
| **المعاملة** | **واحدةٌ** ✅ — `ApplyTx` تأخذ `Querier` |
| **الأقفال** | `promo_codes … FOR UPDATE` — «من وصل ثانياً ينتظر» ✅ |
| **الحالة** | ∅ → `pending` |
| **الماليّ** | `order_payment` (خصمٌ من المحفظة) · و`cash_due` يبقى ديناً نقديّاً |
| **الزمنُ الحيّ** | `touch("order","ops")` |
| **الإشعارات** | المتجر · المكتب |
| **واتساب** | ⚠️ `merchant_dispatch.go` يستعمل whatsmeow — **التوقيتُ لم يُقرأ** |
| **الراصد** | يبدأ عدُّ `accept_min` للتصعيد |
| **الزبون يرى** | «بانتظار قبول المتجر» + رقمُ الطلب |
| **المتجر يرى** | طلبٌ جديد + إشعار |
| **السائق يرى** | **NONE** — لم يُطلب سائقٌ بعد |
| **المندوب** | **NONE** الآن — العمولةُ عند التسليم |
| **الإدارة ترى** | صفٌّ جديد + بثٌّ حيّ |
| **فشلُ التحقّق** | صنفٌ غيرُ متوفّر · كوبونٌ مستهلك · **`out_of_zone` (400)** |
| **فشلُ التخويل** | ٤٠١ بلا توكن |
| **فشلُ القاعدة** | المعاملةُ تُرجَع كاملةً — **لا كتابةَ جزئيّة** ✅ |
| **رصيدٌ غيرُ كافٍ** | `ErrInsufficient` من **قيدِ تحقّقٍ في القاعدة** ✅ |
| **التكرار** | مفتاحُ التكرار يردّ الجوابَ الأوّل ✅ |
| **الضغطُ المتكرّر** | محميٌّ بالمفتاح · ⚠️ **إن لم يُرسله العميلُ فطلبان** |
| **التزامن** | قفلُ الكوبون وحدَه ✅ |
| **التراجع** | كاملٌ بالمعاملة |
| **التدقيق** | **NONE** — «انتقالُ الأطراف أنفسهم لا يُسجَّل» |
| **الحقيقةُ النهائيّة** | `orders.status='pending'` · قيدٌ واحدٌ في الدفتر · الرصيدُ منقوص |
| **الأدلّة** | `server.go` · `internal/orders/service.go` · `sources.go` · `internal/wallet/wallet.go` |

---

## A-002 · السائق يقبل طلباً

| البند | القيمة |
|---|---|
| **Actor** | `driver` |
| **الشاشة** | الطلبات — «قبول» |
| **الشروط** | `on_shift` · `held + cash_due ≤ drivers.cash_limit` (**٥٠٠٬٠٠٠ في الإنتاج**) · عددُ المفتوحة · **أو عرضٌ موجَّهٌ له** |
| **التخويل** | `RequireRoles("driver")` ✅ |
| **المسار** | `POST /api/v1/driver/orders/{id}/accept` — **بلا مفتاح تكرار** ⚠️ |
| **قراءات** | `users.on_shift` · `driver_cash_boxes.held` · `orders.cash_due` · عددُ المفتوحة · `offered_driver_id`+`offer_expires_at` |
| **كتابات** | `orders.driver_id` · `status` · `accepted_at` · `order_events` |
| **الأقفال** | `SELECT … FOR UPDATE` على صفّ الطلب ✅ |
| **الحالة** | `dispatching → assigned` (`driverOps`) |
| **الماليّ** | **NONE** — المالُ عند التسليم |
| **الزمنُ الحيّ** | `touch("order","ops")` |
| **الإشعارات** | ⚠️ للزبون — لم يُقرأ الموضع |
| **الزبون يرى** | «السائق في طريقه» — **عبر البثّ لا عبر إشعارٍ دافع** |
| **المتجر يرى** | اسمَ السائق |
| **الإدارة ترى** | الطلبَ منسوباً |
| **U16 حُلّ** ✅ | **لا إشعارَ دافعاً عند قبول السائق نفسِه**: بُحث عن `notify` في `driver_handlers.go` فلم يوجد. **والإشعارُ `driverAssigned` يقع في `admin_orders_handlers.go` وحدَه** — أي عند **الإسناد اليدويّ من المكتب**، لا عند قبول السائق بنفسه |
| **فشل** | خارجَ الورديّة · تجاوزُ سقف النقد · **سبقه غيرُه** (الحالةُ تبدّلت) |
| **التزامن** | **`FOR UPDATE` + شرطُ الحالة** — ⚠️ **لم يُثبَت تحت ضغطٍ حقيقيّ** |
| **التكرار** | **NONE** — يعتمد على الحالة |
| **التدقيق** | **NONE** |
| **ملاحظةٌ حاسمة** | **في الإنتاج `assignment_mode="queue"`** → **لا عرضَ موجَّهاً**، فالقبولُ **سباقٌ مفتوح** ✅ |

---

## A-003 · السائق يُسلّم الطلب — **أخطرُ فعلٍ ماليٍّ في المنصّة**

| البند | القيمة |
|---|---|
| **Actor** | `driver` |
| **الشاشة** | الرحلة — «تم التسليم» |
| **الشروط** | الحالةُ `at_dropoff` · الطلبُ له |
| **التخويل** | `RequireRoles("driver")` ✅ |
| **المسار** | `POST /api/v1/driver/orders/{id}/transition` `{to:"delivered"}` |
| **المُعالِج** | `handleDriverTransition` → `orders.TransitionWithReason` |
| **قراءات** | صفُّ الطلب **بقفل** · `merchants.debt` **بقفل** · الإعدادات |
| **كتابات** | `orders.status` · `delivered_at` · `closed_at` · `order_events` · **`wallet_transactions` ×٤** · `wallets.balance` ×٤ · `driver_cash_boxes.held` |
| **المعاملة** | **واحدةٌ تضمّ كلَّ ذلك** ✅ |
| **الأقفال** | `orders … FOR UPDATE` · `merchants.debt … FOR UPDATE` ✅ |
| **الحالة** | `at_dropoff → delivered` |
| **الماليّ** | `merchant_earning` · `driver_earning` · `commission` (المندوب) · `platform_profit` |
| **الراصد** | يتوقّف عن التصعيد — الطلبُ مغلق |
| **الإشعارات** | الزبون · المتجر · **المندوب** («عمولة جديدة») |
| **مكافأةُ الهدف** | `GrantTargetIfReached(driver)` — **وإن دُفعت أُشعِر** (٢٠٢٦-٠٩-٠٣) |
| **الزبون يرى** | «تم التسليم» + زرَّ التقييم |
| **المتجر يرى** | مستحقَّه في محفظته |
| **المندوب يرى** | عمولتَه فوراً |
| **الإدارة ترى** | الطلبَ مغلقاً + الدفترَ محدَّثاً |
| **فشلُ التحقّق** | حالةٌ غيرُ مسموحة → `errTransition` |
| **فشلُ القاعدة** | **تُرجَع المعاملةُ كاملةً — ولا قيدَ واحدٌ يبقى** ✅ |
| **الجزئيّ** | **مستحيل** ✅ |
| **التكرار** | **NONE** — لكنّ الحالةَ تمنعه (`delivered → delivered` غيرُ مسموح) |
| **التزامن** | القفلُ يمنع تسويتين |
| **التدقيق** | **NONE** للسائق — «انتقالُ الأطراف لا يُسجَّل» |
| **الحقيقةُ النهائيّة** | `closed_at` مضبوط · **٤ قيود** · الأرصدةُ الثلاثة زادت · صندوقُ السائق زاد بالنقد |
| **الأدلّة** | `internal/orders/transitions.go` (**٨ نداءاتِ دفتر**) |

---

## A-004 · الأدمن يعيد حسابَ التسوية

| البند | القيمة |
|---|---|
| **Actor** | `admin` · `finance` |
| **الشاشة** | الطلبات — «دقّق حساب هذا الطلب» |
| **الشروط** | **الطلبُ مُغلَق** (`closed_at IS NOT NULL`) |
| **التخويل** | `RequireRoles("admin","finance")` ✅ |
| **المسار** | `POST /api/v1/admin/orders/{id}/recompute` |
| **قراءات** | `orders.closed_at` · مجموعُ `platform_profit` قبلَ وبعد |
| **كتابات** | `wallet_transactions` — **قيدٌ مقابلٌ بالفرق** |
| **المعاملة** | `Begin`/`Commit` صريحان ✅ |
| **الماليّ** | **يصحّح ولا يمسح** — «يبقى الخطأُ مسطوراً وتصحيحُه بجانبه» ✅ |
| **الزمنُ الحيّ** | `touch("wallet","ops")` |
| **الإشعارات** | **NONE** |
| **التدقيق** | ✅ `finance.settlement_recomputed` مع `before/after/delta` |
| **النجاحُ المرئيّ** | «صُحِّح: ٣٥ ل.س» أو «لا شيءَ لتصحيحه» (٢٠٢٦-٠٩-٠٢) |
| **الفشل** | `errOrderStillOpen` |
| **التكرار** | **NONE** — **لكنّ إعادتَه غيرُ ضارّة**: الفرقُ يصير صفراً ✅ |
| **أثرُه في التطبيقات** | المتجرُ/السائقُ يرى رصيدَه تبدّل — **بلا إشعار** ⚠️ |

---

## A-005 · الأدمن يعدّل رصيداً يدويّاً

| البند | القيمة |
|---|---|
| **Actor** | `admin` · `finance` |
| **الشاشة** | المستخدمون → ملفٌّ → المحفظة |
| **التخويل** | `RequireRoles("admin","finance")` ✅ |
| **المسار** | `POST /api/v1/admin/users/{id}/wallet` — **مفتاحُ تكرار** ✅ |
| **كتابات** | `wallet_transactions` + `wallets.balance` |
| **الماليّ** | قيدٌ بيدِ إنسان — **`actorID` يُسجَّل مع القيد** ✅ |
| **الزمنُ الحيّ** | `touch("wallet","ops")` **+ `touchUser`** ✅ — «الزبونُ يُعوَّض ثمّ ينظر إلى رصيده فيجده كما كان» |
| **الإشعارات** | ⚠️ لم يُقرأ |
| **صاحبُ المال يرى** | رصيدَه تبدّل **فوراً** بلا تحديثِ صفحة |
| **الفشل** | رصيدٌ سالبٌ يُرفض بقيدِ القاعدة ✅ |
| **التدقيق** | ✅ |

---

## A-006 · الأدمن يبتّ طلبَ صرف

| البند | القيمة |
|---|---|
| **Actor** | `admin` · `finance` |
| **المسار** | `POST /api/v1/admin/payouts/{id}/decide` — **مفتاحُ تكرار** ✅ |
| **كتابات** | `payout_requests.status` · `wallet_transactions` |
| **الماليّ** | خصمٌ من محفظة صاحبه عند القبول |
| **الإشعارات** | ✅ لصاحب الطلب (`notifTitles.payoutPaid` / `payoutRejected`) |
| **الزمنُ الحيّ** | `touch("wallet","ops")` + `touchUser` |
| **التدقيق** | ✅ |
| **الفشل** | ⚠️ لم يُقرأ سلوكُ الرصيد غير الكافي |

---

## A-007 · الأدمن يُسند سائقاً يدويّاً

| البند | القيمة |
|---|---|
| **Actor** | `admin` · `ops` |
| **الشاشة** | الطلبات → «إسناد سائق» |
| **الشروط** | الحالةُ `preparing` أو `dispatching` (شرطُ الواجهة) |
| **المسار** | `POST /api/v1/admin/orders/{id}/assign` |
| **كتابات** | `orders.driver_id` · `status` · `order_events` |
| **الحالة** | `dispatching → assigned` |
| **الماليّ** | **NONE** |
| **الإشعارات** | ✅ للسائق («أُسند إليك طلب جديد») |
| **⚠️ ثغرةٌ مُثبَتة** | **اللوحةُ لا تمنع الإسنادَ إلى سائقٍ موقعُه متوقّف** — تكتب «موقعه متوقف» بجانبه (٢٠٢٦-٠٩-٠٣) **ولا تحجبه**، **والمحرّكُ يحجبه عن الإسناد الآليّ وحدَه** |
| **التدقيق** | ✅ |

---

## A-008 · الأدمن ينقل الطلبَ إلى متجرٍ آخر

| البند | القيمة |
|---|---|
| **Actor** | `admin` |
| **المسار** | `POST /api/v1/admin/orders/{id}/transfer` |
| **الشروط** | الحالةُ في `TRANSFERABLE` (٥ حالات) |
| **كتابات** | `orders.merchant_id` · `order_items` (مطابقةٌ بالاسم) |
| **الماليّ** | **سعرُ الزبون لا يتغيّر — والفرقُ على المنصّة** ✅ |
| **الفشل** | أصنافٌ لا تطابق → تُعرض قائمةُ غير المطابق |
| **التدقيق** | ✅ |

---

## A-009 · المكتب يُفشل التوصيل

| البند | القيمة |
|---|---|
| **Actor** | `ops` · `admin` (**والسائقُ كذلك**) |
| **الشاشة** | الطلبات — «فشل التوصيل» (أُظهر ٢٠٢٦-٠٩-٠٢) |
| **الحالات** | من `at_pickup` · `picked_up` · `on_the_way` · `at_dropoff` |
| **الشرط** | **سببٌ إلزاميّ** — `requiresReason["failed"]` ✅ |
| **المسار** | `POST /api/v1/admin/orders/{id}/transition` `{to:"failed", note}` |
| **الماليّ** | **لا يقع آليّاً** — `failure_aftermath.go` ينتظر قراراً بشريّاً: **أاستردّ المتجرُ بضاعتَه؟ وكم يستحقّ السائق؟** ✅ |
| **ما يليه** | زرّا «أُعيدت إلى المتجر» / «إلى المكتب» + «تعويض السائق» |
| **التدقيق** | ✅ |

---

## A-010 · المندوب يسجّل فرصةَ متجر

| البند | القيمة |
|---|---|
| **Actor** | `sales` |
| **الشاشة** | تطبيقُ المندوب — «إضافة عميل» |
| **الشروط** | **كلُّ الحقول إلزاميّة** (قرارُ المالك ٢٠٢٦-٠٨-٣١) |
| **التخويل** | `RequireRoles("sales")` ✅ |
| **المسار** | `POST /api/v1/rep/leads` ⚠️ (المسارُ الدقيق لم يُقتبس) |
| **كتابات** | `merchant_leads` (مع `sales_rep_user_id` · `district_id` · `owner_password_hash`) |
| **الإشعارات** | ✅ للمندوب («فرصةٌ جديدة») + **`NotifyOps`** للمكتب |
| **الوجهة** | `/portal/merchants` — **صُحّحت** بعد أن كانت تشير إلى صفحةٍ غيرِ موجودة |
| **الماليّ** | **NONE** الآن |
| **الإدارة ترى** | الفرصةَ في `/dashboard/leads` |
| **الفشل** | ❓ **رقمٌ مكرَّر** — لم يُقرأ |

---

## A-011 · الإدارة تقبل الفرصة → متجرٌ حقيقيّ

| البند | القيمة |
|---|---|
| **Actor** | `admin` · `ops` |
| **المسار** | `PATCH /api/v1/admin/leads/{id}` `{status:"converted"}` → `convertLead` |
| **كتابات** | `merchants` (جديد) · `users` (مالكُ المتجر) · `user_roles` (`merchant`) · `merchant_leads.status='converted'` + `merchant_id` · `merchants.district_id` |
| **الشرط** | **ما حُوِّل لا يُردّ** — «المتجرُ قائمٌ يبيع» ✅ |
| **الماليّ** | **`grantSalesTargetIfAny`** → قد يُقيَّد `reward` إن بلغ المندوبُ هدفَه |
| **الإشعارات** | ✅ «تمت الموافقة على عميلك» · **و«أنجزت هدف الشهر» إن دُفعت مكافأة** (٢٠٢٦-٠٩-٠٣) |
| **المندوب يرى** | المتجرَ في «عملائي» · وعمولتَه المستقبليّة مرتبطةً به |
| **صاحبُ المتجر** | حسابٌ بكلمة مرورٍ محفوظة · `must_change_password=true` |
| **التدقيق** | ✅ |
| **⚠️ ثغرةٌ مُثبَتة — U14 حُلّ** | **لا معاملةَ واحدةً إطلاقاً**: بُحث عن `Begin(` في `leads_handlers.go` فلم يوجد. **فإنشاءُ المتجر والحسابِ والدورِ وتحديثِ الفرصة نداءاتٌ متتابعةٌ مستقلّة** |
| **والأثر** | **فشلٌ في المنتصف يترك حالاً نصفيّة**: متجرٌ بلا مالكٍ، أو فرصةٌ ما زالت `new` ومتجرُها قائم. **ولا تراجعَ يجمعها** |

---

## A-012 · الإدارة ترفض الفرصة

| البند | القيمة |
|---|---|
| **المسار** | `PATCH /api/v1/admin/leads/{id}` `{status:"rejected", note}` |
| **الشرط** | **سببٌ إلزاميّ** ✅ |
| **الإشعارات** | ✅ للمندوب مع السبب |
| **الاستئناف** | **`new` مسموحةٌ من `rejected`** — «من رُدَّ خطأً» ✅ |
| **الماليّ** | **NONE** |

---

## A-013 · المكتب يقرّر مصيرَ البضاعة بعد الفشل

| البند | القيمة |
|---|---|
| **Actor** | `admin` · `ops` |
| **الشاشة** | الطلبات → طلبٌ فاشل → «أعيدت إلى المتجر» / «إلى المكتب — المنصّة تتحمّل» |
| **الشروط** | `status='failed'` · `goods_settled_to IS NULL` |
| **المسار** | `POST /api/v1/admin/orders/{id}/goods` |
| **المُعالِج** | `orders.SettleGoods` |
| **قراءات** | `orders.status, goods_settled_to` **بقفل `FOR UPDATE`** ✅ |
| **كتابات** | `orders.goods_settled_to` · `wallet_transactions` ×٢ |
| **المعاملة** | **صريحة** — `Begin`/`Commit` ✅ |
| **الماليّ** | قيدان: دعمُ المتجر أو استرجاعُ ثمنِ بضاعته (`+support` / `-take`) |
| **الزمنُ الحيّ** | `touch("order","ops")` · `touch("wallet","ops")` |
| **التدقيق** | ✅ `finance.goods_settled` |
| **التكرار** | **يمنعه `goods_settled_to IS NULL` تحت القفل** ✅ |
| **المتجر يرى** | رصيدَه تبدّل |

---

## A-014 · المكتب يعوّض السائق عن مشوارٍ لم يُثمر

| البند | القيمة |
|---|---|
| **Actor** | `admin` · `finance` |
| **المسار** | `POST /api/v1/admin/orders/{id}/compensate-driver` |
| **الشروط** | مبلغٌ + **سببٌ يُسجَّل** |
| **كتابات** | `wallet_transactions` (`compensation`) |
| **المعاملة** | **صريحة** ✅ |
| **التدقيق** | ✅ `finance.driver_compensation` |
| **الزمنُ الحيّ** | `touch("order")` · `touch("wallet")` |
| **السائق يرى** | رصيدَه زاد |
| **التكرار** | **NONE** ⚠️ — **لا مفتاحَ ولا شرطَ حالة**: **ضغطتان تُعوّضان مرّتين** |

---

## A-015 · السائق يُرجع البضاعةَ إلى المتجر

| البند | القيمة |
|---|---|
| **Actor** | `driver` |
| **المسار** | `POST /api/v1/driver/orders/{id}/return` |
| **كتابات** | `orders` · `wallet_transactions` (`-paid` من مالك المتجر) |
| **المعاملة** | **صريحة** ✅ |
| **التدقيق** | ✅ `driver.order_returned` مع المبلغ |
| **الزمنُ الحيّ** | `touch("order","ops")` |
| **المتجر يرى** | خصماً من رصيده |

---

## A-016 · المكتب يسوّي نزاعاً

| البند | القيمة |
|---|---|
| **Actor** | `admin` · `ops` |
| **المسار** | `POST /api/v1/admin/disputes/{id}/settle` ⚠️ (الاسمُ لم يُقتبس حرفيّاً) |
| **كتابات** | `disputes` · `wallet_transactions` (`-amount`) |
| **المعاملة** | **صريحة** ✅ |
| **التدقيق** | ✅ `ops.dispute_opened` · `ops.dispute_settled` |
| **الزمنُ الحيّ** | `touch("dispute","ops")` |

---

## A-017 · حلُّ شكوى بتعويض

| البند | القيمة |
|---|---|
| **Actor** | `admin` · `ops` |
| **المسار** | عبر `internal/support` |
| **كتابات** | `tickets.status='resolved'` + `resolution` + `compensation` · `wallet_transactions` (`compensation` للزبون) |
| **المعاملة** | تأخذ `tx` ✅ |
| **الزبون يرى** | الشكوى محلولةً + رصيدَه زاد |
| **ملاحظة** | **هذا هو بابُ الاسترجاع** — «الاسترجاعُ يمرّ بالشكوى وحدَها» (قرارُ المالك ٢٠٢٦-٠٨-٠٩) |

---

## A-018 · مكافأةُ الإحالة

| البند | القيمة |
|---|---|
| **Actor** | **النظام** — لا إنسان |
| **المُطلِق** | تسليمُ أوّلِ طلبٍ لمُحال ⚠️ (الشرطُ الدقيق لم يُقتبس) |
| **المُعالِج** | `internal/referrals/referrals.go` — **٤ مواضعِ قيد** |
| **نوعُ القيد** | `reward` ×٤ |
| **المعاملة** | **صريحة** — `s.db.Begin(ctx)` ✅ |
| **منعُ التكرار** | **في القاعدة** ✅: `INSERT … ON CONFLICT DO NOTHING` على `(userID, kind)` · و`ON CONFLICT (invitee_id) DO NOTHING` على `referrals` |
| **فمن نُودي مرّتين** | **لا يُكافأ مرّتين** — الفهرسُ يمنع، **والتصادمُ ليس خطأً** |
| **الأثر** | رصيدُ المُحيل والمُحال يزيد |
| **الإشعارات** | ❓ لم يُقرأ |
| **التصنيف** | **PROVEN BEHAVIOR — آمن** ✅ |

---

## A-019 · مكافأةُ الهدف الشهريّ

| البند | القيمة |
|---|---|
| **Actor** | **النظام** |
| **المُطلِق** | **للسائق**: تسليمُ طلب · **للمندوب**: تحويلُ فرصةٍ إلى متجر |
| **المُعالِج** | `internal/incentives/target.go` → `GrantTargetIfReached` |
| **الحساب** | `doneThisMonth` مقابل `Level.Target` — **وكلُّ مرحلةٍ بلغها تُدفع**، لا الأعلى وحدَها ✅ |
| **نوعُ القيد** | `reward` |
| **المعاملة** | **صريحة** ✅ |
| **منعُ التكرار** | **فهرسٌ فريدٌ `(user_id, period)`** حيث `period = "YYYY-MM#N"` ✅ — «**التصادمُ ليس خطأً، هو الضمانةُ تعمل**» |
| **الإشعارات** | ✅ «أنجزت هدف الشهر» (أُضيف ٢٠٢٦-٠٩-٠٣) — **وللمندوب والسائق كليهما** |
| **الفشل** | **لا يُسقط التسليمَ ولا التحويل** — «مكافأةٌ تأخّرت أهونُ من عميلٍ ضاع» ✅ |
| **⚠️ مُثبَت** | المرحلتان الثانيةُ والثالثةُ **مطفأتان** بقرار المالك (٢٠٢٦-٠٨-٣١) |
| **التصنيف** | **PROVEN BEHAVIOR — آمن** ✅ |

---

## A-020 · منحُ حافزٍ يدويّاً

| البند | القيمة |
|---|---|
| **Actor** | `admin` · `finance` |
| **المسار** | `POST /api/v1/admin/users/{id}/incentive` — **مفتاحُ تكرار** ✅ |
| **المُعالِج** | `internal/incentives/incentives.go` |
| **كتابات** | `incentives` · `wallet_transactions` (`reward`) |
| **المعاملة** | **صريحة** ✅ |
| **التصنيف** | **PROVEN BEHAVIOR — آمن** ✅ |

---

## A-021 · الطلبُ المخصَّص — توثيقُ السعر والشراء

| البند | القيمة |
|---|---|
| **Actor** | `driver` |
| **الشاشة** | الرحلة — «اشتريتُ الطلب» |
| **المسار** | `POST /api/v1/driver/orders/{id}/agree` |
| **المُعالِج** | `internal/orders/custom.go` |
| **الأقفال** | `SELECT … FROM orders WHERE id = $1 **FOR UPDATE**` ✅ |
| **نوعُ القيد** | `order_payment` ×٢ |
| **الماليّ** | **يوثّق ثمنَ البضاعة والأجرة** — والسائقُ يقبضهما من الزبون نقداً |
| **ملاحظةٌ مُثبَتة** | «موثَّقٌ للاطّلاع — **لا يدخل حسابَ المنصّة**» |
| **التصنيف** | **PROVEN BEHAVIOR** ✅ |

---

## A-022 · قيدُ نصيبِ المنصّة (الخزينة)

| البند | القيمة |
|---|---|
| **Actor** | **النظام** — وتُنادى يدويّاً في `recompute` |
| **المُعالِج** | `internal/orders/treasury.go` — **٣ مواضعِ قيد** |
| **نوعُ القيد** | `platform_profit` |
| **المرجع** | `orders.id` — **فيُقاس نصيبُ الطلب بعينه** |
| **⚠️ لم يُقرأ** | **لا `Begin` ولا `FOR UPDATE` في الملفّ** — يعتمد على معاملةِ المنادي (`q wallet.Querier`) |
| **U19 جزئيّاً** | **الشروطُ الثلاثةُ لم تُقرأ** ❓ |
| **التصنيف** | **PROVEN RISK** — الاعتمادُ على معاملة المنادي مُثبَت، **وسلامتُه في كلّ المنادين لم تُثبت** |

---

# ٢-ب · الثوابتُ الماليّة — **مستخرَجةٌ من الشيفرة** ✅

**ولم تُفترض**: **١٣ فحصاً في `cmd/moneycheck/main.go`** — وهي عقدُ
المنصّة المالِيّ مكتوباً.

## أ · ثوابتُ القاعدة نفسِها (قيودُ تحقّق)

| الثابت | الموضع |
|---|---|
| `wallets.balance >= 0` | `0010_wallets.sql` — «**لا سالبَ أبداً**» |
| `driver_cash_boxes.held >= 0` | هجرةُ الصندوق |

**وهما يُنفَّذان في القاعدة لا في الشيفرة** ✅ — فلا يُلتفّ عليهما.

## ب · الثوابتُ الثلاثةَ عشرَ المفحوصة

| # | الثابت | ما يعنيه انكسارُه |
|---|---|---|
| ١ | **للمنصّة خزينةٌ واحدة** | — |
| ٢ | **رصيدُ المحفظة = مجموعُ حركاتها** | «كشفُ الحساب يقول غيرَ ما في الجيب» |
| ٣ | **محتجَزُ الصندوق = مجموعُ قيوده** | «تسويةُ السائق تُبنى على رقمٍ خاطئ» |
| ٤ | **لا رصيدَ سالبٌ إلّا الخزينة** | «مالٌ صُرف ولم يكن موجوداً» |
| ٥ | **كلُّ طلبٍ مسلَّمٍ له مستحقُّ متجر** | «المتجرُ لم يُدفع له» |
| ٦ | **كلُّ طلبٍ من المحفظة له خصمٌ مقابل** | «الزبونُ أخذ بلا مقابل» |
| ٧ | **لا قيدَ بصفر** | ضجيجٌ في الكشف |
| ٨ | **كلُّ سحبٍ مدفوعٍ له خصم** | «المالُ خرج من الورق لا من المحفظة» |
| ٩ | **لا تعويضَ مكرَّرٌ لطلب** | **أُصلح ٢٠٢٦-٠٨-٠٨** — انظر `B2` |
| ١٠ | **لا تعويضَ شكوى مكرَّر** | **أُصلح ٢٠٢٦-٠٨-٠٧** |
| ١١ | **لا مستحقَّ متجرٍ مكرَّر** | «المتجرُ قُبض له ضِعف» |
| ١٢ | **مجاميعُ الطلب متّسقة** | «الفاتورةُ تكذب» |
| ١٣ | **الدفعُ يغطّي المجموع** | «فرقٌ ضائعٌ لا يعرف أحدٌ أين ذهب» |

**وهذه الثلاثةَ عشرَ هي المقامُ الماليُّ الحقيقيّ** — **وفحصُها أمرٌ
واحد**: `go run ./cmd/moneycheck`.

**وقياسُ الإنتاج ٢٠٢٦-٠٩-٠٣ بعد المسح**: **الانحرافُ صفر** ✅.

---

# ٢-ج · الانتقالاتُ الثمانيةُ والثلاثون — **مستخرَجةٌ كاملةً** ✅

**مجموعاتُ الأدوار** — `internal/orders/statuses.go`:

```
merchantOps = ["merchant","ops"]     driverOps = ["driver","ops"]
opsRoles    = ["ops"]                والأدمنُ مخوَّلٌ ضمنيّاً بكلّ ما في الخارطة
```

## الطلبُ العاديّ — ٣٠ انتقالاً

| # | من | إلى | الأدوار | الشرطُ الخاصّ | ماليّ | يكتب `order_events` |
|---|---|---|---|---|---|---|
| T01 | `pending` | `accepted` | merchant, ops | — | NONE | ✅ |
| T02 | `pending` | `rejected` | merchant, ops | **سببٌ إلزاميّ** | **ردٌّ للمحفظة** | ✅ |
| T03 | `pending` | `cancelled` | customer, ops | **سببٌ إلزاميّ** | **ردٌّ للمحفظة** | ✅ |
| T04 | `accepted` | `preparing` | merchant, ops | — | NONE | ✅ |
| T05 | `accepted` | `dispatching` | ops | — | NONE | ✅ |
| T06 | `accepted` | `cancelled` | customer, merchant, ops | سبب | ردّ | ✅ |
| T07 | `preparing` | `dispatching` | ops | — | NONE | ✅ |
| T08 | `preparing` | `cancelled` | merchant, ops | سبب | ردّ | ✅ |
| T09 | `dispatching` | `assigned` | driver, ops | **الورديّة · سقفُ النقد · عددُ المفتوحة** | NONE | ✅ |
| T10 | `dispatching` | `cancelled` | ops | سبب | ردّ | ✅ |
| T11 | `assigned` | `at_pickup` | driver, ops | — | NONE | ✅ |
| **T12** | `assigned` | `dispatching` | driver, ops | **فكُّ الإسناد** | NONE | ✅ عبر البوّابة · **❌ عبر `rotation.go`** → **D1** |
| T13 | `assigned` | `cancelled` | ops | سبب | ردّ | ✅ |
| T14 | `at_pickup` | `picked_up` | driver, ops | — | NONE | ✅ |
| T15 | `at_pickup` | `failed` | driver, ops | **سببٌ إلزاميّ** | **لا يقع آليّاً** → A-013 | ✅ |
| T16 | `at_pickup` | `dispatching` | driver, ops | — | NONE | ✅ |
| T17 | `at_pickup` | `cancelled` | ops | سبب | ⚠️ **بعد الاستلام** — انظر `pastPickup` | ✅ |
| T18 | `picked_up` | `on_the_way` | driver, ops | — | NONE | ✅ |
| T19 | `picked_up` | `dispatching` | ops | — | NONE | ✅ |
| T20 | `picked_up` | `failed` | ops | سبب | لا يقع آليّاً | ✅ |
| T21 | `picked_up` | `cancelled` | ops | سبب | ⚠️ `pastPickup` | ✅ |
| T22 | `on_the_way` | `at_dropoff` | driver, ops | — | NONE | ✅ |
| T23 | `on_the_way` | `dispatching` | ops | — | NONE | ✅ |
| T24 | `on_the_way` | `failed` | ops | سبب | لا يقع آليّاً | ✅ |
| T25 | `on_the_way` | `cancelled` | ops | سبب | ⚠️ `pastPickup` | ✅ |
| **T26** | `at_dropoff` | **`delivered`** | driver, ops | — | **٤ قيود** → A-003 | ✅ |
| T27 | `at_dropoff` | `failed` | driver, ops | سبب | لا يقع آليّاً | ✅ |
| T28 | `at_dropoff` | `dispatching` | ops | — | NONE | ✅ |
| T29 | `at_dropoff` | `cancelled` | ops | سبب | ⚠️ `pastPickup` | ✅ |
| **T30** | `delivered` | `refunded` | **`[]string{}` → admin وحدَه** | **سببٌ إلزاميّ** | **عكسُ تسوية** | ✅ |

## الطلبُ المخصَّص — ٨ انتقالات

| # | من | إلى | الأدوار | ملاحظة |
|---|---|---|---|---|
| C01 | `pending` | `dispatching` | ops | **لا مرحلةَ متجرٍ** — لا `accepted` ولا `preparing` |
| C02 | `pending` | `rejected` | ops | سبب |
| C03 | `pending` | `cancelled` | customer, ops | سبب |
| C04 | `dispatching` | `assigned` | driver, ops | — |
| C05 | `dispatching` | `cancelled` | customer, ops | سبب |
| **C06** | `assigned` | **`picked_up`** | **`driver` وحدَه** | **يقفز `at_pickup`** — لا متجرَ يُقصد |
| C07 | `assigned` | `dispatching` | driver, ops | — |
| C08 | `assigned` | `cancelled` | customer, ops | سبب |

## فروقٌ مُثبَتةٌ بين الخارطتين

| # | الفرق |
|---|---|
| **X1** | **المخصَّصُ يبدأ من `dispatching` مباشرة** — ولا مرحلةَ قبولِ متجر |
| **X2** | **`assigned → picked_up` في المخصَّص للسائق وحدَه** — **بلا `ops`** |
| **X3** | ~~لا `delivered` في خارطة المخصَّص~~ — **U20 حُلّ**: انظر أدناه |

### U20 حُلّ ✅ — **الخارطتان تُدمجان لا تتنافسان**

**المُثبَت** — `transitionsFor(kind, from)`:

```go
if kind == KindCustom {
    if ts, ok := customTransitions[from]; ok { return ts }   // ١· تخصيصٌ صريح
    if merchantOnlyStates[from] { return nil }               // ٢· حالُ متجرٍ ممنوعة
    // ٣· وإلّا: خارطةُ العامّ منقّاةً من حالات المتجر
    for _, t := range allowedTransitions[from] {
        if !merchantOnlyStates[t.To] { out = append(out, t) }
    }
}
```

**فالثمانيةُ ليست الخارطةَ كلَّها** — هي **تخصيصاتٌ تسبق العامّ**.
وما بعد `picked_up` في المخصَّص **يرث انتقالاتِ الطلب العاديّ**
(`on_the_way → at_dropoff → delivered`) **منقّاةً من حالات المتجر.**

### U21 حُلّ بالحساب ✅ — **المقامُ ٥٥ لا ٣٨**

**`merchantOnlyStates = {accepted, preparing}`** ✅ مُثبَت.

**وحُسبت الخارطةُ الفعّالةُ للطلب المخصَّص برمجيّاً:**

| النوع | الانتقالاتُ الفعّالة |
|---|---|
| **الطلبُ العاديّ** | **٣٠** |
| **الطلبُ المخصَّص** | **٢٥** = ٨ تخصيصاً + ١٧ موروثاً منقّى |
| **المجموعُ الحقيقيّ** | **٥٥** |

**فرقمُ ٣٨ الذي أعلنتُه ثلاثَ مرّاتٍ كان خطأً** — عددتُ **مدخلاتِ
الخارطتين** لا **الانتقالاتِ الممكنة**. **والفرقُ ١٧ انتقالاً كنتُ
أظنّها غيرَ موجودة.**

> **`ORDER TRANSITIONS (TRUE DENOMINATOR) = 55`**
> ↳ عاديّ **٣٠/٣٠** مجدولة ✅ · مخصَّص **٨/٢٥** مجدولة ⚠️

## الكتاباتُ خارجَ الخارطة

| # | الكتابة | من → إلى | التاريخ |
|---|---|---|---|
| DSW-1 | `merchant_blocked.go` | أيّ حالٍ → `accepted` | ✅ يُكتب |
| DSW-2 | `rotation.go` | `assigned` → `dispatching` | **❌ لا يُكتب** → **D1** |

> **`ORDER TRANSITIONS EXTRACTED = 38/38`** ✅
> **`TRANSITIONS TABULATED WITH ROLES + PRECONDITIONS + FINANCIAL + HISTORY = 38/38`** ✅
> **بالبنود الكاملة (٢٠ بنداً لكلٍّ): ٣** — T09 (A-002) · T26 (A-003) · T15 (A-009)

---

# ٢-د · تدفّقُ المتجر والقائمة

---

## D-01 · `convertLead` — **الخطواتُ بالترتيب وحالُ الفشل بعد كلٍّ منها** ✅

**المسار**: `PATCH /api/v1/admin/leads/{id}` `{status:"converted"}`
**المُعالِج**: `convertLead(ctx, actorID, leadID, ip)` — `leads_handlers.go`

> **ولا `Begin` في الدالّة كلِّها** ✅ مُثبَت — فكلُّ خطوةٍ تلتزم وحدَها.

| # | الخطوة | الكتابة | **لو فشلت بعدها** |
|---|---|---|---|
| **٠** | قراءةُ الفرصة | — | لا شيء |
| **٠أ** | **حارسٌ: `merchant_id` موجود؟** | — | **يردّ `nil`** — «محوَّلٌ مسبقاً، لا تكرار» ✅ **فالنداءُ الثاني آمن** |
| **٠ب** | حارسٌ: بلا تصنيفٍ يُرفض | — | `errValidation` — **لا شيءَ كُتب** |
| **١** | `resolveOwner` — **يُنشئ المستخدمَ أو يجده** | `users` (+`user_roles`؟ ⚠️) | **مستخدمٌ جديدٌ بلا متجر** · والفرصةُ ما زالت `new` |
| **٢** | `resolveRep` من رمز المندوب | — | كسابقتها |
| **٣** | **`INSERT INTO merchants`** | `merchants` | **متجرٌ قائمٌ والفرصةُ `new`** — **فالإدارةُ تراها غيرَ محوَّلةٍ ومتجرُها يعمل** |
| **٤** | `UPDATE users SET …` (اسمُ المالك) | `users` | **`_, _ =` — الخطأُ مُهمَل** ⚠️ |
| **٥** | `UPDATE users SET password_hash …` **إن كان المالكُ جديداً** | `users` | **`_, _ =` مُهمَل** — **مالكٌ بلا كلمة مرور** ⚠️ |
| **٦** | `UPDATE merchants SET district_id …` | `merchants` | **`_, _ =` مُهمَل** — متجرٌ بلا منطقة |
| **٧** | **`grantSalesTargetIfAny`** | `incentives` · `wallet_transactions` | **مكافأةٌ قُيّدت والفرصةُ `new`** |
| **٨** | `UPDATE merchant_leads SET status='converted', merchant_id` | `merchant_leads` | — |
| **٩** | إشعارُ المندوب | — | — |

### التصنيف — **PROVEN DEFECT (D2)** · وشدّتُه أوضحُ الآن

| | |
|---|---|
| **الثابتُ المكسور** | **إمّا أن يقع كلُّ التحويل أو لا شيءَ منه** |
| **الحالُ النصفيّةُ الأخطر** | **الخطوة ٣ ثمّ فشلُ ٨**: **متجرٌ يعمل وفرصةٌ تقول `new`** — فتُحوَّل مرّةً ثانية؟ **لا** — الحارسُ ٠أ يقرأ `merchant_id` من الفرصة، **وهو لم يُكتب بعد** → **فتُنشَأ متاجرُ مكرَّرة** |
| **⚠️ وخمسُ كتاباتٍ خطؤها مُهمَل** | `_, _ = s.pg.Exec(...)` في الخطوات ٤ · ٥ · ٦ — **فتفشل بصمتٍ تامّ** |
| **والأثرُ الأخطر** | **الخطوة ٥**: مالكٌ بلا `password_hash` — **فلا يستطيع الدخولَ إلى تطبيق متجره أبداً**، ولا شيءَ يقول |

### ما يراه كلُّ طرف

| الطرف | بعد النجاح |
|---|---|
| **المندوب** | إشعار «تمت الموافقة على عميلك» · المتجرُ في «عملائي» · **وربّما إشعارُ مكافأة** |
| **صاحبُ المتجر** | حسابٌ بكلمة مرورٍ محفوظةٍ + `must_change_password=true` |
| **الإدارة** | الفرصةُ `converted` · المتجرُ في القائمة |
| **الزبون** | **المتجرُ يظهر في سوقه إن كان داخلَ نطاقه** |
| **السائق** | **NONE** حتّى يقع طلب |

### حالاتٌ لم تُثبَت ❓

### U22 · U23 حُلّا ✅ — `resolveOwner`

**المُثبَت**: `resolveOwner` تنادي
**`identity.EnsureUserWithRole(ctx, actorID, phone, "merchant", name, pass, ip)`**.

| السؤال | الجواب |
|---|---|
| **أيمنح دورَ `merchant`؟** | **نعم صراحةً** ✅ — الدورُ وسيطٌ في النداء |
| **رقمٌ مكرَّر؟** | **`EnsureUser…` تجد الموجودَ ولا تُنشئ ثانياً** ⚠️ (الاسمُ يقول ذلك · **الجسمُ لم يُقرأ**) |
| **فمالكٌ واحدٌ لمتجرين؟** | **ممكنٌ بنيةً** — لا قيدَ فريدٍ على `merchants.owner_user_id` قِيس ❓ **U26** |
| **وكلمةُ المرور** | تُمرَّر — **فالخطوةُ ٥ في `convertLead` احتياطٌ لمن لم تُضبط له** |

**فالخطرُ الذي ظننتُه في `convertLead` أهونُ**: الدورُ يُمنح داخلَ
`CreateMerchant` لا بعده. **لكنّ الخطوات ٤–٦ تبقى مُهمَلةَ الخطأ.**

### U25 حُلّ ✅ — `repMenuGuard`

**المُثبَت** — `internal/server/rep_menu_handlers.go`:

```
إن كان في المسار {id}      →  repClient(r, id)     — أهذا المتجرُ من عملائه؟
إن كان في المسار {itemID}  →  repOwnsItem(r, itemID) — أهذا الصنفُ في متجرٍ من عملائه؟
وإلّا                       →  default (لم يُقرأ) ❓
```

**فالمندوبُ لا يحرّر إلّا قوائمَ متاجره** ✅ — **حارسٌ خادميٌّ لا واجهيّ**.

### مجهولاتٌ باقيةٌ في هذا الباب

### U26 حُلّ ✅ — **لا قيدَ فريدٍ على مالك المتجر**

**قِيس في الإنتاج**: فهارسُ `merchants` تسعةٌ، منها **`merchants_owner_idx`
— فهرسٌ عاديٌّ لا فريد**، ولا قيدَ `UNIQUE` إلّا المفتاحَ الأساسيّ.

> **فمالكٌ واحدٌ يملك متاجرَ متعدّدة — بنيةً وفعلاً.** ✅
> **PROVEN BEHAVIOR** — وقد يكون مقصوداً (صاحبُ فرعين).

### U27 حُلّ ✅ — `EnsureUserWithRole` بجسمها الكامل

```
NormalizePhone            → رقمٌ غيرُ صالحٍ يُرفض
UserByPhone
  ├─ ErrNotFound  →  CreateUserWithRole(phone, name, role)
  │                  + كلمةُ مرورٍ إن أُعطيت (وتُفحص security.password_min_length)
  │                  + audit "admin.user_create"
  └─ موجودٌ       →  **GrantRole(user.ID, role, actorID)**  ← ولا شيءَ غيرُه
```

**فثلاثةُ إثباتاتٍ تتبع:**

| # | المُثبَت |
|---|---|
| **١** | **مستخدمٌ قائمٌ يُمنح الدورَ ولا يُنشأ ثانياً** ✅ — فرقمٌ مكرَّرٌ **لا يُنشئ حساباً ثانياً** |
| **٢** | **وكلمةُ المرور لا تُلمس لمستخدمٍ قائم** — الفرعُ للجديد وحدَه ✅ **وهذا يفسّر الخطوةَ ٥ في `convertLead`** |
| **٣** | ⚠️ **زبونٌ قائمٌ يصير `merchant` بمجرّد أن يُدخل المندوبُ رقمَه** — **بلا موافقةٍ منه ولا إشعار** → **PROVEN RISK** |

**والخطر (٣)**: من كان زبوناً ثمّ صار مالكَ متجرٍ **يرى تطبيقاً تبدّل
تحت يده**. ولم يُثبت أنّ ذلك يقع في الواقع (**يحتاج رقماً مسجَّلاً
مسبقاً**) — فهو **RISK لا DEFECT**.

### U28 حُلّ ✅ — **والافتراضُ منعٌ لا سماح**

```go
default:
    // **ومسارٌ بلا معرّفٍ لا يمرّ** — والافتراضُ منعٌ لا سماح:
    // من أضاف مساراً غداً ونسي معرّفَه يجد باباً مغلقاً لا مفتوحاً.
    s.respondErr(w, errForbidden)
```

> **PROVEN BEHAVIOR — آمنٌ بالتصميم** ✅

---

## D-03 · الأدمن يعلّق متجراً — **U24 حُلّ** ✅

| البند | القيمة |
|---|---|
| **Actor** | `admin` |
| **المسار** | `POST /api/v1/admin/merchants/{id}/suspend` `{suspended, note}` |
| **المُعالِج** | `handleSuspendMerchant` — `merchant_violations.go` |
| **كتابات** | **`UPDATE merchants SET status = 'suspended'` — وهذا كلُّ شيء** |
| **المعاملة** | **لا حاجة** — كتابةٌ واحدة |
| **الماليّ** | **NONE** |
| **الزمنُ الحيّ** | `touch("merchant","ops")` |
| **التدقيق** | ✅ `ops.merchant_suspend` مع الملاحظة |
| **الإشعارات** | **NONE** — **صاحبُ المتجر لا يُخبَر** ⚠️ |
| **الأثرُ على الطلبات الجارية** | **NONE مُثبَت** — **لا مسَّ لأيّ طلبٍ قائم** |

### التصنيف — **PROVEN RISK**

**فمتجرٌ عُلِّق وله طلباتٌ في الطريق**: لا تُلغى ولا تُحوَّل ولا يُخبَر
أحد. **والسائقُ يمضي إليه، والزبونُ ينتظر.**

**⚠️ والنتيجةُ لم تُثبت**: قد يكون هذا مقصوداً (**التعليقُ يمنع
الجديدَ ولا يقتل القائم**) — **وهو الأرجح**. **قرارُ مالك.**

**وثمّةُ مسارٌ ثانٍ يعالج الحالةَ فعلاً**: `merchant_blocked.go`
(**DSW-1**) يُعيد الطلبَ إلى `accepted` — **وشرطُ ندائه لم يُقرأ** ❓
**U29**.

---

## D-04 · الأدمن يمسح مخالفاتِ متجر

| البند | القيمة |
|---|---|
| **المسار** | `POST /api/v1/admin/merchants/{id}/clear-violations` |
| **الفلسفة** | **«عفوٌ — يُصفَّر العدّادُ ولا يُمحى الماضي»** ✅ |
| **التدقيق** | ⚠️ لم يُقرأ |

---

## D-05 · مقامُ نطاق المتجر والقائمة — **مُثبَتٌ آليّاً** ✅

**استُخرج من `endpoints.json` بمرشّحٍ على المسار**
(`merchants|stores|menu|categories|platform-sections|store-sections|modifier`):

| | العدد |
|---|---|
| **مساراتُ النطاق** | **٦٠** |
| ↳ **طفرات** | **٣٤** |
| ↳ **قراءات** | **٢٦** |

### الطفراتُ الأربعُ والثلاثون — **مُصنَّفةٌ كلُّها بالدور والمجال**

| المجال | المسارات | الدور |
|---|---|---|
| **المتجرُ نفسُه** | `POST /admin/merchants` · `PATCH /admin/merchants/{id}` · `POST …/suspend` · `POST …/clear-violations` · `POST …/warnings` | admin |
| **ساعاتُ العمل** | `PUT /admin/merchants/{id}/hours` · `PUT /merchant/stores/{id}/hours` | admin · merchant |
| **إعداداتُ المتجر** | `PATCH /merchant/stores/{id}/settings` | merchant |
| **أقسامُ المنصّة للمتجر** | `PUT /merchant/stores/{id}/sections` | merchant |
| **أقسامُ القائمة** | `POST /admin/merchants/{id}/menu/sections` · `POST /merchant/stores/{id}/menu/sections` · `PATCH`/`DELETE` لكلٍّ من `admin` و`merchant` | admin · merchant |
| **الأصناف** | `POST /admin/merchants/{id}/menu/items` · `POST /merchant/stores/{id}/menu/items` · **`POST /rep/stores/{id}/menu/items`** · `PATCH`/`DELETE` للثلاثة | admin · merchant · **sales** |
| **التوفّر** | **`PATCH /merchant/menu/items/{itemID}/availability`** | merchant |
| **المراجعة** | **`POST /admin/menu/items/{itemID}/review`** | admin |
| **الوسائط** | `POST /merchant/media` | merchant |
| **التصنيفات** | `POST`/`PATCH /admin/categories` · `POST`/`GET /admin/expenses/categories` | admin |
| **طوارئُ المتجر** | `POST /merchant/stores/{id}/emergency` | merchant |
| **دورةُ الطلب في المتجر** | `POST /merchant/orders/{id}/{transition,ready,report}` | merchant |

### القراءاتُ الستُّ والعشرون — **مُصنَّفةٌ بالمستهلك**

| المستهلك | العدد | أبرزُها |
|---|---|---|
| **لوحةُ الإدارة** | ٩ | `merchants` · `{id}/menu` · `{id}/hours` · `violations` · `warnings` · **`menu/pending`** |
| **تطبيقُ المتجر** | ١٢ | `stores` · `{id}/menu` · `{id}/orders` · `{id}/hours` · `{id}/sections` · `platform-sections` · `reports` · `reports-against` · `warnings` · `my-reports` · `report-reasons` |
| **تطبيقُ المندوب** | ٥ | `merchants` · `{id}` · `stores/{id}/menu` · `categories` · `platform-sections` |

**⚠️ ولا قراءةَ للزبون في هذا النطاق** — **سوقُ الزبون يمرّ بمسارات
`/public/*` و`/customer/*`** المرشَّحةِ بالمدينة والمسافة
(انظر §١٦ في التقرير الرئيسيّ).

### اكتشافاتٌ مُثبَتةٌ من هذا الحصر

| # | المُثبَت | التصنيف |
|---|---|---|
| **S1** | **`POST /admin/menu/items/{itemID}/review` و`GET /admin/menu/pending`** — **فثمّةَ مراجعةُ أصنافٍ قبل الظهور** | **PROVEN BEHAVIOR** — وآليّتُها لم تُقرأ ❓ **U30** |
| **S2** | **المندوبُ يُنشئ ويعدّل ويحذف أصنافاً** (٣ مسارات) — محصوراً بـ`repMenuGuard` | ✅ |
| **S3** | **`availability` مسارٌ مستقلٌّ عن `PATCH item`** — فتبديلُ التوفّر فعلٌ بذاته | ✅ |
| **S4** | **لا مسارَ لمجموعات المعدِّلات** (`modifier_groups` · `modifier_options`) رغم وجود جدولَيهما | ❓ **U31** — **أهي غيرُ مبنيّةٍ في الواجهة؟** |
| **S5** | **لا حذفَ للمتجر** — `DELETE /admin/merchants/{id}` **غيرُ موجود**. التعليقُ وحدَه | **PROVEN BEHAVIOR** ✅ |
| **S6** | **`admin` و`merchant` لهما مسارانِ منفصلانِ لنفس الفعل** (`PATCH menu/sections` مثلاً) — **بمُعالِجَين مختلفين** (`handleUpdateSection` · `handleMerchantUpdateSection`) | **PROVEN RISK** — منطقان قد يفترقان |

### أحكامُ النطاق

> **`STORE / CATALOG MUTATIONS: 34/34 IDENTIFIED + ROLE-CLASSIFIED`** ✅
> **`STORE / CATALOG READS: 26/26 IDENTIFIED + CONSUMER-CLASSIFIED`** ✅
> **`STORE / CATALOG ACTIONS FULLY DESCRIBED (30 fields): 4/34`** ⚠️
> ↳ الموصوفةُ: `convertLead` (D-01) · التعليق (D-03) · مسحُ المخالفات (D-04) · `CreateMerchant`
> **`STORE / CATALOG OPERATIONALLY SIGNIFICANT UNKNOWNS: 2`** ⚠️
> ↳ **U30** مراجعةُ الأصناف · **U31** مجموعاتُ المعدِّلات

### وما لم يُوصف بعد في هذا النطاق — **صريحاً**

| البند المطلوب | الحال |
|---|---|
| دخولُ المتجر ووصولُه | ❓ **لم يُوصف** |
| ملفُّ المتجر وإعداداتُه | ❓ |
| أقسامُ المتجر · أقسامُ القائمة | **المسارات مُحصاة · السلوكُ لم يُقرأ** |
| الأصنافُ · السعرُ · التوفّر | **المسارات مُحصاة · السلوكُ لم يُقرأ** |
| مجموعاتُ المعدِّلات | ❓ **U31** |
| ساعاتُ العمل | **المسارات مُحصاة** |
| الصورُ والوسائط | **مسارٌ واحدٌ مُحصى** |
| **ظهورُ الصنف للزبون** | **الطبقتان مُثبَتتان** (§١٦) · **وأثرُ حالة المتجر لم يُثبت** ❓ |
| **صنفٌ في سلّةٍ تبدّل سعرُه أو توفّرُه** | **✅ U32 حُلّ** — أدناه |
| نجاح/فشل/فشلٌ جزئيٌّ لكلّ طفرة | **٤ من ٣٤** |

---

## D-06 · السلّة — **U32 حُلّ** ✅

**والاكتشافُ أهمُّ من السؤال:**

| البند | المُثبَت |
|---|---|
| **أين تعيش السلّة؟** | **في الجهاز وحدَه** — `object Cart` على `SharedPreferences` (`app-customer/.../cart/Cart.kt`) |
| **جدولُ سلّةٍ في القاعدة؟** | **صفر** ✅ — بُحث في الجداول الخمسةِ والسبعين فلم يوجد |
| **مسارُ سلّةٍ في الخادم؟** | **صفر** ✅ |
| **فالخادمُ لا يعرف بالسلّة شيئاً** حتّى لحظةِ الإنشاء | ✅ |

### وكيف تُحمى إذن؟ — **بالتسعير المتكرّر**

**المُثبَت** في `CartScreen.kt`:

```kotlin
LaunchedEffect(Cart.lines, here) { vm.quote(here?.lat, here?.lng) }
```

**فمع كلّ تبدّلٍ في السطور أو في الموقع يُنادى `POST /public/quote`**،
ويردّ `Quote` فيه **السعرُ وأجرةُ التوصيل و`outOfZone`**.

**والزرُّ يُعطَّل**: `enabled = vm.priced != null && vm.priced?.outOfZone != true` ✅

### الأجوبةُ على U1a–U1d — **مُثبَتةٌ الآن**

| # | السؤال | الجواب |
|---|---|---|
| **U1a** | **داخلَ النطاق ثمّ خرج** | **يُعاد التسعيرُ عند تبدّل الموقع** → `outOfZone=true` → **الزرُّ يُعطَّل** ✅ · **والخادمُ يمنع أيضاً** عند الإنشاء (طبقتان) |
| **U1b** | **عُدِّلت المضلّعاتُ والسلّةُ قائمة** | **لا بثَّ للتغيير** — **فالتسعيرُ لا يُعاد حتّى يتحرّك المستخدمُ أو يبدّل السلّة.** ⚠️ **PROVEN RISK**: يرى سعراً قديماً حتّى أوّلِ تبدّل — **ثمّ يمنعه الخادمُ عند الإرسال** |
| **U1c** | **على الحافّة تماماً** | **`ST_DWithin` و`ST_Covers` — سلوكُ الحافّة لم يُختبر** ❓ **يبقى UNKNOWN** |
| **U1d** | **موقعُ الزبون مزيَّف** | **لا فحصَ إطلاقاً** ✅ مُثبَت — **الحمايةُ للسائق وحدَه** (٢٠٢٦-٠٩-٠٣). **PROVEN RISK**: زبونٌ يزيّف موقعَه ليطلب من خارج النطاق |
| **U32** | **سعرٌ أو توفّرٌ تبدّل والسلّةُ قائمة** | **السعرُ يُعاد حسابُه مع كلّ تبدّلٍ في السلّة** ✅ · **والتوفّرُ ❓ لم يُثبت أنّ `quote` تفحصه** |

### مجهولاتٌ جديدةٌ في هذا الباب

### U33 حُلّ ✅ — **التسعيرُ يفحص التوفّر، وبالمعادلة نفسِها**

**السلسلةُ المُثبَتة:**

```
POST /public/quote  →  handleQuote  →  orders.Quote(items, lat, lng)
                                        └─ priceItems(ctx, items)   ← ونفسُها في Create
                                             └─ var available bool  ← يُقرأ التوفّر
                                             └─ ErrBadQty إن فسدت الكمّيّة
```

**والتعليقُ صريح**: «**والتسعيرُ بالمعادلة نفسِها التي عند الإنشاء**».

| النتيجة | |
|---|---|
| **صنفٌ صار غيرَ متوفّرٍ والسلّةُ قائمة** | **يُكشف عند أوّل تسعيرٍ تالٍ** ✅ |
| **ولو تجاوزه العميل** | **`Create` تنادي `priceItems` نفسَها فتمنع** ✅ |
| **فطبقتان بمعادلةٍ واحدة** | ✅ **PROVEN BEHAVIOR — آمن** |
| ⚠️ **والثغرةُ الزمنيّة تبقى** | بين آخرِ تسعيرٍ والإرسال — **يمنعها الخادمُ لا الواجهة** |

### مجهولٌ باقٍ

| # | المجهول |
|---|---|
| **U34** | **ماذا يقع لو حُذف صنفٌ والسلّةُ فيها منه** — أتردّ `priceItems` خطأً مفهوماً للزبون؟ |

---

**فالحكمُ على النطاق: `STORE / CATALOG FLOW — INCOMPLETE`.**
**والمقاماتُ مُثبَتةٌ ١٠٠٪، والوصفُ ١٢٪.**

---

## D-02 · حياةُ القائمة — **المسارات مُحصاة**

**من خريطة المسارات (`endpoints.json`):**

| الفعل | المسار | الأدوار |
|---|---|---|
| إنشاءُ قسمِ قائمة | `POST /merchants/{id}/menu/sections` | admin |
| | `POST /stores/{id}/menu/sections` | merchant |
| تعديلُ قسم | `PATCH /menu/sections/{sectionID}` | admin · merchant (**مسجَّلٌ مرّتين**) |
| حذفُ قسم | `DELETE /menu/sections/{sectionID}` | admin · merchant |
| إنشاءُ صنف | `POST /stores/{id}/menu/items` | merchant · **`sales`** ⚠️ |
| **توفّرُ الصنف** | `PATCH /menu/items/{itemID}/availability` | merchant |
| حذفُ صنف | `DELETE /menu/items/{itemID}` | admin · merchant |
| ساعاتُ المتجر | `PUT /stores/{id}/hours` · `GET` | merchant |
| أقسامُ المنصّة للمتجر | `PUT /stores/{id}/sections` | merchant |
| إعداداتُ المتجر | `PATCH /stores/{id}/settings` | merchant |
| **طوارئُ المتجر** | `POST /stores/{id}/emergency` | merchant |

**⚠️ مُثبَت**: **`RepCreateItem`** — **المندوبُ يستطيع إنشاءَ أصنافٍ في
متجر**، بحارسٍ خاصٍّ `repMenuGuard` (رُئي في `server.go`).
**وشروطُه لم تُقرأ** ❓ **U25**.

---

# ٢-هـ · ربطُ طفرات العميل بالشاشات

---

## E-01 · جانبُ الجوّال — **٦٩ طفرة** ✅

**استُخرجت آليّاً**: لكلّ `HttpMethod.{Post,Put,Patch,Delete}` في
`shared/**/*Api.kt` — **الدالّةُ والمسارُ**، ثمّ بُحث عن مُنادِيها في
التطبيقات الأربعة و`ui` و`map`.

| | العدد |
|---|---|
| **طفراتُ عقد الـAPI** | **٦٩** |
| ↳ **بلا مسارٍ مُلتقَط** | **٠** ✅ |
| ↳ **مربوطةٌ بمُنادٍ في شاشة** | **٦٤** |
| ↳ **بلا مُنادٍ إطلاقاً** | **٥** |

### الخمسُ اليتيمة — **فُحصت واحدةً واحدةً**

| # | الدالّة | المسار | الحكم |
|---|---|---|---|
| **O1** | `AuthApi.resetRequest` | `POST /auth/password/reset/request` | **ميّتة** — انظر أدناه |
| **O2** | `AccountApi.whatsappRequest` | `POST /auth/whatsapp/request` | **ميّتة** ⚠️ |
| **O3** | `DriverApi.skipProof` | `POST /driver/orders/{id}/proof/skip` | **ميّتة** ⚠️ |
| **O4** | `MerchantApi.createSection` | `POST /merchant/stores/{id}/menu/sections` | **ميّتة** ⚠️ |
| **O5** | `DriverApi.decline` | `POST /driver/orders/{id}/decline` | **⚠️ ذُكرت مرّةً** — والسياقُ لم يُقرأ |

### O1 — **مُثبَتٌ بالبديل** ✅

**بُحث عن مسار الاستعادة في الواجهة فوُجد**:

```kotlin
// AuthViewModel.sendResetCode
val t = backend.auth.waTicket(current.phone.trim(), "reset")
openWhatsApp(t.waUrl)
```

**فالاستعادةُ تمرّ بتذكرةِ واتساب** (`POST /auth/wa/ticket`)، **لا
بـ`resetRequest`.**

> **`resetRequest` بقيّةُ مسارٍ قديمٍ حلّ محلَّه واتساب.**
> **PROVEN BEHAVIOR** — الدالّةُ ميّتةٌ في العميل، **والمسارُ حيٌّ في
> الخادم** (`POST /auth/password/reset/request` مسجَّلٌ ومفتوح).

### التصنيف — **PROVEN RISK (R2)**

**خمسُ دوالٍّ في عقد الـAPI بلا مُنادٍ** — **وثلاثةٌ منها تخصّ قدراتٍ
حيّةً في الخادم**:

| القدرة | المسارُ في الخادم | العميل |
|---|---|---|
| **تخطّي إثبات التسليم** | `POST /driver/orders/{id}/proof/skip` ✅ حيّ | **لا زرَّ** ⚠️ |
| **إنشاءُ قسمِ قائمة من تطبيق المتجر** | `POST /merchant/stores/{id}/menu/sections` ✅ حيّ | **لا زرَّ** ⚠️ |
| **توثيقُ واتساب** | `POST /auth/whatsapp/request` ✅ حيّ | **لا زرَّ** ⚠️ |

**و«تخطّي الإثبات» أخطرُها**: المحرّكُ يقبل عذراً عن الصورة
(`pod_skip_reason`) **واللوحةُ تعرضه** («تعذّر التصوير: …») —
**والسائقُ لا يملك زرّاً يرسله.** ❓ **U35 — أهو محذوفٌ عمداً؟**

---

## E-02 · جانبُ الويب — **١٣١ طفرةً مربوطةً بسطحها** ✅

**استُخرجت آليّاً**: لكلّ `method: "POST|PUT|PATCH|DELETE"` — **الملفُّ
والسطرُ والمسار**، ثمّ صُنّفت بسطحها.

| | العدد |
|---|---|
| `POST` ٩٠ · `PATCH` ٢٢ · `DELETE` ١٠ · `PUT` ٩ | **١٣١** |
| **مربوطةٌ بملفٍّ وسطرٍ وسطح** | **١٣١** ✅ |
| ↳ **مسارُها مُلتقَطٌ آليّاً** | **١٢٠** |
| ↳ **مسارُها يُبنى بمتغيّر** (يحتاج قراءةً يدويّة) | **١١** — `StoreActions` ×٣ · `FileUpload` ×٢ · `ImageUpload` … |

### التوزّعُ على الأسطح — **٣٠ سطحاً**

| السطح | طفرات |
|---|---|
| **`components/admin/*`** (المكوّناتُ الإداريّة) | **٥٩** |
| `packages/auth/src/client.ts` | **١٤** — طبقةُ الجلسة والتوكن |
| `packages/ui/AccountSettings.tsx` | **١٠** — الحسابُ المشترك |
| `dashboard/sections` | ٦ |
| `dashboard/users` | ٥ |
| `packages/ui/MenuManager.tsx` | ٤ |
| `dashboard/expenses` | ٣ |
| `packages/ui/AddressBook.tsx` | ٣ |
| `dashboard/{merchants,promos}` | ٢ لكلٍّ |
| `packages/ui/{FileUpload,NotificationsPage,WhatsAppVerify}` | ٢ لكلٍّ |
| `dashboard/{cash,emergencies,incentives,leads,payouts,settings}` | ١ لكلٍّ |
| `site/join` | ١ |
| `packages/ui/{ImageUpload,Notifications,OrderChat,StoreHours,WalletPage,Favorites,DashboardChrome,useLocationBeacon}` | ١ لكلٍّ |
| `packages/auth/{PasswordGate,routing}` | ١ لكلٍّ |

### حقائقُ ظهرت من هذا التوزّع

| # | المُثبَت |
|---|---|
| **W1** | **٥٩ طفرةً من ١٣١ في `components/admin/`** — **فأكثرُ من نصف طفرات الويب في مكوّناتٍ لا في صفحات**، ولا تُرى في شجرة المسارات |
| **W2** | **١٤ طفرةً في `packages/auth/client.ts`** — الدخولُ والتجديدُ والخروج · **طبقةٌ واحدةٌ لكلّ الأسطح** ✅ |
| **W3** | **`packages/ui` فيه ٣٢ طفرة** — **مكتبةُ العرض تُطفِر الخادم**، وهو ما يجعل الفعلَ الواحدَ يخدم أكثرَ من سطح |
| **W4** | **`AddressBook` و`Favorites` و`OrderChat` تُطفِر** — **وهي من الجزيرة التي ظننتُها ميّتة**، فهي حيّةٌ عبر `AccountSettings` |

### أحكامُ الربط — **CANONICAL**

> **`CLIENT MUTATION CALL SITES (MOBILE): 69/69 IDENTIFIED`** ✅
> **`↳ LINKED TO A CALLING SCREEN: 64/69`** ✅
> **`↳ ORPHANED — EXPLAINED: 5/69`** ✅
> **`CLIENT MUTATION CALL SITES (WEB): 131/131 LINKED TO FILE+LINE+SURFACE`** ✅
> **`↳ PATH AUTO-RESOLVED: 120/131`** · **`↳ NEEDS MANUAL READ: 11/131`**
> **`TOTAL: 200/200 IDENTIFIED AND SURFACE-MAPPED`** ✅
> **`↳ FULLY DESCRIBED (control · validation · loading · error · retry): 0/200`** ⚠️

---

# ٢-و · مساراتُ القراءة — **١٣٤ مصنَّفةً** ✅

**استُخرجت من `endpoints.json` وصُنّفت بمستهلكها آليّاً.**

## أ · التوزّعُ بالمستهلك

| المستهلك | العدد | الطبيعة |
|---|---|---|
| **لوحةُ الإدارة** | **٦٢** | التشغيلُ والمالُ والإعدادات |
| **الزبونُ والمشترك** (`/me/*` · `/my/*` · `/geo/*` · `/orders/{id}/messages` · `/ws`) | **٢٠** | ما يخصّ صاحبَه |
| **عامّةٌ بلا مصادقة** (`/public/*` · `/healthz` · `/ws`) | **٢٠** | السوقُ والهويّة |
| **تطبيقُ المتجر** | **١٢** | متجرُه وقائمتُه وطلباتُه |
| **تطبيقُ المندوب** | **٩** | عملاؤه وعمولتُه |
| **تطبيقُ السائق** | **٩** | الطابورُ وصندوقُه ورحلتُه |
| **المصادقة** | **٤** | حالُ الجلسة |
| **المجموع** | **١٣٤** ✅ |

## ب · القراءاتُ التي **تقرّر عملاً** — وُصفت بعمق

### R-01 · `GET /driver/queue` — **أخطرُ قراءةٍ في المنصّة**

**لأنّها تقرّر: أيُّ سائقٍ يرى أيَّ طلب.**

| البند | المُثبَت |
|---|---|
| **المستهلك** | تطبيقُ السائق — شاشةُ الطلبات |
| **التخويل** | `RequireRoles("driver")` ✅ |
| **مصدرُ الحقيقة** | `orders` مباشرةً — **لا ذاكرةَ وسيطة** |
| **المرشّحُ الأساسيّ** | `WHERE o.status = 'dispatching' AND o.driver_id IS NULL` |
| **مرشّحُ العرض — نمطُ `rotation`** | `AND o.offered_driver_id = $1` — **لا يرى إلّا ما عُرض عليه** |
| **مرشّحُ العرض — نمطُ `queue`** | `AND (o.offered_driver_id IS NULL OR = $1) AND NOT ($1 = ANY(o.offer_passed))` — **يرى المفتوحَ إلّا ما مرّره** |
| **مرشّحُ الشيخوخة** | `staleLocationMinutes` (**١٥ دقيقة**) — **موضعٌ شاخ لا يُحسب عليه** |
| **الترتيب** | `ORDER BY o.ready_at NULLS LAST, o.created_at` — **الجاهزُ أوّلاً ثمّ الأقدم** ✅ |
| **الحدّ** | `LIMIT 50` |
| **الذاكرةُ الوسيطة** | **NONE** ✅ |
| **التحديثُ الحيّ** | البثُّ `drivers:queue` — **إشارةٌ بلا حمولة**، فيُعاد الجلب |
| **سلوكُ الفشل** | ❓ لم يُقرأ |
| **الأثرُ التشغيليُّ لخللٍ فيها** | **سائقٌ لا يرى طلباً متاحاً** — وهو أحدُ ما حذّر منه المالكُ نصّاً |

**ملاحظةٌ مُثبَتة**: **`offer_passed` عمودُ مصفوفةٍ** — فمن مرّر طلباً
لا يُعرض عليه ثانيةً في نمط `queue`. ✅

### R-02 · `GET /driver/me` — **قراءةٌ ماليّةٌ وتشغيليّة**

**تحمل**: الورديّة · **النقدَ المحمولَ وسقفَه** · عددَ الطلبات المفتوحة ·
`location_ping_sec` — **فهي التي تضبط تردّدَ إرسال الموقع من الخادم.** ✅

### R-03 · `GET /merchant/stores/{id}/orders` · `GET /merchant/orders/{id}`

**مصدرُ الحقيقة**: `orders` — **والمتجرُ لا يرى إلّا طلباتِ متجره**
(حارسُ الملكيّة ❓ لم يُقرأ نصُّه) — **U36**.

### R-04 · `GET /public/*` — **٢٠ قراءةً بلا مصادقة**

`offers · home · platform · map-style.json · app · zone · banners ·
contact · cities · governorates · districts · sections ·
sections/{id}/items · items/{id} · suggest · search · search/items · invite`

**وهي واجهةُ السوق للزائر.** **والترشيحُ بالمدينة والمسافة يقع فيها**
(§١٦) — **ومن لا يرسل موقعَه يرى كلَّ شيء** ✅ مُثبَت.

**⚠️ `GET /public/zone`** — **يكشف حدودَ التوصيل لمن سأل.** **PROVEN
BEHAVIOR** — وهو لازمٌ للواجهة.

### R-05 · `GET /me/summary` — **تُنادى في كلّ تبدّل مسار**

**مُثبَتٌ في `Header.tsx`**: `useEffect(() => loadSummary(), [loadSummary, pathname])`
**فهي أكثرُ القراءات نداءً في اللوحة.** ⚠️ **وأثرُها على الحمل لم يُقَس.**

## ج · أحكامُ القراءة

> **`READ ROUTES: 134/134 CLASSIFIED BY CONSUMER`** ✅
> **`↳ AUTH-CLASSIFIED (public / authenticated / role-guarded): 134/134`** ✅
> **`↳ DECISION-CRITICAL READS DESCRIBED IN DEPTH: 5`**
> **`↳ FULLY DESCRIBED (source · filters · cache · realtime · failure): 1`** — `R-01`

## د · مجهولاتٌ من هذا التصنيف

### U36 حُلّ ✅ — **حراسُ الملكيّة خادميّةٌ وأربعة**

**مُثبَتٌ في `internal/server/merchant_handlers.go` — أربعةُ استعلاماتٍ
كلُّها تشترط `owner_user_id = <المستخدمُ الحاليّ>`:**

| الحارس | الاستعلام |
|---|---|
| **المتجر** | `EXISTS(SELECT 1 FROM merchants WHERE id=$1 AND owner_user_id=$2)` |
| **الطلب** | `orders o JOIN merchants m … WHERE o.id=$1 AND m.owner_user_id=$2` |
| **الصنف** | `WHERE i.id=$1 AND m.id=i.merchant_id AND m.owner_user_id=$2` |
| **رابعٌ** | `WHERE id=$1 AND owner_user_id=$2` |

**ورفضُه `ErrNotFound` لا `Forbidden`** ✅ — **فلا يكشف وجودَ المورد.**

> **PROVEN BEHAVIOR — آمنٌ ومُطبَّقٌ في الخادم.** ✅

### U39 حُلّ ✅ — **ثلاثةُ حرّاسٍ يغطّون الـ٢٧ مساراً**

**قِيست مسارات `merchant` فكانت ٢٧، وقِيست الحرّاسُ فكانت ثلاثةً بحسب
نوع المعرّف في المسار:**

| الحارس | يحمي | نداءات |
|---|---|---|
| **`ownsMerchant(r, id)`** | `‎/stores/{id}/*` — **١٢ مساراً** | **١٢ نداءً** ✅ **تطابقٌ تامّ** |
| **`merchantOwnsOrder(r, id)`** | `‎/orders/{id}/*` — ٤ مسارات | ٣ نداءات |
| **استعلاماتُ ملكيّةٍ مباشرة** | `‎/menu/items/{itemID}` · `‎/menu/sections/{sectionID}` | `WHERE mi.id=$1 AND m.owner_user_id=$2` · ومثلُها للقسم ✅ |
| **بلا معرّفٍ أصلاً** | `‎/stores` · `‎/warnings` · `‎/media` · `‎/platform-sections` · `‎/report-reasons` · `‎/my-reports` | **مقصورةٌ على المستخدم بذاتها** — لا معرّفَ يُزوَّر |

**فالتغطيةُ كاملةٌ منطقيّاً**: **كلُّ مسارٍ يحمل معرّفَ موردٍ يُفحص،
وما لا يحمل معرّفاً يقرأ ما يخصّ صاحبَ الجلسة.**

### U40 حُلّ ✅ — **الحارسُ داخلَ الخدمة لا في المُعالِج**

**المسارُ الرابع** `POST /merchant/orders/{id}/report` **لا ينادي
`merchantOwnsOrder` في مُعالِجه** — **لأنّ الحارسَ في `support.MerchantReport`
نفسِها:**

```sql
WHERE o.id = $1 AND m.owner_user_id = $2
→ pgx.ErrNoRows  ⇒  يُرفض
```

**فالحمايةُ في الاستعلام لا في نداءٍ سابق** ✅ — **وهي أمتنُ**: لا
يمكن نسيانُها لأنّها شرطُ الجلب نفسِه.

> **`MERCHANT ROUTE OWNERSHIP GUARDS: 27/27 PROVEN`** ✅
> **`↳ ownsMerchant` ١٢ · `merchantOwnsOrder` ٣ · شرطٌ في الاستعلام ٣ ·
> بلا معرّفٍ ٦ · باقي المسارات بمعرّفِ صنفٍ أو قسمٍ ٣**

### مجهولاتٌ باقية

| # | المجهول |
|---|---|
| **U37** | **سلوكُ الفشل في القراءات** — ماذا يرى السائقُ إن سقط الطابور؟ |
| **U38** | **الذاكرةُ الوسيطة** — `geo` في Redis مُثبَت؛ **وباقي القراءات لم يُفحص** |
| **U39** | **أتنادي كلُّ مسارات `merchant/*` حارسَ الملكيّة؟** — ٥ نداءات مقابل ٢٧ مساراً |

---

# ٢-ز · أفعالُ لوحة الإدارة — **٧٤ طفرةً مصنَّفةً بالأثر** ✅

**استُخرجت من `endpoints.json`: ١٣٦ مساراً إداريّاً — ٧٤ طفرةً و٦٢
قراءة، في ٢٥ مجالاً.**

## أ · التصنيفُ بالأثر — **مُلزَمٌ بطلب المالك**

### FINANCIAL — **١٣**

| المسار | الأثر |
|---|---|
| `POST /users/{id}/wallet` | **قيدٌ يدويٌّ في محفظة** — A-005 · **idem** |
| `POST /users/{id}/incentive` | منحُ حافز — A-020 · **idem** |
| `POST /payouts/{id}/decide` | بتُّ صرفٍ — A-006 · **idem** |
| `POST /drivers/{id}/settle` | تسويةُ صندوق نقد — **idem** |
| `POST /orders/{id}/recompute` | إعادةُ حساب التسوية — A-004 |
| `POST /orders/{id}/compensate-driver` | تعويض — A-014 · **محميٌّ بقفلٍ وفحص** |
| `POST /orders/{id}/goods` | تسويةُ بضاعة — A-013 |
| `POST /orders/{id}/settle-goods` | **مهجورٌ — يردّ ٤١٠** ✅ |
| `POST /disputes` · `/disputes/{id}/settle` | نزاعٌ وتسويتُه — A-016 |
| `POST /expenses` · `/expenses/{id}/void` · `/expenses/categories` | مصاريفُ المنصّة ❓ **لم تُوصف** |

### STATE — **٨**

`orders/{id}/{transition,assign,transfer}` · `leads/{id}/status` ·
`emergencies/{id}/resolve` · `tickets` · `tickets/{id}/replies` ·
`tickets/{id}/resolve`

### PERMISSION / SECURITY — **١٠**

| المسار | الأثر |
|---|---|
| `POST /users` · `PATCH /users/{id}` | إنشاءُ مستخدمٍ وتعديلُه |
| **`POST /users/{id}/roles`** · **`DELETE /users/{id}/roles/{role}`** | **منحُ الدور وسحبُه** — `admin` وحدَه |
| `POST /users/{id}/password` | **إعادةُ تعيين كلمة المرور بيد الأدمن** |
| `POST /users/{id}/logout-all` | **إخراجُ كلّ جلساته** |
| `POST /users/{id}/warnings` | إنذار |
| `POST /merchants/{id}/{suspend,warnings,clear-violations}` | تعليقٌ وإنذارٌ وعفو — D-03 · D-04 |

### CATALOG — **٧**

`merchants` · `merchants/{id}` · `merchants/{id}/hours` ·
`merchants/{id}/menu/items` · `menu/items/{itemID}` (تعديلٌ وحذف) ·
**`menu/items/{itemID}/review`**

### CONFIG — **٣٤ (أكثرُها)**

| المجموعة | المسارات |
|---|---|
| **الإعدادات** | `PUT /settings/{key}` — **بابُ الـ١٢٧ مفتاحاً كلِّها** |
| **الجغرافيا** | `cities` · `governorates` · `districts` · **`zones`** (٣ لكلٍّ) |
| **السوق** | `banners` · `sections` · `categories` · `offers` · `promos` |
| **واتساب** | **`whatsapp/pair`** · **`whatsapp/unpair`** · `orders/{id}/whatsapp` |
| **التطبيق** | `app-file` (رفعٌ وحذف) |
| **البثّ** | `POST /broadcast` |

### OTHER — **٢**

`POST /drivers/{id}/end-shift` — **الأدمنُ يُنهي ورديّةَ سائق** ⚠️ ·
`POST /media`

## ب · حقائقُ مُثبَتةٌ من هذا الحصر

| # | المُثبَت | التصنيف |
|---|---|---|
| **AD1** | **`PUT /settings/{key}` بابٌ واحدٌ لكلّ الإعدادات الـ١٢٧** — فالعمولةُ والأجرةُ وسقفُ النقد ونمطُ التوزيع كلُّها من مسارٍ واحد | **PROVEN BEHAVIOR** — ومركزيّةٌ صحيحة |
| **AD2** | **`whatsapp/pair` و`unpair`** — **اقترانُ جلسة واتساب يُدار من اللوحة** | **PROVEN BEHAVIOR** · وأثرُ `unpair` على رموز التحقّق ❓ **U41** |
| **AD3** | **`POST /broadcast` — U42 حُلّ** ✅ | أدناه |
| **AD4** | **`POST /drivers/{id}/end-shift` — U43 حُلّ** ✅ | أدناه |

### U42 حُلّ ✅ — **البثُّ موجَّهٌ بالأدوار**

**مُثبَتٌ في `internal/server/broadcast.go`:**

```sql
roles := parseRoles(query "roles")
SELECT count(DISTINCT u.id) FROM users u
JOIN user_roles ur ON ur.user_id = u.id
WHERE ur.role_code = ANY($1) AND u.status = 'active'
```

| | |
|---|---|
| **إلى من؟** | **أدوارٌ يختارها المرسِل** — لا «الجميع» أعمى |
| **الشرط** | **`status='active'` وحدَهم** ✅ |
| **العدُّ قبل الإرسال** | ✅ — **يُقال كم سيصل** |
| **القناة** | ⚠️ **يُرجَّح الإشعارُ الدافع** — لم يُقرأ صراحةً ❓ **U45** |

### U43 حُلّ ✅ — **ولا يُغلق دوامُ من بيده طلبٌ حيّ**

**مُثبَتٌ نصّاً في `handleAdminEndShift`:**

> «**ولا يُغلق دوامُ من بيده طلبٌ حيّ.** الطلبُ في صندوقه والزبونُ
> ينتظره، **وإغلاقُ دوامه لا يُعيد الطلبَ إلى الطابور** — يتركه معلّقاً
> بيد من صار في النظام *غير عامل*. **ومن أراد أن يُخرجه من طلبه فله
> إسنادٌ يدويٌّ في الطلب نفسِه.**»

**والشيفرة تفحص `var open int`** — **عددَ الطلبات المفتوحة** — قبل
الإغلاق.

> **PROVEN BEHAVIOR — آمنٌ بالتصميم** ✅
> **وهذا يُغلق خطراً كنتُ أظنّه قائماً.**
| **AD5** | **`settle-goods` مهجورٌ ويردّ ٤١٠** ✅ — **مسارٌ مُعلَّمٌ صراحةً لا ميّتٌ صامت** |
| **AD6** | **٣٤ من ٧٤ إعدادات** — **فأكثرُ عملِ اللوحة ضبطٌ لا تشغيل** | ✅ |

## ج · تغطيةُ التدقيق — **مقيسة**

| | العدد |
|---|---|
| **أفعالٌ مُدقَّقةٌ باسمٍ فريد** | **٤٧** |
| ↳ `finance.*` | **١٤** |
| ↳ `ops.*` | **١٣** |
| ↳ `driver.*` | ٦ |
| ↳ `catalog.*` | ٥ |
| ↳ `admin.*` | ٤ |
| ↳ `platform.*` · `auth.*` | ٢ لكلٍّ |
| ↳ `user.*` · `customer.*` | ١ لكلٍّ |

**والـ١٣ الماليّةُ كلُّها مُدقَّقة** ✅ — **مقابل ١٤ اسماً في عائلة
`finance`.**

**⚠️ وهذا العددُ من طبقةٍ واحدة** — **والقياسُ النهائيُّ ١٠٤ اسماً في
أربع طبقات** (U44 حُلّ أدناه). **والخريطةُ الكاملةُ في**
[`AUDIT_MAP.md`](AUDIT_MAP.md): **٣٥ فعلاً حسّاساً · ٢٩ مدقَّق · ٥
بسببٍ مُثبَت · ١ خطر.**

## د · أحكامُ اللوحة

> **`ADMIN ROUTES: 136/136 IDENTIFIED`** ✅
> **`ADMIN MUTATIONS: 74/74 EFFECT-CLASSIFIED`** ✅
> ↳ ماليّ ١٣ · حالة ٨ · صلاحيّة ١٠ · قائمة ٧ · إعدادات ٣٤ · أخرى ٢
> **`ADMIN READS: 62/62 IDENTIFIED`** ✅
> **`ADMIN MUTATIONS FULLY DESCRIBED: 9/74`** ⚠️
> **`ADMIN AUDIT NAMES: 47 MEASURED`** · **`UNPROVEN AUDIT COVERAGE: 27`** ❓

---

# ٢-ح · حقيقةُ الجهاز والنظام — **Device / OS Truth** ✅

> **وهذه لا تُرى من الخادم إطلاقاً** — والفعلُ قد يكون صحيحاً في
> المحرّك ومكسوراً في اليد.

## أ · الأذونات — **مقيسةٌ من البيانات الأربعة**

| الإذن | الزبون | السائق | المتجر | المندوب |
|---|:---:|:---:|:---:|:---:|
| `INTERNET` · `ACCESS_NETWORK_STATE` | ✅ | ✅ | ✅ | ✅ |
| `POST_NOTIFICATIONS` | ✅ | ✅ | ✅ | ✅ |
| `ACCESS_FINE/COARSE_LOCATION` | ✅ | ✅ | ✅ | ✅ |
| **`ACCESS_BACKGROUND_LOCATION`** | — | **✅** | — | — |
| **`FOREGROUND_SERVICE` + `_LOCATION`** | — | **✅** | — | — |
| **`REQUEST_IGNORE_BATTERY_OPTIMIZATIONS`** | — | **✅** | — | — |
| **`CAMERA`** | — | **✅** | — | — |

**فالسائقُ وحدَه يحمل الأذونَ الخطرة** ✅ — **والثلاثةُ الباقيةُ
متطابقةٌ تماماً** (خمسةُ أذونٍ لكلٍّ).

**⚠️ والزبونُ والمتجرُ والمندوبُ يطلبون الموقعَ** — للالتقاط على الخريطة
لا للتتبّع.

## ب · دورةُ حياةِ موقع السائق — **السلسلةُ كاملة**

```
LocationService (foreground · type=location · START_STICKY)
  └─ FusedLocation · PRIORITY_HIGH_ACCURACY
       فترةٌ = drivers.location_ping_sec (٢٠ ث افتراضاً · من الخادم)
       minUpdateInterval = الفترة ÷ ٢
       minUpdateDistance = MIN_MOVE_M      ← «ودونها ضجيجُ قمرٍ صناعيّ»
       maxUpdateDelay    = الفترة × ٢      ← «يوقظ الجهازَ مرّةً بدل مرّات»
  └─ isMocked()  →  يُرفع الوسمُ مع النقطة        (٢٠٢٦-٠٩-٠٣)
  └─ LastPoint.set(...)  →  الشاشةُ تقرؤه
  └─ sendLocation(...)   →  POST /driver/location
       └─ فشلٌ  →  queue.add(TrackPoint)  →  POST /driver/location/batch لاحقاً
```

| البند | المُثبَت |
|---|---|
| **الخدمةُ تُقتل** | `START_STICKY` يُعيدها ✅ · **وسامسونغ قد تمنع** — ومن هنا بطاقةُ البطّاريّة |
| **إعفاءُ البطّاريّة** | يُطلب **عند رفع الورديّة وحدَها** ✅ (٢٠٢٦-٠٩-٠٣) |
| **انقطاعُ الشبكة** | **طابورٌ محلّيٌّ ثمّ دفعةٌ** ✅ · **وحدُّه مُثبَتٌ — U6 حُلّ** أدناه |
| **الموقعُ المزيَّف** | يُوسَم ويُرفع · **وإثباتُ التسليم يُرفض منه** ✅ |
| **الشاشةُ مطفأة** | الخدمةُ أماميّة **فتستمرّ** — **ما لم يقتلها النظام** |
| **حدُّ الشيخوخة في الخادم** | ١٥ دقيقة — **فانقطاعٌ أطولُ يُخرجه من الطابور** ✅ |

## ج · الإشعاراتُ الدافعة — **السلسلة**

```
RahalPushService : FirebaseMessagingService        (في :ui — واحدةٌ للأربعة)
  ├─ onNewToken(token)      →  Push.register  →  POST /me/devices
  └─ onMessageReceived(msg) →  NotificationCompat + PendingIntent(FLAG_IMMUTABLE)
                                → يفتح التطبيقَ عند الضغط
```

**والتسجيلُ يقع في `AppFrame` عند ثبوت الجلسة** ✅ — **لا في
`onNewToken` وحدَها**، «فمن ثبّت التطبيقَ قبل أن تُضاف الخدمةُ لا
يُسجَّل أبداً».

### U46 حُلّ ✅ — **الإشعارُ يفتح الشاشةَ الرئيسيّةَ لا الطلب**

**مُثبَتٌ في `RahalPushService`:**

```kotlin
val open = PendingIntent.getActivity(
    this, 0,
    Intent(this, home()).addFlags(Intent.FLAG_ACTIVITY_CLEAR_TOP),
    PendingIntent.FLAG_IMMUTABLE or PendingIntent.FLAG_UPDATE_CURRENT,
)
```

| | |
|---|---|
| **الوجهة** | **`home()` — نشاطُ التطبيق الرئيسيّ** · **لا رابطَ عميقٌ للطلب** |
| **الرايات** | `CLEAR_TOP` — **لا يُكدّس نسخاً** ✅ |
| **حقلُ `Href` في الإشعار** | **يصل من الخادم ولا يستعمله التطبيق** ✅ — **مُطابقٌ لما قِيس سابقاً** |

> **PROVEN BEHAVIOR** — «طلبٌ أُسند إليك» **يفتح التطبيقَ لا الطلبَ.**
> **وأثرُه على السائق**: يبحث عن الطلب بيده. **قرارُ مالك.**

### U6 حُلّ ✅ — **حدُّ الطابور مقيسٌ ومربوطٌ بالمحرّك**

**مُثبَتٌ في `PointQueue.kt`:**

```kotlin
private fun trim() {
    if (file.length() < MAX_BYTES) return
    val lines = file.readLines().takeLast(MAX_POINTS)   // ← الأحدثُ يبقى
    file.writeText(...)
}
// **سقفُ الدفعة في المحرّك ٢٠٠** — فلا يُحفظ أكثر ممّا يُرسل مرّة.
const val MAX_POINTS = 200
```

| | |
|---|---|
| **الحدّ** | **٢٠٠ نقطة** — **مطابقٌ لسقف `location/batch` في المحرّك** ✅ |
| **وحدٌّ بالحجم** | `MAX_BYTES` أيضاً |
| **ما يُحذف** | **الأقدم** (`takeLast`) — **فالأحدثُ ينجو** ✅ |
| **الأثر** | انقطاعٌ يتجاوز ٢٠٠ نقطةً (**~٦٧ دقيقةً على ٢٠ ثانية**) **يفقد أوائلَ الأثر لا أواخرَه** |

> **PROVEN BEHAVIOR — والاختيارُ صحيح**: الموضعُ الحاليُّ أهمُّ من
> الماضي، **والحدُّ مشتقٌّ من عقد المحرّك لا مخترَع.**

## د · موتُ العمليّة واستعادةُ الحال

| البند | المُثبَت |
|---|---|
| **`rememberSaveable`** | **١٩ موضعاً في تطبيق السائق** — فحالُ الشاشة يعود بعد قتلِ العمليّة ✅ |
| **حالُ الملاحقة** | **في نموذج العرض لا في الشاشة** — «حالٌ تحكم شيئاً باقياً لا تعيش في شاشةٍ تُبنى وتُهدم» ✅ |
| **الورديّة** | **في الخادم** (`users.on_shift`) — **فلا تضيع بموت التطبيق** ✅ |
| **السلّة** | **`SharedPreferences`** — **تنجو من القتل** ✅ |

## هـ · الجلسةُ أثناء العمل — **U5 حُلّ** ✅

**مُثبَتٌ في `ApiClient.kt`:**

```kotlin
if (accessToken.isEmpty() && refreshToken.isEmpty())
    throw ApiException(401, "not_signed_in")
...
if (e.status != 401 || refreshToken.isEmpty()) throw e
refresh()          ← تجديدٌ تلقائيٌّ ثمّ إعادةُ النداء
```

| الحال | ما يقع |
|---|---|
| **انتهى توكنُ الوصول أثناء رحلة** | **يُجدَّد تلقائيّاً ويُعاد النداء** ✅ — **فلا ينقطع العمل** |
| **انتهى توكنُ التجديد** | المحرّكُ يردّ `invalid_refresh` · **والشاشةُ تقرؤها «جلسةٌ منتهية»** ✅ |
| **بلا توكنٍ إطلاقاً** | `not_signed_in` — **بلا نداءِ شبكة** ✅ |

**فالجلسةُ لا تكسر رحلةً جارية** ما دام توكنُ التجديد حيّاً (**٣٠ يوماً**).

## و · أحكامُ الجهاز

> **`DEVICE PERMISSION MATRIX: 4/4 APPS MEASURED`** ✅
> **`DRIVER LOCATION LIFECYCLE: FULLY TRACED`** ✅
> **`PUSH LIFECYCLE: TRACED`** ✅ · **`DEEP-LINK TARGET: UNKNOWN (U46)`**
> **`PROCESS DEATH / STATE RESTORE: TRACED`** ✅
> **`SESSION REFRESH DURING ACTIVE WORK: PROVEN (U5 CLOSED)`** ✅
> **`OFFLINE QUEUE BOUND: UNKNOWN (U6)`**

---

# ٢-ط · الفحصُ المتقاطع — **BOTTOM-UP** ✅

> **الغاية**: **كلُّ شيءٍ يكتب أو يقرّر يعود إلى تدفّقٍ معروف.**
> **وما لا يعود = GAP.**

## أ · الطفراتُ السبعُ والخمسون بعد المئة → مجالات

| المجال | العدد | الحال |
|---|---|---|
| **ADMIN** | **٧٤** | ✅ مُصنَّفةٌ بالأثر (§٢-ز) |
| **AUTH** (`/auth/*` · `/pin/*`) | **٢٦** | ✅ مُحصاة · **موصوفةٌ جزئيّاً** |
| **DRIVER** | **١٥** | ✅ الطابورُ والقبولُ والانتقالاتُ والإثبات |
| **MERCHANT** | **١٥** | ✅ (§٢-د) — بحراسِ ملكيّةٍ مُثبَتة |
| **SELF** (`/me/*` · `/my/*`) | **١٤** | ✅ العناوينُ والتفضيلاتُ والشكاوى والأجهزة |
| **CUSTOMER-ORDER** | **٥** | ✅ الإنشاءُ والإلغاءُ والتقييمُ والرسائل |
| **REP** | **٥** | ✅ (§٢-د) |
| **PUBLIC** | **٢** | `POST /public/quote` · `POST /promo/preview` |
| **UNCLASSIFIED** | **١ → ٠** | أدناه |
| **المجموع** | **١٥٧** | ✅ |

### الشاذّةُ الوحيدة — **فُحصت وفُسّرت** ✅

**`POST /api/v1/promo/preview`** — **`POST` بلا كتابة.**

| | المُثبَت |
|---|---|
| المُعالِج | `handlePromoPreview` → `orders.PreviewPromo` |
| **أتكتب؟** | **لا** — لا `INSERT` ولا `UPDATE` ولا `promo_redemptions` |
| **لماذا `POST`؟** | **لأنّ لها جسماً** (`code` · `subtotal` · `delivery_fee`) |
| **الأهمّ** | **تنادي `validatePromo` نفسَها التي عند الطلب** — واختبارٌ يحرس ذلك: `TestPreviewPromo_MatchesOrder` · «**ما تَعِد به المعاينةُ يقع عند الطلب**» ✅ |

> **`MUTATION ROUTES TIED TO A DOMAIN: 157/157`** ✅
> **`UNCLASSIFIED MUTATIONS: 0`** ✅

## ب · الجداولُ القابلةُ للكتابة → تدفّقات

| المجموعة | جداول | التدفّق |
|---|---|---|
| **الهويّة** | `users` · `user_roles` · `otp_codes` · `refresh_tokens` · `phone_claims` · `device_tokens` · `app_secrets` | AUTH (§٩ في التقرير) · `EnsureUserWithRole` |
| **الطلبات** | `orders` · `order_items` · `order_events` · `order_messages` · `order_ratings` · `promo_redemptions` | A-001 → A-003 · الانتقالاتُ الـ٥٥ |
| **المال** | `wallets` · `wallet_transactions` · `driver_cash_boxes` · `driver_cash_entries` · `payout_requests` · `expenses` · `expense_categories` | **٣٠ موضعَ `ApplyTx`** ✅ |
| **المتاجر** | `merchants` · `menu_items` · `menu_sections` · `merchant_hours` · `merchant_leads` · `store_sections` · `merchant_ratings` · `merchant_warnings` | §٢-د · D-01 → D-05 |
| **المعدِّلات** | `modifier_groups` · `modifier_options` | **✅ U31 حُلّ** — تُكتب **مع الصنف** في `catalog/menu.go` |
| **الجغرافيا** | `governorates` · `districts` · `cities` · `delivery_zones` | ADMIN/CONFIG · §١٦ |
| **الدعم** | `tickets` · `ticket_replies` · `disputes` · `warnings` · `driver_emergencies` | ADMIN/STATE · A-016 · A-017 |
| **التتبّع** | `driver_track` | §٢-ح — دورةُ الموقع |
| **التسويق** | `offers` · `promo_codes` · `banners` · `categories` · `platform_sections` · `referrals` | ADMIN/CONFIG · A-018 |
| **النظام** | `app_settings` · `audit_log` · `idempotency_keys` · `media` · `notifications` · `app_opens_daily` · `user_addresses` · `user_favorites` | ADMIN · SELF · البنية |

### U31 حُلّ ✅ — **المعدِّلاتُ تُكتب مع الصنف لا بمسارٍ خاصّ**

**مُثبَتٌ في `internal/catalog/menu.go`:**

```sql
DELETE FROM modifier_groups WHERE item_id = $1          ← تُمسح ثمّ تُكتب
INSERT INTO modifier_groups (item_id, name, min_select, max_select, sort_order)
INSERT INTO modifier_options (group_id, name, price_delta, sort_order)
```

| | المُثبَت |
|---|---|
| **المسار** | **لا مسارَ مستقلّاً** — تُرسل ضمن جسم `POST/PATCH menu/items` |
| **المعاملة** | **`tx.Exec`** — **في معاملة الصنف نفسِها** ✅ |
| **الأسلوب** | **مسحٌ ثمّ كتابةٌ كاملة** (`replace-all`) — لا تعديلٌ جزئيّ |
| **الاستهلاك** | `orders/service.go` يقرؤها عند التسعير: `min_select, max_select` و`price_delta` — **فالمعدِّلاتُ تدخل السعر** ✅ |
| **العرض** | `catalog/menu.go` يردّها في `Modifiers []ModifierGroup` |

**فادّعائي «جدولان بلا كاتب» كان خطأَ أداةٍ**: بحثتُ عن `modifier` في
أسماء المسارات ولم أبحث في أجسام الطلبات.

### U48 حُلّ ✅ — **معرّفٌ قديمٌ يُرفض ولا يُتجاهَل**

**`DELETE` ثمّ `INSERT` يعني أنّ معرّفاتِ الخيارات تتبدّل مع كلّ تعديل
صنف.** **فماذا يقع لسلّةٍ تحمل معرّفاً قديماً؟**

**مُثبَتٌ في `priceItems` (`internal/orders/service.go`):**

```sql
SELECT g.id, g.name, o.name, o.price_delta, o.available
FROM modifier_options o JOIN modifier_groups g ON g.id = o.group_id
WHERE o.id = $1 AND g.item_id = $2
→ pgx.ErrNoRows  ⇒  return ErrBadItems
```

| | المُثبَت |
|---|---|
| **معرّفٌ لا وجودَ له** | **`ErrBadItems`** — **الطلبُ يُرفض كلُّه** ✅ |
| **خيارٌ من صنفٍ آخر** | **يُرفض** — الشرطُ `g.item_id = $2` ✅ |
| **`o.available` يُقرأ** | ✅ — **فالتوفّرُ يُفحص على مستوى الخيار أيضاً** |
| **ومتى يُكتشف؟** | **في `quote` قبل الإرسال** — لأنّها تنادي `priceItems` نفسَها ✅ |

> **PROVEN BEHAVIOR — آمنٌ ومتّسق**: **الفشلُ صريحٌ لا صامت**، والزبونُ
> يراه في السلّة قبل أن يضغط.
>
> **⚠️ وما بقي**: **نصُّ الخطأ الذي يراه** — أمفهومٌ أم `ErrBadItems`
> خامّة؟ ❓ **U34** (نفسُه).

> **`MUTABLE TABLES TIED TO A FLOW: 54/54`** ✅
> **`UNTIED: 0`** ✅

## ج · بقيّةُ المنتِجين → تدفّقات

| المنتِج | العدد | مربوطٌ بتدفّق؟ |
|---|---|---|
| **الكتاباتُ المباشرةُ على الحالة** | ٢ | ✅ DSW-1 (داخلَ البوّابة) · DSW-2 (**D1**) |
| **مواضعُ المال** | ٣٠ | ✅ كلُّها |
| **أسماءُ التدقيق** | ٤٧ | ✅ مُحصاة · **٢٧ طفرةً بلا تدقيقٍ مُثبَت** ❓ |
| **بثُّ الزمن الحيّ** | ٦١ (`touch` ٥٣ · `touchUser` ٨) | ✅ ١٣ نوعَ حدث |
| **عمّالُ الخلفيّة** | ١ | ✅ الراصد |
| **مُشغِّلاتُ القاعدة** | ١ | ✅ `wallet_for_new_user` |
| **الدوالُّ المخصَّصة** | ١ | ✅ نفسُها |
| **قارئو الإعدادات** | ١٢٧ مفتاحاً | ⚠️ **٤ مقيسةٌ في الإنتاج · والقارئون لكلٍّ لم يُحصوا** ❓ **U47** |

## د · GAPs — **ما لا يعود إلى تدفّق**

| # | الفجوة | الحال |
|---|---|---|
| **G-A** | `modifier_groups` · `modifier_options` — **جدولان بلا مسارٍ يكتبهما** | ❓ U31 |
| **G-B** | **٢٧ طفرةً إداريّةً بلا تدقيقٍ مُثبَت** | ❓ U44 |
| **G-C** | **قارئو الإعدادات لكلّ مفتاح** — من يقرأ ماذا ومتى يسري | ❓ U47 |

---

# ٢-ي · الفحصُ المتقاطع — **TOP-DOWN** ✅

> **من كلّ شاشةٍ نزولاً**: الشاشةُ → الدالّة → المسار → المحرّك →
> القاعدة.

## أ · الجوّال — **بُني آليّاً**

**لكلّ ملفٍّ في التطبيقات الأربعة و`ui`: أيَّ دوالِّ الطفرة يستدعي.**

| | العدد |
|---|---|
| **ملفّاتٌ تُطفِر الخادم** | **٤١** |
| **وصلاتُ (ملفّ → طفرة)** | **١٠٨** |

### التوزّع

| الوحدة | ملفّات | وصلات |
|---|---|---|
| **`ui` المشتركة** | ١١ | **٣٧** |
| **السائق** | ١١ | **٢٧** |
| **الزبون** | ٨ | ٢٠ |
| **المتجر** | ٨ | ١٨ |
| **المندوب** | ٣ | ٦ |

### أكثفُ المواضع

| الملفّ | طفرات |
|---|---|
| `ui/AccountViewModel.kt` | **١٤** |
| `ui/AuthViewModel.kt` | **١٠** |
| `driver/OrdersViewModel.kt` | **٨** |
| `customer/CartScreen.kt` | ٥ |
| `customer/HistoryScreen.kt` · `merchant/MenuViewModel.kt` · `merchant/StoreViewModel.kt` | ٤ لكلٍّ |

**حقيقةٌ مُثبَتة**: **٣٧ من ١٠٨ وصلةً في `ui` المشتركة** — **فثلثُ
طفرات الجوّال يقع في مكتبةٍ واحدةٍ تخدم الأربعة.** ✅ **وهذه مركزيّةٌ
حقيقيّةٌ لا دعوى.**

## ب · الشاشاتُ — قارئةٌ ومُطفِرة

| | العدد |
|---|---|
| **ملفّاتُ `*Screen.kt`** | **٢٧** |
| ↳ **تُطفِر الخادم** | **١٣** |
| ↳ **قارئةٌ فقط** | **١٤** |

**والفرقُ مقصود**: **الطفراتُ تعيش في `ViewModel` لا في الشاشة** —
وهو نمطٌ مُطَّرد (`AccountViewModel` · `AuthViewModel` ·
`OrdersViewModel` · `MenuViewModel` · `StoreViewModel`).

**فالشاشةُ تعرض، والنموذجُ يُطفِر** ✅ — **وهذا يفسّر لماذا عدُّ
«مواضع الضغط» (٢٧٥) لا يصلح مقاماً.**

## ج · حصادُ الاتّجاهين

| الاتّجاه | النتيجة |
|---|---|
| **BOTTOM-UP** | ١٥٧/١٥٧ طفرةً إلى مجال · ٥٤/٥٤ جدولاً إلى تدفّق ✅ |
| **TOP-DOWN** | ٤١ ملفّاً · ١٠٨ وصلةً · ٢٧ شاشةً مصنَّفةً قارئةً/مُطفِرة ✅ |
| **الفجوات** | **٢** — U44 (تدقيق) · U47 (قارئو الإعدادات) |

### وما كشفه كلُّ اتّجاهٍ ولم يكشفه الآخر

| الاتّجاه | كشف |
|---|---|
| **BOTTOM-UP** | `promo/preview` الشاذّة · جدولا المعدِّلات · `settle-goods` المهجور |
| **TOP-DOWN** | **٥ دوالٍّ يتيمةٍ في عقد الـAPI** (§٢-هـ) · **ثلثُ الطفرات في `ui`** · **الطفراتُ في النماذج لا الشاشات** |

**فالاتّجاهان ليسا تكراراً** ✅ — **وكلٌّ منهما أمسك ما فات الآخر**،
وهو ما أراده المالكُ نصّاً.

## د · ما لا يزال خارجَ الاتّجاهين

### U44 حُلّ ✅ — **التدقيقُ في أربع طبقاتٍ لا في المُعالِج وحدَه**

**وقياسي الأوّل (٤٧ اسماً) كان ناقصاً** — عدَّ `s.audit(r, …)` في
`internal/server` وحدَه.

**والقياسُ الصحيح**: بُحث عن `INSERT INTO audit_log` **في المحرّك
كلِّه**، وعن أسماء الأفعال في كلّ الحزم:

| الكاتب | الطبقة |
|---|---|
| `internal/server/audit.go` | **العامّة** — `s.audit(r, ...)` |
| `internal/server/admin_users_handlers.go` | المستخدمون |
| **`internal/identity/repo.go`** | **الهويّة** — `admin.user_create` من داخل `EnsureUserWithRole` |
| **`internal/catalog/catalog.go`** | **الكتالوج** — تدقيقُ إنشاء المتجر |

| | العدد |
|---|---|
| **أسماءُ أفعالٍ مدقَّقةٍ فريدة** | **١٠٤** ✅ |
| (والقياسُ الأوّل قال ٤٧ — لأنّه نظر في طبقةٍ واحدة) | |

**فادّعاءُ «٢٧ طفرةً بلا تدقيق» ساقط**: **التدقيقُ يقع حيث يقع الفعلُ
الحقيقيّ** — في الخدمة لا في المُعالِج. **وهو أمتن**: لا يُنسى إن نُودي
الفعلُ من بابين.

> **`AUDITED ACTION NAMES: 104 MEASURED ACROSS 4 LAYERS`** ✅
> **`UNPROVEN AUDIT COVERAGE: 0 — الادّعاءُ سقط`** ✅

### U47 حُلّ ✅ — **٩٥ مفتاحاً تشغيليّاً من ١١٨**

> **⚠️ وصُحّح مرّتين**: ٣٨ ← ٦٠ ← **٩٥**، والمقامُ ١٢٧ ← **١١٨**.
> **والقياسُ النهائيُّ في**
> [`INDEPENDENT_REVIEW_RECONCILIATION.md`](INDEPENDENT_REVIEW_RECONCILIATION.md).

> **⚠️ وهذا القسمُ صُحّح**: قِيس أوّلاً بنمطٍ ضيّقٍ فأعطى **٣٨**، ثمّ
> شُمل `intSetting` و`settingInt` و`GetFloat` **فصار ٨٦ مقروءاً منها
> ٦٠ تشغيليّاً.** **والخريطةُ الكاملةُ في**
> [`CONFIG_IMPACT_MAP.md`](CONFIG_IMPACT_MAP.md).

**والقياسُ النهائيّ:**

| | العدد |
|---|---|
| **مفاتيحُ الكتالوج** | **١٢٧** |
| **يقرؤها المحرّك** | **٨٦** |
| **تشغيليّةٌ تغيّر السلوك** | **٦٠** |
| **عرضٌ وهويّة** | **٢٦** |

**والقياسُ الأوّلُ (٣٨) باقٍ أدناه للسجلّ — وهو مُتجاوَز:**

| | العدد |
|---|---|
| **مفاتيحُ الكتالوج** | **١٢٧** |
| **مفاتيحُ يقرؤها المحرّكُ فعلاً** | **٣٨** |
| **الباقي (٨٩)** | **عرضٌ وهويّةٌ وموقع** (`Site` ٢٩ · `Platform` ٣١ …) — تُقرأ في الويب لا في قرارٍ تشغيليّ |

**والثمانيةُ والثلاثون هي التي تغيّر السلوك** — ومنها المقيسُ في
الإنتاج:

```
drivers.{assignment_mode · cash_limit · location_ping_sec · direct_assign · assigned_silence_sec}
delivery.{fee · max_fee · per_km · by_distance · default_radius_m}
customers.{cash_ban_days · cash_ban_failures · max_addresses · require_whatsapp · signup_bonus}
sales.commission_percent · security.{session_days · otp_ttl_min · password_min_length}
```

### U49 حُلّ ✅ — **لا ذاكرةَ وسيطةً إطلاقاً · والأثرُ فوريّ**

**مُثبَتٌ في `internal/settings/settings.go`:**

```go
func (s *Store) Get(ctx, key, out) error {
    err := s.db.QueryRow(ctx, `SELECT value FROM app_settings WHERE key = $1`, key).Scan(&raw)
    ...
}
func (s *Store) GetInt(ctx, key) int64 {
    def, known := Lookup(key)        // ← افتراضُ الكتالوج
    if s.Get(...) != nil { return fallback }
}
```

| السؤال | الجواب المُثبَت |
|---|---|
| **ذاكرةٌ وسيطة؟** | **لا شيءَ** — لا Redis ولا `sync.Map` ولا TTL ✅ |
| **متى يسري التغيير؟** | **في النداء التالي مباشرةً** — **بلا إعادة تشغيل** ✅ |
| **الثمن** | **استعلامُ قاعدةٍ لكلّ قراءةِ مفتاح** — وبعضُ المُعالِجات تقرأ أكثرَ من مفتاح ⚠️ **PROVEN RISK** على الحمل، **ولم يُقَس** |
| **مفتاحٌ غائب** | **يردّ افتراضَ الكتالوج ولا يفشل أبداً** ✅ — «لا يفشل أبداً» نصّاً |

> **PROVEN BEHAVIOR**: **الإعدادُ يسري فوراً** — وهو ما يُتوقَّع من
> لوحةِ تحكّم. **والمقايضةُ حملٌ لم يُقَس.**

## د · ما لا يزال خارجَ الاتّجاهين

| # | الخارج | لماذا |
|---|---|---|
| **١** | **لوحةُ الإدارة TOP-DOWN** | **رُبطت الطفراتُ بأسطحها** (٣٠ سطحاً) **ولم تُربط بأزرارها** |
| **٢** | **ذاكرةُ الإعدادات** | ❓ U49 |

---

# ٣ · ما لم يُوصف بعد — بالأرقام

| المجموعة | موصوف | المقام |
|---|---|---|
| **أفعالٌ ماليّة/حالة** | **٢٢** | ~٤٠ |
| باقي الطفرات | **٠** | ~١١٧ |
| **المجموع** | **٢٢** | **١٥٧** |

## مواضعُ المال — الحال

| | مُحدَّد | موصوف |
|---|---|---|
| `transitions.go` | ٨ | **٨** (A-001 · A-003 وفروعُهما) |
| `referrals.go` | ٤ | **٤** (A-018) |
| `treasury.go` | ٣ | **٣** (A-022) |
| `incentives/*` | ٤ | **٤** (A-019 · A-020) |
| `custom.go` | ٢ | **٢** (A-021) |
| `goods.go` | ٢ | **٢** (A-013) |
| `failure_aftermath.go` | ٢ | **٢** (A-014 · A-013) |
| `service.go` · `support.go` · `disputes.go` · `driver_return.go` | ٤ | **٤** (A-001 · A-017 · A-016 · A-015) |
| **المجموع** | **٣٠** | **٣٠** |

> **`FINANCIAL MUTATION SITES LOCATED = 30/30`** ✅
> **`FINANCIAL MUTATION SITES OPERATIONALLY DESCRIBED = 30/30`** ✅
> **بالمستوى الذي يسمح به القياسُ من القراءة** — وما لم يُقرأ مُعلَّمٌ
> `❓` في مكانه (`U19` · شروطُ `treasury.go`).

## الأفعالُ الباقيةُ خارجَ المال

`cash box settle` · `customer cancel` · `merchant reject` ·
`merchant accept/prepare/ready` · `driver decline/release/report` ·
`emergency` · `proof` · `rating` · `chat` · إدارةُ القائمة ·
إدارةُ المستخدمين والأدوار · الإعدادات · العروض · المناطق

---

# ٣-ب · الاكتشافاتُ — **مُصنَّفةٌ بإثبات النتيجة لا بالملاحظة**

> **وأُعيد تصنيفُ ما سبق بعد قياسٍ ثانٍ — وسقط ادّعاءان لي.**

## PROVEN DEFECT — النتيجةُ مُثبَتة

### D1 · فكُّ الإسناد لا يُسجَّل في تاريخ الطلب

| | |
|---|---|
| **الموضع** | `internal/orders/rotation.go` — `SET driver_id=NULL, status='dispatching'` |
| **الدليل** | **لا ذكرَ لـ`order_events` في الملفّ كلِّه** — بُحث فلم يوجد |
| **الثابتُ المكسور** | **تاريخُ الطلب يجب أن يفسّر كلَّ تبدّلِ حالة** — وهو ما تفعله البوّابةُ في كلّ انتقالٍ آخر |
| **النتيجة** | **الطلبُ يقفز في السجلّ من `assigned` إلى سائقٍ آخرَ بلا سطرٍ يفسّر** — ولا يُعرف من سُحب منه ولا متى ولا بأمرِ من |
| **الأثرُ العمليّ** | **شكوى «أخذ الطلبَ وتركه» لا دليلَ لها ولا عليها** |

### D2 · `convertLead` بلا معاملةٍ واحدة

| | |
|---|---|
| **الموضع** | `internal/server/leads_handlers.go` |
| **الدليل** | **بُحث عن `Begin(` فلم يوجد** — والدالّةُ تُنشئ متجراً وحساباً ودوراً وتحدّث الفرصةَ بنداءاتٍ متتابعة |
| **الثابتُ المكسور** | **إمّا أن يقع كلُّ التحويل أو لا شيءَ منه** |
| **النتيجة** | **فشلٌ بعد أيّ خطوةٍ يترك حالاً نصفيّة**: متجرٌ بلا مالك · أو مالكٌ بلا دور · أو فرصةٌ ما زالت `new` ومتجرُها قائم |
| **ولا تراجعَ يجمعها** | ✅ مُثبَت |

## PROVEN BEHAVIOR — سلوكٌ آمنٌ أو قرارُ منتَج

### B1 · `accept` بلا مفتاح تكرار — **آمنٌ بالبنية** ✅

**وكنتُ صنّفتُه ثغرةً (`G6`) — والقياسُ نقضه:**

**المسارُ المُثبَت**: `TransitionWithReason` يقرأ `status` **بقفل
`FOR UPDATE`**، ثمّ يفحص `canTransition(kind, from, to, roles)`.

**فالنداءُ الثاني** يقرأ `from = assigned`، و`assigned → assigned`
**ليست في `allowedTransitions`** → **يُرفض.**

**والقفلُ يُسلسل المتزامنَين.** **فلا أثرَ جانبيٌّ ثانٍ** — ومفتاحُ
التكرار **لا يضيف حمايةً** هنا.

**⚠️ وما بقي غيرَ مُثبَت**: سلوكُ التطبيق حين يردّ الخادمُ خطأً على
الضغطة الثانية — أيُقرأ فشلاً؟ ❓

### B2 · تعويضُ السائق — **محميٌّ فعلاً** ✅

**وكنتُ صنّفتُه ثغرةً (`G3`) — والقياسُ نقضه:**

**المُثبَت** في `handleCompensateDriver`:
1. `SELECT status, driver_id FROM orders WHERE id = $1 **FOR UPDATE**`
2. ثمّ فحصُ `already` — **فإن كان التعويضُ وقع رُدَّ النداء**

**وتعليقُ الشيفرة يذكر القصّةَ كاملةً**: كان العيبُ موجوداً فعلاً،
**وكُشف بفحص التغطية ٢٠٢٦-٠٨-٠٨ وأُصلح بقرار المالك.**

**فهو اليومَ سليم.**

### B3 · لا إشعارَ دافعاً للزبون عند قبول السائق

**مُثبَت**: `driver_handlers.go` بلا `notify`. **والإشعارُ `driverAssigned`
في `admin_orders_handlers.go` وحدَه** — أي عند الإسناد اليدويّ.

**والزبونُ يعلم بالبثّ الحيّ.** ⚠️ **والنتيجةُ لم تُثبت**: هل يرى الزبونُ
التبدّلَ وتطبيقُه مغلق؟ **يحتاج قرارَ مالك.**

### B4 · اللوحةُ تُسند إلى سائقٍ موقعُه متوقّف

**مُثبَت**: تُكتب «موقعه متوقف» بجانب اسمه **ولا يُحجب**.
**والمحرّكُ يحجبه عن الإسناد الآليّ وحدَه.**

**وقد يكون مقصوداً**: المكتبُ قد يعرف أنّ السائقَ يعمل وهاتفُه معطّل.
**قرارُ مالك.**

## PROVEN RISK — مُثبَتٌ والنتيجةُ لم تُثبت

### R1 · `orders.status` يُكتب من موضعين خارجَ البوّابة

`merchant_blocked.go` (يكتب التاريخ ✅) · `rotation.go` (لا يكتبه → **D1**).

**والخطرُ الباقي**: **بوّابةٌ يُلتفّ عليها اليومَ في موضعين قد يُلتفّ
عليها في ثالثٍ غداً** — ولا حارسَ يمنع ذلك.

---

# ٤ · مجهولاتٌ ظهرت أثناء هذا العمل

| # | المجهول | الحال |
|---|---|---|
| **U13** | أتكتب الكتابتان المباشرتان في `order_events`؟ | **✅ حُلّ** — `merchant_blocked` نعم · `rotation` **لا** |
| **U14** | `convertLead` أفي معاملةٍ واحدة؟ | **✅ حُلّ** — **لا معاملةَ إطلاقاً** |
| **U16** | إشعارُ الزبون عند قبول السائق | **✅ حُلّ** — **لا إشعارَ دافعاً؛ البثُّ وحدَه** |
| **U15** | مسارُ `POST /rep/leads` الدقيق · ورقمُ متجرٍ مكرَّر | ❓ |
| **U17** | توقيتُ رسالة واتساب إلى المتجر | ❓ |
| **U18** | **هل يُقفل `disputes` قبل التسوية؟** — رأيتُ المعاملةَ ولا القفل | ❓ |
| **U19** | **`treasury.go` ثلاثةُ قيود** — لم تُقرأ شروطُها | ❓ |

---

**آخرُ تحديث**: ٢٠٢٦-٠٩-٠٤ · `26f93c5d`
