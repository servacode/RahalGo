# رحّال غو — مطابقةُ المراجعة المستقلّة · الدفعةُ الخامسة

## البنية · التعافي · الخدمات الخارجيّة · منظومةُ الاختبار القائمة

> **CODE TRUTH BASELINE**: `26f93c5d` — **ولا سطرَ شيفرةٍ تشغيليّةٍ مسّ.**
>
> **وقاعدةُ الفصل بأمر المالك**: **نقصُ أداةِ اختبارٍ أو تشغيلٍ لا يرفع
> عدّادَ عيوب التطبيق.** **فثلاثةُ عدّاداتٍ جديدةٍ مستقلّة:**
> `OPERATIONAL RISK` · `TEST-INFRA GAP`.

---

## ٠ · الحصيلة

| ID | الادّعاء | الحكم | الرمز |
|---|---|---|---|
| **R5-1** | التحويلُ التلقائيّ يقبل ثمّ يعجز عن الإبلاغ | **PROVEN RISK** | **R21** |
| **R5-2** | الراصدُ يسم الإنذارَ ثمّ يفقد الإشعار الباقي | **PROVEN RISK** | **R22** |
| **R5-3** | الدفعُ طلقةٌ واحدةٌ بلا صندوقِ إرسالٍ ولا إعادة | **PROVEN RISK** | **R23** |
| **R5-4** | `/healthz` أخضرُ وخدماتٌ ساقطة | **OPERATIONS MISMATCH** | **OPS-1** |
| **R5-5** | «الإصدارُ مسموح» بلا تحقّقٍ تشغيليّ كامل | **DOCUMENTATION / SEMANTIC RISK** | **TIG-1** |
| **R5-6** | دورةُ E2E القديمة شائخة | **TEST-INFRA GAP — ولا تعمل أصلاً** | **TIG-2** |
| **R5-7** | لا نسخَ احتياطيَّ ولا استعادة | **PROVEN OPERATIONAL RISK** | **OPS-2** |
| **R5-8** | `moneycheck` لا يرى `D5` | **TEST / INVARIANT GAP** | **TIG-3** |
| **R5-9** | فشلُ التحويل التلقائيّ بلا أثرٍ يُقرأ | **SILENT OPERATIONAL RISK (جزئيّاً)** | **R24** |

**والمراجعةُ محقّةٌ في التسعة.**
**ولا عيبَ منتجٍ جديدٍ في هذه الدفعة** — **أربعةُ مخاطرَ منتجٍ وخمسةُ
نواقصِ أدوات.**

---

# R5-1 — **R21** · قُبل ولم يُبلَّغ

## INTENDED CONTRACT — **مكتوبٌ في الملفّ نفسِه**
`auto_transfer.go:60`:
> «ولو قُبل ثمّ تعذّر الإبلاغُ **لَبقي الطلبُ في «مقبول» بلا أن يعلم به
> المتجر** — وهي حالٌ أسوأُ من `pending`: الشاشةُ تقول إنّ شيئاً جرى»

## ACTUAL CODE PATH
```
:44  settings.GetBool("orders.auto_transfer")   → غير مشتعل ⇒ return
:49  SELECT status … ≠ pending                  ⇒ return
:68  if !selfManage && !s.merchantReady()       ⇒ return   ← الحارس
:74  orders.Transition(… "accepted" …)          ⇒ يُقرّ
:84  loadOrderMessage — لا هاتفَ                ⇒ return   ← بعد القبول ❗
:90  merchant.SendText(...)  فشل                ⇒ return   ← بعد القبول ❗
:96  UPDATE orders SET sent_to_merchant_at = now()
:106 orders.AutoDispatch(...)
```

## FAILURE WINDOW
**`merchantReady()` يقول `s.merchant.Ready()`** (`server.go:107`) —
**حالُ البوتِ في تلك اللحظة لا ضمانُ وصولِ رسالة.**

**وبوتُ واتساب هاتفٌ حقيقيّ**: **تنقطع شبكتُه، وتُغلق جلستُه، ويردّ المزوّدُ
خطأً** — **بين الفحص والإرسال ثوانٍ تكفي.**

## الحالةُ النهائيّة
```
orders.status         = accepted
sent_to_merchant_at   = NULL
AutoDispatch          = لم يُنادَ إطلاقاً    ← الأخطر
المتجر                = قد لا يكون علم
الزبون                = يقرأ «قُبل»
```

**فليست حالَ «قُبل ولم يُبلَّغ» وحدَها** — **بل «قُبل ولم يُبلَّغ ولم يُطلب
له سائق».**

## RECOVERY PATH — **قائمةٌ ويدويّة**
| الوسيلة | الموضع |
|---|---|
| **العمودُ يُقرأ في اللوحة** | `sent_to_merchant_at` يُردّ في كلّ طلب (`orders/queries.go:28`) |
| **زرُّ الإبلاغ اليدويّ** | `merchant_dispatch.go:258-283` — يرسل ويسم العمود **ويُدقَّق** (`ops.order_notify`) |
| **زرُّ «طلب سائق»** | قائمٌ في اللوحة |
| **الإنذارُ الآليّ** | **`no_driver` لا يراه** (يشترط `preparing`/`dispatching`) · **و`too_long` يلتقطه** بعد مهلة التسليم |

## EXISTING GUARD / TEST
`merchantReady()` — **يقلّل ولا يمنع.**
**ولا اختبارَ لفشل الإرسال بعد القبول** — قِيس.

## VERDICT
> # **PROVEN RISK — R21**
> **ولا يُسمّى عيباً**: **الحالةُ مرئيّةٌ بعمودٍ صريح، ومُستدرَكةٌ بزرٍّ
> قائم، ويُنذَر عنها آخرَ الأمر.**
>
> **وهو خطرٌ لأنّ الحارسَ يوحي بضمانٍ لا يملكه** — **والعقدُ المكتوبُ فوقه
> يصف الحالةَ التي يقع فيها.**

---

# R5-2 — **R22** · إنذارٌ يُوسَم ثمّ يضيع إشعارُه

## INTENDED CONTRACT
`watchdog.go:122`:
> «`escalate` **يُنشئ إشعاراً باقياً** لكل طلبٍ عَلِق ولم يُنذَر بعد»

**والمقايضةُ مكتوبةٌ صراحةً**:
> «والعلامة تُوضع **قبل** الإشعار لا بعده … **وإشعارٌ ضائع أهون من إشعارٍ
> يتكرّر مئتي مرّة** — الثاني يُدرَّب المستخدم على تجاهله فيصير كالصمت»

## ACTUAL CODE PATH
```
:134 UPDATE orders SET alerted_at = now() WHERE alerted_at IS NULL   ← يُقرّ
:140 RowsAffected() == 0 ⇒ continue
:142 notify.NotifyOps(...)  → NotifyRoles → notifyQuery → NotifyMany
                             → INSERT فشل ⇒ Log + return   (notifications.go:307)
```

## FAILURE WINDOW
**`alerted_at` مُقرَّةٌ والإشعارُ لم يُكتب** — **ولا يُعاد أبداً**:
الدورةُ التالية تجد `RowsAffected() == 0` فتتخطّى.

**و`lastKey` يزيد الأمر**: `RunWatchdog:112` — **لا يُعاد البثُّ الحيُّ ما
لم تتبدّل مجموعةُ الإنذارات.** **فشاشةٌ مغلقةٌ لا تلتقط شيئاً.**

## RECOVERY PATH — **وهي التي تُخفّف الحكم**
**`Alerts(ctx)` تُحسب من `orders` في كلّ نبضة، ولا تُصفّى بـ`alerted_at`**
(قِيس: `watchdog.go:36-58` — الشروطُ حالاتٌ وأزمنةٌ لا غير).

**والنقطةُ `GET /admin/orders/alerts`** (`server.go:777`) — **فمن يفتح اللوحةَ
يرى الإنذارَ حيّاً وإن ضاع صفُّ الإشعار.**

**فالضائعُ جرسُ الإشعار لا الإنذارُ نفسُه.**

## VERDICT
> # **PROVEN RISK — R22**
> **والمقايضةُ مكتوبةٌ ومقصودةٌ للحالة الأولى** (منعُ التكرار) —
> **ولم تُكتب لحالة فشل الكتابة**، وهي التي تقع هنا.
>
> **والضررُ محدودٌ بجرسٍ واحد** — **وقائمةُ الإنذارات تُحسب حيّةً.**
> **ويبقى خطراً لأنّ من لا يفتح اللوحةَ لا يُنبَّه.**

---

# R5-3 — **R23** · الدفعُ طلقةٌ واحدة

## INTENDED CONTRACT — **مكتوبٌ وواضح**
`notifications.go:256`:
> «**وبعد الحفظ لا قبله**: **الصندوقُ هو الحقيقة، والدفعُ تنبيهٌ إليها.**
> فإن سقط الدفعُ بقي الخبرُ محفوظاً يُقرأ عند الفتح»

## ACTUAL CODE PATH
```
Notify → INSERT INTO notifications        ← الحقيقةُ تُحفظ ✅
       → pusher.SendToUser (Urgent = Kind == KindOrder)
           → t.Send(ctx, tokens, msg)
             err ⇒ s.logger.Error(...)     ← لا غير  (push.go:188-192)
```
**وقِيس**: `grep -rn 'outbox\|retry' internal/push/ internal/notifications/` →
**صفر.**

## الفصلُ المطلوب
```
PERSISTENT NOTIFICATION TRUTH   = ✅ مضمونة — صفٌّ في القاعدة قبل الدفع
REAL-TIME WAKEUP DELIVERY       = ❓ أفضلُ جهدٍ بلا إعادةٍ ولا صندوق
```

## وهل من قناةِ إيقاظٍ ثانية؟
| القناة | تُوقظ تطبيقاً مغلقاً؟ |
|---|---|
| الويب سوكت | **لا** — يحتاج التطبيقَ حيّاً |
| الإشعارُ في الصندوق | **لا** — يُقرأ عند الفتح |
| **البديلُ الوحيد** | **آليّاتُ المهلة**: `sweepAutoAccept` للمتجر · `SweepExpiredOffers` وانتقالُ الدور للسائق |

**فعرضُ سائقٍ ضاع دفعُه ينتقل إلى غيره بعد المهلة** ✅ — **وطلبُ متجرٍ ضاع
دفعُه يُقبل تلقائيّاً بعد مهلته** ✅. **والتعويضُ زمنيٌّ لا فوريّ.**

## VERDICT
> # **PROVEN RISK — R23**
> **والقرارُ مكتوبٌ في شطره الأوّل** (لا يُفشل الفعلَ التجاريّ) —
> **ولا نصَّ يعالج ضياعَ الإيقاظ العاجل.**
>
> **والضررُ زمنٌ لا حالةٌ فاسدة**: **متجرٌ لا يعلم بطلبه حتّى يفتح، أو حتّى
> تنقضي مهلةُ القبول التلقائيّ.**

---

# R5-4 — **OPS-1** · ما معنى `/healthz` بالضبط

## ACTUAL CODE PATH
`health.go:16-33` — **`pg.Ping` و`rdb.Ping` لا غير**، والبنيةُ:
`{status, postgres, redis}`.

## ما لا يُفحَص
```
OSRM · FCM · جاهزيّةُ واتساب · مزوّدُ SMS
مجلّدُ الوسائط (أيُقرأ؟ أيُكتب؟) · نمطُ الخرائط
حلقةُ الراصد الخلفيّة
```

## ولا نقطةَ جاهزيّةٍ ثانية
**قِيس**: `grep -rn 'readyz\|/ready' internal/` → **لا شيء.**
**وحالُ واتساب تُقرأ من نقطةٍ إداريّةٍ منفصلة** (`server.go:659`
`GET /admin/…/whatsapp`) — **خلفَ مصادقةٍ إداريّة، وليست جاهزيّةً.**

## ومن يقرؤه؟
**`deploy/compose.yml`**: **فحصُ الصحّةِ الوحيدُ للقاعدة** (`pg_isready`) —
**ولا حاويةَ تعتمد `/healthz` في إقلاعها أو إعادةِ تشغيلها.**
**وقِيس**: `grep healthz deploy/ .github/` → **صفر.**

## المعنى الحقيقيّ
```
/healthz = CORE DATABASE + CACHE LIVENESS
         ≠ PLATFORM READY FOR BUSINESS OPERATIONS
```

## VERDICT
> # **OPERATIONS MISMATCH — OPS-1**
> **والبنيةُ صادقةٌ في حقولها** (`postgres` و`redis` بالاسم) — **والاسمُ
> `status: "ok"` هو الذي يَعِد بأكثرَ ممّا يفحص.**
>
> **ولا يُسمّى عيبَ شيفرة**: **لا عقدَ مكتوبٌ يقول إنّه جاهزيّةٌ كاملة.**
> **والنقصُ أنّ OSRM يسقط فتسقط الملاحةُ كلُّها و`/healthz` أخضر.**

---

# R5-5 — **TIG-1** · «الإصدارُ مسموح» — ما يعنيه

## ACTUAL CODE PATH
`scripts/release-gate.py` — **المراحلُ الحاجبة**: بناءُ Go · اختباراتُ
المحرّك · منظومةُ QA · حرّاسُ الويب · وحدةُ أندرويد.
**وغيرُ الحاجبة**: التنسيق · الأنواع · الأداء · **واجهةُ أندرويد على جهاز**.

**ولا يُشغَّل**: `scripts/e2e/cycle.py` · `moneycheck` · `mediacheck` ·
**ولا قبولٌ على جهازٍ حقيقيٍّ حاجب.**

## ⚠️ وسببُ استبعادِ الاثنين مكتوبٌ وسليم
`release-gate.py:84`:
> «**هما تدقيقُ بياناتِ إنتاجٍ لا اختبارُ شيفرة** … وتشغيلُهما على قاعدة
> الاختبار يعطي «خللٌ في ٧ من ١٣» دائماً — وهو صحيحٌ ولا معنى له.
> **وحارسٌ يُنذر في كلّ تشغيلٍ يُتجاهَل بعد أسبوع**»

**فليس نسياناً بل قراراً معلَّلاً** — **ومحلُّهما الإنتاجُ بأمرِهما.**

## والتحفّظُ مكتوبٌ كذلك
`docs/qa/RELEASE_GATES.md:78`:
> «**والحكم «مسموح» يعني: ما بُني من حرّاس أخضر.** ولا يعني أنّ التطبيق
> جاهز — **الطبقات التي لم تُبنَ لا تُصدر حكماً**»

## ولكنّ التحفّظَ ليس في مخرَج الأداة
**السكربتُ يطبع `✔ الإصدارُ مسموح` مجرّدةً** (`:214`) — **ومن قرأ الطرفيّةَ
ولم يقرأ الوثيقةَ قرأ حكماً أوسعَ ممّا يعنيه.**

## VERDICT
> # **DOCUMENTATION / SEMANTIC RISK — TIG-1**
> **وليس عيبَ منتجٍ ولا نقصَ نيّة** — **التحفّظُ مكتوبٌ في موضعه.**
>
> ## وجوابُ سؤال المالك
> **`RELEASE ALLOWED` القديم لا يُعاد استعمالُه كما هو في المنظومة الجديدة.**
>
> **والسبب**: **حكمٌ اسمُه «مسموح» ومعناه «ما بُني أخضر» يحتاج وثيقةً بجانبه
> ليُقرأ صحيحاً** — **والأحكامُ لا تُقرأ بوثائقَ بل بأسمائها.**
> **فإمّا أن يُسمّى باسمه** (`GUARDS GREEN`) **وإمّا أن يُحال إلى التقاعد.**

---

# R5-6 — **TIG-2** · دورةُ E2E القديمة **لا تعمل**

## القياس
| البند | النتيجة |
|---|---|
| **الملفّات** | `scripts/e2e/cycle.py` ٣٠٤ سطراً · `cycle_part2.py` ٢٧٣ · `scripts/e2e-cycle.py` ١٧٧ |
| **الطريقة** | `requests` على `http://localhost:8080/api/v1` + SQL مباشر |
| **النقاطُ المُنادَاة** | **٢٣ نقطة** |
| **نقاطٌ شائخة** | **٠** — **قِيست كلُّها فوُجدت مسجَّلة** (و`/auth/me` و`/auth/password` داخلَ مجموعة `/auth` في `server.go:286-287`) |
| **حالاتُ الطلب المفترضة** | **٧** — `accepted` · `preparing` · `dispatching` · `assigned` · `at_pickup` · `picked_up` · `delivered` — **كلُّها قائمةٌ** ✅ |
| **افتراضاتُ إعداداتٍ شائخة** | **٤** |

## الأربعةُ الشائخة
| # | الافتراض | الحقيقة |
|---|---|---|
| ١ | **`len(settings) == 23`** (`cycle.py:110`) | **١١٨ تعريفاً** — والفرقُ خمسةُ أضعاف |
| ٢ | `merchants.default_commission_percent` | **غيرُ موجود** — والقائمُ `merchants.commission_percent` |
| ٣ | `drivers.share_mode` | **غيرُ موجود** |
| ٤ | `drivers.share_percent` | **غيرُ موجود** |

**والسبعةُ الباقيةُ في قائمته قائمةٌ كلُّها** ✅
(`merchants.default_prep_minutes` · `sales.commission_percent` ·
`sales.activation_orders` · `drivers.cash_limit` ·
`drivers.max_active_orders` · `payouts.min_amount` ·
`security.password_min_length`).

## ولا يعمل — **لا «يعمل ويخطئ»**
`cycle.py:109` يقرأ بـ`S_MAP.get(k)` **فيبتلع الغياب**.
**و`cycle.py:192` يقرأ بـ`S_MAP["merchants.default_commission_percent"]`**
— **`KeyError` وسقوطٌ فوريّ.**
**و`cycle_part2.py:10,13` مثلُه** لمفتاحين آخرين.

```
RUNNABLE AGAINST CURRENT CODE      = NO   (KeyError عند cycle.py:192)
STALE SETTING ASSUMPTIONS          = 4
STALE ENDPOINTS                    = 0
STALE STATE ASSUMPTIONS            = 0
CAN IT BE TRUSTED AS RELEASE EVIDENCE = NO
```

## VERDICT
> # **TEST-INFRA GAP — TIG-2**
> **وهيكلُه سليم** — النقاطُ والحالاتُ كلُّها حيّة. **والشائخُ الإعداداتُ
> وحدَها، وأربعةُ مواضع.**
>
> **فالقرارُ بين `Reuse` و`Rewrite` قرارُ مالكٍ لاحق** — **والقياسُ يقول
> إنّ إصلاحَه أقربُ من كتابته**، **وإن كان لا يغطّي إلّا ما يغطّيه.**
> **ولا يُستشهَد به الآن دليلَ إصدار.**

---

# R5-7 — **OPS-2** · لا نسخَ احتياطيَّ ولا استعادة

## القياس — **المستودعُ وحدَه، ولا يُفترض شيءٌ خارجه**
**بُحث في**: `deploy/` · `scripts/` · `.github/` · `backend/cmd/` ·
`docs/DEPLOY.md` عن `pg_dump` · `pg_restore` · `backup` · `restore` ·
`snapshot` · `cron` · `systemd` · `timer`.

**والنتيجةُ سطرٌ واحد** — `scripts/README.md:12`:
```
docker exec rahalgo-postgres pg_dump -U rahalgo -d rahalgo -Fc -f /tmp/before-wipe.dump
```
**يدويٌّ، وقبل مسحِ قاعدةِ تطويرٍ لا حمايةً لإنتاج، والوجهةُ `/tmp` داخلَ
الحاوية.**

## والإنتاجُ يعتمد أربعةَ مجلّداتٍ على الخادم
`deploy/compose.yml:159` — **`pgdata` · `redisdata` · `uploads` ·
`caddy_data`.**

## الأجوبةُ الستّة
```
AUTOMATED POSTGRES BACKUP = NO
AUTOMATED UPLOADS BACKUP  = NO
OFF-HOST COPY             = NO
RETENTION POLICY          = NO
RESTORE PROCEDURE         = NO
RESTORE TEST / DRILL      = NO
```

## VERDICT
> # **PROVEN OPERATIONAL RISK — OPS-2**
> **وليس عيبَ شيفرةٍ ولا نقصَ اختبار** — **نقصُ تشغيل.**
>
> **وهو أثقلُ ما في هذه الدفعة أثراً**: **قرصٌ يعطب أو حاويةٌ تُحذف بالخطأ
> تأخذ الدفترَ والطلباتِ وصورَ إثبات التسليم معاً** — **ولا شيءَ في
> المستودع يعيدها.**
>
> **ولا يُفترض أنّ Hetzner تحفظ لقطات** — **لا دليلَ عليه في المشروع.**
> **وقرارُه للمالك.**

---

# R5-8 — **TIG-3** · `moneycheck` لا يرى `D5`

## القياس
```
grep -c 'expenses'         cmd/moneycheck/main.go  → 0
grep 'operating_expense'   cmd/moneycheck/main.go  → لا شيء
```
**والثلاثةَ عشرَ فحصاً**: الخزينةُ الواحدة · رصيدُ المحفظة = مجموعُ حركاتها ·
الصندوقُ = قيوده · لا سالبَ إلّا الخزينة · مستحقُّ المتجر · دفعُ المحفظة ·
لا قيدَ بصفر · السحبُ المدفوع · تكرارُ التعويض · تكرارُ تعويض الشكوى ·
تكرارُ مستحقّ المتجر · اتّساقُ المجاميع · تغطيةُ الدفع.

**ولا واحدٌ منها يمسّ `expenses`.**

## الأثر
```
D5 يقع  →  صفُّ مصروفٍ بلا قيدِ خزينة  →  moneycheck = PASS
```
**وتقريرُ المصروفات يُبنى من جدول `expenses`** (`expenses_handlers.go:157`)
— **فيقرأ المالكُ مصروفاً لم يخرج من خزينته، ولا حارسَ يقول.**

## الثوابتُ الناقصة — **تُسمّى ولا تُضاف**
| # | الثابتُ المقترح |
|---|---|
| ١ | **كلُّ مصروفٍ غيرِ ملغًى له حركةُ خزينةٍ سالبةٌ بمبلغه** — `kind='operating_expense'` و`ref = expense.id` |
| ٢ | **وكلُّ مصروفٍ ملغًى له حركتان**: السالبةُ الأصليّةُ وموجبةٌ مقابلة |
| ٣ | **ولا حركةَ `operating_expense` بلا صفِّ مصروفٍ يقابلها** — الاتّجاه المعاكس |
| ٤ | **ولا مصروفٌ قُيّد مرّتين** — كما في `لا تعويضَ مكرَّر` |

## VERDICT
> # **TEST / INVARIANT GAP — TIG-3**
> **ولا يرفع عدّادَ عيوب التطبيق** — **`D5` مسجَّلٌ أصلاً في الدفعة الثانية.**
> **وهذا نقصُ الحارس الذي كان سيكشفه.**

---

# R5-9 — **R24** · فشلُ التحويل التلقائيّ — ماذا يبقى منه

## ACTUAL CODE PATH
`customer_handlers.go:362`:
```go
go s.autoTransfer(context.WithoutCancel(r.Context()), o.ID, userIDFrom(r))
```
**و`WithoutCancel` مقصودةٌ ونافعة** — **انقطاعُ الزبون لا يقتل التحويل** ✅.
**والدالّةُ لا تردّ نتيجة، والزبونُ أخذ `201` قبلها.**

## أنماطُ الفشل — **مصنَّفةً واحداً واحداً**

| # | الفشل | الحالةُ الباقية | التصنيف |
|---|---|---|---|
| ١ | قراءةُ الإعداد تفشل ⇒ `GetBool` تردّ الافتراض | **الطلبُ `pending`** — يراه المكتب · وإنذارُ `no_accept` | **RECOVERABLE / VISIBLE** ✅ |
| ٢ | قراءةُ الحالة تفشل | `pending` | **VISIBLE** ✅ |
| ٣ | **البوتُ غيرُ جاهز** | `pending` — **وهو المقصودُ بالتصميم** | **VISIBLE — بالقصد** ✅ |
| ٤ | `Transition` يفشل | `pending` | **VISIBLE** ✅ |
| ٥ | **`SendText` يفشل** | **`accepted` · `sent_to_merchant_at = NULL` · بلا استدعاءِ سائق** | **SILENT OPERATIONAL RISK** ❗ |
| ٦ | **لا هاتفَ للمتجر** | كسابقه | **SILENT OPERATIONAL RISK** ❗ |
| ٧ | `AutoDispatch` يفشل | `accepted` مُبلَّغ · بلا سائق — **وزرُّ «طلب سائق» قائم** | **VISIBLE** ✅ |

## ما يبقى أثراً للخامس والسادس
| المطلوب | أموجودٌ هو؟ |
|---|---|
| **حدثُ تدقيق** | **لا** — `s.audit` لا تُنادى في `autoTransfer` إطلاقاً (قِيس) |
| **حالةُ فشلٍ محفوظة** | **لا** — لا عمودَ ولا جدول |
| **علامةُ إعادةِ محاولة** | **لا** — ولا يُعاد أبداً |
| **إشعارُ عمليّات** | **لا** |
| **مقياس** | **لا** |
| **سجلّ** | **نعم** — `s.logger.Error("التحويلُ التلقائيّ: تعذّر الإبلاغ")` |
| **أثرٌ غيرُ مباشر** | **نعم** — `order_events` فيه القبولُ بنصّ `تحويلٌ تلقائيّ` (`Transition`)، **و`sent_to_merchant_at IS NULL` يفرّق المُبلَّغَ من غيره** |

## VERDICT
> # **PROVEN RISK — R24 · جزئيّ**
> **وخمسةٌ من سبعةِ أنماطٍ مرئيّةٌ ومُستدرَكة** — **والاثنان الباقيان
> يتركان حالةً صحيحةَ الشكل خاطئةَ المعنى**، **ولا يعلم بها أحدٌ إلّا من
> قرأ عموداً في جدول.**
>
> **وهو الوجهُ التشغيليُّ لـ`R21`** — **العطبُ واحدٌ والزاويةُ اثنتان.**

---

# الحصيلةُ النهائيّة — **بعدّاداتٍ مفصولة**

| العدّاد | كان | صار |
|---|---|---|
| `PROVEN DEFECTS` (منتج) | ١٩ | **١٩** — **بلا زيادة** ✅ |
| `PROVEN RISKS` (منتج) | ٢٠ | **٢٤** — +R21 · R22 · R23 · R24 |
| `PRODUCT DECISIONS REQUIRED` | ٧ | **٧** |
| **`OPERATIONAL RISKS`** — **عدّادٌ جديد** | — | **٢** — **OPS-1** الجاهزيّة · **OPS-2** النسخُ الاحتياطيّ |
| **`TEST-INFRA GAPS`** — **عدّادٌ جديد** | — | **٣** — **TIG-1** معنى الحكم · **TIG-2** دورةُ E2E · **TIG-3** ثوابتُ المصروفات |
| `RUNTIME VALIDATION REQUIREMENTS` | ٨ | **٨** — **بلا زيادة** |
| `CODE TRUTH BASELINE` | `26f93c5d` | **`26f93c5d`** ✅ |

**وثلاثةٌ من مخاطر المنتج الأربعة نمطٌ واحد**: **فعلٌ يُقرّ ثمّ تُحاول قناةٌ
خارجيّةٌ ولا تُعاد** — **الإبلاغُ (R21) · الإشعارُ (R22) · الدفعُ (R23).**
**ولا صندوقَ إرسالٍ في المشروع كلِّه** (قِيس: `outbox` صفرُ نتائج).

**ولا تغييرَ في شيفرةٍ تشغيليّة.**
