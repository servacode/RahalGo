# رحّال غو — عقدُ منتجِ تطبيق السائق النهائيّ

## DRIVER FINAL PRODUCT DECISIONS

> **`CODE TRUTH BASELINE = 26f93c5d`** · **ولا سطرَ شيفرةٍ مسّ.**
>
> **وهذه عقودُ منتجٍ معتمدةٌ من المالك ٢٠٢٦-٠٩-٠٤** — **تقول كيف *يجب* أن
> يعمل التطبيق.** **ولا تقول إنّه يعمل هكذا.**
>
> **وكلُّ عقدٍ قِيس مقابلَ الشيفرة** — **بثلاثة أحكام**: `SATISFIED` ·
> `PARTIAL` · `CONTRADICTS`.

---

# ٠ · الحصيلة

| | |
|---|---|
| **عقودُ منتجٍ معتمدةٌ للسائق** | **١١** — ٥ من المراجعة (`DPC-1…5`) + ٦ اليوم (`DPC-6…11`) |
| **قراراتُ مالكٍ مفتوحة** | **٠** ✅ |
| **`SATISFIED`** | **٢** |
| **`PARTIAL`** | **٢** |
| **`CONTRADICTS`** | **٢** |
| **فجواتُ عقدٍ ضدّ الشيفرة** | **١٢** |
| **عيوبٌ أو مخاطرُ جديدةٌ في السجلّ المجمَّد** | **٠** |

---

# ١ · CODE COMPARISON — **الستّةُ بنداً بنداً**

## `DPC-6` · **DD-1** — رقمُ المتجر والتواصلُ معه

### العقد
**رؤيةُ السائق لرقم المتجر مسموحة** · **والتواصلُ المباشرُ خارجَ رحّال غو
جائزٌ عند الحاجة التشغيليّة.**
**ولا يتغيّر عقدُ الزبون**: `CUSTOMER PHONE MUST NOT BE EXPOSED TO DRIVER`.

```
CURRENT CODE = SATISFIED
CONTRACT GAP = NO
```

| البند | الدليل |
|---|---|
| **رقمُ المتجر يصل السائق** | `driver_handlers.go:229` — **`MerchantPhone *string \`json:"merchant_phone"\`«** · والقيمةُ من `NULLIF(mo.phone::text, '')` |
| **رقمُ الزبون محجوب** | **لا حقلَ له في بنية الردّ إطلاقاً** (`driver_handlers.go:225-263`) — **والمصدرُ واحدٌ `driverOrderSelect` لكلّ أسطح السائق** |
| **اسمُ الزبون يصل** | `cu.full_name` ✅ — **وهو ما يلزمه للتسليم** |

> **`DD-1 = CLOSED`** — **والشيفرةُ توافق العقدَ في شقّيه معاً.**
> **و`OBS-D4` تُغلق بهذا** — **ولم تعد ملاحظةً مفتوحة.**

---

## `DPC-7` · **DD-2** — الخروجُ وبيده طلبٌ حيّ

### العقد
```
ACTIVE ORDER → LOGOUT BLOCKED
```
**ورسالةٌ واضحة**: «لا يمكنك تسجيل الخروج قبل إنهاء الطلب الجاري».

```
CURRENT CODE = CONTRADICTS
CONTRACT GAP = YES
```

| # | الدليل |
|---|---|
| **١** | **`HomeViewModel.logout():229`** — **`LocationService.stop` ثمّ `session.clear()` ثمّ `auth.logout`** — **ولا فحصَ لطلبٍ حيٍّ إطلاقاً** |
| **٢** | **`MainActivity:338`** — **`onLogout = vm::logout` بلا شرط** · **و`hasTrip` محسوبةٌ في الشاشة نفسِها (`:484`) ولا تُستعمَل هنا** |
| **٣** | **ولا حارسَ في المحرّك** — **`POST /auth/logout` عامٌّ لكلّ الأدوار** |

**والحالةُ الناتجةُ بعد الخروج:**
```
الجلسة      = مُبطَلة
الخدمة      = موقوفة
الموقع      = لا يُرسَل
orders.driver_id = ما زال يشير إليه ⚠️
on_shift    = true في الخادم ⚠️
```
**وطلبٌ بيد رجلٍ خرج** — **لا يراه مسحُ الدور** (يشترط `driver_id IS NULL`)
**ولا إنذارُ «بلا سائق»** — **ولا يُلتقط إلّا بإنذار «طال» بعد مهلة التسليم.**

**⚠️ والعقدُ الأخُ مُنفَّذٌ أصلاً**: **إنهاءُ الورديّة ممنوعٌ بطلبٍ حيّ**
(`driver_handlers.go:186-194` → `has_active_orders`). **فالبابُ الأمامي
مغلقٌ والخلفيُّ مفتوح.**

> **`DD-2 = CLOSED`** — **وأثقلُ فجوةٍ في هذه الدفعة.**

---

## `DPC-8` · **DD-3** — سقفُ النقد: عرضٌ وتحذيرٌ وسياسة

### العقد
**يحدّده الأدمن ويُعدَّل من اللوحة** · **ويرى السائقُ دائماً**: **المحمول ·
الحدّ · المتبقّي أو مقدارَ التجاوز.**
**ولا تُقصّ القيمةُ عند الحدّ** — **٥٦٠٬٠٠٠ تُعرض ٥٦٠٬٠٠٠** ومعها
«تجاوزتَ بـ٦٠٬٠٠٠».
**وتحذيراتٌ تدريجيّةٌ**: اقترابٌ · بلوغٌ · تجاوز.
**ولا يكتشف وضعَه بعد الضغط على القبول.**

```
CURRENT CODE = PARTIAL
CONTRACT GAP = YES (3 من 6)
```

### وما هو مُنفَّذٌ أصلاً — **وأكثرُ ممّا توقّعت** ✅
| البند | الدليل |
|---|---|
| **يعرض المحمولَ حقيقةً** | `CashScreen.kt:84` — **`money(held)` بلا قصٍّ ولا `coerce`** ✅ |
| **يعرض الحدّ** | `:106` |
| **يعرض المتبقّي** | `:112` — `cash_left` حين `left > 0` |
| **يعرض التجاوزَ بمقداره** | `:114` — **`cash_over + money(-left)`** ✅ **وهو نصُّ العقد حرفاً** |
| **تحذيرٌ عند الاقتراب** | `:127` — **`ratio > 0.8f` → «قريبٌ من الحدّ»** ✅ |
| **تحذيرٌ عند البلوغ والتجاوز** | `:126` — **`ratio >= 1f` → «بلغتَ الحدّ»** ✅ |
| **يُعدَّل من اللوحة** | `drivers.cash_limit` مفتاحٌ حسّاسٌ في المعجم ✅ |

### والفجواتُ الثلاث
| # | الفجوة | الدليل |
|---|---|---|
| **١** | **العرضُ في شاشة الصندوق وحدَها** — **لا في «الحال» ولا في بطاقة الطلب** | `CashScreen` خلفَ القائمة الجانبيّة · **فيلزم أن يفتحها ليعرف** |
| **٢** | **المانعُ يُكتشف بعد الضغط** | `errCashLimitFull` عند القبول — **`OBS-D3`** |
| **٣** | **الحساب ناقص** — **`held` وحدَه لا مجموعُ `cash_due` الحيّ** | **`D7` المسجَّل** — **والعقدُ يسمّي هذا صراحةً** |

### ⚠️ وفرقُ سياسةٍ يُسجَّل ولا يُحسَم
**العقدُ يعامل السقفَ حدّاً تشغيليّاً بعرضٍ وتحذير.** **والشيفرةُ تعامله
منعاً صلباً**: `held + cashDue > limit ⇒ errCashLimitFull` — **لا قبولَ.**
**والمالكُ لم يقل أيُلغى المنعُ أم يبقى مع التحذيرات** — **فيُسجَّل الفرقُ
كما أمر، ولا يُفسَّر.**

> **`DD-3 = CLOSED`** — **والعرضُ والتحذيراتُ مُنفَّذةٌ، والموضعُ والحسابُ
> والسياسةُ فجوات.**

---

## `DPC-9` · **DD-6** — GPS ضعيفٌ أو منقطع

### العقد
```
GPS PROBLEM → DRIVER CONTINUES + CLEAR WARNING
```
**ولا يُفقَد الطلبُ ولا تُنهى الورديّةُ ولا يُسجَّل خروجٌ بسبب GPS.**
**ويُعلَم السائقُ أنّ الأثرَ يمسّ**: التتبّعَ والملاحةَ وتقديرَ الوصول
وتحديثَ موقعِه للإدارة.

```
CURRENT CODE = PARTIAL
CONTRACT GAP = YES (شقُّ التحذير)
```

### الشقُّ الأوّل — **«لا يُفقَد شيء»** ✅ **مُنفَّذٌ بالكامل**
**قِيس**: **لا مسارَ في المشروع كلِّه ينهي ورديّةً أو يُسقط طلباً أو يُسجّل
خروجاً بسبب جودة موقع.**
**و`GpsQuality` تصنّف ولا تُسقط**: `GOOD_ACCURACY_M = 25` ·
`FixGrade.ACCEPTED` / **`DEGRADED`** / `REJECTED` بأسبابٍ ثلاثة
(`ACCURACY` · `STALE` · `TELEPORT`) — **والرفضُ لنقطةٍ لا لرحلة.**

### الشقُّ الثاني — **التحذير** ❌
| # | الفجوة | الدليل |
|---|---|---|
| **١** | **`FixGrade` لا تصل الواجهةَ إطلاقاً** | **قِيس**: `FixGrade` في `app-driver/trip/` = **صفر** — **فالتصنيفُ داخليٌّ لا يُرى** |
| **٢** | **«الموقعُ مفعَّل» تعني الإذنَ لا الجهاز** | `HomeViewModel:86` — **`hasLocation() = LocationPermission.granted`** — **فGPS مطفأٌ والإذنُ ممنوحٌ يُقرأ «سليماً»** (وهو `OBS-D2`) |
| **٣** | **سحبُ الإذن أثناء العمل** | `LocationService` → `SecurityException` → **`stopSelf()`** — **والإشعارُ يختفي بلا كلمة** |
| **٤** | **ولا تحذيرَ في شاشة الرحلة** | `TripPanel` لا يعرض حالَ الموقع |

**وشاشةُ «لماذا لا تصلني طلبات»** (`OrdersScreen:203`) **تقول `why_location_off`**
✅ — **وهي أقربُ ما هو قائمٌ من العقد، وتعتمد الإذنَ لا الدقّة.**

> **`DD-6 = CLOSED`** — **والنصفُ الحامي مُنفَّذ، والنصفُ المُخبِر لا.**

---

## `DPC-10` · **DD-7** — صوتٌ مميّزٌ لعرضٍ جديد

### العقد
**صوتٌ خاصٌّ بوصول عرضِ طلبٍ** — **واضحٌ ومميّزٌ عن باقي إشعارات التطبيق**
· **يُسمع أثناء القيادة والخلفيّة والشاشةِ المطفأة** · **ومرتبطٌ بالعرض
الحقيقيّ.** **وللعرض على الطريق تنبيهٌ مناسبٌ بلا تشتيتٍ خطير.**

```
CURRENT CODE = CONTRADICTS
CONTRACT GAP = YES
```

| # | الفجوة | الدليل |
|---|---|---|
| **١** | **لا صوتَ مخصَّصٌ إطلاقاً** | **قِيس**: `setSound` · `RingtoneManager` · `Uri` في `RahalPushService` و`PushChannels` = **صفر** — **فالقناةُ تستعمل صوتَ النظام الافتراضيّ** |
| **٢** | **العرضُ لا يُميَّز عن غيره** | **قناتان لا غير**: `URGENT` (اهتزازٌ وأولويّةٌ عالية) و`DEFAULT` — **وكلُّ `KindOrder` عاجل**: القبولُ والتحضيرُ والطريقُ والتسليمُ والإلغاء |
| **٣** | **المعرّفان الثابتان** | `3001`/`3002` — **فعرضٌ ثانٍ يطمس الأوّل** (`D-06`) |
| **٤** | **والطابورُ يصل بالبثّ بلا صوتٍ أصلاً** | `TopicDriverQueue` **إشارةٌ بلا حمولة** — **فالتطبيقُ المفتوحُ يُحدَّث صامتاً** |

**والمقاطعُ الصوتيّةُ في `res/raw` ملاحيّةٌ لا تنبيهيّة** — **٣٩ مقطعاً
للمنعطفات والوصول، ولا مقطعَ لعرضِ طلب.**

> **`DD-7 = CLOSED`** — **والبنيةُ الصوتيّةُ قائمةٌ للملاحة، ولا شيءَ منها
> للعروض.**

---

## `DPC-11` · **DD-8** — إيقافُ سائقٍ بيده طلب

### العقد
```
DRIVER SUSPENSION BLOCKS FUTURE WORK, NOT THE CURRENT ACTIVE DELIVERY
```
**الطلبُ الحاليُّ يستمرّ** · **ولا تُقطع نداءاتُه** · **ولا يُوقَف موقعُه** ·
**والإيقافُ `PENDING` حتّى ينتهي آخرُ طلبٍ حيّ** · **ثمّ يسري: لا طلباتٍ
جديدةً ولا ورديّةً جديدة.**
**والحالاتُ الخطيرةُ** (احتيالٌ · سلامةٌ · طوارئُ · اختراقٌ) **تدخّلٌ إداريٌّ
صريحٌ يعالج الطلبَ نفسَه — لا إيقافٌ عاديٌّ يتركه معلَّقاً.**

```
CURRENT CODE = CONTRADICTS
CONTRACT GAP = YES
```

| # | الدليل |
|---|---|
| **١** | **`AdminUpdateUser` (`identity/admin.go:158`)**: `status ≠ active` ⇒ **`RevokeAllTokens`** + **`invalidateStatusCache`** — **بلا أيّ فحصٍ لطلبٍ حيّ** |
| **٢** | **`RequireAuth` (`middleware.go:40`)**: `ActiveStatus ≠ active` ⇒ **٤٠١ على كلّ نداء** — **بما فيها `transition` و`proof` و`location`** |
| **٣** | **فالطلبُ يعلق بيده وهو لا يستطيع أن يحرّكه** — **ولا أن يسلّمه ولا أن يعتذر** |
| **٤** | **ولا مفهومَ `PENDING SUSPENSION` في المشروع** — **قِيس: لا عمود ولا حال** |

**⚠️ ونقيضُ ذلك في المتجر**: **`PC-11` من عقد الزبون يقول «التعليقُ يمنع
الجديدَ ولا يمسّ الجاري» — والشيفرةُ تنفّذه للمتجر** ✅ (`OpenNowSQL`).
**فالمنظومةُ تعرف هذا النمط وتطبّقه على المتجر ولا تطبّقه على السائق.**

> **`DD-8 = CLOSED`** — **وهو فجوةُ عقدٍ ومعمار: يحتاج حالَ إيقافٍ مؤجَّلٍ
> لا فحصاً في مكان.**

---

# ٢ · جدولُ المقارنة المختصر

| ID | العقد | الحال | فجوة | الدليلُ الأوّل |
|---|---|---|---|---|
| **DD-1** | رقمُ المتجر مسموح · ورقمُ الزبون محجوب | **SATISFIED** | **لا** | `driver_handlers.go:229` · **ولا حقلَ لهاتف الزبون** |
| **DD-2** | لا خروجَ بطلبٍ حيّ | **CONTRADICTS** | **نعم** | `HomeViewModel.kt:229` |
| **DD-3** | عرضٌ وتحذيرٌ وسياسةُ سقف | **PARTIAL** | **نعم (٣)** | `CashScreen.kt:84,114,126` |
| **DD-6** | يستمرّ ويُحذَّر | **PARTIAL** | **نعم (شقُّ التحذير)** | `GpsQuality.kt:80` · `HomeViewModel.kt:86` |
| **DD-7** | صوتٌ مميّزٌ للعرض | **CONTRADICTS** | **نعم** | **`setSound` = صفر** |
| **DD-8** | الإيقافُ يمنع الجديدَ لا الجاري | **CONTRADICTS** | **نعم** | `identity/admin.go:158` · `middleware.go:40` |

---

# ٣ · عقدُ منتجِ السائق الكامل — **١١ عقداً**

| ID | العقد | الحال |
|---|---|---|
| `DPC-1` | خرائطُ تلقائيّةٌ مُدارةٌ في الخلفيّةِ ويُعلَم بها السائق | **PARTIAL** — ٤ من ٧ |
| `DPC-2` | جاهزيّةُ الخلفيّة مرتَّبةٌ ومشروحةٌ قبل أوّل ورديّة | **PARTIAL** — الأجزاءُ لا الترتيب |
| `DPC-3` | درجٌ واعٍ بالسياق | **CONTRADICTS** — `gesturesEnabled = false` |
| `DPC-4` | لا طابورَ عامٌّ أثناء الرحلة | **CONTRADICTS** |
| `DPC-5` | العرضُ على الطريق داخلَ الخريطة بأرقام قرار | **PARTIAL** — المنطقُ لا الواجهة |
| **`DPC-6`** | **رقمُ المتجر مسموح · ورقمُ الزبون محجوب** | **SATISFIED** ✅ |
| **`DPC-7`** | **لا خروجَ بطلبٍ حيّ** | **CONTRADICTS** |
| **`DPC-8`** | **سقفُ النقد: عرضٌ وتحذيرٌ وسياسة** | **PARTIAL** |
| **`DPC-9`** | **GPS ضعيفٌ: يستمرّ ويُحذَّر** | **PARTIAL** |
| **`DPC-10`** | **صوتٌ مميّزٌ للعرض** | **CONTRADICTS** |
| **`DPC-11`** | **الإيقافُ يمنع الجديدَ لا الجاري** | **CONTRADICTS** |

## الفجواتُ الاثنتا عشرة
```
١  المشغِّلُ التلقائيُّ للخرائط معلَّقٌ بتبويب «الحال»       DPC-1
٢  لا إشعارَ عند اكتمال التنزيل                            DPC-1
٣  المنطقةُ من موقع الجهاز لا من منطقة العمل                DPC-1
٤  لا مسارَ جاهزيّةٍ واحدٌ مشروحٌ ولا حالُ «جاهز»            DPC-2
٥  الورديّةُ تُفتح بلا إذنِ خلفيّةٍ ولا إعفاءِ بطّاريّة       DPC-2
٦  gesturesEnabled = false عامّاً                          DPC-3
٧  الطابورُ العامُّ يُعرض ويُجلب أثناء الرحلة                DPC-4
٨  لا عرضَ على الخريطة ولا أرقامَ قرارٍ تُردّ                DPC-5
٩  الخروجُ لا يُمنع بطلبٍ حيّ                               DPC-7
١٠ سقفُ النقد: موضعُ العرض · وحسابُ المكشوف · والسياسة      DPC-8
١١ جودةُ الموقع لا تصل الواجهةَ ولا تُميَّز عن الإذن         DPC-9
١٢ لا صوتَ مخصَّصٌ للعرض ولا تمييزَ بين أنواعه               DPC-10 · DPC-11
```
**(و`DPC-11` فجوتُه المعماريّةُ داخلةٌ في الثانية عشرة عدّاً — وهي مستقلّةٌ
موضوعاً: لا مفهومَ إيقافٍ مؤجَّل.)**

---

# ٤ · حالُ القرارات الستّة السابقة

| ID | الحال |
|---|---|
| **DD-1** | ✅ **CLOSED** — `DPC-6` |
| **DD-2** | ✅ **CLOSED** — `DPC-7` |
| **DD-3** | ✅ **CLOSED** — `DPC-8` |
| **DD-4** | ✅ **CLOSED سابقاً** — `DPC-4` |
| **DD-5** | ✅ **CLOSED سابقاً** — `DPC-5` |
| **DD-6** | ✅ **CLOSED** — `DPC-9` |
| **DD-7** | ✅ **CLOSED** — `DPC-10` |
| **DD-8** | ✅ **CLOSED** — `DPC-11` |

```
DRIVER OPEN OWNER DECISIONS = 0
```

## والملاحظاتُ الستّ
| ID | الحال |
|---|---|
| **OBS-D1** | ⚠️ **باقيةٌ ملاحظةً** — الخدمةُ لا تعود بعد إعادة التشغيل · **و`DPC-2` يلامسها** |
| **OBS-D2** | ✅ **دخلت في `DPC-9`** — فجوةُ التحذير |
| **OBS-D3** | ✅ **دخلت في `DPC-4` و`DPC-8`** |
| **OBS-D4** | ✅ **أُغلقت بـ`DPC-6`** — **لا مشكلةَ في رقم المتجر** |
| **OBS-D5** | ✅ **أُغلقت بـ`DPC-4` و`DPC-5`** |
| **OBS-D6** | ⚠️ **باقيةٌ** — لا مفتاحَ إعداداتٍ للملاحة |

---

# ٥ · التصنيفُ القانونيّ — **ولا عدّادَ مجمَّدٌ تبدّل**

| البند | أيصير عيباً؟ | لماذا |
|---|---|---|
| **DD-2** الخروج | **لا** | **العقدُ وُضع اليوم** — `CONTRACT GAP` |
| **DD-3** الحساب الناقص | **مسجَّلٌ أصلاً** | **`D7`** — **والعقدُ يجعل إصلاحَه واجباً لا اختياراً** |
| **DD-6** التحذير | **لا** | **العقدُ جديد** — `CONTRACT GAP` |
| **DD-7** الصوت | **لا** | **ميزةٌ غيرُ مبنيّة** — `PRODUCT REQUIREMENT` |
| **DD-8** الإيقاف | **لا** | **العقدُ جديد ويحتاج معماراً** — `CONTRACT GAP` |
| **DD-1** | **لا** | **موافق** |

```
NEW PROVEN DEFECTS = 0
NEW PROVEN RISKS   = 0
FROZEN COUNTERS    = 19 عيباً · 24 خطراً — بلا تبديل
```

## ⚠️ وعيبٌ واحدٌ تبدّلت أولويّتُه
**`D7`** — **كان «حارسٌ يقيس غيرَ ما كُتب ليقيسه».** **و`DPC-8` يجعله
مطلباً صريحاً**: **«يجب أن يعرف السائقُ وضعَه النقديَّ بوضوح»** — **ولا
يعرفه بحسابٍ ناقص.**

---

# ٦ · فحوصُ التشغيل المطلوبة — **٩**

| ID | ما يُقاس | جهازٌ حقيقيّ؟ |
|---|---|---|
| `RV-2` | سائقان على طلبٍ واحد | لا |
| `RV-5` | سائقٌ واحدٌ على طلبين — و`held` بعدهما | لا |
| `RV-8` | سباقُ مسح طابور المواقع | **نعم** |
| `RV-D1` | `D17` الملاحةُ بعد موت العمليّة | **نعم** |
| `RV-D2` | `D18` فترةُ النبض بعد `START_STICKY` | **نعم** |
| `P-D1` | زمنُ تبديل الورديّة | **نعم** |
| `P-D2` | زمنُ أوّلِ تنزيلِ خرائطَ وأثرُه | **نعم** |
| `P-D3` | `OBS-D1` الورديّةُ وإعادةُ التشغيل | **نعم** |
| **🆕 `P-D4`** | **أيُسمَع صوتُ العرض أثناء القيادة والشاشةِ المطفأة** — بعد تنفيذ `DPC-10` | **نعم** |

---

# ٧ · وسومُ التجاوز

| ما يُوسَم | الوسم | بأيّ عقد |
|---|---|---|
| `DRIVER_APP_CLOSURE.md §22` — الأسئلةُ الثمانية | **RESOLVED** | `DPC-4…11` |
| `DRIVER_OWNER_REVIEW.md §9` — الستُّ المفتوحة | **CLOSED** | `DPC-6…11` |
| `OBS-D4` رقمُ المتجر | **RESOLVED — مسموح** | `DPC-6` |

**ولا يُحذف تاريخ.**

---

# ٨ · المخرَجُ النهائيّ

```
DRIVER APP FINAL OWNER CLOSEOUT COMPLETE

DD-1 MERCHANT CONTACT          = CLOSED   ← SATISFIED
DD-2 LOGOUT WITH ACTIVE ORDER  = CLOSED   ← CONTRADICTS
DD-3 CASH LIMIT UX/POLICY      = CLOSED   ← PARTIAL
DD-6 GPS DEGRADED BEHAVIOR     = CLOSED   ← PARTIAL
DD-7 NEW ORDER SOUND           = CLOSED   ← CONTRADICTS
DD-8 SUSPENSION WITH ACTIVE ORDER = CLOSED ← CONTRADICTS

DRIVER OPEN OWNER DECISIONS = 0
DRIVER OWNER PRODUCT DECISIONS CLOSED = YES

DRIVER PRODUCT CONTRACT   = COMPLETE   (11 عقداً)
DRIVER CODE UNDERSTANDING = COMPLETE
DRIVER OWNER MANUAL REVIEW = COMPLETE

DRIVER APP PRODUCT REVIEW = CLOSED

DRIVER CONTRACT GAPS AGAINST CODE      = 12
DRIVER PRODUCT REQUIREMENTS            = 3    (الخرائط · الجاهزيّة · صوتُ العرض)
DRIVER CROSS-SYSTEM/ADMIN REQUIREMENTS = 1    (XPRQ-1 خريطةُ رحّال غو)
DRIVER UX/PERFORMANCE GAPS             = 3    (زرُّ الورديّة · الدرج · أرقامُ قرار العرض)
DRIVER RUNTIME CHECKS REQUIRED         = 9

DRIVER FUNCTIONAL VALIDATION           = PENDING
DRIVER PROGRAMMATIC VALIDATION         = PENDING
DRIVER SECURITY/ABUSE VALIDATION       = PENDING
DRIVER PERFORMANCE/UX VALIDATION       = PENDING
DRIVER REAL DEVICE VALIDATION          = PENDING
DRIVER NAVIGATION VALIDATION           = PENDING
DRIVER BACKGROUND/LIFECYCLE VALIDATION = PENDING

DRIVER APP VERIFIED         = NO
DRIVER READY FOR PRODUCTION = NOT YET EVALUATED

OPERATIONAL CODE CHANGES = 0
```

**ولا يُنتقل إلى تطبيق المتجر حتّى يُراجَع هذا التقرير.**
