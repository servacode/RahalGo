# رحّال غو — مراجعةُ المالك اليدويّة لتطبيق الزبون

## CUSTOMER APP — OWNER MANUAL REVIEW RECONCILIATION

> **`CODE TRUTH BASELINE = 26f93c5d`** · **ولا سطرَ شيفرةٍ مسّ.**
> **والنسخةُ المُجرَّبة**: `app-customer-release.apk` · `1.0.6` · `versionCode 10`
> **R8 مُفعَّل · موقَّعةٌ بمفتاح الرفع · على `api.rahalgo.com`** —
> **أي النسخةُ التي ستصل الناسَ بعينها.**
>
> **وثمانيةُ ملاحظاتٍ من المالك رُبطت بالشيفرة** — **وسبعٌ منها لها سببٌ
> مُثبَتٌ في الشيفرة، والثامنةُ سببُها قرارُ المالك نفسِه.**

---

# ١ · OWNER-APPROVED PRODUCT RULES — **٧ قواعد**

**هذه قواعدُ منتجٍ معتمدةٌ لا أسئلةٌ مفتوحة.**

| # | القاعدة | الحالُ اليوم |
|---|---|---|
| **PR-1** | **التصفّحُ يجوز بمنطقةٍ يختارها المستخدم** — محافظةً أو مدينةً أو منطقة | **مُنفَّذٌ جزئيّاً** — `CityPicker` يختار **مدينةً** لا محافظةً ولا منطقة |
| **PR-2** | **عنوانُ التوصيل هو المرجعُ النهائيُّ لما يجوز عرضُه وطلبُه** | **غيرُ منفَّذ** — انظر OWNER-02 |
| **PR-3** | **تغييرُ عنوان التوصيل يُحدّث السوقَ فوراً** | **غيرُ منفَّذ** |
| **PR-4** | **لا يُعرض للتوصيل ما لا يصل فعلاً إلى العنوان** | **غيرُ منفَّذ** — الترشيحُ بدائرةِ نقطةٍ لا بقدرةِ توصيل |
| **PR-5** | **خارجَ التغطية: رسالةٌ واضحةٌ لا سوقٌ وهميّ** | **غيرُ منفَّذ** — الاكتشافُ في السلّة |
| **PR-6** | **خارجَ التغطية: زرُّ «اطلب تغطية منطقتي»** | **غيرُ موجودٍ إطلاقاً** |
| **PR-7** | **طلباتُ التغطية تصير بياناتٍ تقود قرارَ التوسّع** | **غيرُ موجودٍ إطلاقاً** |

---

# ٢ · OWNER MANUAL OBSERVATIONS — **٨**

| ID | الملاحظة | الفئة | السببُ في الشيفرة | حالُ الإثبات |
|---|---|---|---|---|
| **OWNER-01** | بطءُ السلايدر والصور أوّلَ تشغيل | **PERFORMANCE / UX** | **ثلاثةُ أسبابٍ مُثبَتة** | ساكنٌ ✅ · والزمنُ يحتاج قياساً |
| **OWNER-02** | السوقُ لا يتحدّث عند تغيير العنوان | **PRODUCT GAP + مركزيّة** | **ثلاثةُ أسبابٍ مُثبَتة** | **مُثبَتٌ كاملاً** ✅ |
| **OWNER-03** | خارجَ التغطية بلا رسالةٍ ولا طلب | **NEW PRODUCT REQUIREMENT** | **لا وجودَ للميزة** | **مُثبَتٌ بالعدم** ✅ |
| **OWNER-04** | تأخيرُ فتح الإضافات | **PERFORMANCE / UX** | **رحلةُ ذهابٍ وإيابٍ بعد اللمسة** | **مُثبَتٌ كاملاً** ✅ |
| **OWNER-05** | تأخيرُ الخريطة أوّلَ مرّة | **PERFORMANCE** | **سلسلةُ تهيئةٍ من خمس حلقات** | ساكنٌ ✅ · والزمنُ يحتاج قياساً |
| **OWNER-06** | مركزيّةُ الحال | **ARCHITECTURE** | **خمسةُ مصادرَ للموقع** | **مُثبَتٌ كاملاً** ✅ |
| **OWNER-07** | لا مركزَ دعمٍ واضح | **PRODUCT / UI GAP** | **سطحان من أحدَ عشر** | **مُثبَتٌ كاملاً** ✅ |
| **OWNER-08** | الدرجُ لا يعمل بالسحب | **⚠️ قرارُ المالك نفسِه** | **`gesturesEnabled = false`** | **مُثبَتٌ — وهو تعارضُ طلبين** |

---

# ٣ · STATIC CORRELATION — بنداً بنداً

## OWNER-01 · بطءُ الصور أوّلَ تشغيل

### ما هو صحيحٌ في البناء ✅
| | |
|---|---|
| **مخزنُ القرص** | **٢٠٠ م.ب** — `Images.kt:118` |
| **مخزنُ الذاكرة** | **٢٥٪ من ذاكرة التطبيق** — `:130` |
| **صورٌ مصغَّرة** | `imageThumbUrl` للأصناف · **و`mediaSized(…, 960, sizes)` للبانرات** |
| **نداءٌ واحدٌ للصفحة** | `/public/home` يجمع البانراتِ والأقسامَ والرفوفَ والهويّة |

### والأسبابُ الثلاثةُ المُثبَتة للبطء

| # | السبب | الموضع | الأثر |
|---|---|---|---|
| **١** | **لا تحميلَ مسبقاً إطلاقاً** — قِيس: `prefetch\|preload\|enqueue` في `mobile/` = **صفر** | — | **الصورةُ لا تبدأ إلّا حين تُركَّب على الشاشة** — فالبانرُ الثاني لا يُجلب حتّى يُعرض |
| **٢** | **مكانُ التحميل فارغٌ تماماً** | `RemoteImage.kt:60` — **`loading = {}`** | **فراغٌ أبيضُ لا هيكلَ ولا وميض** — **والفراغُ يُقرأ بطئاً حتّى لو كان الزمنُ نفسَه** |
| **٣** | **`crossfade(true)`** | `Images.kt:135` | **تلاشٍ يضيف زمناً مُدرَكاً على أوّل ظهور** |

### وأوّلُ تشغيلٍ يختلف بنيويّاً
```
أوّلُ مرّة : لا قرصَ ولا ذاكرة → كلُّ صورةٍ رحلةُ شبكةٍ كاملة
ما بعدها  : القرصُ يخدمها → فورية
```
**فالفرقُ الذي رآه المالكُ حقيقيٌّ وبنيويّ** — **ولا يُصلَح بمخزنٍ أكبر بل
بتحميلٍ مسبقٍ وهيكلِ انتظار.**

> **`PERFORMANCE OBSERVATION`** — **والزمنُ لا يُقاس ساكناً.** → **`RV-C1`**

---

## OWNER-02 · السوقُ لا يتحدّث عند تغيير العنوان — **وهذا أثقلُ ما ظهر**

### مصادرُ النطاق الخمسة — **مُثبَتة**

```
CURRENT BROWSE SCOPE SOURCE   = BrowseScope.point   (StateFlow في الذاكرة)
CURRENT DELIVERY SCOPE SOURCE = لا وجودَ له — يُحسب في الخادم عند quote/create وحدَه
SELECTED CITY SOURCE          = CityScope.chosen    (SharedPreferences "rahalgo_city")
SELECTED ADDRESS SOURCE       = selectedAddress(addresses) = **أوّلُ عنوانٍ is_default**
CURRENT LOCATION SOURCE       = LastPoint.value     (GPS في الذاكرة)
```

### وسلّمُ الأولويّة — `CityScope.resolve` (`CityScope.kt:89`)
```kotlin
chosen?.let { return … }                         ← ١ المدينةُ المختارةُ يدوياً
address?.takeIf { lat≠0 || lng≠0 }?.let { … }    ← ٢ العنوانُ الافتراضيّ
LastPoint.value?.let { … }                       ← ٣ موقعُ الجهاز
cities.singleOrNull()?.let { … }                 ← ٤ مدينةٌ واحدةٌ في المنصّة
return null                                      ← ٥ بلا نطاق
```

### والأسبابُ الثلاثةُ لِما رآه المالك

| # | السبب | الدليل |
|---|---|---|
| **١** | **`selectedAddress` يردّ العنوانَ الافتراضيَّ لا الذي عدّله** | `Address.kt:154` — **`addresses.firstOrNull { it.isDefault }`**. **فتعديلُ العنوان الثاني إلى دمشق لا يغيّر شيئاً ما لم يُجعل افتراضيّاً** |
| **٢** | **المدينةُ المختارةُ يدويّاً تعلو العنوانَ دائماً** | `CityScope.kt:90` — **وهي محفوظةٌ على القرص، فتبقى بعد الخروج والدخول** (وهو `OBS-C2`) |
| **٣** | **عنوانُ التوصيل في السلّة ليس مصدرَ نطاقٍ أصلاً** | `DeliveryAddress.picking` سطحٌ منفصلٌ — **ولا يمسّ `BrowseScope` إطلاقاً** |

### والآليّةُ نفسُها سليمة ✅
**`CityGate`** (`CityScope.kt:130`) **يراقب `address` و`chosen` و`cities` و`here`**،
**و`BrowseScope.set` تردّ «أتغيّرت»**، **و`Refresh.bump()` تُعيد تحميلَ السوق**
(`ShopViewModel:143`).
**فالوصلةُ موجودةٌ ومبنيّة** — **والخللُ في *أيُّ* عنوانٍ يدخلها و*مَن* يعلو مَن.**

### وفجوةُ `PR-4` أعمقُ من ذلك
**الترشيحُ اليومَ نقطةٌ ودائرة** — `ST_DWithin` حولَ نقطةِ النطاق.
**و`PR-4` يطلب قدرةَ توصيلٍ فعليّة**: **أيصل هذا المصدرُ إلى هذا العنوان؟**
**وهذا سؤالٌ لا يُجيبه ترشيحُ الدائرة** — **ويحتاج قرارَ تصميمٍ لاحقاً.**

> **`PRODUCT GAP` + `CENTRALIZATION GAP`** — **ولا يُصنَّف عيباً**: **لا عقدَ
> مكتوبٌ سابقٌ يقول إنّ العنوانَ يحكم السوق.** **والعقدُ وُضع اليوم — `PR-2`.**

---

## OWNER-03 · خارجَ التغطية

### القياس
```
coverage request / service-area request / demand collection endpoint = لا شيء
grep: coverage · service_area · demand · waitlist  في backend = صفر
```

### وما هو موجودٌ اليوم
| الطبقة | ما يقع |
|---|---|
| **التصفّح** | **يُعرض السوقُ كاملاً** — والدائرةُ ترشّح ولا تمنع (وصفرٌ يعني بلا حدّ) |
| **التسعيرة** | `/public/quote` تردّ **`out_of_zone = true`** والرسمَ صفراً |
| **السلّة** | `CartScreen:328` — **`Note("خارج منطقة التغطية")` وزرُّ الإرسال يُعطَّل** |
| **الإنشاء** | `ErrOutOfZone` |

**فالرسالةُ موجودةٌ — ولكن في آخر خطوةٍ لا في أوّلها.** **وهو بعينُه ما اعترض
عليه المالك.**

> **`NEW PRODUCT REQUIREMENT — PRQ-1`**
> **ثلاثةُ أجزاءٍ لا واحد**: **كشفٌ مبكّر** · **رسالةٌ وخيارُ طلب** ·
> **تجميعُ الطلبات جغرافيّاً للإدارة.**
> **والثالثُ يحتاج جدولاً ونقطتين وشاشةَ إدارة — ولا شيءَ منها قائم.**

---

## OWNER-04 · تأخيرُ فتح الإضافات — **مُثبَتٌ بالضبط**

### المسار
```
لمسةٌ على الصنف → picking = item                    (ShopScreen:100)
→ ItemOptionsSheet تُفتح فوراً
→ LaunchedEffect(item.id) { api.itemDetail(item.id) }  ← رحلةُ شبكةٍ **بعد** الفتح
→ groups == null → CircularProgressIndicator          (ItemOptionsSheet:114)
→ ثمّ تُرسم الخيارات
```

### وثلاثةُ أسبابٍ مُثبَتة
| # | السبب | الدليل |
|---|---|---|
| **١** | **نداءٌ إضافيٌّ بعد اللمسة** | `GET /public/items/{id}` — **والقائمةُ لا تحمل المعدِّلات** |
| **٢** | **ولا رايةَ تقول أللصنف خيارات** | `data class Item` (`Shop.kt:85-104`) — **لا `has_modifiers`**. **فصنفٌ بلا خياراتٍ يدفع الرحلةَ نفسَها** |
| **٣** | **والدوّارُ يظهر في كلّ مرّة** | **ولا مخزنَ للمعدِّلات** — **فالصنفُ نفسُه يُجلب كلّما فُتح** |

**وحالُ الانتظار واضحةٌ** ✅ (دوّارٌ ورسالةُ فشل) — **فالعلّةُ أداءٌ لا غموضُ واجهة.**

> **`PERFORMANCE / UX OBSERVATION`** → **`RV-C2`**

---

## OWNER-05 · تأخيرُ الخريطة أوّلَ مرّة

### سلسلةُ التهيئة — خمسُ حلقات
```
١ MapStyleRepository.init         CustomerApplication.kt:51  (عند الإقلاع ✅)
٢ MapLibre.getInstance            MapHost.kt:40 — **عند أوّل تركيبٍ للخريطة**
٣ ensureManifest / fetchManifest  — **شبكةٌ إن لم يُخزَّن**
٤ الأرشيفُ والبلاطات (PMTiles)    — **قرصٌ أو شبكةٌ أوّلَ مرّة**
٥ map.setStyle(fromJson)          MapCanvas.kt:186 — **بناءُ نمطٍ كامل**
```

### والأسبابُ الساكنةُ الممكنة
| # | السبب |
|---|---|
| ١ | **`MapLibre.getInstance` تقع عند أوّل فتحٍ لا عند الإقلاع** — **ولا تسخينَ مسبق** (قِيس: `prewarm\|warmup` = صفر) |
| ٢ | **البيانُ والبلاطاتُ تُجلب أوّلَ مرّةٍ من `maps.rahalgo.com`** |
| ٣ | **`ACCESS_FINE_LOCATION` تُطلب عند الإقلاع** ✅ — **لكنّ أوّلَ تثبيتِ GPS بطيءٌ بطبعه** |
| ٤ | **العنونةُ العكسيّة نداءٌ ثانٍ** — `/geo/reverse` |
| ٥ | **`rememberSaveable` للفتح** ✅ — فالدورانُ لا يعيد التهيئة |

> **`PERFORMANCE OBSERVATION`** — **ولا رقمَ زمنٍ يُدَّعى بلا قياس.** → **`RV-C3`**

---

## OWNER-08 · الدرجُ لا يعمل بالسحب — **⚠️ وهذا ليس عطباً**

### الدليل — `MainActivity.kt:482`
```kotlin
gesturesEnabled = false,
```

### والعلّةُ مكتوبةٌ فوقه بخمسةَ عشرَ سطراً — **وهي بلاغُ المالك نفسِه**
> **(بلاغُ المالك ٢٠٢٦-٠٩-٠١:** «**عند سحب الخريطة تُفتح القائمةُ الجانبيّة —
> بكلّ التطبيقات**».)
> «**وحافّةُ الشاشة تلتقط السحبَ قبل الخريطة** … **والتعطيلُ عامٌّ لا مشروط**»

### والحال
| | |
|---|---|
| **الفتحُ بالسحب** | **مُعطَّلٌ عمداً في التطبيقات الأربعة** |
| **الإغلاقُ بالسحب** | **مُعطَّلٌ بالرايةِ نفسِها** — **وهو أثرٌ جانبيٌّ لم يُقصد** |
| **الفتحُ بالزرّ** | ✅ في الشريط العلويّ |
| **الإغلاقُ بالزرّ الخلفيّ** | ✅ `BackHandler(enabled = drawer.isOpen)` (`:444`) |
| **النقرُ على الظلّ** | ✅ سلوكُ `ModalNavigationDrawer` الافتراضيّ |
| **أتبقى نصفَ مفتوحة؟** | **لا** — بلا إيماءةٍ لا حالةَ وسطى |

> **`OWNER DECISION CONFLICT — ODC-1`**
> **طلبان متعارضان لك، بينهما ثلاثةُ أيّام**:
> **٢٠٢٦-٠٩-٠١**: «سحبُ الخريطة يفتح القائمة — عطّلوه»
> **اليوم**: «القائمةُ لا تعمل بالسحب»
>
> **ولا يُصلَح أحدُهما إلّا بكسر الآخر** — **إلّا بحلٍّ ثالث.**
> **وثلاثةُ حلولٍ ممكنة** (لا أختار):
> **أ)** إيماءةٌ مشروطةٌ بالشاشة — تُطفأ حيث خريطةٌ وتُشعل حيث لا خريطة
> **ب)** الإغلاقُ بالسحب وحدَه (`gesturesEnabled` لا يفرّق — يحتاج تنفيذاً يدويّاً)
> **ج)** حافّةٌ أضيقُ للفتح لا تنازع الخريطة

---

# ٤ · CUSTOMER STATE CENTRALIZATION MAP

| العنصر | مصدرُ الحقيقة | يُحفظ أين | من يغيّره | من يراقب | كيف تُبطَل الشاشاتُ التابعة | أيختلف مصدران؟ |
|---|---|---|---|---|---|---|
| **الجلسةُ والمستخدم** | **الخادم** (`/auth/me`) | `SharedPreferences` (التوكنان) | `AuthViewModel` | `AppFrame` | إعادةُ بناءٍ كاملة | **لا** ✅ |
| **موقعُ الجهاز** | `LastPoint` (ذاكرة) | **لا يُحفظ** | GPS · مُلتقِطُ النقطة | `CityGate` · `CartScreen` | `LaunchedEffect(here)` | **لا** |
| **نطاقُ التصفّح** | **`BrowseScope.point`** (ذاكرة) | **لا يُحفظ** | **`CityGate` وحدَه** ✅ | `CustomerApi` في كلّ نداء | `Refresh.bump()` | **لا** ✅ |
| **المدينةُ المختارة** | **`CityScope.chosen`** | **`SharedPreferences`** | `CityPicker` | `CityGate` | كسابقه | **⚠️ نعم — تعلو العنوان** |
| **عنوانُ التوصيل** | **الخادم** (`/my/addresses`) | القاعدة | `AccountViewModel` | `CityGate` **بالافتراضيّ وحدَه** · `CartScreen` | ⚠️ **جزئيّاً** | **⚠️ نعم** |
| **قابليّةُ التوصيل** | **الخادم عند `quote`/`create` وحدَه** | — | — | `CartScreen` | **لا تُبطَل — تُسأل عند الحاجة** | **⚠️ نعم — السوقُ لا يعرفها** |
| **نطاقُ السوق** | **مشتقٌّ من `BrowseScope`** | — | — | `ShopViewModel` | `Refresh.tick` ✅ | **لا** |
| **السلّة** | **الجهاز** — `Cart` عامّ | `SharedPreferences` | أيُّ شاشة | `CartScreen` | حالُ Compose مباشرةً | **لا خادمَ يُخالف** |
| **الإعدادات** | **الخادم** — تصل مع `/public/home` | — | الإدارة | `ShopViewModel` | `Refresh.tick` | **لا** |
| **الحالُ اللحظيّة** | `LiveSocket` | — | الخادم | `ShellViewModel` · `OrdersViewModel` | `onEvent → refresh + Refresh.bump` | ⚠️ **تموت بصمت — D19** |
| **الإشعارات** | **الخادم** — الصندوقُ هو الحقيقة | القاعدة | الخادم | `InboxSheet` | نداءٌ عند الفتح | **لا** |
| **الصور** | Coil — قرصٌ وذاكرة | `cacheDir/images` | — | — | **لا إبطالَ إطلاقاً** | **⚠️ صورةٌ بُدّلت بالمسار نفسِه تبقى قديمة** |
| **الانتظار** | **حالُ كلّ شاشةٍ على حدة** | — | — | — | **لا مركزيّةَ** | — |
| **خريطةُ الأخطاء** | **`ApiErrors.kt` — مركزيّةٌ واحدة** ✅ | — | — | كلُّ الشاشات | — | **لا** ✅ |
| **التحديث** | **`Refresh.tick` — إشارةٌ واحدةٌ عامّة** ✅ | — | أيُّ شيء | كلُّ نموذج | `drop(1).collect { load() }` | **لا** ✅ |

## الحصيلة
**المركزيّةُ جيّدةٌ في خمسةٍ** — الأخطاءُ · التحديثُ · النطاقُ · الجلسةُ · الإعدادات.
**والخللُ في ثلاثةٍ فقط:**

| # | الخلل |
|---|---|
| **١** | **`CityScope.chosen` تعلو عنوانَ التوصيل، وتعيش على القرص** — فتبقى بعد كلّ شيء |
| **٢** | **«عنوانُ التوصيل» ثلاثةُ أشياءَ لا واحد**: الافتراضيُّ (للنطاق) · المختارُ في السلّة (للطلب) · والمعدَّلُ (لا يحرّك شيئاً) |
| **٣** | **قابليّةُ التوصيل ليست حالاً مركزيّةً أصلاً** — **تُسأل في السلّة ولا يعرفها السوق** |

---

# ٥ · GEOGRAPHIC / DELIVERY SCOPE CONTRACT

## العقدُ القائمُ اليوم — مُثبَت
```
التصفّح : ST_DWithin حولَ نقطةِ BrowseScope · نصفُ القطر delivery.default_radius_m
          وصفرٌ يعني بلا حدّ  → **والرقّةُ اليومَ على الصفر**
الطلب   : مضلّعاتُ delivery_zones → ErrOutOfZone
```
**فطبقتان مختلفتان بمنطقين مختلفين** — **والأولى تعرض والثانيةُ تمنع.**

## والعقدُ الذي اعتمده المالكُ اليوم — `PR-2` … `PR-4`
```
عنوانُ التوصيل ⇒ يحكم العرضَ والطلبَ معاً
تغييرُه       ⇒ يُعيد تحميلَ كلّ ما يعتمد الجغرافيا
ولا يُعرض     ⇒ ما لا يصل فعلاً
```

## الفجوةُ بين الاثنين — **ثلاثُ نقاط**
| # | الفجوة | ما تحتاجه |
|---|---|---|
| ١ | **نقطةُ النطاق ليست عنوانَ التوصيل** | ربطُ `BrowseScope` بالعنوان المختار، وكسرُ أولويّة `chosen` |
| ٢ | **الدائرةُ ليست قدرةَ توصيل** | سؤالٌ لكلّ مصدر: **أيصل إلى هذا العنوان؟** — **وهذا تصميمٌ جديد** |
| ٣ | **لا حالَ «خارجَ التغطية» في السوق** | `PRQ-1` |

**⚠️ ويلزم قرارٌ قبل التنفيذ**: **ماذا يرى الزبونُ قبل أن يضيف عنوانَه الأوّل؟**
**سوقاً بموقع جهازه؟ أم مدينةً يختارها؟ أم لا شيءَ حتّى يضع عنواناً؟**

---

# ٦ · CUSTOMER SUPPORT SURFACE GAP MAP

## ما يملكه المحرّكُ فعلاً
| القدرة | النقاط |
|---|---|
| **تذاكرُ كاملةٌ بردود** | `POST /admin/tickets` · `/{id}/replies` · قراءةٌ وتصفية |
| **شكوى على طلب** | `POST /my/orders/{id}/complaint` · **وأسبابٌ مصنَّفة**: `not_received` · `missing_items` · `wrong_items` · `quality` · `delivered`… **وكلٌّ له ذنبٌ** (`driver` أو `merchant`) |
| **بلاغُ السائق** | `POST /driver/orders/{id}/report` |
| **بلاغُ المتجر** | `POST /merchant/orders/{id}/report` · **و`reports-against`** |
| **تعويضٌ في التذكرة** | `compensation` · `resolution` |
| **دردشةُ الطلب** | `comms` — **وبحدِّ معدّل** |

## وما يملكه الأدمن
**كلُّ ما سبق + إنشاءُ تذكرةٍ وردٌّ عليها وحلٌّ وتعويض.**

## وما يعرضه تطبيقُ الزبون
| السطح | ما فيه |
|---|---|
| **`complaint`** | **شكوى على طلبٍ منتهٍ · واحدةٌ لكلّ طلب · بنافذةِ `support.complaint_window_hours`** |
| **`TicketsScreen`** | **قراءةٌ فقط** — `GET /my/tickets` · **ولا ردَّ ولا متابعة** |
| **`ChatsScreen` / `OrderChat`** | **دردشةٌ مرتبطةٌ بطلب** |

## الفجوة — **تسعةٌ من أحدَ عشر**
```
BACKEND ALREADY SUPPORTS = تذاكرُ بردودٍ وتعويض · شكاوى مصنَّفةٌ بالذنب · بلاغاتُ سائقٍ ومتجر
ADMIN ALREADY SUPPORTS   = إنشاءٌ وردٌّ وحلٌّ وتعويضٌ وتصفية
CUSTOMER UI EXPOSES      = ٢/١١  (شكوى على طلبٍ · قراءةُ تذاكر)
MISSING CUSTOMER SURFACES = ٩/١١
```

| # | الناقص | أيدعمه المحرّك؟ |
|---|---|---|
| ١ | **بلاغٌ عن سائقٍ خارجَ الطلب** | **جزئيّاً** — الشكوى تحمل الذنب |
| ٢ | **بلاغٌ عن منتج** | **لا** |
| ٣ | **مشكلةُ عنوانٍ أو توصيل** خارجَ الطلب | **لا** |
| ٤ | **مشكلةُ محفظةٍ أو مال** | **لا سطحَ للزبون** |
| ٥ | **مشكلةُ حساب** | **لا** |
| ٦ | **مشكلةٌ تقنيّة** | **لا** |
| ٧ | **اقتراح** | **لا** |
| ٨ | **مرفقات** | **لا في مسار الزبون** — والرفعُ موجودٌ للصورة الشخصيّة |
| ٩ | **ردُّ الزبون على تذكرته ومتابعتُها** | **الردودُ للأدمن وحدَه** ⚠️ |

**⚠️ وأثقلُها التاسع**: **الزبونُ يفتح شكوى، ويرى حالَها، ولا يستطيع أن يردّ
على سؤال الإدارة.** **والدردشةُ بديلٌ — لكنّها مرتبطةٌ بطلبٍ لا بتذكرة.**

> **`PRODUCT / UI GAP — PRQ-2`** — **مركزُ دعمٍ واحد.**
> **ولا يُبنى الآن.** **وأكثرُه واجهةٌ فوق قدراتٍ قائمة، والباقي نقاطٌ جديدة.**

---

# ٧ · PERFORMANCE / UX RUNTIME CHECKS NEEDED — **٤ جديدة**

| ID | ما يُقاس | البيئة | جهازٌ حقيقيّ؟ | الدليلُ المطلوب |
|---|---|---|---|---|
| **RV-C1** | **زمنُ أوّلِ رسمٍ للصور** — أوّلُ تشغيلٍ مقابل ما بعده | جهاز + شبكةٌ حقيقيّة | **نعم** | زمنُ أوّلِ بانرٍ مرئيّ · وعددُ الصور وأحجامُها في `/public/home` |
| **RV-C2** | **زمنُ فتح الإضافات** من اللمسة إلى الرسم | جهاز | **نعم** | زمنُ `GET /public/items/{id}` · ونسبةُ الأصناف بلا معدِّلات |
| **RV-C3** | **زمنُ أوّلِ خريطة** | جهاز | **نعم** | زمنُ كلّ حلقةٍ من الخمس على حدة |
| **RV-C4** | **إيماءةُ الدرج بعد أيّ حلٍّ يُختار** | جهاز | **نعم** | ألا يفتح سحبُ الخريطةِ الدرجَ · وأن يُغلق الدرجُ بالسحب |

---

# ٨ · REMAINING OWNER DECISIONS

> # ⚠️ **CLOSED — OWNER DECISION ٢٠٢٦-٠٩-٠٤**
>
> **ثمانٍ من العشرة أدناه أُغلقت بعشرة عقودِ منتجٍ معتمدة** — **وبقيت
> `D-03` و`B3`.** **والعقودُ والفجواتُ الأربعَ عشرةَ مقابلَ الشيفرة في**
> [`CUSTOMER_FINAL_PRODUCT_DECISIONS.md`](CUSTOMER_FINAL_PRODUCT_DECISIONS.md).
>
> **و`PR-1…PR-7` صارت `PC-1`** · **و`RV-C1…RV-C4` صارت `P1…P6`.**



## أ · ما حُسم اليوم — **٧ قواعد** (`PR-1` … `PR-7`) — **تخرج من المفتوح**

## ب · وما بقي مفتوحاً — **١٠**

| # | ID | السؤال | الحال |
|---|---|---|---|
| ١ | **ODC-1** | **الدرجُ — أيَّ الحلّين الثلاثة؟** | **جديدٌ — تعارضُ طلبين لك** |
| ٢ | **U46** | أيفتح الإشعارُ الطلبَ بعينه؟ | من قبل |
| ٣ | **D-06** | إشعارٌ لكلّ طلبٍ أم واحدٌ يطمس؟ | من قبل |
| ٤ | **D-04** | الطلبُ الخاصُّ ومنطقةُ التوصيل | **⚠️ يتقاطع مع `PR-2`** — انظر أدناه |
| ٥ | **D-03** | تعليقُ متجرٍ وطلباتُه الجارية | من قبل |
| ٦ | **B3** | إشعارٌ عند قبول السائق؟ | من قبل |
| ٧ | **OBS-C1** | السلّةُ بعد الخروج | من قبل |
| ٨ | **OBS-C3** | نصُّ `refunded` العربيّ | من قبل |
| ٩ | **OBS-C4** | خريطةُ السائق للزبون | من قبل |
| ١٠ | **NEW-1** | **ماذا يرى الزبونُ قبل أوّل عنوان؟** | **جديدٌ — يلزم لتنفيذ `PR-2`** |

### ⚠️ و`D-04` تبدّل معناه اليوم
**`PR-2` يقول: عنوانُ التوصيل يحكم ما يجوز طلبُه.**
**والطلبُ الخاصُّ اليومَ بلا حارسِ منطقةٍ إطلاقاً** (`D6`/`D8`/`D-04`).
**فإمّا أن `PR-2` يشمله فيصير `D-04` محسوماً بالإيجاب، وإمّا يُستثنى صراحةً.**
**ولم تقل أيَّهما — فيبقى مفتوحاً ومُعلَّماً.**

---

# ٩ · التصنيفُ القانونيّ — **ولا عدّادَ مجمَّدٌ تبدّل**

**فُحصت الملاحظاتُ الثماني بالشروط الأربعة** (عقدٌ واضح · شيفرةٌ تخالفه ·
قياسٌ مُعاد · مطابقةٌ للتعريف):

| الملاحظة | أيصير عيباً؟ | لماذا |
|---|---|---|
| OWNER-01 | **لا** | **لا عقدَ زمنيٍّ مكتوب** — `PERFORMANCE GAP` |
| OWNER-02 | **لا** | **العقدُ (`PR-2`) وُضع اليوم** — والشيفرةُ سبقته. `PRODUCT GAP` |
| OWNER-03 | **لا** | **ميزةٌ غيرُ مبنيّة** — `PRODUCT REQUIREMENT` |
| OWNER-04 | **لا** | **لا عقدَ زمنيّ** — `PERFORMANCE GAP` |
| OWNER-05 | **لا** | كسابقه |
| OWNER-06 | **لا** | **وصفُ بنيةٍ لا خرقُ ثابت** |
| OWNER-07 | **لا** | **فجوةُ واجهةٍ فوق قدراتٍ قائمة** — `UI GAP` |
| OWNER-08 | **لا** | **قرارُ المالك نفسِه مُنفَّذٌ كما طُلب** — `DECISION CONFLICT` |

```
NEW PROVEN DEFECTS = 0
NEW PROVEN RISKS   = 0
FROZEN COUNTERS    = بلا تبديل   (19 عيباً · 24 خطراً)
```

---

# ١٠ · CUSTOMER CLOSURE READINESS

```
CUSTOMER OWNER REVIEW RECONCILIATION COMPLETE

OWNER-APPROVED PRODUCT RULES = 7    (PR-1 … PR-7)
OWNER MANUAL OBSERVATIONS    = 8    (OWNER-01 … OWNER-08)
NEW PRODUCT REQUIREMENTS     = 2    (PRQ-1 التغطية · PRQ-2 مركزُ الدعم)
NEW UX/PERFORMANCE GAPS      = 4    (OWNER-01 · 04 · 05 · 08)
NEW PROVEN DEFECTS           = 0
NEW PROVEN RISKS             = 0

CUSTOMER OPEN OWNER DECISIONS      = 2    ← بعد قرارات ٢٠٢٦-٠٩-٠٤
CUSTOMER RUNTIME CHECKS STILL REQUIRED = 8   (RV-1 · RV-4 · RV-6 · RV-7 + RV-C1…RV-C4)

CUSTOMER CODE UNDERSTANDING   = COMPLETE
CUSTOMER OWNER MANUAL REVIEW  = COMPLETE
CUSTOMER PRODUCT DECISIONS CLOSED = YES  ← ٢٠٢٦-٠٩-٠٤
CUSTOMER APP REVIEW CLOSED        = NO

OPERATIONAL CODE CHANGES = 0
```

**ولا يُنتقل إلى تطبيق السائق.**
