# الانتقالُ من الاختبارات القائمة

> §٥٥ من طلب المالك. **ولا يُحذف شيءٌ الآن.**

---

# المقيسُ اليوم

| الطبقة | العدد | الحال |
|---|---|---|
| **ملفّاتُ اختبارِ Go** | **١٩٨** | — |
| **دوالُّ الاختبار** | **٥٦٩** | — |
| **`internal/qa`** | **مِسنَدٌ ٤٨١ سطراً + ٢٠ ملفّاً** | **أثمنُ ما في المشروع** |
| **كوتلن** | **٥٢ ملفّاً** | **بلا اختبارٍ مُجهَّزٍ على جهاز** |
| **الويب** | **٠** | **و`eslint` أُضيف ٢٠٢٦-٠٩-٠٥** |
| **الأدوات** | **`moneycheck` · `apidoc` · `mediacheck` · حرّاسُ الويب** | **تعمل** |
| **دورةُ E2E القديمة** | **`scripts/e2e-cycle.py`** | **`RUNNABLE = NO`** — `TIG-2` |

---

# ما يُعاد استعمالُه — **وهو الأكثر**

## `internal/qa/harness.go` — **نواةُ المنظومة**

**فيه اليومَ**: **إقلاعُ خادمٍ حقيقيّ · وجلساتٌ وأدوارٌ · و`Call`/`GET`/`POST`
· و`POSTKey` لمنع التكرار · و`NewUser` بأدوار · و`NewItem` · و`Setting` ·
و`ExpiredToken` · و`seedZone` · و`CountOrders`.**

**وهو بذرةُ خمسِ طبقاتٍ دفعةً واحدة**: `L4` · `L5` · `L8` · `L9` · `L11`.

**وما يُضاف إليه**: **مصنعُ البيانات (§٥) · وحقنُ العطل (§١٤) · والتقاطُ
الإشعاراتِ والبثِّ (§٦) · وادّعاءُ الخصوصيّة.**

## الملفّاتُ العشرون في `qa` — **تُبقى وتُربَط**

| الملفّ | يصير |
|---|---|
| `auth_test.go` · `authz_test.go` | **بذرةُ `L11`** |
| `contract_test.go` | **بذرةُ `L5`** |
| `idempotency_test.go` | **بذرةُ §١٣** |
| `parallel_test.go` | **بذرةُ §١٢** — **وأثمنُها** |
| `money_test.go` | **بذرةُ `L3` الماليّة** |
| `lifecycle_test.go` · `cancel_test.go` · `return_test.go` | **بذرةُ `L8`** |
| `custom_test.go` | **بذرةُ تطابق `XQ-4`** |
| `notify_test.go` · `merchant_notify_test.go` | **بذرةُ §١٩** |
| `route_nav_test.go` · `reroute_test.go` · `nav_bearing_test.go` | **بذرةُ §٢٥ إعادةً** |
| `mapdata_test.go` · `city_test.go` · `alternatives_test.go` | **تُبقى** |

**والناقصُ ليس اختباراتٍ بل ربطاً**: **٥٦٩ دالّةً لا تعرف أيَّ عيبٍ تحرس ولا
أيَّ تدفّقٍ تغطّي.**

## الأدواتُ الأربع

| الأداة | تصير |
|---|---|
| **`cmd/moneycheck`** | **محرّكُ §٧** — **يُوسَّع من ١٣ إلى ١٥ مجموعة** |
| **`cmd/apidoc`** | **حارسُ عقد `L5`** — **يُربَط بانحرافٍ في CI** |
| **`cmd/mediacheck`** | **بذرةُ §٣٠** — **يُوسَّع لفحص السرد** |
| **حرّاسُ الويب** | **جزءٌ من `L6`** |
| **`TestTruthDocIsCurrent`** | **النمطُ الذي يُنسَخ لـ`TEST_TRUTH`** |

---

# ما شاخ — **ولا يُحذف الآن**

| البند | الحال | القرار |
|---|---|---|
| **`TIG-1`** — «الإصدارُ مسموح» | **التحفّظُ في وثيقةٍ لا في المخرَج** | **يُعاد بناءُ `release-gate.py` على بوّابة §١٢** |
| **`TIG-2`** — دورةُ E2E | **`KeyError` عند `cycle.py:192`** · **٤ افتراضاتِ إعداداتٍ شائخة** | **`REWRITE`** — **وما فيها من سيناريوهاتٍ يُنقَل إلى `L8`** |
| **`TIG-3`** — `moneycheck` بلا مصاريف | **صفرُ ذكرٍ لـ`expenses`** | **يُغلَق بالثابت ١٥ في §٧** |
| **`TIG-4`** — فحصٌ بصفرِ مهامّ | **✅ أُغلق ٢٠٢٦-٠٩-٠٥** | **والحارسُ يُعمَّم على كلّ مرحلةٍ في CI** |

---

# الترتيبُ المقترَحُ للبناء

**ولا يُبنى شيءٌ قبل مراجعتك** — **وهذا ترتيبُ القيمة لا جدولُ عمل.**

| # | ما يُبنى | لماذا أوّلاً |
|---|---|---|
| **١** | **جدولُ الخصوصيّة + ادّعاؤه** | **يحرس `D20` و`D21` و`D22` معاً** — **ثلاثةُ عيوبٍ بحارسٍ واحد** |
| **٢** | **`TEST_TRUTH` وحارسُه** | **بلاه لا رابطَ ولا أثرَ تغيير** |
| **٣** | **توسيعُ `moneycheck` — الثوابتُ ١٤ و١٥** | **`D5` و`TIG-3`** |
| **٤** | **مصنعُ البيانات في `qa`** | **بلاه كلُّ اختبارٍ يبني عالمَه** |
| **٥** | **حقنُ العطل** | **تسعُ عمليّاتٍ جزئيّةٍ بلا اختبار** |
| **٦** | **مصفوفةُ الحالات المولَّدة** | **٣٠٠ حالةٍ من وثيقةٍ قائمة** |
| **٧** | **السباقُ بحاجز** | **عشرُ نقاط** |
| **٨** | **محرّكُ أثر التغيير** | **يجيب سؤالَك الأصليّ** |
| **٩** | **متصفّحُ الإدارة** | **أكبرُ فجوةِ طبقة** |
| **١٠** | **أندرويد مُجهَّز** | **أغلاها وأبطؤها** |

**والأوّلُ أرخصُها وأعلاها عائداً**: **جدولٌ وبيانةٌ ودالّةُ ادّعاءٍ
واحدة — تحرس ثلاثةَ عيوبٍ مجمَّدةٍ وتمنع رابعاً.**
