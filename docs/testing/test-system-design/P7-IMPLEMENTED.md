# `P-7` — عقودُ البثِّ والإشعار · **مُنفَّذٌ ومُثبَتٌ على قاعدةٍ حقيقيّة**

> **٢٠٢٦-٠٩-٠٥ · المرحلةُ السابعة.**
> **ولم تُصلَح عيوبٌ** — `PRODUCT DEFECTS FIXED = 0`.
> **ومِعراضٌ واحدٌ أُضيف بإذنٍ صريح** — `TESTABILITY-ONLY PRODUCTION REFACTOR`.

---

# ١ · السؤالُ الذي يجيب عنه

> **لكلّ حدث**: **من يجب أن يستقبل؟ ومن يجب ألّا يستقبل؟ وبأيّ قناة؟
> وبأيّ حمولة؟ وبأيّ خصوصيّة؟ وماذا يقع عند السقوط؟**

**ولا اختباراتِ إشعاراتٍ متناثرةٍ بلا مصفوفة.**

---

# ٢ · ما بُني

| الملفّ | ما فيه |
|---|---|
| `backend/internal/eventmap/extract.go` | **المستخرِج** — مواضعُ الإطلاق والبواثُّ والغرف |
| `backend/internal/eventmap/contracts.go` | **أحدَ عشرَ عقدَ حدث** |
| `backend/internal/qa/fakepush.go` | **ناقلُ دفعٍ مبرمَجٌ يُسجّل** — نجاحٌ · `500` · مهلةٌ · قطعُ اتّصال |
| `backend/internal/qa/event_contract_test.go` | **الالتقاطُ والخصوصيّةُ وحارسُ الانحراف** |
| `backend/internal/qa/event_audience_test.go` | **الجمهورُ والدفعُ والوعي** |
| `backend/internal/orders/watchdog_r22_test.go` | **`R22` و`XOB-5`** — في حزمةِ المحرّك |
| `backend/internal/server/server.go` | **المِعراضُ الوحيد** — `WithPushTransport` |

---

# ٣ · المِعراض — **واحدٌ من اثنين، والثاني لم يلزم**

```
TESTABILITY SEAMS ADDED       = 1/2
TESTABILITY-ONLY PRODUCTION FILE CHANGES = 1
PRODUCTION WIRING UNCHANGED   = PROVEN
PRODUCT BEHAVIOR CHANGED      = NO
```

## ولماذا لزم الأوّل

**`push.NewFCM` تُبنى من متغيّرات البيئة داخل `server.New` نفسِها** —
**فلا موضعَ يستطيع اختبارٌ أن يضع فيه ناقلاً بديلاً**، و`R23` لا يُثبَت
أصلاً.

**والحلُّ خيارٌ متغيّرٌ في آخر التوقيع** — **مُتوافقٌ مع كلّ مُنادٍ قائم**:

```go
func New(…, otpPair func(), opts ...Option) *Server
func WithPushTransport(t push.Transport) Option
```

**و`cmd/api` لم يتبدّل حرفاً** — فالمسارُ الذي يمرّ به الإنتاجُ هو المسارُ
نفسُه.

## والثاني لم يلزم — **وهذا مقصود**

**طُلب مِعراضٌ للراصد.** **ولم يُضَف**: `escalate` غيرُ مصدَّرة، **وحزمةُ
`internal/orders` نفسُها تبلغها** — فكُتب اختبارُ `R22` هناك.

**ومِعراضٌ لا يلزم لا يُضاف.**

## والحارسُ يُثبت أربعةَ أشياء

```
cmd/api لا يمرّر خياراً — المسارُ الافتراضيُّ هو مسارُ الإنتاج
لا رايةَ ولا متغيّرَ بيئةٍ يختار الناقلَ البديل
مِسنَدٌ بلا خيارٍ أُقلع — والناقلُ هو ما يبنيه المسارُ الافتراضيّ
والحقنُ يعمل حين يُطلَب

PRODUCTION WIRING UNCHANGED = PROVEN
```

---

# ٤ · الاستخراجُ — **ولا رقمَ يُثبَّت بيد**

```
NOTIFICATION EMIT SITES     = 47
REALTIME PUBLISHERS         = 14
ROOMS                       = 5   [customer:* driver:* merchant:* ops user:*]
XOB-4 UNTARGETED USER SITES = 27
EVENTS CONTRACTED           = 11/11
```

**والقياسُ السابقُ كان ٤٩ موضعاً و٢٤ بلا توجيه** — **والآن ٤٧ و٢٧.**
**والرقمُ يتبدّل بالعمل**، **ولذلك يُستخرَج ولا يُحفَظ**: الحارسُ يشترط
حدّاً أدنى لا رقماً مثبَّتاً، **فيكشف شيخوخةَ المستخرِج ولا يسقط بكلّ
إضافةٍ سليمة.**

## وحارسُ الانحراف — **أُثبت بالتشويه**

```
DRIFT SELF-TEST = PROVEN — 47 ⇒ 46 موضعاً بعد التشويه، والأصلُ سليم
EVENT CONTRACT DRIFT GUARD = PASS
```

**ويسقط إن أشار عقدٌ إلى اختبارٍ لا وجودَ له** — **وقد سقط فعلاً** حين
كان يقرأ حزمةَ `qa` وحدَها و`R22` في `orders`. **وحارسٌ يقرأ نصفَ الشجرة
يقول «لا وجودَ له» وهو موجود.**

---

# ٥ · العقودُ الأحدَ عشرَ

```
HELD = 3 · EXPECTED FAIL = 5 · PARTIAL = 2 · UNPROVEN = 1
REALTIME = 8 · PUSH = 5 · IN_APP = 8
ROLES COVERED = 5   (زبون · متجر · سائق · مندوب · عمليّات)
CHANNELS COVERED = 3
```

| # | الحدث | القنوات | الحال | الدليل | السجلّ |
|---|---|---|---|---|---|
| **`EV-01`** | **طلبٌ جديدٌ للمتجر** | لحظيّ · دفعٌ · داخليّ | ❌ **EXPECTED FAIL** | **٧١ حقلاً وصل غرفةَ المتجر · ١٩ منها محظور** | **`D20`** |
| **`EV-02`** | **تحديثُ الطلب للزبون** | لحظيّ · دفعٌ · داخليّ | ❌ **EXPECTED FAIL** | **`driver_phone` وصل لحظيّاً وREST — ٩ و١٠ خروق** | **`D21` `D23`** |
| **`EV-03`** | **الطلبُ الخاصُّ لصاحبه** | لحظيّ | ❌ **EXPECTED FAIL** | **العاديُّ وصل والخاصُّ لا** | **`D22`** |
| **`EV-04`** | **قبولٌ تلقائيٌّ للمتجر** | داخليّ · دفع | ◐ **PARTIAL** | لم يقع في النافذة — **لا بابَ يُنادي `sweepAutoAccept`** | `MD-5` |
| **`EV-05`** | **إسنادُ سائقٍ للمتجر** | لحظيّ · داخليّ | ◐ **PARTIAL** | **أُعلِم · و٣ حقولِ هويّةِ سائقٍ تسرّبت** | `MD-2` `D20` |
| **`EV-06`** | **حركةُ محفظة** | داخليّ · دفع | ✅ **HELD** | **صاحبُها `0⇒1` وغيرُه `0⇒0`** | — |
| **`EV-07`** | **إنذارُ الراصد** | لحظيّ · داخليّ | ❌ **EXPECTED FAIL** | **الأوّلُ أشعر مرّةً والثاني صفراً** | **`R22` `XOB-6`** |
| **`EV-08`** | **دفعٌ إلى جهاز** | دفع | ❌ **EXPECTED FAIL** | **٣ إخفاقاتٍ · ٣ نداءاتٍ · ولا رابع** | **`R23` `D12`** |
| **`EV-09`** | **تحويلٌ تلقائيٌّ للمتجر** | داخليّ · لحظيّ | ◐ **UNPROVEN** | لم يقع في النافذة — **الخيطُ لا يُنتظَر** | `R21` `XOB-7` |
| **`EV-10`** | **طلبان لا يختلطان** | لحظيّ · داخليّ | ✅ **HELD** | **معرّفان متمايزان ولا اختلاط** | — |
| **`EV-11`** | **غرفةٌ لا يدخلها غيرُ أهلها** | لحظيّ | ✅ **HELD** | **زبونٌ آخرُ لم يستقبل · ومتجرٌ آخرُ لم يستقبل** | `R14` |

---

# ٦ · `D20` — **٧١ حقلاً تدخل غرفةَ المتجر**

```
وصل إلى غرفة المتجر: 71 حقلاً
D20 REALTIME PRIVACY = EXPECTED FAIL — 19 حقلاً محظوراً
```

**ومنها ما يُقرأ بلا شرح:**

```
customer_phone   ← +96399514886421
address_text     ← الرقة — شارع الاختبار
lat              ← 35.9506
subtotal         ← 3000
ops_stage_late   ← مراحلُ مكتب العمليّات
```

**والحَكَمُ عقدُ `P-1` لا قائمةٌ ثانية** (البند ١) — **ومن نسخ حقلاً أوجد
مصدرَي حقيقةٍ يفترقان.**

---

# ٧ · `D22` — **الطلبُ الخاصُّ لا يصل صاحبَه**

```
الطلبُ العاديُّ وصل صاحبَه: true
الطلبُ الخاصُّ وصل صاحبَه: false

D22 CUSTOM OWNER REALTIME = EXPECTED FAIL
```

**والمقارنةُ هي الدليل**: العاديُّ في القناة نفسِها وللمستلم نفسِه يصل،
**والخاصُّ لا** — **فالعلّةُ في المسار لا في المِسنَد.**

**وهذا هو الجزءُ التشغيليُّ الذي أُجّل من `P-1`** — وقد نُفّذ.

---

# ٨ · `D21`/`D23` — **تكافؤُ القنوات**

```
REALTIME — 9 خرقاً في حمولةٍ وصلت الزبون
REST     — 10 خرقاً في حمولة الزبون

D21/D23 CROSS-CHANNEL PRIVACY = EXPECTED FAIL
```

**و`driver_phone` تصل الزبونَ في القناتين** — **فلا يكفي إصلاحُ REST**،
وهو ما طلب البندُ ٧ إثباتَه.

**ومنها ما لم يكن في `P-1`**: `events` **تحمل معرّفاتِ الفاعلين**
(`actor_id`) — **الخطُّ الزمنيُّ يصل الزبونَ بمن فعل ماذا.**

---

# ٩ · `R23` — **الدفعُ يُحاوَل مرّةً**

**وبالمِعراض المعتمَد**، ناقلٌ مبرمَجٌ يردّ ثلاثةَ أنواعِ فشل:

```
HTTP 500           ← الفعلُ ردّ 200
TIMEOUT            ← الفعلُ ردّ 200
CONNECTION RESET   ← الفعلُ ردّ 200

نداءاتُ الدفع = 3 لثلاث عمليّات

R23 FCM DELIVERY = RISK CONFIRMED
  الدفعُ يُحاوَل مرّةً واحدةً · ولا إعادةَ ولا صفَّ انتظارٍ ولا حالٌ معلَّقة
```

**والبند ٢٩ محفوظ**: **سقوطُ الدفع ليس سقوطَ العمل** — الفعلُ ردَّ `200`
في الثلاث، **وهذا صواب.**

```
DEFECT CANDIDATE — يُعرَض على المالك ولا يُجمَّد
```

---

# ١٠ · `R22` — **الوسمُ يخرس الإنذارَ إلى الأبد**

```
التشغيلُ الأوّل: نداءاتُ الإشعار=1 · موسومٌ=true
التشغيلُ الثاني: نداءاتُ الإشعار=0

R22 WATCHDOG ALERT = RISK CONFIRMED
```

**والحارسُ `RowsAffected == 0` يمنع الإعادةَ** — **ولا مسارَ يُصلحه.**

## و`XOB-6` — **نفسُ الدليل، ولا يُجمَّد وحدَه**

```
RECOMMENDATION — XOB-6 يُطوى دليلاً في R22 لا عيباً مستقلّاً
```

**فهو وصفُ الآليّة التي يقوم عليها `R22`** — **وعيبان لعلّةٍ واحدة
يضاعفان العدَّ ولا يضيفان معرفة.**

## و`XOB-5` — **يبتلع ولا يضرّ الإنذار**

```
العمرُ تضاعف     ⇒ المفتاحُ نفسُه: true
الحالُ تبدّلت    ⇒ المفتاحُ نفسُه: true
السببُ تبدّل     ⇒ المفتاحُ نفسُه: false
```

**فالمفتاحُ يحمل المعرّفَ والسببَ وحدَهما** — **وتبدّلُ العمرِ والحالِ
يُبتلَع.**

**والأثرُ محدودٌ ومقيس**: البلعُ يمنع **إعادةَ البثِّ إلى غرفة
العمليّات**، **ولا يمنع `escalate`** — فالوسمُ هو الذي يمنعها.

```
RECOMMENDATION — ملاحظةٌ لا تُجمَّد: لا ضررَ مُثبَتٌ على الإنذار نفسِه
```

---

# ١١ · ما ثبت أنّه **سليم**

| العقد | الدليل |
|---|---|
| **جمهورُ الطلب** | **`5/5`** — صاحبُه ومتجرُه استقبلا · وزبونٌ آخرُ ومتجرٌ آخرُ وسائقٌ لم يستقبلوا |
| **جمهورُ المحفظة** | **صاحبُها `0⇒1` · وغيرُه `0⇒0`** |
| **عزلُ الغرف** | **زبونٌ لا يستقبل بثَّ طلبِ غيرِه · ومتجرٌ لا يستقبل بثَّ متجرٍ آخر** |
| **طلبان متزامنان** | **معرّفان متمايزان ولا اختلاط** |
| **استهدافُ رموز الدفع** | **لم يُستهدَف رمزُ غيرِ صاحبه · ولا حقلَ طلبٍ ركب المغلّف** |

---

# ١٢ · ⚠️ وادّعيتُ نجاحين لم يقعا — **وصحّحتُهما**

**أوّلُ صيغةٍ قالت:**

```
AUTO-ACCEPT MERCHANT AWARENESS = PASS — المتجرُ أُشعِر
R21 = PASS — المتجرُ يعلم عبر 1 بثٍّ و1 إشعاراً
```

**وحالُ الطلب في الاثنين `pending`** — **فلا قبولٌ تلقائيٌّ وقع ولا
تحويل.** **وما وصل المتجرَ إشعارُ الطلب الجديد لا خبرَ الحدثِ المقصود.**

**فصار الحكمُ مشروطاً بوقوع الحدث:**

```
AUTO-ACCEPT = PARTIAL — لم يقع القبولُ التلقائيُّ (الحالُ "pending")
  TESTABILITY SEAM REQUIRED — لا بابَ يُنادي sweepAutoAccept

R21 AUTO-TRANSFER COMMUNICATION = PARTIAL
  لم يقع تحويلٌ — والخيطُ لا يُنتظَر (XOB-7)
```

**ونجاحٌ يُقاس بحدثٍ لم يقع ادّعاء.**

## و`kind` كادت تُقرأ خرقاً

**فحصُ الخصوصيّة على مغلّف الدفع ردَّ حقلاً محظوراً: `kind`.**
**وهو تصادمُ اسمٍ مقيسٌ في `P-1`** — في المغلّف نوعُ إشعار، وفي الطلب
نوعُ طلب. **فاستُثنيت مفاتيحُ المغلّف نفسِها**، والسؤالُ الصحيحُ صار:
**أتركب حقولُ طلبٍ المغلَّف؟** — **ولا تركب.**

---

# ١٣ · مصالحةُ `D15`

```
D15 RECONCILIATION = STILL VALID
```

## والتعارضُ سببُه أنّي ضربتُ خطوةً أخرى

**خطواتُ `AdminCreateUser` أربعٌ** (`identity/admin.go:78…101`):

```
١ CreateUserWithRole   ← مستخدمٌ + الدورُ الأوّلُ في **معاملةٍ واحدة**
٢ GrantRole للباقي     ← **ولا تُنادى إن كان الدورُ واحداً**
٣ SetTempPassword      ← **خارجَ أيّ معاملة** (repo.go:396)
٤ Audit
```

**واختبارُ `P-6` سلّح نقطةً على `user_roles`** — فأصابت الإدراجَ **داخل
معاملة الخطوة الأولى** فرُجعت كلُّها، **والدورُ كان واحداً فلم تُنادَ
الثانيةُ أصلاً.** **فما اختُبر ليس ما يدّعيه `D15`.**

## والقياسُ الصحيح

```
FAILPOINT D15/set-temp-password أصابت 1 مرّة (UPDATE users)
مستخدمون=1 · أدوارٌ=2 · كلمةٌ=false
الإعادة: 409 phone_taken

D15 STILL VALID — حسابٌ بأدواره بلا كلمة مرور
  وRETRY = BLOCKED بـphone_taken — **كما قال السجلُّ حرفاً**
```

**والسجلُّ كان دقيقاً**: «حسابٌ بأدواره بلا كلمة مرور · والإعادةُ تردّ
`phone_taken`».

```
D15 RECOMMENDED ACTION = يبقى مجمَّداً كما هو · ولا تبديلَ في العدّ
```

**وصُحّحت خريطةُ `P-6`**: `PF-03` كان `PASS` وصار `DEFECT_REPRODUCED`
بالدليل الصحيح.

---

# ١٤ · مرشَّحاتٌ وملاحظاتٌ — **تُعرَض ولا تُجمَّد**

| # | ما ثبت | الدليل | التوصية |
|---|---|---|---|
| **١** | **الدفعُ يضيع بلا إعادةٍ ولا أثر** | ٣ إخفاقاتٍ · ٣ نداءاتٍ · ولا رابع | **`R23` DEFECT CANDIDATE** |
| **٢** | **الوسمُ يخرس الإنذارَ إلى الأبد** | ١ ثمّ ٠ | **`R22` RISK CONFIRMED** |
| **٣** | **`XOB-6` نفسُ علّة `R22`** | الترتيبُ هو الآليّة | **يُطوى دليلاً لا عيباً** |
| **٤** | **`XOB-5` يبتلع ولا يضرّ** | العمرُ والحالُ خارجَ المفتاح | **ملاحظةٌ لا تُجمَّد** |
| **٥** | **`entity_id` فارغٌ في مغلّف المحفظة** | `map[entity:wallet entity_id: kind:wallet]` | **ملاحظةٌ جديدة** |
| **٦** | **`events` تحمل `actor_id` إلى الزبون** | خرقٌ لحظيٌّ وREST | **دليلٌ إضافيٌّ لـ`D23`** |

---

# ١٥ · ما لم يُدَّعَ

```
R14 EXISTING WS REVOCATION      = PARTIAL — عزلُ الغرف مُثبَتٌ · وإبطالُ جلسةٍ
                                   على وصلةٍ مفتوحةٍ يحتاج طبقةَ WS (P-8)
R16 REDIS REVOCATION FAIL-OPEN  = DEFERRED — miniredis في الذاكرة · REQUIRES_P0
D19 LIVE SOCKET TOKEN REFRESH   = DEFERRED TO P-8
D12 SERVER-SIDE PUSH UNREGISTER = PARTIAL — الاستهدافُ مُثبَتٌ · وسلوكُ الخروج
                                   في أندرويد ⇒ P-8
XOB-7 FIRE-AND-FORGET           = DEFECT CANDIDATE غيرُ مجمَّد — **موتُ العمليّة
                                   لم يُثبَت end-to-end**
```

**و`XOB-7` وجهُه في الملاحظة نفسِها**: **عملٌ لا يُنتظَر ولا يُرصَد** —
فالاختبارُ لا يستطيع أن يقول متى انتهى، **وذلك بعينُه ما تشتكي منه
الملاحظة.**

---

# ١٦ · `TEST_TRUTH` بعد `P-7`

```
TESTS   701  (كانت 683)
FLOWS     34/34
DEFECTS   25/25
RISKS     24/24
GAPS      26/26
SETTINGS  95/95
TRUTH DRIFT = NONE
```

**والمصفوفاتُ الأربعُ آليّة:**

```
FINANCIAL_INVARIANTS.json      ← P-4
CONCURRENCY_MATRIX.json        ← P-5
FAILURE_INJECTION_MATRIX.json  ← P-6
EVENT_CONTRACT_MATRIX.json     ← P-7  (وفيها ما استُخرج من الشيفرة)
```
