# `P-10` — بوّابةُ الإطلاق بالدليل · **مُنفَّذةٌ والحكمُ صدر**

> **٢٠٢٦-٠٩-٠٦ · المرحلةُ العاشرةُ والأخيرة.**
> **ولم تُصلَح عيوبٌ ولم تُمسّ شيفرةُ إنتاج** — `PRODUCT DEFECTS FIXED = 0`.
> **ولم يُنشَر شيء.**

---

# ١ · السؤالُ الوحيد

```
READY FOR PRODUCTION = YES / NO
```

**ولا درجةَ تجميليّة. ولا رأيَ بشريٍّ غيرَ موثَّق. ولا «كلُّ شيءٍ تقريباً
جيّد».**

**والقانونُ الأعلى:**

```
BLOCKER > SCORE
```

**فدرجةُ ٩٢ من ١٠٠ لا تتغلّب على مانعٍ واحد.** **وأُثبت ذلك بفحصٍ ذاتيّ:
٩٤ من ١٠٠ والحكمُ `NO`.**

---

# ٢ · ما بُني

| الملفّ | ما فيه |
|---|---|
| `internal/gate/model.go` | **تسعُ حالاتٍ · ستُّ شدّاتٍ · ٢٤ مجالاً** · وصيغةُ التنازل |
| `internal/gate/evidence.go` | **طبقةُ الدليل** — تقرأ الحقيقةَ والمصفوفاتِ الستَّ وتقيس التشغيل |
| `internal/gate/verdicts.go` | **قراراتُ المالك وما لا يُقاس من شيفرة** |
| `internal/gate/rules.go` | **بناءُ ٩٩ قاعدةً** من الدليل |
| `internal/gate/engine.go` | **الحكمُ وهويّةُ المرشَّح** |
| `internal/gate/report.go` | **التقريرُ البشريُّ وحكمُ المجالات** |
| `cmd/releasegate/` | **الأمرُ ورموزُ الخروج الثلاثة** |

## وثلاثةُ حقولٍ أُضيفت إلى الحقيقة (البند ٤٧)

**لم أكتب شدّةَ عيبٍ ولا مجالَه بيد** — **استخرجتُهما من السجلّ المجمَّد:**

```
Defect.Domain    عمودُ «المجال»
Defect.Severity  عمودُ «الشدّةُ المرشَّحة» مترجَماً إلى TQ-1
Risk.Domain      عمودُ «المجال»
```

**وسجلُّ المخاطر لا عمودَ شدّةٍ فيه — قِيس ولم يُفترَض.** **فشدّةُ الخطر
تُستعار من أخطرِ تدفّقٍ يحمله**، **وما لا تدفّقَ له تبقى شدّتُه
`UNKNOWN`** — **وتُعامَل معاملةَ العالية لا الهيّنة.**

---

# ٣ · الأرقام

```
GATE RULES     = 99
BLOCKING RULES = 37
WARNING RULES  = 62

الحالات:  NOT_RUN 35 · EXPECTED_FAIL 30 · NOT_IMPLEMENTED 24 · FAIL 6 · REQUIRES_STAGING 2 · PASS 2
الشدّات:  BLOCKER 21 · CRITICAL 17 · HIGH 39 · MEDIUM 11 · UNKNOWN 11

WAIVER = FORBIDDEN في 19 قاعدة — مالٌ وخصوصيّةٌ وأمنٌ ووسائطُ وتدقيقٌ في شدّةٍ مانعةٍ أو حرجة
WAIVERS CREATED = 0
```

---

# ٤ · الفحوصُ الذاتيّة — **١٤/١٤**

**وبوّابةٌ تقول `NO` دائماً لا تُثبت شيئاً** — **فيلزم أن يُرى الطريقان.**

| الحرف | الفحص | النتيجة |
|---|---|---|
| **أ** | كلُّ الإلزاميّ ناجحٌ ⇒ `YES` | **PASS** — ١٩ قاعدةً · درجةٌ ١٠٠ |
| **ب** | مانعٌ واحدٌ يسقط ⇒ `NO` | **PASS** — **الدرجةُ ٩٤ والحكمُ `NO`** |
| **ج** | جهازٌ مطلوبٌ لم يُشغَّل ⇒ `NO` | **PASS** — دَينُ الجهاز ٢ |
| **د** | بيئةُ تجهيزٍ لم تُشغَّل ⇒ `NO` | **PASS** — دَينُ البيئة ٢ |
| **هـ** | دليلٌ من بناءٍ آخرَ ⇒ `NO` | **PASS** — شائخٌ ١ · طازجٌ ١٨ |
| **و** | عالٍ غيرُ مانعٍ ⇒ `YES` مع تحذير | **PASS** — ٦ ساقطاتٍ والحكمُ `YES` · درجةٌ ٦٨ |
| **ز** | خطرٌ صار عيباً ⇒ سببٌ واحد | **PASS** — `R10→D24` · `R22→D26` · `R23→D27` |
| **ح** | نائمةٌ مانعةٌ مُوقَظةٌ ⇒ `NO` | **PASS** |
| **ط** | نائمةٌ مطفأةٌ لكن تُوقَظ ⇒ تحذيرٌ يُسمّي موقظَها | **PASS** |
| **ي** | حرجٌ بلا تتبّعٍ لا يمرّ صامتاً | **PASS** — دَينُ التتبّع منع |

**وأربعةُ حرّاسٍ للبوّابة نفسِها**: لا قاعدةَ بلا عقدٍ ولا دليلٍ مطلوبٍ ولا
شدّةٍ ولا سياسةِ تنازل · وحالاتُ اللاّنجاح لا تُحتسب نجاحاً · ورموزُ
الخروج موثَّقة · وسجلُّ التنازلات فارغٌ والسياسةُ تُنفَّذ.

---

# ٥ · مصالحتان مقيستان — **ولم تُخمَّن**

## `TestMerchantProfile_SeesHisStore` (البند ١٢)

**الحكم: `TEST INFRA DEBT` — وليس عيبَ منتج.**

**قِيس**: الهجرةُ `0125_drop_rating_comments.sql` **حذفت العمودَ عمداً**
بقرار المالك ٢٠٢٦-٠٨-٣١ («تقييمٌ بدون أيّ تعليق»). **فالمخطَّطُ صحيحٌ
والاختبارُ شائخ** — يُدرج عموداً لم يعد موجوداً.

⚠️ **لكنّ العقدَ الذي كان يحرسه صار بلا حارس**: ملفُّ المتجر في الإدارة
يُظهر الإنذاراتِ والنزاعاتِ وتقييماتِ السائقين. **فالبوّابةُ تُظهره
`FAIL`** — **ولا يُقال «الحزمةُ خضراء» وفيها عقدٌ عارٍ.**

**والإصلاحُ سطرٌ واحد** (حذفُ `comment` من جملة الإدراج) — **ولم أفعله:
`P-10` لا يصلح.**

## `TestRepTarget_GrantedOnDelivery` (البند ١١)

**الحكم: `STALE / CONTRACT-MISMATCHED`** — والعقدُ تبدّل ٢٠٢٦-٠٨-٣١
(`5da3dbed`). **ومُتخطّىً بـ`t.Skip` مع تاريخه، فلا يدخل الحزمةَ
الإلزاميّةَ كاذباً، والتتبّعُ محفوظٌ لا محذوف.**

---

# ٦ · دَينُ التتبّع (البند ٩)

**ولا يُخبَّأ وراء السقوط الآمن.**

```
DEFECTS DIRECTLY ADDRESSABLE (P-9 graph) = 21/27
RISKS   DIRECTLY ADDRESSABLE             = 13/24
GAPS    DIRECTLY ADDRESSABLE             = 23/26

DEFECTS WITH OWN EVIDENCE (tests)        = 14/27
RISKS   WITH OWN EVIDENCE                = 11/24
GAPS    WITH OWN EVIDENCE                =  4/26

RELEASE-BLOCKING UNMAPPED ITEMS = 15
  D13 · R4 · R11 · R13 · R24 · XG-12 · XG-14 · XG-20 · XG-21 · XG-22
  XG-25 · XG-26 · XG-27 · XG-28 · XG-29

TOTAL UNMAPPED = 42
```

**والفرقُ بين الرقمين مقصود**: **«يبلغه رسمُ الأثر» غيرُ «له دليلٌ
يخصُّه».** **رسمُ الأثر يقول أيَّ حزمةٍ تُشغَّل، ولا يقول إنّ في الحزمة
اختباراً يمسُّ `XG-25` بعينه.**

**و`SAFE FALLBACK` يحمي اختيارَ الاختبارات ولا يحوّل تتبّعاً مفقوداً إلى
تغطيةٍ مُثبَتة.**

---

# ٧ · الحكمُ على رحّال غو اليوم

```
RELEASE CANDIDATE ID = rc-3d5e3222-…-dirty
SOURCE COMMIT        = 3d5e32221369f060d9369aed54f230025c0fc4ca
MIGRATION VERSION    = 0126_mocked_location.sql

READY FOR PRODUCTION = NO
```

**ولم تُثبَّت `NO`** — **الفحصُ (أ) يُخرج `YES` من البيانات نفسِها حين
تكون كلُّها ناجحة.**

```
BLOCKERS                     = 35
CRITICAL UNRESOLVED          = 17
REQUIRED VALIDATIONS NOT RUN = 14
DEVICE DEBT                  =  6
STAGING DEBT                 =  2
TRACEABILITY DEBT            =  2
OPS DEBT                     = 11
ADVISORY SCORE               =  2/100
```

## الأسبابُ الجذريّةُ الستّة — **لا قائمةُ خمسِمئةِ اختبار**

**١ · قبولُ الجهاز الحقيقيِّ لم يبدأ.** لا جهازَ متّصلٌ أصلاً · ٢٢ بنداً
من ٢٧ لم يُشغَّل · والملاحةُ بلا سَوقةٍ حقيقيّة · والخلفيّةُ والغفوةُ بلا
جهاز. **وستّةُ إثباتاتٍ بنيويّةٍ لا تُغني عن واحد.**

**٢ · المالُ فيه ستُّ فجواتٍ مانعةٍ أو حرجةٍ مُثبَتة.** أشدُّها
`XG-10`/`XG-11`: **الاستردادُ يسقط كلَّه إن كان المتجرُ قد سحب** —
مُثبَتٌ بسباقٍ حيٍّ (`C-08`: السحبُ فاز بـ٤٠٠٠٠ والمستردُّ للزبون صفر).

**٣ · بيئةُ التجهيز لم تُنشأ.** `R16` (إبطالُ الجلسة عبر عُقَد) و**تحقّقُ
نشرٍ شبيهٍ بالإنتاج** — كلاهما `REQUIRES STAGING`.

**٤ · لا نسخَ احتياطيٌّ مُستعاد.** **ونسخةٌ لم تُستعَد ليست نسخة.**

**٥ · ١٥ سجلّاً مانعاً أو حرجاً بلا دليلٍ يخصُّه** — **والحزمةُ كلُّها قد
تُشغَّل ولا اختبارَ فيها يمسُّ `XG-25` أصلاً.**

**٦ · لم يُسجَّل تشغيلٌ كاملٌ على مرشَّحٍ ثابت.** **والشجرةُ متّسخةٌ حين
وُلِّد هذا الحكم** — فـ`GATE-FRESH-01` = `FAIL`.

## وحكمُ المجالات

| المجال | الحالُ | مانعٌ غيرُ مُرضىً |
|---|---|---|
| `FINANCIAL` | **EXPECTED_FAIL** | ١٠ |
| `PRIVACY` | **EXPECTED_FAIL** | ١ |
| `SECURITY` | **EXPECTED_FAIL** | ٣ |
| `ORDER_INTEGRITY` | **EXPECTED_FAIL** | ٤ |
| `CONCURRENCY` | **EXPECTED_FAIL** | ٠ |
| `FAILURE_RECOVERY` | **EXPECTED_FAIL** | ١ |
| `NOTIFICATION_REALTIME` | **EXPECTED_FAIL** | ٠ |
| `ANDROID_REAL_DEVICE` | **NOT_RUN** | ٢ |
| `NAVIGATION` | **NOT_RUN** | ١ |
| `BACKGROUND_DOZE` | **NOT_RUN** | ١ |
| `STAGING` | **REQUIRES_STAGING** | ٢ |
| `BACKUP_RESTORE` | **NOT_RUN** | ١ |
| `OPERATIONAL_READINESS` | **EXPECTED_FAIL** | ٢ |
| `ADMIN_RBAC` | **NOT_IMPLEMENTED** | ١ |
| `AUDIT` | **NOT_RUN** | ١ |
| `MEDIA_SECURITY` | **NOT_IMPLEMENTED** | ١ |
| `PERFORMANCE` | **NOT_RUN** (`BASELINE 0/3`) | ٠ |
| `LOAD_SOAK` | **NOT_RUN** | ٠ |
| `HEALTH_READINESS` | **FAIL** | ٠ |
| `TRACEABILITY_DEBT` | **FAIL** | ١ |
| `TEST_COVERAGE` | **FAIL** | ٢ |
| `TEST_INFRA_DEBT` | **FAIL** | ٠ |
| `SETTINGS_READINESS` | **NOT_RUN** | ٠ |
| `EVIDENCE_FRESHNESS` | **FAIL** | ١ |

---

# ٨ · الأداءُ — **ولا رقمَ يُخترَع** (البند ٢٩)

```
PERFORMANCE SLO = BASELINE PHASE · BASELINE 0/3
```

**ولم يُعتمد `SLO` مطلقٌ بعد، فلا يُقال `PASS` ولا يُخترَع عدد.**

---

# ٩ · الصحّةُ والجهوزيّة — **مقيستان من الشيفرة** (البند ٣٢)

```
/healthz يفحص:  postgres · redis
تبعيّاتٌ حرجة:  postgres · redis · media · push
بابُ جهوزيّةٍ منفصل: لا
```

**فـ`OPS1` قائم**: **الموجّهُ يرسل الحملَ إلى عُقدةٍ لم تجهز**، **وسقوطُ
مخزنِ الوسائط أو الدفع لا يظهر في الصحّة.**

---

# ١٠ · التنازلات (البند ٣٧)

**الصيغةُ مبنيّةٌ والسجلُّ فارغ.**

```
WAIVERS CREATED BY P-10 = 0
```

**وكلُّ تنازلٍ لاحقٍ يلزمه**: مالكٌ · سببٌ · نطاقٌ · انتهاءٌ · خطرٌ ·
تخفيف. **وتنازلٌ على مالٍ أو خصوصيّةٍ أو أمنٍ في شدّةٍ مانعةٍ أو حرجةٍ
يُرفَض آليّاً** — **أُثبت بفحصٍ ذاتيّ: التنازلُ رُفض والحكمُ بقي `NO`.**

---

# ١١ · الأمرُ ورموزُ الخروج (البند ٤٤)

```
go run ./cmd/releasegate         التقريرُ والحكم
go run ./cmd/releasegate -json   الحكمُ الآليُّ وحدَه

make gate  ·  make gate-json
```

```
0  READY FOR PRODUCTION = YES
1  NOT READY — موانعُ قائمة
2  GATE INTERNAL ERROR — دليلٌ ناقصٌ أو غيرُ مقروء
```

**والفرقُ بين ١ و٢ جوهريّ**: **«غيرُ جاهز» حكمٌ صحيح، و«عطبُ بوّابة» لا
حكمَ فيه** — **ومن خلطهما نشر على عمى.**

## وربطُها بخطّ النشر (البند ٤٥)

**لا خطَّ نشرٍ يُبنى هنا.** **والبوّابةُ صالحةٌ للاستدعاء كما هي:**

```yaml
- run: cd backend && go run ./cmd/releasegate -json
  # رمزُ خروجٍ غيرُ صفرٍ يمنع رفعَ أيّ artifact
```

⚠️ **وملاحظةٌ صادقة**: هذا الحكمُ وُلِّد **قبل التزامه**، فالشجرةُ متّسخةٌ
و`GATE-FRESH-01` = `FAIL`. **وتشغيلٌ في `CI` على وسمٍ مسحوبٍ يُخرجه
`PASS`** — **والعطبُ في لحظة التوليد لا في القاعدة.**

---

# ١٢ · المخرَجات

```
docs/testing/system/RELEASE_GATE.json   ← الحكمُ الآليُّ كاملاً
docs/testing/test-system-design/P10-IMPLEMENTED.md
TEST_TRUTH — 727 اختباراً (١٤ فحصَ بوّابةٍ مسجَّلاً) · TRUTH DRIFT = NONE
```

**والمصفوفاتُ التسع:**

```
FINANCIAL_INVARIANTS · CONCURRENCY_MATRIX · FAILURE_INJECTION_MATRIX
EVENT_CONTRACT_MATRIX · ANDROID_DEVICE_MATRIX · ANDROID_TEST_MATRIX
CHANGE_IMPACT_RULES · CHANGE_IMPACT · RELEASE_GATE
```

---

# ١٣ · الطريقُ إلى نعم (البند ٤١)

```
REMAINING REQUIREMENTS TO READY = 35
```

**مرتَّبةٌ بالخطورة في `RELEASE_GATE.json`، ورؤوسُها:**

1. **قبولُ الأجهزة الحقيقيّة** على `SM-A525F` — ومصفوفةٌ كاملة.
2. **سدُّ `XG-10` و`XG-11` و`XG-18` و`XG-21` و`XG-22` و`XG-29`** — ستُّ فجواتٍ مانعة.
3. **بيئةُ تجهيزٍ مخصَّصة** — `R16` وتحقّقُ نشرٍ شبيهٍ بالإنتاج.
4. **نسخٌ احتياطيٌّ واستعادةٌ مُثبَتان.**
5. **سَوقةٌ حقيقيّةٌ للملاحة** وخلفيّةٌ وغفوةٌ على جهاز.
6. **١٥ سجلّاً مانعاً بلا دليل** — تُربَط باختباراتها.
7. **`AQ-1`** — أقلُّ صلاحيّةٍ في الخادم.
8. **تشغيلٌ كاملٌ للأوضاع الإلزاميّة على مرشَّحٍ نظيف.**

**والبوّابةُ لا تُصلح شيئاً منها** — **تقولها فقط، بالدليل.**
