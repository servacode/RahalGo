# رحّال غو — مطابقةُ المراجعة المستقلّة · الدفعةُ الرابعة

## أندرويد · موتُ العمليّة · الانقطاع · اللحظيّ

> **CODE TRUTH BASELINE**: `26f93c5d` — **ولا سطرَ شيفرةٍ تشغيليّةٍ مسّ.**

---

## ٠ · الحصيلة

| ID | الادّعاء | الحكم | الرمز |
|---|---|---|---|
| **R4-1** | طابورُ المواقع يعبر بين حسابين على الجهاز | **PROVEN DEFECT** | **D16** |
| **R4-2** | موتُ العمليّة يوقف الملاحة ولا تعود | **PROVEN DEFECT** | **D17** |
| **R4-3** | `START_STICKY` يفقد فترةَ النبض | **PROVEN DEFECT** | **D18** |
| **R4-4** | الخدمةُ تعود بلا التحقّق من الورديّة | **PROVEN RISK** | **R17** |
| **R4-5** | وصلةُ البثّ لا تُجدّد التوكن عند إعادة الوصل | **PROVEN DEFECT** | **D19** |
| **R4-6** | الرفعُ بلا تجديدِ توكن | **PROVEN RISK** | **R18** |
| **R4-7** | سباقٌ في مسح الطابور يفقد نقطة | **PROVEN RISK — REQUIRES RUNTIME RACE** | **R19 · RV-8** |
| **R4-8** | التطبيقُ يمسح الدفعةَ ولا يقرأ `accepted` | **PROVEN RISK** | **R20** |
| **R4-9** | معرّفا إشعارٍ ثابتان يطمسان الأحداث | **PRODUCT DECISION** | **D-06** |

**والمراجعةُ محقّةٌ في التسعة.**
**وسحبت واحدةً بنفسها — وهي مُثبَتةُ السحب** (انظر آخرَ الوثيقة).

---

# R4-1 — **D16** · طابورُ المواقع يعبر بين حسابين

## INTENDED CONTRACT
`PointQueue` — **«ما لم يصل يُحفظ حتّى يصل»**. **ولا نصَّ يقول لمن يصل.**

## ACTUAL CODE PATH
| الطبقة | ما فيها |
|---|---|
| الملفّ | **`File(context.filesDir, "points.jsonl")`** — **اسمٌ ثابتٌ للتطبيق كلِّه** |
| النقطة | `TrackPoint` (`shared/model/Driver.kt:77`) — **`lat` · `lng` · `at` · `speed` · `accuracy` · `bearing` · `mocked`.** **ولا `driver_id` ولا `session_id` ولا `shift_id`** |
| الخروج | `HomeViewModel.logout():229` — `LocationService.stop` · `session.clear()` · `auth.logout`. **ولا `PointQueue.clear()`** |
| `session.clear()` | `AndroidSession.kt:54` — **`prefs.remove(ACCESS).remove(REFRESH)`** فقط |
| الإرسال | `LocationService:196-199` — `queue.all()` → `api.sendBatch(waiting)` → `queue.clear()` |
| النسبة | `driver_location_batch.go:150` — **`s.saveTrackBatch(ctx, userIDFrom(r), good)`** |

**وقِيس**: `grep -rn 'PointQueue' mobile/app-driver/` → **موضعُ إنشاءٍ واحدٌ في
الخدمة، ولا `clear` خارجَ مسار النجاح.**

## PROCESS / LIFECYCLE STATE
**الملفُّ في `filesDir`** — **يعيش ما دام التطبيقُ مثبَّتاً**: لا يزول
بالخروج، ولا بموت العمليّة، ولا بإغلاق الورديّة.

## SERVER TRUTH
**المحرّكُ ينسب الدفعةَ للمصادَق الحالي** — **ولا حقلَ في الجسم يقول صاحبَها**،
**ولا يستطيع أن يعرف.** **ويقبل عمرَ ساعتين** (`batchMaxAge`).

## FAILURE WINDOW — **مُثبَتٌ كاملاً**
```
١  السائقُ A على الجهاز · الشبكةُ تنقطع
٢  sendLocation تفشل → queue.add(نقاطُ A)        LocationService:176
٣  A يخرج → الملفُّ باقٍ                          HomeViewModel:229
٤  B يدخل ويفتح ورديّةً على الجهاز نفسِه
٥  أوّلُ نقطةٍ ناجحةٍ لـB تُشعل تفريغَ الطابور     LocationService:195
٦  sendBatch(نقاطُ A) بتوكن B
٧  saveTrackBatch(B, نقاطُ A)                     driver_location_batch.go:150
```
**والشرطُ الوحيدُ أن يكون عمرُ نقاط A دون الساعتين.**

## الأجوبة
```
CAN DRIVER A'S QUEUED LOCATIONS BE WRITTEN AS DRIVER B = YES
OWNER BINDING ANYWHERE (ملفّ · نقطة · جسمُ الطلب · المحرّك) = NONE
```

## ⚠️ وبابٌ ثانٍ يوسّعه
**نقطةُ `A` تدخل الطابورَ أيضاً عند `401`** — **فالخروجُ نفسُه قد يُدخل
آخرَ نقاطِه إليه**: `session.clear()` يمحو التوكن، **ونقطةٌ في الطريق تفشل
فتُحفظ.**

## EXISTING TEST COVERAGE
`mobile/app-driver/src/test/**` — **قِيس: لا اختبارَ لملكيّة الطابور.**

## VERDICT
> # **PROVEN DEFECT — D16**
> **`Queued locations of one driver are written as another`**
>
> **وهو عطبُ نزاهةِ بياناتٍ وخصوصيّةٍ معاً**: **مسارُ A يُكتب في أثر B** —
> **فيُقرأ في اللوحة أنّ B كان حيث لم يكن**، **ويُبنى عليه دفاعٌ في شكوى.**
> **وأثرُ A يضيع في الوقت نفسِه.**

---

# R4-2 — **D17** · الملاحةُ لا تعود بعد موت العمليّة

## INTENDED CONTRACT
`OrdersViewModel` — **«المفروض الرحلة تبقى مستمرّة مهما حصل وأينما ذهب»**.

## ACTUAL CODE PATH
| الحال | أين تعيش | تنجو من إعادة بناء الشاشة؟ | تنجو من **موت العمليّة**؟ |
|---|---|---|---|
| الطلبُ الجاري | **الخادم** — `/driver/orders` | ✅ | ✅ |
| `openId` | `mutableStateOf` في النموذج (`:103`) | ✅ | ❌ — **ويعوّضه `openId ?: mine.firstOrNull()`** (`OrdersLoader.kt:85`) |
| **`following`** | **`mutableStateOf(false)` في النموذج** (`:611`) | ✅ | ❌ **ولا تعويض** |
| `navSession` | `lazy` في النموذج | ✅ | ❌ |

**و`follow(true)` تُنادى في موضعٍ واحد** — `:445`، **داخلَ القبول وحدَه.**
**وقِيس**: `load()` (`:350-371`) **تكتب `offers` و`mine` و`me` و`route`** —
**ولا تمسّ `following`.**
**ولا `SavedStateHandle` ولا `rememberSaveable` للحال الملاحيّة** — قِيس:
`rememberSaveable` في التطبيق للتبويب والبحث لا غير.

## PROCESS / LIFECYCLE STATE — **وموتُ العمليّة ليس إعادةَ بناء**
**إعادةُ بناء الشاشة**: النموذجُ يبقى ⇒ **الملاحةُ مستمرّة** ✅ — **وهو ما
عولج ٢٠٢٦-٠٨-٢٤** (التعليقُ فوق `following`).
**موتُ العمليّة**: **النموذجُ نفسُه يُهدم** ⇒ `following = false` من جديد.

## FAILURE PATH
```
سائقٌ في رحلةٍ والملاحةُ تعمل
→ النظامُ يقتل العمليّة (ذاكرة · قيدُ خلفيّة · المصنّع)
→ يفتح التطبيق
→ الطلبُ يعود من الخادم ويُفتح ✅
→ following = false · navSession متوقّفة · لا صوت · لا تقدّمَ في المسار
→ حتّى يضغط «اتبعني» بيده
```

**والجواب `B`.**

## VERDICT
> # **PROVEN DEFECT — D17**
> **والعقدُ مكتوبٌ نصّاً**، **والخادمُ يحفظ نصفَه** — **والنصفُ الثاني
> (الحالُ الملاحيّة) لا يُحفظ في أيّ مكان.**
>
> **وأخطرُ ما فيه أنّه صامت**: **الشاشةُ تعمل والخريطةُ تُرى** — **والسائقُ
> لا يعرف أنّ الصوتَ مات** حتّى يفوته منعطف.
> **وهو بعينُه ما شكا منه المالك في زرّ «اتبعني»** (٢٠٢٦-٠٩-٠٢).

---

# R4-3 — **D18** · فترةُ النبض تسقط إلى الافتراض بعد `START_STICKY`

## ACTUAL CODE PATH
`LocationService.kt:88`:
```kotlin
val seconds = intent?.getLongExtra(EXTRA_PING_SEC, 0L)?.takeIf { it > 0 } ?: DEFAULT_PING_SEC
…
return START_STICKY
```
`DEFAULT_PING_SEC = 20L` (`:258`).

**والقيمةُ الحقيقيّة**: `HomeViewModel.syncService:103` →
`LocationService.start(app, me.locationPingSec)` → **من `drivers.location_ping_sec`.**

## PROCESS / LIFECYCLE STATE — **عقدُ أندرويد**
**`START_STICKY` يعيد الخدمةَ بـ`Intent` فارغ** (`null`) **إن لم تكن هناك
نداءاتُ بدءٍ معلّقة** — **وهو نصُّ عقد النظام.** **والشيفرةُ تعالج `null`
بأمانٍ** (`intent?`) — **وتقع في الافتراض.**

## PERSISTED STATE
**لا شيء** — **قِيس: لا `SharedPreferences` ولا ملفّ يحفظ `pingSec`.**

## FAILURE PATH — مُثبَت
```
الإدارة: drivers.location_ping_sec = 60
السائقُ يفتح ورديّةً        → 60 ثانية ✅
النظامُ يقتل العمليّة       → START_STICKY بـintent = null
الخدمةُ تعود                → 20 ثانية ❌ — ثلاثةُ أضعاف الطلب
```

**ولا تعود ٦٠ إلّا حين يُنادى `syncService` من جديد** — **وهو يلزمه أن
يفتح السائقُ التطبيقَ ويصل `/driver/me`.** **والخدمةُ تعمل في الخلفيّة بلا
شاشة، فقد لا يقع ذلك ساعات.**

## VERDICT
> # **PROVEN DEFECT — D18**
> **مفتاحُ لوحةٍ يُلغيه النظامُ بلا أن يعلم أحد** — **والأثرُ بطّاريّةٌ
> وحزمةٌ وأثرٌ أكثفُ ممّا قرّره المالك.**
> **ويدخل في `Configuration Runtime Validation`**: **إعدادٌ يسري ثمّ يعود.**

---

# R4-4 — **R17** · الخدمةُ تعود بلا سؤالٍ عن الورديّة

## INTENDED CONTRACT
`LocationService` — **«متى يبدأ ومتى يقف يقرّره حال الورديّة في `HomeViewModel`»**.

## ACTUAL CODE PATH — عند العودة
`onStartCommand:88-94`: **`startForegroundSafely()` ثمّ `request(seconds)`
ثمّ `START_STICKY`.**
**ولا `driver/me` ولا فحصَ `onShift` ولا هويّةَ ورديّةٍ في `Intent` أو على القرص.**

## SERVER TRUTH — **ولا حارسَ هناك أيضاً**
`driver_location.go:36` (`POST /driver/location`) — **قِيس: يتحقّق من
الإحداثيّات وحدَها ثمّ `UPDATE users …`.** **ولا شرطَ `on_shift`.**
**و`driver_location_batch.go` مثلُه.**

**فالنقاطُ تُقبل من سائقٍ أغلق ورديّتَه.**

## FAILURE PATH
```
١ السائقُ على ورديّة
٢ النظامُ يقتل العمليّة
٣ الإدارةُ (أو جهازٌ آخر) تُغلق ورديّتَه
٤ START_STICKY يعيد الخدمة
٥ الخدمةُ ترسل الموقعَ — والمحرّكُ يقبل
```

**وأمّا الجلسةُ المُبطَلةُ أو الحسابُ الموقوف**: **`RequireAuth` يردّ `401`**
⇒ **النقطةُ لا تُكتب** ✅ — **لكنّها تدخل الطابور** (`:176`) **فتُرسَل باسم
من يدخل بعده** (**D16**).

## VERDICT
> # **PROVEN RISK — R17**
> **وليس عيباً ماليّاً ولا حالةَ فسادٍ** — **ولكنّه خصوصيّةٌ وبطّاريّة**:
> **موقعُ رجلٍ أنهى عملَه يُرسل ويُكتب**، **وإشعارُ الورديّة في شريطه.**
> **والعقدُ المكتوبُ يقول «ما دامت الورديّة مفتوحة».**
>
> **والحارسُ غائبٌ في الطرفين معاً** — **فلا التطبيقُ يسأل ولا المحرّكُ يرفض.**

---

# R4-5 — **D19** · وصلةُ البثّ لا تُجدّد توكنَها

## ACTUAL CODE PATH
`LiveSocket.kt:70-107`:
```kotlin
while (isActive) {
    try {
        val url = … + "/api/v1/ws?token=" + session.accessToken()
        http.webSocket(url) { … }
    } catch (e: CancellationException) { throw e }
      catch (e: Exception) { }          ← يُبتلع بصمت
    onState(false)
    delay(wait); wait = (wait * 2).coerceAtMost(MAX_RETRY_MS)
}
```
**وقِيس**: **`refresh` لا تُذكر في الملفّ إطلاقاً.**

**و`onState(false)` لا يُنادي شيئاً**: في التطبيقات الأربعة **سطرُ سجلٍّ لا غير**
(`ShellViewModel.kt:97` · `OrdersViewModel.kt:245`).

## FAILURE PATH
```
١ الوصلةُ تفتح بتوكنٍ صالح
٢ يمضي أكثرُ من ١٥ دقيقةً بلا نداءِ API — والوصلةُ حيّةٌ لأنّ
  ws.go لا يعيد فحصَ الانتهاء (R14 من الدفعة الثالثة)
٣ الشبكةُ تنقطع → المقبسُ يموت
٤ الشبكةُ تعود → الحلقةُ تعيد الوصلَ بالتوكن نفسِه المنتهي
٥ handleWS: VerifyAccess تفشل → 401
٦ الحلقةُ تنتظر وتعيد — بالتوكن المنتهي نفسِه، إلى المهلة القصوى
```

**ولا يخرج من هذا إلّا نداءُ API ناجحٌ يُجدّد** — **`ApiClient.call` عند `401`.**

## التطبيقاتُ الأربعة — **والفرقُ حقيقيّ**

| التطبيق | ما يُجدّد التوكن تلقائيّاً | الأثر |
|---|---|---|
| **driver** | **`LocationService` ينادي `sendLocation` كلَّ نبضة** — **ويُجدّد عند `401`** | **يتعافى في نبضةٍ أو نبضتين** ✅ |
| **customer · merchant · rep** | **`ShellViewModel.refresh()` — ولا تُنادى إلّا من `onEvent` (أي من البثّ نفسِه) أو بفعلِ المستخدم** | **حلقةٌ مقفلة**: **البثُّ ميّتٌ فلا `onEvent`، ولا `onEvent` فلا تجديد** ❌ |

**فالثلاثةُ الخاملةُ تبقى بلا لحظيٍّ حتّى يلمس صاحبُها الشاشة.**

## ⚠️ ولا يظهر عطباً
**`catch (e: Exception) {}` بلا سجلّ** — **بقرارٍ مكتوب**: «انقطاع الوصل ليس
عطباً يقع كلّ يومٍ عشرات المرّات». **والقرارُ صحيحٌ للانقطاع** — **وخاطئٌ
لـ`401`**: **رفضُ مصادقةٍ ليس انقطاعَ شبكة**، **ويُبتلع كما يُبتلع.**

## VERDICT
> # **PROVEN DEFECT — D19**
> **والمتجرُ أشدُّ ما يتأثّر**: **طلبٌ جديدٌ يصل بالبثّ** — **ووصلةٌ ميّتةٌ
> تعني متجراً لا يعلم أنّ عنده طلباً** حتّى يفتح الشاشة.
> **والدفعُ يعوّض جزئيّاً** — **وهو طبقةٌ ثانيةٌ لا الأولى.**

---

# R4-6 — **R18** · الرفعُ بلا تجديد

## ACTUAL CODE PATH
`ApiClient.kt:198-247`:
```kotlin
header("Authorization", "Bearer " + session.accessToken())
…
if (res.status.value >= 400) { … throw ApiException(...) }
```
**ولا `refresh()` ولا إعادةَ محاولة** — **بخلاف `call()` التي تُجدّد وتعيد.**
**والتعليقُ يقولها**: «**ولا يُجدّد التوكن هنا**».

## المواضعُ الأربعة
`AccountApi.kt:50` الصورةُ الشخصيّة · `DriverApi.kt:345` **إثباتُ التسليم** ·
`MerchantApi.kt:298` صورُ الأصناف · `RepApi.kt:129` صورُ المندوب.

## FAILURE PATH
```
توكنُ الوصول انتهى · وتوكنُ التجديد صالح
→ المستخدمُ يرفع صورة       → 401 ApiException
→ يعيد الضغط                 → 401 ثانيةً — التوكنُ لم يتبدّل
→ ولا يخرج إلّا بنداءِ API آخرَ يُجدّد
```

## وإثباتُ التسليم — **أيمنع التسليمَ؟**
**لا** — **`delivery_proof_skipped` مسارٌ قائمٌ ومُدقَّق** (`delivery_proof.go:142`).
**فالسائقُ يتخطّى ويُسلّم.** **والضررُ فقدُ الإثبات لا تعطّلُ الطلب.**

**والسائقُ أقلُّهم عرضةً** — **`LocationService` يُجدّد له بنبضاته.**
**والمتجرُ والمندوبُ أكثرُ**: **شاشةٌ تُفتح ثمّ تُرفع صورةٌ بعد دقائقِ سكون.**

## VERDICT
> # **PROVEN RISK — R18**
> **والتعليقُ يذكره ولا يعلّله** — **فليس قراراً مكتوباً بل وصفٌ لما هو كائن.**
> **والضررُ محدود**: **فشلٌ يُرى ويُستدرَك بفعلٍ آخر** — **ولا مالَ ولا حالةَ تفسد.**

---

# R4-7 — **R19 · RV-8** · سباقٌ يمسح نقطةً لم تُرسَل

## ACTUAL CODE PATH
`LocationService:160-208` — **كلُّ نقطةٍ تُطلق `scope.launch` جديدة**،
**والمسارُ داخلها**:
```
queue.all()            ← @Synchronized
api.sendBatch(waiting) ← شبكةٌ خارجَ أيّ قفل
queue.clear()          ← @Synchronized — تحذف الملفّ كلَّه
```

**و`PointQueue` يُزامن الدوالَّ فرادى** (`add` · `all` · `clear` · `isEmpty`)
— **ولا قفلَ يمتدّ على العمليّة.** **و`clear()` = `file.delete()`** — **لا
حذفَ ما قُرئ بل الملفَّ كلَّه.**

## RACE WINDOW
```
A: waiting = queue.all()   → Q
A: sendBatch(Q) …………… شبكةٌ تنتظر
                              B: sendLocation تفشل → queue.add(R)
A: نجحت
A: queue.clear()           → يُحذف Q و R معاً — و R لم تُرسَل قطّ
```

**وأهو قابلُ الوصول؟** **نعم** — **لا قفلَ ولا `single-flight` ولا
`Mutex`** (قِيس)، **ونداءُ الشبكة قد يطول أكثرَ من فترة النبض**
(`ms/2` أدنى فاصلٍ بين النقاط، و`MIN_SEND_GAP_MS = 5s`).
**فنقطتان في الجوّ معاً حالٌ عاديّةٌ على شبكةٍ بطيئة.**

## EXISTING TEST COVERAGE
**لا اختبارَ تزامنٍ للطابور** — قِيس.

## VERDICT
> # **PROVEN RISK — R19 · REQUIRES RUNTIME RACE**
> **النافذةُ مُثبَتةٌ ساكنةً** (لا قفل)، **والوقوعُ يحتاج توقيتاً** —
> **فيُضاف `RV-8`.**
> **والضررُ نقطةٌ أو نقطتان في ثقبٍ أصلاً** — **لا حالةَ تفسد.**

---

# R4-8 — **R20** · الدفعةُ تُمحى بلا قراءةِ العدد

## INTENDED CONTRACT — **مكتوبٌ في المحرّك نصّاً**
`driver_location_batch.go:117`:
> «**ويعيد كم قُبل وكم رُفض** — **وتطبيقٌ يمسح طابورَه على ردٍّ ناجحٍ بلا
> عددٍ يمسح ما لم يُقبَل**، فيضيع الأثرُ وهو يظنّ أنّه سلّمه»

## ACTUAL BEHAVIOR
`DriverApi.kt:311`: `api.call<Ack>(…)` — **`Ack` عامٌّ، والعددان يُرميان.**
`LocationService:198`: **`api.sendBatch(waiting)` ثمّ `queue.clear()`** —
**أيُّ `200` يمحو الطابورَ كلَّه.**

## هل الرفضُ دائمٌ أم مؤقّت؟
`trackPoint.valid` (`:102`):

| سببُ الرفض | أدائمٌ هو؟ |
|---|---|
| `lat == 0 && lng == 0` | **دائم** — والمسحُ صواب |
| خارجَ المدى | **دائم** |
| `at` صفر | **دائم** |
| **`at.Before(now - 2h)`** | **دائم** — **بل يزداد** |
| **`at.After(now + 5m)`** — **ساعةُ الجهاز متقدّمة** | **مؤقّت** ❗ |

**فحالةٌ واحدةٌ يضيع فيها ما كان يُقبَل**: **جهازٌ ساعتُه تسبق أكثرَ من
خمس دقائق.** **نقاطُه تُرفض الآن، وتصير صالحةً حين يلحق الزمنُ الحقيقيُّ بها**
— **وقد مُحيت.**

**ونافذةُ الصلاحيّة قائمة**: **بين `+5m` و`+2h` من الفارق** — **وساعةٌ
متقدّمةٌ ربعَ ساعةٍ حالٌ شائعةٌ في هواتفَ بلا مزامنةِ شبكة.**

## VERDICT
> # **PROVEN RISK — R20**
> **والعقدُ المكتوبُ في المحرّك يصف هذا الخطأَ بعينه ويحذّر منه** —
> **والتطبيقُ يقع فيه.**
> **ولا يُسمّى عيباً**: **الرفضُ دائمٌ في أربعةٍ من خمسة**، **والخامسُ
> يلزمه جهازٌ بساعةٍ مغلوطة.**

---

# R4-9 — **D-06** · معرّفا إشعارٍ ثابتان

## ACTUAL CODE PATH
`RahalPushService.kt:134`: **`manager.notify(if (urgent) ID_URGENT else ID_NEWS, note)`**
— **`3001` و`3002` لا غير.**

## INTENDED CONTRACT — **ما هو مكتوبٌ وما ليس**
**المكتوب** (`:132`):
> «**ومعرّفانِ لا واحد**: العاجلُ لا يطمس الخبر، والخبرُ لا يطمس العاجل»

**فالعقدُ المكتوبُ عن فصل النوعين — ولا نصَّ يقول إنّ كلَّ حدثٍ عاجلٍ يجب
أن يبقى ظاهراً.**

## ACTUAL BEHAVIOR
**طلبان عاجلان متتاليان**: **الثاني يستبدل الأوّل** — **إشعارٌ واحدٌ يمثّل
آخرَ حدث.** **وكذلك عروضُ السائق.**

**وأثرُه محدودٌ بأمرين:**
| | |
|---|---|
| **الفتحُ إلى الشاشة لا إلى الطلب** | **مسجَّلٌ أصلاً — `U46`** (قرارُ منتج) |
| **الحقيقةُ في الخادم** | **البثُّ والقائمةُ يريان كلَّ الطلبات** — **والإشعارُ تنبيهٌ لا سجلّ** |

## VERDICT
> # **PRODUCT DECISION REQUIRED — D-06**
> **ولا يُسمّى عيباً بلا مطلب** — **والسؤالُ للمالك:**
>
> **«إشعارٌ واحدٌ يقول آخرَ ما وقع»** أم **«كلُّ طلبٍ حيٍّ إشعارٌ قائمٌ حتّى
> يُفتح»؟**
>
> **والأولى أهدأُ، والثانيةُ لا يفوت معها طلب.** **ومتجرٌ عليه ثلاثةُ طلباتٍ
> في دقيقةٍ يرى إشعاراً واحداً اليوم.**

---

# ⚠️ نقطةٌ سقطت للمراجعة — **وتُسجَّل**

**الاشتباهُ بأنّ المتجرَ والمندوبَ لا يسجّلان رمزَ الدفع بعد الدخول — ساقط.**

**والدليلُ مقيس** — `mobile/ui/…/AppFrame.kt:289`:
```kotlin
val ctx = LocalContext.current
LaunchedEffect(vm.user?.id) { Push.register(ctx) }
```
**داخلَ البوّابة المشتركة، بعد ثبوت الجلسة** — **فالثلاثةُ تسجّل.**
**والسائقُ يسجّل من `Backend.kt:106` كذلك.**

**والتعليقُ فوقها يحفظ تاريخَ العلّة**: «**وقِيس ٢٠٢٦-٠٨-٢٩: `Push.register`
ينادِيها السائقُ وحدَه، والثلاثةُ الباقيةُ لها خدمةٌ تتلقّى ولا نقطةٌ تُرسَل
إليها**» — **فالعلّةُ كانت وأُصلحت.**

**ولا تُسجَّل مأخذاً.**

---

# الحصيلةُ النهائيّة

| | كان | صار |
|---|---|---|
| `PROVEN DEFECTS` | ١٥ | **١٩** — +D16 · D17 · D18 · D19 |
| `PROVEN RISKS` | ١٦ | **٢٠** — +R17 · R18 · R19 · R20 |
| `PRODUCT DECISIONS REQUIRED` | ٦ | **٧** — +D-06 |
| `RUNTIME VALIDATION REQUIREMENTS` | ٧ | **٨** — **+RV-8** سباقُ مسح الطابور |
| `CODE TRUTH BASELINE` | `26f93c5d` | **`26f93c5d`** ✅ |

**وثلاثةٌ من الأربعة عيوبُ دورةِ حياةٍ لا منطق**: **حالٌ تعيش في الذاكرة
وحدَها** (D17) · **قيمةٌ تصل في `Intent` لا تُحفظ** (D18) · **حلقةٌ تعيد
المحاولةَ بما رُفض** (D19). **والرابعُ ملفٌّ بلا صاحب** (D16).

**ولا تغييرَ في شيفرةٍ تشغيليّة.**
