# رحّال غو — مطابقةُ المراجعة المستقلّة · الدفعةُ الثانية

> **CODE TRUTH BASELINE**: `26f93c5d` — **ولا سطرَ شيفرةٍ تشغيليّةٍ مسّ.**
>
> **ولا يُقبَل ادّعاءٌ ولا يُدافَع عن تقرير** — كلُّ بندٍ قِيس من الشيفرة.

---

## ٠ · الحصيلة

| ID | الادّعاء | الحكم | الرمز |
|---|---|---|---|
| **R2-1** | سقفُ المفتوح مربوطٌ ببوّابة واتساب | **PROVEN DEFECT** | **D4** |
| **R2-2** | المصروفُ والخزينةُ بلا ذرّيّة | **PROVEN DEFECT** | **D5** |
| **R2-3** | مفتاحُ منع التكرار يعلق `in_progress` | **PROVEN RISK** | **R8** |
| **R2-4** | الطلبُ الخاصُّ يلتفّ على حظر النقد | **PROVEN DEFECT** | **D6** |
| **R2-5** | لا حدَّ معدّلٍ عامّ | **ARCHITECTURE MISMATCH + RISK** | **R9** |
| **R2-6** | سقفُ النقد يقيس المحصَّل لا المكشوف | **PROVEN DEFECT** | **D7** |
| **R2-7** | سباقُ `max_active_orders` لسائقٍ واحد | **PROVEN RISK — REQUIRES RUNTIME RACE** | **R10** · **RV-5** |
| **R2-8** | تفاوتُ حرّاسِ الطلب الخاصّ | **٢ عيبان · ١ خطر · ١ قرارُ منتج · ٤ فروقٍ مقصودة** | **D8 · D9 · R11** |
| **R2-9** | التدقيقُ أفضلُ جهد | **PROVEN BEHAVIOR + DOCUMENTATION MISMATCH** | **R12** |

**والمراجعةُ الخارجيّةُ محقّةٌ في التسعة** — **وسقط لها تعليلان لا حكمان**
(آليّةُ فشل الخزينة في R2-2، وإلغاءُ السياق في R2-9). **والأحكامُ كلُّها ثبتت.**

---

# R2-1 — **D4** · سقفُ الطلبات المفتوحة يعيش داخل بوّابة واتساب

## CLAIM
`checkOpenLimit` في الطلب العادي **مُعشَّشٌ داخل** `if RequireWhatsApp(...)`،
**ومستقلٌّ في الطلب الخاصّ.**

## CODE PATH
`internal/orders/service.go:335` → **البوّابة** → `:358` `checkOpenLimit`
**داخلَها**. وفي `internal/orders/custom.go:93` **مستقلٌّ بلا شرط.**

## PRECONDITIONS
`RequireWhatsApp(ctx, "customers.require_whatsapp")` — و`settings.go:232`:
**المفتاحُ العامُّ `auth.require_whatsapp` ثمّ مفتاحُ الدور، والاثنان لازمان.**

## READS
| المفتاح | الافتراض | الموضع |
|---|---|---|
| `auth.require_whatsapp` | **`true`** | `catalog.go:1181` |
| **`customers.require_whatsapp`** | **`false`** | **`catalog.go:1445`** |
| `orders.max_open_per_customer` | `3` | `catalog.go:717` |

**ولا هجرةَ تزرع أيّاً منهما** — قِيس: `grep` في `migrations/*.sql` **صفر**.
**فعلى قاعدةٍ نظيفةٍ تُقرأ الافتراضات.**

## FAILURE PATH
**لا يلزم فشلٌ ولا تزامن.** **الافتراضُ وحدَه يُطفئ الحارس**:
`customers.require_whatsapp = false` ⇒ `RequireWhatsApp` ترجع `false`
⇒ **`checkOpenLimit` لا يُنادى أصلاً** ⇒ **لا سقف.**

**وإطفاءُ واتساب من اللوحة يُطفئ سقفَ الطلبات معه** — **وهو أثرٌ لا يقوله
اسمُ المفتاح ولا نصُّه في الشاشة.**

## EXISTING TEST COVERAGE
**`TestCreate_OpenOrdersAreCapped`** (`internal/orders/flood_test.go:38`) —
**وهو نفسُه يشهد**: يُشعل المفتاحين صراحةً في `:51-52`، **وتعليقُه**:

> «**حارسُ الكمّ يعيش داخل بوّابة واتساب** … **فعلى قاعدةٍ نظيفةٍ لا يُنادى
> الحارسُ أصلاً فتُفتح ستّةُ طلباتٍ والسقفُ ثلاثة**.»

**فالاختبارُ يمرّ لأنّه يُهيّئ الشرط، لا لأنّ الحارسَ مستقلّ.**

## ولا حارسَ يعوّض
**لا حدَّ معدّلٍ على `/orders`** (انظر **R2-5**)، **ومنعُ التكرار يمنع
المفتاحَ نفسَه لا الطلبَ الجديد.**

## VERDICT
> # **PROVEN DEFECT — D4**
> **`Normal-order open limit is coupled to WhatsApp requirement`**
>
> **والثابتُ المكسور مكتوبٌ في الشيفرة نفسِها** (`service.go:346`):
> «**لا شيءَ كان يمنع خمسين طلباً في دقيقة**».
> **وبالافتراضات الحاليّة لا شيءَ يمنعها الآن.**

## CANONICAL CHANGE REQUIRED
**نعم** — `PROVEN DEFECTS` تزيد، **ويُضاف مطلبُ تشغيلٍ RV-6**: **قراءةُ قيمتَي
المفتاحين في الإنتاج** — **فهما اللتان تقرّران أهو حيٌّ اليوم أم نائم.**

---

# R2-2 — **D5** · المصروفُ يُقيَّد والخزينةُ قد لا تنقص

## CODE PATH · الإنشاء
`internal/server/expenses_handlers.go:270` **`INSERT INTO expenses` — يُقرّ
وحدَه** ثمّ `:281` `wallet.Apply(-amount)` — **ومعاملتُها معاملةٌ أخرى**
(`wallet.go:234` `Begin/Commit`).

## CODE PATH · الإلغاء
`:306` **`UPDATE expenses SET voided_at = now() WHERE voided_at IS NULL`
— يُقرّ وحدَه** ثمّ `:313` `wallet.Apply(+amount)`.

## TRANSACTION BOUNDARY
```
W1  INSERT/UPDATE expenses      ── معاملةٌ ضمنيّةٌ تُقرّ فوراً
W2  wallet.Apply                ── Begin … Commit مستقلّة
```
**ولا معاملةَ تجمعهما، ولا تعويضَ إن سقطت الثانية.**

## FAILURE PATH — **وتصحيحُ تعليل المراجعة**

**المراجعةُ ظنّت الفشلَ يأتي من رصيدٍ لا يكفي. وهذا لا يقع**:
`migrations/0048_treasury.sql:39` — **`CHECK (balance >= 0 OR is_treasury)`**
— **فالخزينةُ وحدَها يُسمح لها بالسالب**، فلا `ErrInsufficient`.

**والمسلكُ الحقيقيُّ أوسع:**

| # | السبب | يحتاج عطلاً؟ |
|---|---|---|
| **F1** | **لا خزينةَ في القاعدة** — `treasuryOn` ترجع `""` (`treasury.go:93`) ثمّ `INSERT INTO wallets (user_id) VALUES ('')` **يسقط بمعرّفٍ غيرِ صالح** | **لا** — حالةُ إعدادٍ فقط |
| **F2** | **إلغاءُ سياق الطلب** بين W1 و W2 | لا — انقطاعُ متصفّح |
| **F3** | خطأُ قاعدةٍ أو بِركةٍ لحظيّ | نعم |

**و`F1` وحدَها تكفي**: **وفحصُ الدفتر الأوّل** (`moneycheck` #1: «للمنصّة
خزينةٌ واحدة») **موجودٌ لأنّ غيابَها حالةٌ ممكنة.**

## الحالةُ النهائيّةُ الخاطئة

| | الإنشاء | **الإلغاء** |
|---|---|---|
| الصفّ | مصروفٌ مُقيَّد | **مُلغى** |
| الخزينة | **لم تنقص** | **لم تُسترجَع** |
| التقارير | **مصروفٌ يُحتسب بلا خروجِ مال** (`expenses_handlers.go:157` والأرباح) | **مصروفٌ اختفى من التقرير ومالُه لم يعد** |
| **الإعادة** | ممكنة — **وتُنشئ صفّاً ثانياً** | **مستحيلة** — `WHERE voided_at IS NULL` لم تعد صادقة |

## EXISTING TEST COVERAGE
**لا فحصَ في الدفتر يربط `expenses` بالدفتر** — قِيس: **١٣ فحصاً في
`cmd/moneycheck`، ولا واحدَ منها يذكر `expenses`.** **فالانحرافُ صامتٌ تماماً.**

## VERDICT
> # **PROVEN DEFECT — D5**
> **`Expense row and treasury ledger are non-atomic`**
>
> **والإلغاءُ أخطرُ من الإنشاء**: **مالٌ لا يعود، وبابٌ لا يُعاد فتحُه.**
> **والنيّةُ مكتوبةٌ فوق الدالّة**: «**فحذفُ الصفّ يترك قيدَه في الخزينة
> بلا صاحب**» — **وهذا بعينُه ما يقع، مقلوباً.**

---

# R2-3 — **R8** · نجاحٌ ثمّ انقطاعٌ يترك المفتاحَ معلّقاً

## CODE PATH
`idempotency.go:104` **الحجزُ يُقرّ** (`done=false`) → `:111` `next(cw, r)`
**والفعلُ التجاريُّ يُقرّ** → `:129` `storeIdempotency`.

## FAILURE WINDOW
**بين إقرار الفعل و`storeIdempotency`.** **والسياقُ هو `r.Context()`** —
**فانقطاعُ العميل يُلغيه**، فتفشل `UPDATE … SET done = true` **ويُبتلع الخطأ**
(`:191` تسجيلٌ لا غير).

## الحالةُ الناتجة
| | |
|---|---|
| الفعلُ التجاريّ | **مُقرٌّ ونهائيّ** |
| صفُّ المفتاح | **`done = false` — ولا يُحذف** |
| الإعادةُ بالمفتاح نفسِه | `claimIdempotency` ترجع `false` → `replayIdempotency` تجد `done=false` → **`409 in_progress`** |
| المدّة | **حتّى التقليم — `idempotencyTTL = 24h`** (`:56`) |
| الردّ | **ضاع** — ولا سبيلَ إلى `id` الطلب من هذا الطريق |

## ⚠️ والتعليقُ يناقض الشيفرة
**`storeIdempotency:191`** يقول:
> «**والفشلُ هنا يعني أنّ إعادةً لاحقةً ستُنفّذ ثانيةً**»

**وهذا غيرُ واقع**: **`releaseIdempotency` لا تُنادى في هذا المسار** —
**فالإعادةُ لا تُنفَّذ ثانيةً بل تُردّ `409` أربعاً وعشرين ساعة.**
**والشيفرةُ أأمنُ ممّا يصفه تعليقُها** (لا ازدواجَ مال)، **والتعليقُ خاطئ.**

## EXISTING TEST COVERAGE — **لا يغطّي النافذة**
**`TestIDEM_002_LostResponse`** (`internal/qa/idempotency_test.go:59`):
```go
_ = h.POSTKey(...)   // الردُّ يُهمَل — كأنّه ضاع
again := h.POSTKey(...)
```
**والنداءُ الأوّل يمرّ بالوسيط كاملاً** و**`storeIdempotency` تنجح** —
**فالمُهمَلُ هو ردُّ الاختبار لا الحفظ.** **والنافذةُ المطلوبة (إلغاءُ
السياق بين الإقرار والحفظ) غيرُ محاكاةٍ إطلاقاً.**

## VERDICT
> # **PROVEN RISK — R8**
> **وليس عيباً**: **لا مالَ يزدوج ولا حالةَ تفسد** — **والضررُ توفّرٌ:
> نقطةٌ مقفلةٌ لصاحبها يوماً.** **ومن أعاد بمفتاحٍ جديدٍ نفَّذ الفعلَ ثانيةً**
> — **وهو الطريقُ الوحيدُ للخروج، وهو بعينُه ما بُني المفتاحُ ليمنعه.**

---

# R2-4 — **D6** · الطلبُ الخاصُّ لا يعرف حظرَ النقد

## CODE PATH
| | الطلبُ العاديّ | **الطلبُ الخاصّ** |
|---|---|---|
| `payment == "cash"` | `service.go:287` → **`cashBlocked`** → `ErrCashBlocked` | **`custom.go:79`**: `if payment != "wallet" { payment = "cash" }` **ثمّ لا فحص** |

**وقِيس**: `grep 'cashBlocked' internal/` → **موضعٌ واحدٌ فقط** (`service.go:288`)
**وتعريفُها.** **ولا نداءَ لها في `custom.go` ولا في المُعالِج**
(`custom_order_handlers.go:34` يمرّر مباشرةً).

## PRECONDITIONS
`customers.cash_ban_failures > 0` **و** `customers.cash_ban_days > 0`
**و** للزبون إخفاقاتٌ بذنبه في النافذة. **وحينها**: `/orders` تردّ
`ErrCashBlocked`، **و`/orders/custom` تُنشئ.**

## ولماذا الضررُ أوسعُ لا أضيق
**الطلبُ الخاصُّ `cash_due = 0` و`subtotal = 0`** — **لأنّ السائقَ يشتري
من جيبه** (`custom.go:197`: «**ما قبضه في الخاصّ مالُه هو: ثمنٌ دفعه من
جيبه**»).

**فالخسارةُ في الطلب العاديّ على المنصّة، وفي الخاصّ على السائق نفسِه** —
**وحظرُ النقد وُضع ليمنع أن يشتري أحدٌ لمن لا يدفع.**

## EXISTING TEST COVERAGE
`internal/qa/custom_test.go` — **ولا فحصَ فيه لحظر النقد** (قِيس).

## VERDICT
> # **PROVEN DEFECT — D6**
> **`Custom orders bypass customer cash ban`**
>
> **وقرارُ المالك مكتوبٌ في `cashban.go`**: «**والدفعُ المقدَّمُ يحمي المالَ
> ويُبقيه زبوناً**» — **وبابٌ ثانٍ للنقد يُبطل القرار كلَّه.**

---

# R2-5 — **R9** · لا حدَّ معدّلٍ عامّ

## القياس
**وسائطُ الجذر** (`server.go:200-228`): `RequestID` · `RealIP` · `Recoverer` ·
`securityHeaders` · `Timeout` · `clientKind` · `minVersion` · `CORS`.
**ولا حدَّ معدّل.**

**والبوّابة** (`deploy/Caddyfile`): `grep -i 'rate\|limit'` → **صفر**.
**ولا مُلحقَ `rate_limit` ولا WAF ولا CDN — ولا دليلَ على أيٍّ منها.**

## ما هو محميٌّ موضعيّاً — **٩ مواضع**

| الموضع | ما يحمي |
|---|---|
| `identity/service.go:201,239` | طلبُ رمز OTP |
| `identity/service.go:322,344` | تحقّقُ الرمز واستعادةُ كلمة المرور |
| `identity/service.go:575` | محاولاتُ الدخول |
| `identity/admin_pin.go:144,150` | رمزُ الإدارة السرّيّ |
| `comms/messages.go:93` | رسائلُ الطلب |
| `server/geo_handlers.go:40,56` | العكسُ والبحثُ الجغرافيّ |
| `server/leads_handlers.go:77,267,442` | الفرصُ العامّة |

```
GLOBAL / API-WIDE RATE LIMITER = NO
ENDPOINT GROUPS WITH LOCAL LIMITER ≈ 12 مسارا
ENDPOINTS WITH NO LIMITER        ≈ 279 من 291
```

## القاعدةُ المُلزمة
`docs/GROUND-RULES.md:73` — «**كل نقطة API محمية بصلاحيات RBAC صريحة +
Rate Limiting**».

**والشطرُ الأوّل مُستوفًى** (حرّاسُ الأدوار مُثبَتون في الكشف)،
**والثاني غيرُ مُستوفًى في ٩٦٪ من المسارات.**

## VERDICT
> # **ARCHITECTURE / DOCUMENTATION MISMATCH — R9**
> **ولا يُسمّى عيباً برمجيّاً**: **لا سلوكَ ينكسر** — **قاعدةٌ مُلزمةٌ لم
> تُنفَّذ**، وهي إمّا أن تُنفَّذ أو تُعدَّل. **وقرارُها للمالك.**
>
> **وأثرُه يتضاعف بـD4**: **إنشاءُ الطلب بلا سقفٍ وبلا حدِّ معدّل.**

---

# R2-6 — **D7** · سقفُ النقد يقيس ما قُبض لا ما سيُحمَل

## القياس
`driver_handlers.go:531-543` يقرأ:
```
held    = driver_cash_boxes.held        ← ما دخل الصندوق فعلاً
cashDue = orders.cash_due (هذا الطلب)
limit   = drivers.cash_limit
```
ثمّ `:578`: `if held + cashDue > limit → errCashLimitFull`.

**و`held` لا يزيد إلّا بـ`cashbox.Collect`** — **وموضعُ ندائها التسليم.**
**فطلبٌ مقبولٌ لم يُسلَّم لا يظهر في `held` إطلاقاً.**

## المثالُ المطلوب — مُثبَتٌ من الشيفرة
```
limit = 500000 · held = 0
A: cash_due = 300000 → 0 + 300000 ≤ 500000  ✅ يمرّ
B: cash_due = 300000 → held ما زال 0 → ✅ يمرّ أيضاً
بعد التسليمين: held = 600000 > limit
```

**وشرطُ بلوغِ الحالة**: أن يُسمح بطلبين معاً. **وبابان:**
| الباب | الافتراض |
|---|---|
| `drivers.max_active_orders` | **١** — **ويُرفع إلى ٢٠ من اللوحة** |
| **`offeredToMe`** — طلبُ المسار | **يتخطّى عدّادَ النشط صراحةً** (`:571` `if !offeredToMe && …`) |

**فالثاني يبلغها بالافتراضات كما هي.**

## و`AssignDriver` أضعف
**يستعمل `cashbox.OverLimit`** — `cashbox.go:72`: **`held >= limit`** —
**ولا يضيف `cash_due` الطلبِ الجاري أصلاً.** **فبابان بمقياسين.**

## المعنى المُثبَتُ للسقف — **(B) لا (A)**
`catalog.go:1014-1020`:
> «**سقفُ النقد بيد السائق — ما يُسمح أن يحمله قبل أن يورّد**»
> «**ويُقاس بما بحوزته وبنقد الطلب معاً**»
> «**وسائقٌ يحمل مليوناً يخاف على نفسه قبل أن نخاف على مالنا**»

**فالمقصودُ المكشوفُ لا المحصَّل** — **والنصُّ صريح.**

## VERDICT
> # **PROVEN DEFECT — D7**
> **`Cash limit measures collected cash, not committed exposure`**
>
> **والحارسُ يقيس كمّيّةً غيرَ التي كُتب ليقيسها** — **والحالةُ الخاطئة
> تُبلَغ بالإعدادات كما هي، بلا عطلٍ ولا تزامن.**

---

# R2-7 — **R10** · سائقٌ واحدٌ · طلبان · قبولٌ متزامن

## CODE PATH
`driver_handlers.go:531` — **قراءةٌ واحدةٌ بلا قفل**:
`(SELECT count(*) FROM orders WHERE driver_id = u.id AND closed_at IS NULL)`
**ثمّ** `:598` تحديثُ **صفِّ الطلب** (`WHERE id = $1 AND driver_id IS NULL`).

## TRANSACTION BOUNDARY
**لا معاملة، ولا `FOR UPDATE` على `users`، ولا قفلٌ استشاريّ**
(`pg_advisory_lock` — **قِيس: موضعٌ واحدٌ في `internal/testdb/reset.go`
وحدَه، وهو عُدّةُ اختبارٍ لا مسارٌ تشغيليّ**).
**والقفلُ الوحيدُ على صفِّ الطلب** — **وهما طلبان مختلفان.**

## CONCURRENCY PATH
```
T1: SELECT count → 0        T2: SELECT count → 0
T1: 0 < max ✅              T2: 0 < max ✅
T1: UPDATE order A ✅       T2: UPDATE order B ✅
النتيجة: طلبان بيده والسقفُ واحد
```
**ولا قيدَ في المخطّط يمنعه** — قِيس: **لا `EXCLUDE` ولا فهرسَ فريدٍ على
`(driver_id) WHERE closed_at IS NULL`.**

**والسقفُ النقديُّ لا يعوّض** — **لأنّه هو نفسُه يقرأ `held` الثابت (D7).**

## EXISTING TEST COVERAGE
**ولا اختبارَ تزامنٍ للقبول أصلاً** — قِيس: اختبارا التزامن الوحيدان في
`internal/orders/promo_race_test.go` (رموزُ الخصم)، **و`parallel_test.go`
عن الملاحة لا عن الإسناد.**


## VERDICT
> # **PROVEN RISK — R10 · REQUIRES RUNTIME RACE**
> **النافذةُ مُثبَتةٌ ساكنةً** (لا قفل · لا قيد)، **والنتيجةُ لا تُثبَت
> إلّا بتزامنٍ حقيقيّ** — كما في `RV-2`.
> **ويُضاف مطلبُ تشغيلٍ `RV-5`.**

---

# R2-8 — تكافؤُ حرّاس الطلب الخاصّ

**والتعليقُ يقول** (`custom.go`): «**وما عدا ذلك فهو طلبٌ كسائر الطلبات**».
**وقِيس، فإذا هي ستّةُ فروق:**

| # | الحارس | العاديّ | الخاصّ | التصنيف |
|---|---|---|---|---|
| ١ | **سقفُ المفتوح** | `:358` **داخلَ بوّابة واتساب** | **`custom.go:93` مستقلّ** ✅ | **الخاصُّ أصحّ** — انظر **D4** |
| ٢ | **منعُ التكرار** | `server.go:354` ✅ | **`server.go:371`** ✅ | **متكافئان** |
| ٣ | **حظرُ النقد** | `:288` ✅ | **غائب** ❌ | **PROVEN DEFECT — D6** |
| ٤ | **توثيقُ واتساب** | `:335` ✅ | **غائب** ❌ | **PROVEN DEFECT — D8** |
| ٥ | **منطقةُ التوصيل** `DeliveryAt` | `:363` ✅ **و`ErrOutOfZone`** | **غائب** — **ولا `zone` يُكتب** ❌ | **PRODUCT DECISION REQUIRED** |
| ٦ | **حدثُ `''→pending`** | `service.go:504` **داخلَ المعاملة** ✅ | **غائب** — **`INSERT` مفردٌ بلا حدث** ❌ | **PROVEN DEFECT — D9** |
| ٧ | **التحقّقُ من المدخلات** | أصنافٌ ومعدِّلاتٌ وأسعار | **نصٌّ وعنوانٌ وقصٌّ عند `MaxCustomRequest`** | **فرقٌ مقصود** |
| ٨ | **حالةُ الزبون وصلاحيّتُه** | **حارسُ الدور في المسار** | **الحارسُ نفسُه** | **متكافئان** |
| ٩ | **البثُّ والإشعار** | `publishOrder` + `notifyCreated` (المتجر) | **`s.touch("order","ops")` وحدَه** | **فرقٌ مقصود** — **لا متجرَ يُشعَر** |
| ١٠ | **أعمدةُ المال** | محسوبة | **أصفارٌ عمداً** | **فرقٌ مقصود ومُوثَّق** |

## D8 — توثيقُ واتساب لا يلزم الطلبَ الخاصّ
**والعلّةُ المكتوبة** (`service.go:326`): «**الرقمُ الوهميّ يعني سائقاً يقف
أمام بابٍ لا أحد فيه**» — **وهي في الخاصّ أشدّ**: **السائقُ يشتري بماله
أوّلاً** ثمّ يقف أمام الباب.

## D9 — طلبٌ خاصٌّ بلا حدثِ إنشاء
**والثابتُ هو ثابتُ `D1` نفسُه**: **تاريخُ الطلب يجب أن يفسّر كلَّ تبدّلِ
حالة.** **وكلُّ طلبٍ خاصٍّ يخالفه** — **لا حالةٌ نادرةٌ بل مئةٌ في المئة.**
**والخطُّ الزمنيُّ** (`stages.go:309`) **يُبنى من `order_events`** — فيبدأ
الطلبُ الخاصُّ من العدم.

## القرارُ المطلوب — منطقةُ التوصيل
**الطلبُ الخاصُّ يُنشأ لأيّ نقطةٍ على الأرض** — **ولا `ErrOutOfZone`.**
**وهو قد يكون مقصوداً**: «**ما ليس في المنصّة**» قد يكون خارجَها مكاناً أيضاً.
**ولا نصَّ يقول ذلك** — **فهو قرارُ مالكٍ لا حكمُ مهندس.**

---

# R2-9 — **R12** · التدقيقُ أفضلُ جهدٍ — **قرارٌ مكتوب**

## السؤال
**أيمكن أن ينجح فعلٌ حسّاسٌ نهائيّاً بلا صفِّ تدقيق؟** — **نعم، في الطبقات الأربع.**

| الطبقة | السياق | الخطأ | يموت مع انقطاع العميل؟ |
|---|---|---|---|
| `server/audit.go:53` | **`context.Background()` بمهلة ٥ ثوانٍ** في `go func` | **يُسجَّل ويُهمَل** | **لا** ✅ |
| `admin_users_handlers.go:258` | **`r.Context()`** | **`_, _ =`** | **نعم** ❌ |
| `identity/repo.go:559` | **`ctx` المُمرَّر — وهو سياقُ الطلب** | **`_, _ =`** | **نعم** ❌ |
| `catalog/catalog.go:595` | **`ctx` المُمرَّر** | **`_, _ =`** | **نعم** ❌ |

**⚠️ وتصحيحُ تعليل المراجعة**: **`server/audit.go` لا يموت مع سياق الطلب** —
**يفتح سياقاً جديداً عمداً.** **والثلاثةُ الأخرى تموت.**

## أهو قرارٌ مكتوب؟
**نعم — وصريح** (`audit.go:42`):
> «**وسقوطُه صامت**: الكتابةُ في الخلفيّة **ولا تُفشل الفعل**»

**و`identity/repo.go:558`**: «**فشل التدقيق لا يُفشل العملية الأصلية**».

**وسبعةُ أفعالٍ كانت تسقط بـ`23502` وأُصلحت ٢٠٢٦-٠٨-١٠** — **فالمرضُ عُرف
ودُووي سببُه، وبقي المبدأ.**

## VERDICT
> # **PROVEN BEHAVIOR** — **أفضلُ الجهد قرارُ تصميمٍ مكتوبٌ ومقصود.**
>
> **ولا يُسمّى عيباً**: **إسقاطُ فعلٍ ماليٍّ ناجحٍ لأنّ سطرَ سجلٍّ لم يُكتب
> أسوأُ من سطرٍ ناقص.**
>
> **ويبقى فرقان مع `GROUND-RULES`:**
>
> **`R12`** — **ثلاثُ طبقاتٍ من أربعٍ تكتب بسياق الطلب** — **فانقطاعُ
> المسؤول يُضيّع التدقيقَ وحدَه دون الفعل.** **وواحدةٌ منها `admin.password_reset`.**
> **والطبقةُ الأولى تُثبت أنّ الحلَّ معروفٌ في البيت نفسِه** — **سياقٌ مستقلّ.**
>
> **و`DOCUMENTATION MISMATCH`**: «**سجل تدقيق لكل عملية حساسة**» **وعدٌ لا
> يضمنه أفضلُ الجهد.** **يُنفَّذ أو يُعدَّل — وقرارُه للمالك.**

---

# الحصيلةُ النهائيّة

| | كان | صار |
|---|---|---|
| `PROVEN DEFECTS` | ٣ | **٩** — **+D4 · D5 · D6 · D7 · D8 · D9** |
| `PROVEN RISKS` | ٧ | **١٢** — **+R8 · R9 · R10 · R11 · R12** |
| `PRODUCT DECISIONS REQUIRED` | ٤ | **٥** — **+ منطقةُ التوصيل في الطلب الخاصّ** |
| `RUNTIME VALIDATION REQUIREMENTS` | ٤ | **٦** — **+RV-5 سباقُ السائق الواحد · +RV-6 قيمتا مفتاحَي واتساب في الإنتاج** |
| `CODE TRUTH BASELINE` | `26f93c5d` | **`26f93c5d`** ✅ |

**و`R11`** — **الطلبُ الخاصُّ يُنشأ بـ`INSERT` مفردٍ بلا معاملة**:
**لا كتابةَ ثانيةً معه اليوم فلا ضررَ قائم**، **ولكنّ إضافةَ حدثِ الإنشاء
(D9) تجعله كتابتين بلا معاملة** — **وهو `D2` نفسُه في مكانٍ آخر.**

**ولا تغييرَ في شيفرةٍ تشغيليّة.**
