# دورة ٧٠أ-س١ — إعادةُ التجهيز إلى مستوى الإنتاج وإثباتُ الرصد

> **خطّةٌ تُكتب قبل أيّ تغيير.** (اتّفاقُ العمل، البند ١.)
>
> **والإنتاجُ ممنوعٌ.** لا نشر، ولا تعديلَ إعدادٍ عامل.

---

## ١ · حالُ التجهيز الآن — مقيساً

```
SOURCE     = c79e6f7b
BUILD      = stg-20260906T094858Z-c79e6f7b
MIGRATION  = 0128_branches_areas.sql   (١٢٨ هجرةً مطبَّقة)
قاعدة      = PostgreSQL 16.4 · 25 MB · ٦١ جدولاً
الحاويات   = api(5d88d574e8bf) · web(e3a02428798f) · caddy(5f5c8640aae0)
             postgres(44126d872ac9) · redis(e7723ff73d96) · إعادةُ تشغيل = ٠
```

**والإنتاجُ على `97f55d3b` وهجرة `0146`** — **فالفارقُ ثماني عشرةَ هجرة.**

## ٢ · تصنيفُ البيانات — والجوابُ حاسم

| الجدول | الصفوف |
|---|---|
| `users` | **٧** — **كلُّها أُنشئت ٢٠٢٦-٠٩-٠٦**، يومَ بُنيت البيئة |
| `orders` · `order_items` · `merchants` | **٠ · ٠ · ٠** |
| `device_tokens` · `refresh_tokens` | **٠ · ٠** |
| `audit_log` | ١ |
| `cities` | ١ |

**فهذه مساندُ تجربةٍ تُرمى، لا بياناتٌ يُبنى عليها قرار.** **ولا صفَّ
ماليٍّ واحد**: `wallets` و`wallet_transactions` و`driver_cash_boxes`
و`driver_cash_entries` و`payout_requests` **قائمةٌ فارغة**.

**ولا بياناتٍ مشتقّةٍ من الإنتاج**: **تاريخُ الإنشاء يومٌ واحدٌ هو يومُ
بناء البيئة**، ولا أثرَ لاستيرادِ نسخة.

## ٣ · سلامةُ التكاملات الخارجيّة — قبل أن يُشغَّل شيء

| التكامل | الحال | الدليل |
|---|---|---|
| **واتساب** | **لا جلسةَ أصلاً** | **لا جدولَ `whatsmeow_*` واحداً في القاعدة** · و`OTP_PROVIDER=dev` **والشيفرةُ لا تُقلع واتساب إلّا عند `whatsapp`** (`cmd/api/main.go:89`) |
| **الدفع (FCM)** | **معطَّل** | **صفرُ متغيّرٍ يبدأ بـ`FCM`** في الحاوية ⇒ لا ناقلَ مهيّأ |
| **الرسائل القصيرة** | **معطَّلة** | `SMS_URL=` و`SMS_OTP_URL=` **فارغان** — والعقدُ يُسقط الإرسالَ بخطأٍ صريحٍ لا بابتلاعٍ صامت |
| **OSRM · العنونة** | داخليّان | عنوانان على الشبكة نفسِها |
| **الخطّافات · الدفعُ الماليّ** | لا وجودَ لها في المنصّة | — |

**وحادثةُ دورةِ ٦٤ لا تتكرّر هنا**: تلك وقعت لأنّ النسخةَ المستعادةَ
كانت **دَفقةَ إنتاجٍ تحمل جلسةَ واتساب حيّة**. **وهذه القاعدةُ لا
تحملها.**

## ٤ · النسخُ الاحتياطيّ والتراجع

**نسخةٌ طازجةٌ بـ`pg_dump`** إلى `/srv/rahalgo-staging/deploy/staging/backups`
قبل أيّ هجرة، **ويُسجَّل مسارُها وحجمُها وبصمتُها ويُتحقَّق أنّها
تُقرأ.**

**والتراجعُ ثلاثةُ أشياء:** استعادةُ القاعدة من النسخة · إرجاعُ
الصورة السابقة (`5d88d574e8bf` محفوظةٌ ولا تُحذف) · إعادةُ إنشاء
`api` وحدَها. **ولا يمسّ الإنتاجَ شيءٌ منها.**

## ٥ · فحصٌ قبليٌّ للهجرات ٠١٢٩ ← ٠١٤٦

**وأخطرُها `0144`** — لقطةُ اقتصاد الطلب، **وفيها قيدٌ يرفض «نصفَ
اقتصاد»**. **وشرطُها يقع على `orders`** — **وعددُها صفر**، فلا صفَّ
يُرفض. **ويُقرأ نصُّ كلّ هجرةٍ في المدى قبل التشغيل**، ويُبلَّغ عن
أيّ شرطٍ لا تستوفيه البياناتُ القائمة.

**ولا تُختلَق بياناتٌ لتمرّ هجرة.** **ولا سعرٌ ولا لقطةٌ ماليّةٌ ولا
تسويةٌ ولا مآلُ طلب.** **وإن وقف شيءٌ: يُبلَّغ ويُوقَف.**

## ٦ · الأثرُ المرشَّح

**أثرٌ واحدٌ يُبنى مرّةً ويُحفَظ** — لا بناءٌ لكلّ بيئة. **ويُنشر على
التجهيز، ويُحتفَظ به عينِه لترقيةٍ محتملةٍ في ٧٠ب.**

## ٧ · مصفوفةُ الإثبات

| البند | كيف يُثبَت |
|---|---|
| الهجرة | `0146` في `/public/identity` وفي `schema_migrations` |
| تخويلُ الرصد | بلا جلسة · زبونٌ · موظّفٌ بلا القدرة · ومالكُها |
| `/healthz` | سليمٌ ثمّ بعطبٍ محقون — **ولا يرسم بنيةً في الحالين** |
| القاعدةُ والذاكرة | أرقامٌ حيّةٌ من بنيةِ التجهيز نفسِها |
| أسبابُ المصافحة | خمسةُ فروعٍ تُمشى بجلساتٍ حقيقيّةٍ على HTTP |
| نسخُ العملاء | معلومٌ ومجهولٌ وسقفُ التعداد |
| الخصوصيّة | فحصٌ نصّيٌّ على الجسد الحقيقيّ — **صفر** |

## ٨ · ما هو ممنوعٌ صراحةً

**الإنتاجُ لا يُمسّ.** ولا يُصلَح `D-28`. ولا Sentinel. ولا تُبدَّل
سياسةُ مفاتيحِ العملاء. **و`XG-34` يبقى `OPEN/HIGH`.**
