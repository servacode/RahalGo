# رحّال غو — مراجعةُ المالك اليدويّة لتطبيق السائق

## DRIVER APP — OWNER MANUAL REVIEW RECONCILIATION

> **`CODE TRUTH BASELINE = 26f93c5d`** · **ولا سطرَ شيفرةٍ مسّ.**
> **والنسخةُ المُجرَّبة**: `app-driver-release.apk` · **`0.2.0` · `versionCode 4`**
> **R8 مُفعَّل · موقَّعةٌ بمفتاح الرفع · على `api.rahalgo.com`.**
>
> **وسبعةُ بنودٍ من المالك رُبطت بالشيفرة** — **وستّةٌ منها لها مسارٌ مُثبَت،
> والسابعُ مطلبُ منظومةٍ لا تطبيق.**

---

# ٠ · الحصيلة

| | |
|---|---|
| **قواعدُ منتجٍ معتمدة** | **٥** (`DPC-1` … `DPC-5`) |
| **ملاحظاتٌ يدويّة** | **٢** (`OWNER-02` · جزءٌ من `OWNER-01`) |
| **مطالبُ منتجٍ جديدةٌ للسائق** | **٢** |
| **مطالبُ منظومةٍ / إدارة** | **١** — **خريطةُ رحّال غو** |
| **فجواتُ أداءٍ وتفاعل** | **٣** |
| **عقودٌ توافقها الشيفرةُ اليوم** | **٦ بنودٍ جزئيّة** |
| **عيوبٌ أو مخاطرُ جديدةٌ في السجلّ المجمَّد** | **٠** |

---

# ١ · OWNER-APPROVED DRIVER PRODUCT RULES — **٥**

| ID | القاعدة | الحال |
|---|---|---|
| **DPC-1** | `OFFLINE MAPS = AUTOMATIC + BACKGROUND-MANAGED + DRIVER-INFORMED` | **مُنفَّذٌ جزئيّاً — ٤ من ٧** |
| **DPC-2** | **جاهزيّةُ الخلفيّة مرتَّبةٌ ومشروحةٌ قبل أوّل ورديّة** | **الأجزاءُ موجودةٌ والترتيبُ لا** |
| **DPC-3** | **الدرجُ كعقد الزبون** — `MAP DOES NOT OPEN DRAWER` + `OPEN DRAWER CLOSES BY SWIPE` | **غيرُ منفَّذ — نفسُ الرايةِ العامّة** |
| **DPC-4** | `NO ACTIVE ORDER → QUEUE` · `ACTIVE ORDER → TRIP UI` · **ولا طابورَ عامٌّ أثناء الرحلة** | **غيرُ منفَّذ** |
| **DPC-5** | `ELIGIBLE SAME-ROUTE ORDER → INLINE MAP OFFER` | **المنطقُ في المحرّك ✅ والواجهةُ لا** |

---

# ٢ · OWNER-01 · الخرائطُ دون اتّصال — **٤ من ٧ منفَّذةٌ أصلاً**

## القياس
```
MANUAL DOWNLOAD FLOW    = YES  ← OfflineCard وزرُّ «تنزيل» (HomeScreen:211)
AUTO DOWNLOAD           = YES  ← HomeScreen:163 · Trigger.AUTOMATIC  ⚠️ بشرط
DOWNLOAD STATE PERSISTED= YES  ← MapPackageStore + MapArchiveLease
REGION SELECTION SOURCE = موقعُ الجهاز — OfflineMap.pickRegion(here.lat, here.lng)
COMPLETION NOTIFICATION = NO   ← لا إشعارَ ولا رسالة · البطاقةُ تختفي وحدَها
RETRY / RESUME          = YES  ← WorkManager · retryOrFail · وقياسُ الفشل مصنَّف
BACKGROUND MANAGED      = YES  ← MapPackageWorker خارجَ الشاشة
```

## الفجواتُ الثلاث
| # | الفجوة | الدليل |
|---|---|---|
| **١** | **التنزيلُ التلقائيُّ معلَّقٌ بفتح تبويب «الحال»** | **المُشغِّلُ في `HomeScreen` داخلَ `LaunchedEffect`** — **ولا `enqueue` في `DriverApplication`** (قِيس). **فسائقٌ يدخل ويذهب إلى الطلبات مباشرةً لا يبدأ تنزيلاً** |
| **٢** | **لا إشعارَ عند النجاح** | **قِيس**: `notif\|Flash\|toast` في `MapPackageWorker` و`OfflineMap` = **صفر**. **والسائقُ يعرف بالغياب لا بالخبر** |
| **٣** | **المنطقةُ من موقع الجهاز لا من منطقة عمله** | `pickRegion(here.lat, here.lng)` — **و«منطقةُ عمل السائق» لا وجودَ لها في النموذج** |

## وما هو سليمٌ ولا يُمسّ ✅
**آليّةُ التنزيل نفسُها مبنيّةٌ وناضجة**: **عاملٌ في الخلفيّة · وإعادةُ محاولةٍ
مصنَّفةُ الفشل · وحالةٌ محفوظةٌ على القرص · وحصّةُ أرشيفٍ (`lease`) تمنع الحذفَ
أثناء الاستعمال · وبيانٌ (`manifest`) يُحدَّث.**
**والملاحةُ المتّصلةُ تعمل أثناء التنزيل** ✅ — **ولا يوقف شيئاً.**

> **التصنيف**: **`PRODUCT REQUIREMENT` جزئيّ** — **ثلاثةُ أجزاءٍ تُضاف إلى
> آليّةٍ قائمة، لا بناءٌ من الصفر.**

---

# ٣ · OWNER-02 · زرُّ الورديّة ثقيل — **والسببُ مُثبَت**

## المسار
```
ShiftCard.onToggle
 → HomeViewModel.toggleShift(on)
     if (state.busy) return                  ← ولا تداخُلَ ✅
     state = busy = true                     ← ودوّارٌ يظهر فوراً ✅ (HomeScreen:530)
     backend.driver.setShift(on)             ← ١ رحلةُ شبكةٍ كاملة
     val me = backend.driver.me()            ← ٢ رحلةٌ ثانيةٌ كاملة ⚠️
     syncService(me)                         ← بدءُ الخدمة الأماميّة أو إيقافُها
     …ثمّ تستقرّ الحال
```

## السببُ الساكنُ للثقل
| # | السبب | الدليل |
|---|---|---|
| **١** | **رحلتان لا واحدة** — **والرايةُ لا تنقلب حتّى تعود الثانية** | `HomeViewModel:126,130` |
| **٢** | **لا تفاؤلَ في الواجهة** — **الحالُ الجديدةُ لا تُعرض قبل يقين الخادم** | `state` يُحدَّث بعد `me()` |
| **٣** | **و`/driver/me` نداءٌ ثقيل** — **يعيد الحالَ كاملاً والأرقامَ والإعدادات** | وقصدُه مكتوبٌ: «من بدّل الرايةَ وحدَها أبقى بقيّةَ الشاشة على حال ما قبل الضغطة» |
| **٤** | **ثمّ بدءُ خدمةٍ أماميّة** — **وله كلفةٌ في اللحظة نفسِها** | `syncService` |

## وما هو صحيحٌ في العقد المطلوب ✅
**لا تداخُلَ في الطلبات** ✅ · **ودوّارٌ يظهر** ✅ · **والفشلُ يعيد الحالَ
ورسالةً مفهومةً من `ApiErrors`** ✅ · **والفرقُ البصريُّ بين الحالين قائمٌ**
في `ShiftCard`.

> **التصنيف**: **`UX / PERFORMANCE GAP`** — **والزمنُ لا يُدَّعى بلا قياس.**
> → **`P-D1`**

---

# ٤ · OWNER-03 · جاهزيّةُ الخلفيّة — **الأجزاءُ موجودةٌ والترتيبُ لا**

## القياس
```
READINESS FLOW              = NO   ← لا مسارَ مرتَّبٌ واحد
NOTIFICATION PERMISSION     = YES  ← AskStartupPermissions عند الإقلاع
FINE LOCATION               = YES  ← معها في الطلب نفسِه
BACKGROUND LOCATION FLOW    = YES  ← LocationDisclosure ثمّ askBackground (MainActivity:447)
BATTERY OPTIMIZATION FLOW   = YES  ← BatteryGuard.ask (HomeScreen:232)
FIRST-SHIFT GATING          = جزئيّ ← الورديّة تُمنع بلا إذنِ موقعٍ · ولا تُمنع بلا خلفيّةٍ ولا بطّاريّة
RECHECK AFTER REVOCATION    = جزئيّ ← recheckLocation عند العودة · و`stopSelf()` بلا رسالةٍ صريحة
```

## الفجواتُ الأربع
| # | الفجوة |
|---|---|
| **١** | **لا تسلسلَ واحداً مشروحاً** — **الأذوناتُ تُطلب في ثلاثة مواضعَ متباعدة**: الإقلاعُ · بطاقةُ الموقع · بطاقةُ البطّاريّة |
| **٢** | **لا حالَ «جاهزٌ للعمل بالخلفيّة ✅»** — **ولا مجموعَ يُقرأ** |
| **٣** | **الورديّةُ تُفتح بلا إذنِ خلفيّةٍ ولا إعفاءِ بطّاريّة** — **والحارسُ على الموقع الأماميّ وحدَه** (`HomeViewModel:102`) |
| **٤** | **سحبُ الإذن أثناء العمل**: **`SecurityException` → `stopSelf()`** — **الإشعارُ يختفي بلا كلمة** (وهو `OBS-D2`) |

## وما هو سليم ✅
**الإفصاحُ قبل طلب الخلفيّة موجودٌ ومكتوب** (شرطُ بلاي) · **و`BatteryGuard`
له ثلاثةُ مستوياتِ نيّةٍ احتياطيّة** · **والتعليقُ يشرح لماذا لا يكفي كونُ
الخدمة أماميّةً على هواتف سامسونغ وشاومي وهواوي.**

> **التصنيف**: **`PRODUCT REQUIREMENT`** — **ترتيبُ ما هو قائمٌ في مسارٍ
> واحدٍ مشروح، لا بناءُ أذوناتٍ جديدة.**

---

# ٥ · OWNER-04 · الدرج — **نفسُ المشكلة ونفسُ العقد**

```
MainActivity.kt:532   gesturesEnabled = false
```
**والتطبيقُ يستعمل `ModalNavigationDrawer` نفسَه ومكوّنَ `Drawer` المشترك** —
**والرايةُ مكتوبةٌ في كلّ تطبيقٍ على حدة، لا في الطبقة المشتركة.**

**فالعقدُ `PC-10` نفسُه ينطبق** — **والتنفيذُ يحتاج معالجةً واعيةً بالسياق**:
**رايةٌ واحدةٌ لا تفرّق بين فتحٍ وإغلاق.**

**وفي السائق أشدّ**: **تبويبُ الرحلة خريطةٌ كاملةُ الشاشة** — **فالتعارضُ
يوميّ.**

> **`DPC-3`** — **وهو `ODC-1` نفسُه في تطبيقٍ ثانٍ.**

---

# ٦ · OWNER-05 · طلبٌ أساسيٌّ واحد — **والطابورُ يظهر أثناء الرحلة**

## القياس
| البند | الحال |
|---|---|
| **تبويبُ الرحلة يُفتح تلقائيّاً عند وجود طلب** | ✅ `MainActivity:500` — **`if (!hasTrip && tab == 0) tab = 1`** — **والعكسُ ينقله إلى الطلبات** |
| **الملاحةُ تُغلق عند انتهاء الطلب** | ✅ `:499` `orders.closeNav()` |
| **الطابورُ العامُّ يُخفى أثناء الرحلة** | ❌ **لا** — **`OrdersScreen:129` يعرض `state.offers` بلا شرط** |
| **الطابورُ يُجلب أثناء الرحلة** | ❌ **يُجلب** — `loadOnce` تنادي `feed.queue()` دائماً |
| **الأزرارُ المعطَّلة** | ⚠️ **البطاقةُ تُعرض ثمّ يُردّ عند الضغط** — **`OBS-D3`** |

**فالسائقُ في رحلةٍ يستطيع أن يفتح تبويبَ الطلبات ويرى الطابورَ العامَّ
كاملاً** — **وأكثرُه لا يستطيع قبولَه** (`max_active_orders = 1`).

> **`DPC-4` — `CONTRACT GAP`**
> **والبنيةُ نصفُها قائم**: **`hasTrip` محسوبةٌ ويُنتقل بها بين التبويبين** —
> **والناقصُ إخفاءُ الطابور وكفُّ جلبِه.**

---

# ٧ · OWNER-06 · الطلبُ على الطريق — **المنطقُ في المحرّك والواجهةُ لا**

## ما هو موجودٌ في المحرّك ✅
`internal/orders/sameroute.go` — **`SameRouteDriver`**:

| الشرط | القيمة |
|---|---|
| **قربُ الاستلامين** | **`SameRouteRadiusM = 800`** متر |
| **قربُ التسليمين** | **`SameRouteSpreadM = 2000`** متر |
| **حالُ الطلب المرساة** | **`assigned` أو `at_pickup` وحدَهما** — **«من استلم بضاعتَه ومضى لا يعود إلى مطعمٍ ثانٍ والطعامُ يبرد»** |
| **سقفُ النشط** | `max_active_orders + same_route_extra` |
| **سقفُ النقد** | `drivers.cash_limit` |

**والقبولُ يتخطّى عدّادَ النشط لما عُرض عليه بعينه** (`driver_handlers.go:571`).

## وما هو ناقصٌ في الواجهة ❌
| # | الناقص |
|---|---|
| **١** | **لا عرضَ داخلَ الخريطة** — **قِيس: لا مكوّنَ «طلب إضافي» في `trip/`** |
| **٢** | **العرضُ يصل مع الطابور العامّ** — **فيراه في تبويب الطلبات لا في رحلته** |
| **٣** | **ولا يُعرض له ما يقرّر به**: **لا انحرافٌ ولا مسافةٌ إضافيّةٌ ولا وقتٌ ولا عائد** — **والمحرّكُ يحسب `BetweenPickupsM` و`BetweenDropoffsM` ولا يردّهما** |
| **٤** | **الرفضُ يقع بزرّ `decline`** ✅ — **والمهلةُ تُخفيه** ✅ |

## ⚠️ وفرقٌ بين ما يقيسه المحرّكُ وما طلبه العقد
**المحرّكُ يقيس قربَ نقطتين** — **والعقدُ يطلب انحرافاً وزمناً مضافاً وتأخيرَ
الطلب الحاليّ.** **والفرقُ حقيقيّ**: **متجران على بعد ٨٠٠ متر قد يفصل بينهما
جسرٌ فيصير الانحرافُ عشرَ دقائق.**

> **`DPC-5` — `CONTRACT GAP` + `PRODUCT REQUIREMENT`**
> **الأهليّةُ قائمةٌ بمقياسٍ أبسط** · **والعرضُ داخلَ الخريطة غيرُ موجود** ·
> **وأرقامُ القرار غيرُ محسوبةٍ ولا مُرسَلة.**

---

# ٨ · OWNER-07 · خريطةُ رحّال غو — **مطلبُ منظومةٍ لا تطبيق**

> **`CROSS-SYSTEM / ADMIN PRODUCT REQUIREMENT — XPRQ-1`**
> **يُناقَش تفصيلاً في `ADMIN PANEL CLOSURE`** — **ولا يُبنى الآن.**

## وما تملكه المنظومةُ اليومَ من حقيقةٍ جغرافيّة

| الطبقة | ما هو موجودٌ فعلاً | ما ينقص |
|---|---|---|
| **السائقون** | **`users.last_location` و`last_location_at`** · **و`driver_track` أثرٌ كامل** · **و`on_shift`** · **و`driver_id` على الطلب** | **لا نقطةَ إدارةٍ تردّ سائقين على خريطة** |
| **المتاجر** | **`merchants.location`** · **والحالُ والدوامُ والإغلاقُ الطارئ** | **لا منطقةَ خدمةٍ لكلّ متجر** — **والتغطيةُ للمنصّة كلِّها** |
| **المندوبون** | **`users.invite_code`** · **والمتاجرُ المنسوبةُ إليه** (`sales_rep_user_id`) | **لا منطقةَ مسؤوليّةٍ جغرافيّة** · **ولا أثرَ موقعٍ للمندوب** |
| **الزبائن** | **`user_addresses.location`** · **و`orders.dropoff`** | **ولا يُتتبَّع** ✅ **وهو الصواب بعقد `PC-6`** |
| **الطلباتُ الحيّة** | **`pickup` و`dropoff` والحالُ والسائقُ** · **وإنذاراتُ الراصد** | **لا عرضَ جغرافيّ** |
| **مناطقُ التغطية** | **`delivery_zones` مضلّعاتٌ ودوائر** ✅ · **و`cities` بنصف قطر** | **ولا طبقةَ «غيرُ مغطّى»** · **ولا `PRQ-1` طلباتُ التغطية** |
| **الفروع** | ❌ **لا وجودَ لمفهوم الفرع إطلاقاً** — **قِيس** | **بناءٌ كامل** |

## ⚠️ وثلاثةُ أشياءَ يلزم بناؤها قبل الخريطة
| # | الناقص | يعتمد عليه |
|---|---|---|
| ١ | **مفهومُ الفرع** — `branch` وربطُ الموارد به | التوسّعُ إلى دمشق وحلب |
| ٢ | **منطقةُ خدمةٍ لكلّ مصدر** | **`PR-4` من عقد الزبون كذلك** |
| ٣ | **طلباتُ التغطية** — `PRQ-1` | خريطةُ الطلب والكثافة |

**فالخريطةُ ليست شاشةً فوق ما هو قائم** — **بل ثلاثةُ نماذجَ جديدةٍ ثمّ شاشة.**

---

# ٩ · إعادةُ تقييم الأسئلة القديمة — **٨**

> # ⚠️ **CLOSED — OWNER DECISION ٢٠٢٦-٠٩-٠٤**
>
> **الستُّ الباقيةُ أدناه أُغلقت كلُّها بـ`DPC-6…DPC-11`.**
> **والمقارنةُ بالشيفرة في**
> [`DRIVER_FINAL_PRODUCT_DECISIONS.md`](DRIVER_FINAL_PRODUCT_DECISIONS.md):
> **`SATISFIED` واحد · `PARTIAL` اثنان · `CONTRADICTS` ثلاثة.**
> **و`DRIVER OPEN OWNER DECISIONS = 0`.**



| ID | السؤال | الحال بعد قرارات اليوم |
|---|---|---|
| **DD-1** | **رقمُ المتجر للسائق** | ⚠️ **ما زال مفتوحاً** — **ولم تمسّه السبعة** |
| **DD-2** | **الخروجُ وبيده طلبٌ حيّ** | ⚠️ **ما زال مفتوحاً** |
| **DD-3** | **المانعُ يُكتشف بعد الضغط** | ✅ **أُغلق ضمناً بـ`DPC-4`** — **فالطابورُ لا يُعرض أصلاً أثناء الرحلة، والمانعُ الغالبُ هو الطلبُ الحيّ** · ⚠️ **ويبقى سقفُ النقد خارجَ الرحلة** — **فيُعدّ نصفَ مغلق** |
| **DD-4** | **متى تبدأ الملاحةُ تلقائيّاً** | ✅ **أُغلق بـ`DPC-4`** — **الرحلةُ هي الواجهةُ عند وجود طلب** · **و`D17` يبقى عيباً مسجَّلاً** |
| **DD-5** | **الطلباتُ المتعدّدةُ وترتيبُها** | ✅ **أُغلق بـ`DPC-5`** — **طلبٌ أساسيٌّ واحدٌ، والإضافيُّ عرضٌ على الخريطة** |
| **DD-6** | **GPS ضعيفٌ أو مطفأ** | ⚠️ **ما زال مفتوحاً** — **و`DPC-2` يلامسه ولا يحسمه** |
| **DD-7** | **صوتٌ مميّزٌ لوصول عرض** | ⚠️ **ما زال مفتوحاً** — **وأثرُه يزيد مع `DPC-5`** |
| **DD-8** | **إيقافُ سائقٍ بيده طلب** | ⚠️ **ما زال مفتوحاً** |

## والباقي مفتوحاً — **٦ (منها واحدٌ نصفُ مغلق)**
```
DD-1  رقمُ المتجر للسائق
DD-2  الخروجُ وبيده طلبٌ حيّ
DD-3  سقفُ النقد يُكتشف بعد الضغط  ← نصفُ مغلق
DD-6  ماذا يرى عند GPS ضعيف
DD-7  صوتٌ مميّزٌ للعرض
DD-8  إيقافُ سائقٍ بيده طلب
```

---

# ١٠ · الملاحظاتُ الستُّ السابقة — **حالُها**

| ID | ما هو | الحال |
|---|---|---|
| **OBS-D1** | **الخدمةُ لا تعود بعد إعادة التشغيل** | ⚠️ **باقيةٌ** — **و`DPC-2` يلامسها ولا يحسمها** |
| **OBS-D2** | **GPS مطفأٌ أو إذنٌ سُحب بلا رسالة** | ⚠️ **باقيةٌ** — **ودخلت في فجوة `DPC-2` الرابعة** |
| **OBS-D3** | **السقفان يُكتشفان بعد الضغط** | ⚠️ **نصفُ مُعالَجةٍ بـ`DPC-4`** |
| **OBS-D4** | **رقمُ المتجر ظاهر** | ⚠️ **باقيةٌ — `DD-1`** |
| **OBS-D5** | **دعمُ المحرّك للمتعدّد أوسعُ من الواجهة** | ✅ **حُسمت بـ`DPC-4` و`DPC-5`** — **والعقدُ الآن: أساسيٌّ واحدٌ + إضافيٌّ على الخريطة** |
| **OBS-D6** | **لا مفتاحَ إعداداتٍ للملاحة** | ⚠️ **باقيةٌ** |

---

# ١١ · DRIVER STATE CENTRALIZATION MAP — **مُحدَّثة**

**والقراراتُ الجديدةُ تضيف أربعةَ عناصرَ يعتمد كلٌّ منها على مركزيّةِ حال.**

| العنصر | مصدرُ الحقيقة | يُحفظ | من يكتب | من يراقب | بعد موت العمليّة | أيختلف مصدران؟ |
|---|---|---|---|---|---|---|
| **الورديّة** | `users.on_shift` | — | `/shift` · **والإدارة** | `syncService` | يُعاد | ⚠️ **نعم — `R17`** |
| **خدمةُ الموقع** | نظامُ أندرويد | — | `syncService` | — | ⚠️ **`D18`** | ⚠️ **نعم** |
| **طابورُ المواقع** | `points.jsonl` | القرص | `LocationService` | — | ينجو | ⚠️ **نعم — `D16`** |
| **الطلبُ الحيّ** | `/driver/orders` | — | الخادم | `hasTrip` | يُعاد | لا |
| **الملاحة** | `following` (ذاكرة) | لا | `follow()` | `TripScreen` | ⚠️ **`D17`** | لا |
| **🆕 جاهزيّةُ الخرائط** | **`MapPackageStore` + `lease`** | **القرص** | `MapPackageWorker` | `HomeScreen` وحدَه ⚠️ | **تنجو** ✅ | **لا** |
| **🆕 جاهزيّةُ الخلفيّة** | **نظامُ أندرويد** — أذوناتٌ وإعفاء | — | حواراتُ النظام | **ثلاثةُ مواضعَ متفرّقة** ⚠️ | تُقرأ من النظام | **لا — ولكن لا مجموعَ لها** |
| **🆕 العرضُ على الطريق** | **`SameRouteDriver` في المحرّك** | — | الراصدُ والدور | **الطابورُ العامّ** ⚠️ | — | **لا** |
| **🆕 التعافي بعد إعادة التشغيل** | **`on_shift` في الخادم** | — | — | **لا أحد** ⚠️ | **لا يقع** | ⚠️ **نعم — `OBS-D1`** |

**والخللُ المركزيُّ يتّسع من ثلاثةٍ إلى خمسة**: **الورديّة · الخدمة ·
الطابور** + **جاهزيّةُ الخلفيّة بلا مجموع** · **والتعافي بلا مراقب.**

---

# ١٢ · التصنيفُ القانونيّ — **ولا عدّادَ مجمَّدٌ تبدّل**

| البند | أيصير عيباً؟ | لماذا |
|---|---|---|
| **OWNER-01** | **لا** | **العقدُ وُضع اليوم** · **والآليّةُ قائمةٌ وتعمل** — `PRODUCT REQUIREMENT` |
| **OWNER-02** | **لا** | **لا عقدَ زمنيٍّ مكتوب** — `UX / PERFORMANCE GAP` |
| **OWNER-03** | **لا** | **الأذوناتُ كلُّها مطلوبةٌ فعلاً** — والناقصُ ترتيبُها — `PRODUCT REQUIREMENT` |
| **OWNER-04** | **لا** | **الشيفرةُ تنفّذ قرارَ المالك ٢٠٢٦-٠٩-٠١** — `DECISION CONFLICT` كما في الزبون |
| **OWNER-05** | **لا** | **العقدُ جديد** — `CONTRACT GAP` |
| **OWNER-06** | **لا** | **العقدُ جديد** — `CONTRACT GAP` + `PRODUCT REQUIREMENT` |
| **OWNER-07** | **لا** | **ميزةٌ غيرُ مبنيّة** — `CROSS-SYSTEM REQUIREMENT` |

```
NEW PROVEN DEFECTS = 0
NEW PROVEN RISKS   = 0
FROZEN COUNTERS    = 19 عيباً · 24 خطراً — بلا تبديل
```

---

# ١٣ · فحوصُ التشغيل المطلوبةُ للسائق

| ID | ما يُقاس | جهازٌ حقيقيّ؟ |
|---|---|---|
| **RV-2** | سائقان على طلبٍ واحدٍ معاً | لا |
| **RV-5** | سائقٌ واحدٌ على طلبين معاً — و`held` بعدهما | لا |
| **RV-8** | سباقُ مسح طابور المواقع | **نعم** |
| **RV-D1** | **`D17`** — الملاحةُ بعد موت العمليّة | **نعم** |
| **RV-D2** | **`D18`** — فترةُ النبض بعد `START_STICKY` | **نعم** |
| **🆕 P-D1** | **زمنُ تبديل الورديّة** — رحلتان وخدمةٌ أماميّة | **نعم** |
| **🆕 P-D2** | **زمنُ أوّلِ تنزيلِ خرائطَ وأثرُه على الاستعمال** | **نعم** |
| **🆕 P-D3** | **`OBS-D1`** — الورديّةُ وإعادةُ تشغيل الجهاز | **نعم** |

```
DRIVER RUNTIME CHECKS STILL REQUIRED = 9   ← +P-D4 (٢٠٢٦-٠٩-٠٤)
```

---

# ١٤ · الخلاصة

```
DRIVER OWNER REVIEW RECONCILIATION COMPLETE

OWNER-APPROVED DRIVER PRODUCT RULES = 5    (DPC-1 … DPC-5)
OWNER MANUAL DRIVER OBSERVATIONS    = 7    (OWNER-01 … OWNER-07)

NEW DRIVER PRODUCT REQUIREMENTS     = 2    (تلقائيّةُ الخرائط · جاهزيّةُ الخلفيّة)
NEW CROSS-SYSTEM/ADMIN REQUIREMENTS = 1    (XPRQ-1 خريطةُ رحّال غو)
NEW DRIVER UX/PERFORMANCE GAPS      = 3    (زرُّ الورديّة · الدرج · أرقامُ قرار العرض)

NEW PROVEN DEFECTS = 0
NEW PROVEN RISKS   = 0

DRIVER OPEN OWNER DECISIONS = 0    ← أُغلقت كلُّها ٢٠٢٦-٠٩-٠٤
    DD-1  رقمُ المتجر للسائق
    DD-2  الخروجُ وبيده طلبٌ حيّ
    DD-3  سقفُ النقد يُكتشف بعد الضغط   (نصفُ مغلق)
    DD-6  ماذا يرى عند GPS ضعيفٍ أو مطفأ
    DD-7  صوتٌ مميّزٌ لوصول عرض
    DD-8  إيقافُ سائقٍ بيده طلب

DRIVER RUNTIME CHECKS STILL REQUIRED = 9   ← +P-D4 (٢٠٢٦-٠٩-٠٤)

DRIVER CODE UNDERSTANDING   = COMPLETE
DRIVER OWNER MANUAL REVIEW  = COMPLETE

DRIVER PRODUCT DECISIONS CLOSED = YES  ← ٢٠٢٦-٠٩-٠٤
DRIVER APP PRODUCT REVIEW       = CLOSED  ← ٢٠٢٦-٠٩-٠٤

DRIVER APP VERIFIED         = NO
DRIVER READY FOR PRODUCTION = NOT YET EVALUATED

OPERATIONAL CODE CHANGES = 0
```

**ولا يُنتقل إلى تطبيق المتجر.**
