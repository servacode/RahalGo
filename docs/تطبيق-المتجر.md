# تطبيقُ المتجر — المسلسل

> **يُروى هنا ما يجري في التطبيق من أوّل لحظةٍ إلى آخرها.**
>
> **وفي كلّ مشهدٍ ثلاثةُ مسارات:**
>
> | | |
> |---|---|
> | **🖥 الواجهة** | ما تراه عينُ صاحب المتجر على الزجاج |
> | **⚙ الخلفيّة** | ما تفعله الشيفرةُ والمحرّكُ في اللحظة نفسِها |
> | **⚠ وماذا لو** | **كلُّ الحالات الأخرى** — لا مسارَ النجاح وحدَه |
>
> **ومقيسٌ من الشيفرة سطراً سطراً** (٢٠٢٦-٠٨-٣١).
> **وحيثما وجدتُ فرقاً بين ما يُعرض وما يجري، كتبتُه ولم أُجمّله.**

---

## لماذا هذا التطبيقُ بعد المندوب

**(ترتيبُ المالك ٢٠٢٦-٠٨-٣٠:** «المندوبُ هو من سيفتح المتاجر ·
**والمتاجرُ هي من تبيع الأصناف** · والزبونُ يشتري · والسائقُ يوصّل».)

**والسلسلةُ تُختبر بترتيبها**: من يفتح أوّلاً يُختبر أوّلاً.

**وهو أكبرُ من المندوب بأربعين بالمئة** — ٤٠٩١ سطراً و٧٧ دالّةً و١٢٣
نصّاً — **لأنّ عنده ما لا يملكه المندوب: الطلباتُ الحيّةُ وسجلُّها.**
**وكلُّ طلبٍ في المنصّة يمرّ به.**

---

# الفصل الأوّل · الميلاد

## المشهد ١ — الإصبعُ يلمس الأيقونة

**🖥 الواجهة**

شاشةٌ بلون الأرض جزءاً من ثانية. **لا شعارَ يقفز.**

**⚙ الخلفيّة**

`MerchantApplication.onCreate` ثمّ `MainActivity`:

```kotlin
Net.install(this)      // مراقبُ الشبكة
Images.install(this)   // مُحمِّلُ الصور
Crash.start(...)
Backend.of(this)       // النواة: `android-merchant`
```

**⚠ وماذا لو** — كأخيه المندوب حرفاً: ضيقُ الذاكرة يُفرغ ذاكرةَ الصور،
والقرصُ الممتلئُ يُبطئ ولا يكسر، **والانهيارُ يُرفع أثرُه عند الفتحة
التالية.**

## المشهد ٢ — البوّابةُ تقرّر

**🖥 الواجهة** — دائرةٌ تدور، ثمّ إحدى ثلاث:

**⚙ الخلفيّة**

| الحال | ما يظهر |
|---|---|
| توكنٌ صالح | **الطلبات** — أوّلُ تبويبٍ عنده |
| لا توكن | شاشةُ الدخول |
| **انقطاعُ شبكة** | «لا يوجد اتصال» وزرُّ إعادة |
| **نسخةٌ شاخت** | **شاشةُ التحديث** |

> **⚠ والأخيرةُ لم تكن تظهر حتّى ٢٠٢٦-٠٨-٣١** — `ApiClient.outdated`
> متغيّرٌ عاديٌّ لا يراه Compose، **فيقول التطبيقُ «لا يوجد اتصال»
> والمحرّكُ يردّ ٤٢٦.** (أُصلح في `:ui` فأصاب الأربعة.)

## المشهد ٣ — شاشةُ الدخول

**🖥 الواجهة**

```
        [ شعارُ المنصّة ]
         تسجيل الدخول
   ادخل برقمك وكلمة مرورك
   ┌─ رقم الهاتف ──────────┐
   ┌─ كلمة المرور ─────👁 ─┐
      [   تسجيل الدخول   ]
        نسيت كلمة المرور؟
```

**⚙ الخلفيّة**

- **النصُّ مركزيٌّ** — `login_title` و`login_submit` من `:ui`.
  **وكان «تسجيل دخول المتجر» حتّى ٢٠٢٦-٠٨-٣٠.**
- **لا بابَ لإنشاء حساب** — حسابُ المتجر يفتحه مندوبٌ أو إدارة.
- **وكلمتُه الأولى يسلّمها المندوب** — ونصُّ المندوب يقول: «سيطلب منه
  تبديلها عند أول دخول».

**⚠ وماذا لو**

| الحال | 🖥 يرى | ⚙ يجري |
|---|---|---|
| كلمةٌ خطأ | «رقمُ الهاتف أو كلمةُ المرور غير صحيحة» | لا يُقال أيُّهما |
| ٥ محاولاتٍ لرقم | «حاول بعد قليل» | `loginMaxPerPhone` |
| **حسابُه أُوقف** | «هذا الحساب موقوف» | `account_suspended` |
| **متجرُه لم يُوافَق عليه بعد** | يدخل ولا متجرَ له | **حالٌ لم تُقس بشراً** |

---

# الفصل الثاني · الطلبات — قلبُ التطبيق

## المشهد ٤ — أوّلُ شاشةٍ يراها

**🖥 الواجهة**

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  ☰            🔔      💰 ٤٥٠٠٠
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

   الطلبات
   اقبل الطلب ثم حضّره ثم أعلن جاهزيته

   لا طلبات الآن — سنُعلمك حين يصل طلب

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 حسابي  الأصناف  صنف جديد  متجري  الطلبات
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

**⚙ الخلفيّة**

**والتبويبُ الأوّلُ هو الطلبات** (`Tab.Orders`) — **بخلاف المندوب الذي
يبدأ بلوحته.** **ومن يفتح تطبيقَه وطلبٌ ينتظر يجب أن يراه بلا ضغطة.**

**⭐ والتبويباتُ تتبدّل بوضع الإدارة:**

```kotlin
if (selfManage) { /* تبويبُ الطلبات يظهر */ }
```

**فمتجرٌ لا يدير طلباتِه لا يرى التبويبَ أصلاً** — والمكتبُ يديرها عنه.
**وتبويبٌ يظهر بلا عملٍ خلفه يُعلّم صاحبَه ألّا ينظر.**

**⚠ وماذا لو**

| الحال | 🖥 يرى | ⚙ يجري |
|---|---|---|
| لا طلبات | **«لا طلبات الآن — سنُعلمك حين يصل طلب»** | **وعدٌ لا فراغ** |
| **وضعُ المتاجر مطفأ** | **لا تبويبَ للطلبات أصلاً** | `selfManage=false` |
| النداءُ فشل | خطأً + «إعادة» | — |
| سحب للأسفل | تحديثاً | `refresh` |

## المشهد ٥ — طلبٌ يصل

**🖥 الواجهة**

**إشعارٌ يوقظ الجهاز** — ثمّ في الشاشة بطاقةٌ فيها:

```
   #١٠٤٥                    ⟨بانتظار قبولك⟩
   ┌────────────────────────────┐
   │ شاورما دجاج ×٢     ٢٤٠٠٠  │
   │ بطاطا ×١            ٥٠٠٠  │
   │ ملاحظة الزبون: بلا بصل     │
   │ ─────────────────────────  │
   │ لك ٢٦١٠٠ ل.س              │
   └────────────────────────────┘

   [  استلمت الطلب  ]  [ رفض — غير متوفر ]
```

**⚙ الخلفيّة**

**والطلبُ لا يصل المتجرَ مباشرةً** — **يصل المنصّةَ أوّلاً فتوافق ثمّ
يُحوَّل.**

> **(قرارُ المالك ٢٠٢٦-٠٨-٣٠:** «بالحالتين المنصّةُ يجب أن تستلم الطلبَ
> أوّلاً» — **ووضعُ المتاجر يقرّر من يضغط الأزرارَ بعدها لا من يستلم.**)

**و«لك ٢٦١٠٠»** (`ord_net`) — **مستحقُّه لا قيمةُ الطلب.** والفرقُ هامشُ
المنصّة، **ورقمٌ يقرؤه صاحبُ المتجر على أنّه له وهو ليس له يُفسد الثقة
في أوّل تحاسب.**

**⚠ وماذا لو**

| الحال | 🖥 يرى | ⚙ يجري |
|---|---|---|
| ضغط **«استلمت الطلب»** | يتقدّم الطلبُ فوراً | `transition(accepted)` **ثمّ `preparing` آليّاً** |
| ضغط **«رفض — غير متوفر»** | يُطلب سببٌ | `transition(rejected, note)` |
| **رفضٌ بلا سبب** | نصٌّ جاهز: «الصنف غير متوفر حالياً» | `order_reject_note` |
| **ضغط مرّتين** | الزرُّ معطَّل | `busy` |
| الشبكةُ قُطعت | خطأٌ والطلبُ كما هو | **ولا يتقدّم بلا جواب** |
| **سبقه المكتبُ** | الطلبُ يتبدّل عند التحديث | حالتُه من المحرّك لا من الشاشة |

> **⭐ و«بدأت التحضير» لا يُضغط عادةً** — القبولُ ينقل إلى التحضير
> آليّاً. (قرارُ المالك ٢٠٢٦-٠٨-٣٠: «بعدما يتمّ استلامُ الطلب يدخل
> بمرحلة التحضير بشكلٍ تلقائيّ — لا داعيَ أن يقول بدأتُ التحضير».)

## المشهد ٥·ب — آلةُ الحالات — **وأزرارُه ثلاثةٌ لا أكثر**

**🖥 الواجهة** — **الزرُّ يتبدّل بحال الطلب، ولا يظهر منه إلّا واحد:**

| الحال | الزرُّ المعروض |
|---|---|
| `pending` | **«استلمت الطلب»** · ومعه «رفض — غير متوفر» |
| `accepted` | «بدأت التحضير» |
| `preparing` | **«جاهز للاستلام»** |
| ما بعدها | **لا زرّ** — والحالُ تُكتب نصّاً |

**⚙ الخلفيّة**

**و«بدأت التحضير» لا يُرى في وضع المتاجر** — المحرّكُ ينقل من القبول
إلى التحضير بنفسه (`autoPreparing`). **فالحالةُ «مقبول» لا تُرى إلّا
لحظة.**

**وتبقى في الشيفرة لوضع المنصّة**: **المكتبُ يقبل والمتجرُ يبدأ، وهما
شخصان.**

**⭐ وما بعد الجاهزيّة ليس بيده** — **وزرٌّ لا يفعل شيئاً أسوأُ من
غيابه**: يضغطه فلا يتغيّر شيءٌ فيظنّ التطبيقَ عطبان.

## المشهد ٥·ج — ويقرأ رحلةَ طلبه بعشر كلمات

**🖥 الواجهة** — بعد أن يخرج الطلبُ من يده، **يقرأ أين صار:**

| الحال | ما يُكتب |
|---|---|
| `dispatching` | **بانتظار سائق** |
| `assigned` | أُسند لسائق |
| `at_pickup` | **السائق عندك** |
| `picked_up` | استلمه السائق |
| `on_way` | في الطريق |
| `at_dropoff` | وصل الزبون |
| `delivered` | **سُلّم** |
| `rejected` | **اعتذرت عنه** |
| `cancelled` | أُلغي |
| `failed` | تعذّر تسليمه |
| `refunded` | استُرجع |

**⭐ و«اعتذرت عنه» بصيغة الفاعل** — لا «رُفض». **ومن اعتذر يعرف أنّه
هو من فعل**، ولا يقرأ حكماً عليه.

> **(بلاغُ المالك ٢٠٢٦-٠٨-٢٩:** «الاعتذارُ ما ينتظر المتجرُ سائقاً —
> سيكون ملغيَّ الطلب».) **وكلُّ حالةٍ تقول اسمَها**، فلا يقف صاحبُ
> المتجر ينتظر سائقاً لطلبٍ اعتذر عنه.

**⚠ وماذا لو**

| الحال | 🖥 يرى | ⚙ يجري |
|---|---|---|
| حالٌ لا يعرفها التطبيق | **«أُعلن جاهزاً — بانتظار السائق»** | `else ->` — **افتراضٌ متفائلٌ قد يكذب** |
| الطلبُ تقدّم في المكتب | يتبدّل عند التحديث | الحالُ من المحرّك |

## المشهد ٦ — يُعلن جاهزيّته

**🖥 الواجهة**

الزرُّ يصير **«جاهز للاستلام»**، وبعد ضغطه: **«أُعلن جاهزاً — بانتظار
السائق»**.

**⚙ الخلفيّة** — `markReady(id)`.

**⚠ وماذا لو**

| الحال | 🖥 يرى | ⚙ يجري |
|---|---|---|
| أعلن جاهزاً | **«بانتظار السائق»** | والطلبُ ينزل للسائقين |
| **لا سائقَ متاحاً** | يبقى «بانتظار السائق» | **ولا حدَّ زمنيّاً ظاهراً** |
| ضغط قبل التحضير | الزرُّ غيرُ ظاهر | الأزرارُ تتبع الحال |

---

# الفصل الثالث · متجري

## المشهد ٧ — يفتح «متجري»

**🖥 الواجهة**

```
   متجري
   ┌ مفتوح الآن ────────── ●━━○ ┐
   ┌ وقتُ التحضير: ٢٠ دقيقة ────┐
   ┌ ساعاتُ الدوام ─────────────┐
   ┌ العنوان ونقطةُ المتجر ─────┐
   ┌ أقسامُ السوق ──────────────┐
```

**⚙ الخلفيّة** — `StoreViewModel` بتسعة أفعال: `load` · `setOpen` ·
`setPrepMinutes` · `rename` · `saveAddress` · `saveSections` ·
`saveHours` · `setPoint` · `emergency`.

## المشهد ٧·أ — مفتاحُ «مفتوح الآن»

**🖥 الواجهة** — مفتاحٌ يقول **«مفتوح»** أو **«مغلق»**.

**⚙ الخلفيّة** — `setOpen`.

**⚠ وماذا لو**

| الحال | 🖥 يرى | ⚙ يجري |
|---|---|---|
| يفتح دوامَه | يتبدّل فوراً | ويبدأ استقبالُ الطلبات |
| **حسابُه غيرُ موثَّق** | **«وثق حسابك أولا»** | `err_whatsapp_required` — **والدوامُ لا يُفتح بلا توثيق** |
| **خارجَ ساعات دوامه** | مغلقٌ ولو فتح المفتاح | **الوقتُ يحكم لا الزرّ** |

## المشهد ٧·ب — الإغلاقُ الطارئ

**🖥 الواجهة**

```
   إغلاق طارئ
   يُقرأ فوراً من الإدارة، ولا يُحسب رفضاً عليك
        [ أغلق الآن ]    إلغاء
```

**⭐ و«لا يُحسب رفضاً عليك»** هي كلُّ الفكرة: **صاحبُ المتجر الذي انقطع
عنه الغازُ يرفض الطلباتِ واحداً واحداً فتُعدّ عليه مخالفات** — حتّى
يُحظر. **والبابُ الطارئُ يُخرجه من ذلك بضغطةٍ ويُخبر الإدارةَ فوراً.**

**⚙ الخلفيّة** — `emergency` — ويُقرأ في لوحة العمليّات لحظتَه.

## المشهد ٧·ج — أقسامُ السوق

**🖥 الواجهة**

```
   أقسام السوق                    [ حفظ ]
   أضف الأقسام التي ينزل فيها متجرك — وعندها
   لن تظهر لك إلا هي عند إضافة صنف

   ⟨ لا قسم بعد — متجرك لا يظهر في السوق حتى تختار ⟩
                              [ أضف قسماً ]
```

**⭐ وهذا أخطرُ سطرٍ في الشاشة**: **«متجرك لا يظهر في السوق حتى تختار».**

**فمتجرٌ بلا أقسامٍ غيرُ موجودٍ للزبون** — لا مغلقاً ولا فارغاً، **بل
غيرُ ظاهرٍ أصلاً.** ولولا هذا النصُّ لَظنّ صاحبُه المنصّةَ معطوبة.

**⚙ الخلفيّة** — `saveSections` · `GET /merchant/platform-sections`.

**⚠ وماذا لو**

| الحال | 🖥 يرى | ⚙ يجري |
|---|---|---|
| لا قسمَ بعد | **«متجرك لا يظهر في السوق حتى تختار»** | ولا يظهر في تصفّح الزبون |
| **أضاف كلَّ الأقسام** | **«أضفت كل الأقسام»** | ولا زرَّ إضافةٍ بعدها |
| حذف قسماً فيه أصناف | الأصنافُ تبقى | **وتختفي من السوق** |
| **بعد الاختيار** | **لا تظهر له إلّا أقسامُه عند إضافة صنف** | فلا يضع شاورما في «ألبسة» |

## المشهد ٧·د — وقتُ التحضير وساعاتُ الدوام

**🖥 الواجهة**

```
   وقت التحضير:  [ ٢٠ ] دقيقة

   ساعات الدوام                   [ حفظ ]
   الأحد    ٠٩:٠٠ — ٢٣:٠٠
   الاثنين  ٠٩:٠٠ — ٢٣:٠٠
   ...
   الجمعة   مغلق
```

**⚙ الخلفيّة** — `setPrepMinutes` · `saveHours` · وأيّامٌ سبعةٌ بأسمائها
(`days_sun` … `days_sat`).

> **⚠ وعطبٌ كان يُغلق التطبيق** — `createConfigurationContext` بلا
> نافذةٍ يرمي `BadTokenException` عند فتح مُنتقي الوقت. **فيُغلق
> التطبيقُ في وجه صاحبه وهو يضبط دوامَه.** (بلاغُ المالك ٢٠٢٦-٠٨-٢٩:
> «عندما أعدّل ساعات العمل يُغلق التطبيق»، وأُصلح بـ`ContextThemeWrapper`
> وبثلاثة `runCatching` — **فغلافُ لغةٍ تجميليٌّ لا يجوز أن يُسقط
> شاشة.**)

**⚠ وماذا لو**

| الحال | 🖥 يرى | ⚙ يجري |
|---|---|---|
| **الأرقامُ عربيّةٌ في المُنتقي** | كانت تُرفض ساعاتُه | **`String.format(Locale.ROOT)`** — والتنسيقُ باللغة يكتب ٠٩ بأرقامٍ هنديّة |
| يومٌ مغلق | **«مغلق»** | ولا ساعاتٍ له |
| بدايةٌ بعد نهاية | يُرفض | — |

## المشهد ٧·هـ — العنوانُ والنقطةُ والاسم

**🖥 الواجهة** — حقلُ العنوان · و**«نقطة المتجر»** فيها **«لم يُحدَّد
بعد»** وزرُّ **«حدّد»** · و**«تعديل اسم المتجر»**.

**⚙ الخلفيّة** — `saveAddress` · `setPoint` · `rename`.

**⭐ ونقطةٌ لم تُحدَّد تُقال صراحةً** — **ومتجرٌ بلا دبّوسٍ يستقبل
طلباتٍ ولا يعرف السائقُ مسافتَه** (وقع فعلاً في الطلب `#1003`،
والمحرّكُ يردّ `-1` أي «لا يُعرف»).

## المشهد ٧·و — وأرقامُ يومه

**🖥 الواجهة**

```
   طلبات اليوم  ١٢   ·   سُلّم  ١٠   ·   ملغي  ٢
   مستحق اليوم           ٧٥٦٠٠٠ ل.س
```

**⚙ الخلفيّة** — تُقرأ مع `load`، **و«مستحق اليوم» لا «مبيعات اليوم»**:
الفرقُ عمولةُ المنصّة، **ورقمٌ يُقرأ على أنّه له وهو ليس له يُفسد الثقة
في أوّل تحاسب.**

---

# الفصل الرابع · الأصناف

## المشهد ٨ — «أصنافي»

**🖥 الواجهة**

```
   أصنافي
   أقسام السوق التي فيها أصنافك

        [   أضف صنفاً   ]

   ┌ [صورة] برغر            ٣ ┐
   ┌ [صورة] فروج وبروستد    ١ ┐
```

**ثمّ يدخل في قسمٍ فيرى:**

```
   ‹ كل الأقسام
   برغر
   اضغط الصنف لتبدّل توفره

   [صورة]  مشوي              ●━━○
           ٢٥٠ ل.س ← ٣٠٠ ل.س  متوفر
           ⟨متوفر⟩ ⟨خارج الدوام الآن⟩
```

**⚙ الخلفيّة** — `openGroup` · `back` · `toggle`.

**⭐ والتقسيمُ بأقسام السوق لا بأقسام المتجر** — **هو ما يراه الزبون،
وهو ما يجب أن يراه من يملأ القائمة.**

**⭐ و«٢٥٠ ← ٣٠٠»**: سعرُه ثمّ سعرُ البيع. **والفرقُ هامشُ المنصّة
يُحسب عند العرض ولا يُخزَّن** — **فتغييرُ الهامش في اللوحة يظهر فوراً
بلا إعادة حسابِ ألف صنف.**

> **وكان تحت الحقل: «ما تقبضه أنت — والمنصّة تضيف هامشها فوقه»**
> (`item_price_hint`). **رُفع بقرار المالك ٢٠٢٦-٠٨-٣٠**: «ملاحظةُ ما
> تقبضه أنت والمنصّة تضيف هامشها فوقه غيرُ ضروريّة». **والمفتاحُ باقٍ
> ميّتاً في الملفّ.**

**⚠ وماذا لو**

| الحال | 🖥 يرى | ⚙ يجري |
|---|---|---|
| لا أصناف | **«لا أصناف بعد — تُضاف من لوحة الإدارة أو من مندوبك»** | **ويقول من يضيفها** — فلا يقف ينتظر |
| **صنفٌ ينتظر المراجعة** | **«بانتظار المراجعة»** | `menu_requires_approval` مرفوع |
| **المتجرُ خارجَ دوامه** | «خارج الدوام الآن» | والصنفُ متوفّرٌ ولا يُطلب |
| **صنفٌ بلا قسم** | في «غير مصنّف» | **ولا يظهر للزبون** |
| ضغطُ الصنف | **يتبدّل توفّرُه** | `toggle` — **والتلميحُ يقوله فوق القائمة** |

## المشهد ٩ — يضيف صنفاً أو يعدّله

**🖥 الواجهة** — **النموذجُ هو نموذجُ المندوب نفسُه**
(`ui.menu.ItemEditor`) — وُحِّدا ٢٠٢٦-٠٨-٣٠، **و٥١٨ سطراً حُذفت.**

**⚙ الخلفيّة** — `newItem` · `editItem` · `editDraft` · `saveItem` ·
`deleteItem` · `cancelEdit` · `pickImage` · `clearImage`.

**⭐ ولفظُ السعر يختلف بالجمهور**: المتجرُ يقرأ **«السعر»** عامّاً
(قرارُ المالك: «لا شراءَ ولا مبيع، أفضلُ وأعمّ»)، **والمندوبُ يقرأ «سعر
المتجر»** لأنّه يكتب سعرَ متجرِ غيره. **والمحرّرُ واحدٌ واللفظُ
يُمرَّر.**

**⭐ ولا مفتاحَ توفّرٍ داخلَ النموذج** — يُقلب من السطر وحدَه.
**وحقلٌ يُكتب من موضعين يمحو أحدُهما ما فعله الآخر**: من أوقف صنفاً
ثمّ عدّل اسمَه أعاده متوفّراً.

**⚠ وماذا لو** — كما في المندوب حرفاً: اسمٌ فارغٌ يقول «اكتب اسم
الصنف» · سعرٌ بحروفٍ يقول «اكتب سعراً صحيحاً» · **وبلا قسمٍ يُنبَّه ولا
يُمنع** · والحذفُ يسأل «يُحذف من السوق ولا يعود — أمتأكد؟».

---

# الفصل الخامس · السجلّ والبلاغ

## المشهد ٩ — سجلُّ الطلبات

**🖥 الواجهة** — طلباتٌ مضت بحالاتها: **سُلّم · أُلغي · اعتُذر عنه ·
تعذّر تسليمه · استُرجع.**

**⚙ الخلفيّة** — `HistoryScreen` وفيه **زرُّ البلاغ**:

```kotlin
if (vm.reportable.contains(o.id)) { /* أبلغ */ }
```

**⭐ والبلاغُ بعد انتهاء الطلب لا في أثنائه** — (طلبُ المالك ٢٠٢٦-٠٨-٣٠:
«لازم نضيف البلاغ مع الطلب مشان يرفع بلاغاً ضدّ سائق»). **ومن أبلغ
والطلبُ جارٍ أبلغ عمّا لم يقع بعد.**

**⚠ وماذا لو**

| الحال | 🖥 يرى | ⚙ يجري |
|---|---|---|
| طلبٌ قابلٌ للبلاغ | زرُّ «أبلغ» | `reportable` من المحرّك |
| **مضت المهلة** | لا زرّ | `err_complaint_window` |
| **له بلاغٌ مفتوح** | «لك بلاغ مفتوح على هذا الطلب» | ولا يُكرَّر |
| بلا سبب | يُطلب سبب | `err_bad_fail_reason` |

## المشهد ١٠ — يُبلّغ عن سائق

**🖥 الواجهة**

```
   إبلاغ عن سائق الطلب #١٠٤٥
   يصل الإدارةَ ولا يصل السائق

   ○ تأخّر كثيراً
   ○ تعامل بسوء
   ○ لم يحضر
   ○ أخذ الطلب ولم يسلّمه

   ┌─ ملاحظة (اختياريّة) ──────┐

        [   إرسال   ]
```

**⚙ الخلفيّة**

```
GET  /api/v1/merchant/report-reasons     أربعةُ أسباب
POST /api/v1/merchant/orders/{id}/report
```

**⭐ و«يصل الإدارةَ ولا يصل السائق»** — يُكتب صراحةً. **ومن ظنّ بلاغَه
يصل من شكا منه لا يشكو**، ويبقى العطبُ في المنصّة بلا خبر.

**⭐ والأسبابُ أربعةٌ يردّها المحرّكُ ولا تُكتب في الشاشة** — **والمحرّكُ
يرفض ما سواها.** فسببٌ يُضاف في اللوحة يظهر في التطبيق بلا نشرة.

**⚠ وماذا لو**

| الحال | 🖥 يرى | ⚙ يجري |
|---|---|---|
| طلبٌ انتهى ويقبل البلاغ | زرُّ **«إبلاغ»** | `reportable` من المحرّك |
| **مضت المهلة** | لا زرّ | `err_complaint_window` |
| **له بلاغٌ مفتوحٌ عليه** | «لك بلاغ مفتوح على هذا الطلب» | ولا يُكرَّر |
| بلا سبب | يُطلب سبب | `err_bad_fail_reason` |
| **الطلبُ لم يُغلق بعد** | لا زرّ | `err_order_not_closed` |

## المشهد ١١ — يقرأ ما أرسله

**🖥 الواجهة**

```
   بلاغاتي
   ما أرسلتَه وما صار به

   على الطلب #١٠٤٥        ⟨مفتوحة⟩
   على الطلب #١٠٣١        ⟨محلولة⟩
```

**⚙ الخلفيّة** — `GET /api/v1/merchant/my-reports`

**⭐ وليس `/my/tickets`** — والفرقُ خطير:

> **`/my/tickets` يُفلتر بـ`customer_id`، وبلاغُ المتجر يحمل في تلك
> الخانة زبونَ الطلب.** **فلو رُبط به لَرأى صاحبُ المتجر ما شُكي به
> عليه.**

**والبابُ الصحيحُ يفلتر بالمتجر** — فيرى ما أرسله لا ما أُرسل ضدَّه.

## المشهد ١٢ — وما عليه هو

**🖥 الواجهة**

```
   الإنذارات
   ما على متجرك — يُقرأ قبل أن يُحظر
```

**⚙ الخلفيّة** — `GET /api/v1/merchant/warnings`

**⭐ «يُقرأ قبل أن يُحظر»** — والحظرُ يقع بعدد المخالفات في نافذةٍ
زمنيّة. **ومن حُظر ولم يكن يرى إنذاراتِه يظنّ الحظرَ تحكّماً** — وهو
عدٌّ معلومٌ كان يستطيع أن يقرأه.

**⚠ وماذا لو**

| الحال | 🖥 يرى | ⚙ يجري |
|---|---|---|
| لا إنذارات | فراغٌ مفهوم | — |
| **بلغ العتبة** | إنذاراتُه مقروءةً | `merchants.ban_days` نافذةُ العدّ |
| **حُظر** | «هذا الحساب محظور» عند الدخول | `err_user_blocked` |

## المشهد ١١·ب — وشكاوى عليه هو

**🖥 الواجهة**

```
   الشكاوي والبلاغات
   ┌ شكاوى رفعتها ┐ ┌ شكاوى عليّ ┐

   لا شكاوى على متجرك — أحسنت
```

**⚙ الخلفيّة** — `GET /merchant/stores/{id}/reports-against`

**⭐ وتبويبان لا قائمةٌ واحدة**: **ما أرسله وما أُرسل ضدَّه** — **وخلطُهما
يجعله يقرأ شكوى عليه على أنّها شكواه.**

**⭐ و«لا شكاوى على متجرك — أحسنت»** — **والفراغُ يُمدح لا يُترك
صامتاً.**

**وحالُ كلّ شكوى بأربع كلمات:** «قيد النظر» · «فُصل فيه» · «رُدّ» ·
«أُغلق». **ومن رفع شكوى ولم يعرف مصيرَها لا يرفع الثانية.**

## المشهد ١١·ج — وأسبابُ البلاغ أربعة

| السبب | نصُّه |
|---|---|
| `driver_late` | السائق تأخّر بالاستلام |
| `driver_conduct` | سلوك السائق |
| `driver_refused` | السائق رفض أخذ الطلب |
| `other` | سبب آخر |

**⭐ وثلاثةٌ منها عن السائق والرابعُ مفتوح** — **والمحرّكُ يرفض ما
سواها**، فسببٌ يُضاف في اللوحة يظهر في التطبيق بلا نشرة.

## المشهد ١٣ — تقاريرُ متجره

**🖥 الواجهة**

```
   طلبات اليوم        ١٢
   سُلّم               ١٠
   ملغي                ٢
   ─────────────────────
   المبيعات      ٨٤٠٠٠٠
   عمولة المنصّة  ٨٤٠٠٠
   مستحقك        ٧٥٦٠٠٠
```

**⚙ الخلفيّة** — **ثلاثةُ أرقامٍ لا رقمان**: المبيعاتُ والعمولةُ
والمستحقّ. **ومن عرض المبيعاتِ وحدَها أوهم صاحبَه أنّها له.**

---

# الفصل السادس · التقاريرُ والقائمةُ الجانبيّة

## المشهد ١٤ — «التقارير»

**🖥 الواجهة**

```
   التقارير
   مبيعاتك على مدًى تختاره

   [ ٧ أيّام ] [ ٣٠ يوماً ] [ مدة محددة ]   [ طباعة التقرير ]

   طلبات اليوم  ١٢  ·  سُلّم ١٠  ·  ملغي ٢
   ──────────────────────────────────────
   المبيعات      ٨٤٠٠٠٠
   عمولة المنصّة  ٨٤٠٠٠
   مستحقك       ٧٥٦٠٠٠
```

**⚙ الخلفيّة** — `GET /merchant/stores/{id}/reports` بمدىً يُختار.

**⭐ وثلاثةُ أرقامٍ لا رقمان**: **المبيعاتُ والعمولةُ والمستحقّ.**
**ومن عرض المبيعاتِ وحدَها أوهم صاحبَه أنّها له**، ثمّ فوجئ بالفرق في
أوّل تحاسب.

**⚠ وماذا لو**

| الحال | 🖥 يرى | ⚙ يجري |
|---|---|---|
| مدىً بلا طلبات | أصفاراً مفهومة | — |
| «مدة محددة» | ورقةَ اختيارِ مدى | `sheet_period` |
| **طباعة** | ورقةَ المنصّة نفسَها | `ui.DocPrint` — **واللوغو بايتاتٌ لا عنوان** |

## المشهد ١٥ — القائمةُ الجانبيّة

**🖥 الواجهة**

```
   ☰
   سجل الطلبات
   التقارير
   الشكاوي والبلاغات
   الإنذارات
   ──────────────
   التعليمات · من نحن · تواصل معنا
   شروط الاستخدام · سياسة الخصوصية
   ──────────────
   السمة الغامقة
   خروج
```

**⚙ الخلفيّة** — `Drawer` المشترك من `:ui`، وبنودُ المتجر تُضاف إليه.

**⭐ وأربعةُ بنودٍ تخصّه**: سجلُّه · تقاريرُه · **شكاواه** ·
**وإنذاراتُه** — **وهي التي لم تكن في الوثيقة الأولى إطلاقاً.**

## المشهد ١٦ — الإشعارات

**⚙ الخلفيّة**

```kotlin
override fun isUrgent(kind: String) = kind == KIND_ORDER   // "order"
```

**⭐ والعاجلُ محجوزٌ لطلبٍ جديدٍ وحدَه** — يوقظ الجهاز.

**وما عداه خبرٌ صامت**: إنذارٌ · جوابُ شكوى · حركةُ محفظة.

**ومن جعل كلَّ شيءٍ عاجلاً أطفأ صاحبُه الإشعاراتِ كلَّها — فخسر
العاجلَ معها.** **وطلبٌ لا يُسمع إشعارُه طلبٌ يبرد.**

---

# ملحق أ · اثنا عشرَ مفتاحاً ميّتاً

**قِيس ٢٠٢٦-٠٨-٣١**: ١١١ مفتاحاً حيّاً و**١٢ لا تقرؤها شاشة.**

| المفتاح | نصُّه | ماذا يعني موتُه |
|---|---|---|
| `order_due` | «مستحقك من هذا الطلب» | **يُعرض `ord_net` بدله** — «لك %s» |
| `order_report` | «أبلغ عن مشكلة» | البلاغُ في السجلّ بنصٍّ آخر |
| `order_seconds_left` | «%d ثانية للقبول» | **عدّادُ قبولٍ تلقائيٍّ لم يُبنَ في الشاشة** |
| `ord_since` · `ord_min` · `ord_hr` | «منذ %s» · «د» · «س» | **عمرُ الطلب لا يُعرض** |
| `store_sections_pick` · `ok_sections_saved` | | بقايا صياغةٍ سابقة |
| `login_hint` · `item_price_hint` · `wallet_title` · `retry` | | نُقلت إلى `:ui` |

**ومفتاحٌ ميّتٌ ليس فوضىً فقط**: **من عدّل نصَّه ظنّ أنّه عدّل ما
يراه المستخدم — ولا يتبدّل شيء.**

**والثلاثةُ الأولى تدلّ على ميزاتٍ نُويت ولم تُبنَ** — لا على نصوصٍ
زائدة. **وهي أوّلُ ما يُسأل عنه المالك.**

---

# ملحق ب · العقد

> **كلُّ بندٍ جملةٌ تُفحص** — `✔` يحصل · `✗` لا يحصل · `؟` لم يُجرَّب.
> **ويُملأ بالتشغيل لا باليد.**

## أ · الإقلاع والدخول

| # | يجب أن يحصل | الحال |
|---|---|---|
| أ-١ | يُفتح في أقلَّ من ثانيتين | ؟ |
| أ-٢ | نافذةُ أذونٍ واحدة | ؟ |
| أ-٣ | «تسجيل الدخول» بلا ذكر الدور | ؟ |
| أ-٤ | لا بابَ لإنشاء حساب | ؟ |
| أ-٥ | الجلسةُ المحفوظةُ تدخل مباشرةً | ؟ |
| أ-٦ | **شاشةُ التحديث تظهر عند ٤٢٦** | ؟ |
| أ-٧ | انقطاعُ الشبكة → شريط | ؟ |

## ب · الطلبات

| # | يجب أن يحصل | الحال |
|---|---|---|
| ب-١ | الطلباتُ أوّلُ تبويب | ؟ |
| ب-٢ | **فراغٌ يعد: «سنُعلمك حين يصل طلب»** | ؟ |
| ب-٣ | **وضعُ المتاجر مطفأ → لا تبويبَ للطلبات** | ؟ |
| ب-٤ | «استلمت الطلب» ينقل إلى التحضير آليّاً | ؟ |
| ب-٥ | «رفض» يطلب سبباً | ؟ |
| ب-٦ | «جاهز» ينزل الطلبَ للسائقين | ؟ |
| ب-٧ | **«لك %s» مستحقُّه لا قيمةُ الطلب** | ؟ |
| ب-٨ | ضغطتان → فعلٌ واحد | ؟ |
| ب-٩ | **إشعارُ طلبٍ جديدٍ يوقظ** | ؟ |
| ب-١٠ | ملاحظةُ الزبون تُعرض | ؟ |

## ج · متجري

| # | يجب أن يحصل | الحال |
|---|---|---|
| ج-١ | قلبُ «مفتوح» يُحفظ فوراً | ؟ |
| ج-٢ | **ساعاتُ الدوام لا تُغلق التطبيق** | ؟ |
| ج-٣ | وقتُ التحضير يُحفظ | ؟ |
| ج-٤ | العنوانُ والنقطة | ؟ |
| ج-٥ | **أقسامُ السوق تُختار ولا تُنشأ** | ؟ |
| ج-٦ | الإغلاقُ الطارئ يوقف الطلبات | ؟ |
| ج-٧ | **غيرُ الموثَّق لا يفتح دوامَه** | ؟ |

## د · الأصناف

| # | يجب أن يحصل | الحال |
|---|---|---|
| د-١ | شبكةُ أقسامٍ ثمّ دخول | ؟ |
| د-٢ | **«السعر» عامّاً لا «سعر المتجر»** | ؟ |
| د-٣ | مفتاحُ التوفّر يقول الحال | ؟ |
| د-٤ | اسمٌ فارغٌ يقول لماذا | ؟ |
| د-٥ | سعرٌ بحروفٍ يقول لماذا | ؟ |
| د-٦ | بلا قسمٍ يُنبَّه ولا يُمنع | ؟ |
| د-٧ | الحذفُ يسأل قبله | ؟ |
| د-٨ | الصورةُ تظهر فورَ رفعها | ؟ |
| د-٩ | المُعدِّلاتُ إضافةً وحذفاً | ؟ |

## هـ · السجلّ والبلاغ

| # | يجب أن يحصل | الحال |
|---|---|---|
| هـ-١ | السجلُّ يعرض الحالاتِ الخمس | ؟ |
| هـ-٢ | **البلاغُ بعد الانتهاء لا في أثنائه** | ؟ |
| هـ-٣ | بلاغٌ مفتوحٌ لا يُكرَّر | ؟ |
| هـ-٤ | مضتِ المهلةُ → لا زرّ | ؟ |

## و · المال

| # | يجب أن يحصل | الحال |
|---|---|---|
| و-١ | المحفظةُ = الدفتر | ؟ |
| و-٢ | مستحقُّه يُقيَّد عند التسليم | ؟ |
| و-٣ | **الاسترجاعُ يعكسه** | ؟ |
| و-٤ | لا يرى مالَ غيره | ؟ |

---

**العقد: ٤١ بنداً · وكلُّها `؟` — لم يُنفَّذ الاختبارُ بعد.**

---

# ملحق ج · فهرسُ النصوص — 123 مفتاحاً

> **كلُّ كلمةٍ يراها صاحبُ المتجر ومكانُها.** ومن أراد تبديلَ لفظٍ
> عرف أينَ يبدّله، **ومن أراد اختبارَ شاشةٍ عرف ما ينتظر أن يقرأ.**
>
> **و`ميت` تعني: مكتوبٌ ولا تقرؤه شاشة.**

## أخرى

| المفتاح | النصّ | |
|---|---|---|
| `app_name` | رحال غو — المتجر |  |
| `tab_store` | متجري |  |
| `tab_account` | حسابي |  |
| `menu_title` | أصنافي |  |
| `menu_hint` | اضغط الصنف لتبدّل توفره |  |
| `menu_sections_hint` | أقسام السوق التي فيها أصنافك |  |
| `menu_back` | ‏‹ كل الأقسام |  |
| `menu_empty` | لا أصناف بعد — تُضاف من لوحة الإدارة أو من مندوبك |  |
| `tab_add` | أضف صنفاً |  |
| `menu_warnings` | الإنذارات |  |
| `menu_reports` | الشكاوي والبلاغات |  |
| `menu_sales` | التقارير |  |
| `sales_hint` | مبيعاتك على مدًى تختاره |  |
| `sales_week` | ٧ أيّام |  |
| `sales_month` | ٣٠ يوماً |  |
| `history_hint` | ما انتهى أمره — يُفتح عند سؤال |  |
| `history_empty` | لا طلبات في السجل بعد |  |
| `wallet_title` | محفظتي | **ميت** |
| `retry` | أعد المحاولة | **ميت** |
| `days_sun` | الأحد |  |
| `days_mon` | الاثنين |  |
| `days_tue` | الثلاثاء |  |
| `days_wed` | الأربعاء |  |
| `days_thu` | الخميس |  |
| `days_fri` | الجمعة |  |
| `days_sat` | السبت |  |
| `sales_custom` | مدة محددة |  |
| `sales_print` | طباعة التقرير |  |
| `no_store` | لا متجر مرتبط بحسابك — راجع الإدارة |  |
| `v_item_name` | اكتب اسم الصنف |  |
| `v_item_price` | اكتب سعر الشراء |  |
| `v_item_section` | اختر قسم السوق — وصنف بلا قسم لا يراه زبون |  |
| `v_store_name` | اكتب اسم المتجر |  |
| `rs_driver_late` | السائق تأخّر بالاستلام |  |
| `rs_driver_refused` | السائق رفض أخذ الطلب |  |
| `tk_open` | قيد النظر |  |
| `tk_resolved` | فُصل فيه |  |
| `tk_rejected` | رُدّ |  |
| `tk_closed` | أُغلق |  |
| `need_sections_title` | اختر أقسام متجرك أولاً |  |
| `need_sections_body` | الصنف ينزل في قسم من أقسام السوق — واختيار الأقسام يمنع أن ينزل صنفك في المكان الخطأ فلا يجده الزبون |  |
| `need_sections_go` | اذهب إلى متجري |  |

## الدخول

| المفتاح | النصّ | |
|---|---|---|
| `login_hint` | ادخل برقمك وكلمة المرور | **ميت** |

## الطلبات

| المفتاح | النصّ | |
|---|---|---|
| `orders_hint` | اقبل الطلب ثم حضّره ثم أعلن جاهزيته |  |
| `orders_empty` | لا طلبات الآن — سنُعلمك حين يصل طلب |  |
| `order_accept` | استلمت الطلب |  |
| `order_reject` | رفض — غير متوفر |  |
| `order_start` | بدأت التحضير |  |
| `order_ready` | جاهز للاستلام |  |
| `order_ready_done` | أُعلن جاهزاً — بانتظار السائق |  |
| `order_note` | ملاحظة الزبون |  |
| `order_due` | مستحقك من هذا الطلب | **ميت** |
| `order_seconds_left` | %1$d ثانية للقبول | **ميت** |
| `order_report` | أبلغ عن مشكلة | **ميت** |
| `order_reject_note` | الصنف غير متوفر حالياً |  |
| `ord_since` | منذ %1$s | **ميت** |
| `ord_min` | د | **ميت** |
| `ord_hr` | س | **ميت** |
| `ord_net` | لك %1$s |  |

## الأصناف

| المفتاح | النصّ | |
|---|---|---|
| `item_add` | أضف صنفاً |  |
| `item_price_hint` | ما تقبضه أنت — والمنصة تضيف هامشها فوقه | **ميت** |

## متجري

| المفتاح | النصّ | |
|---|---|---|
| `store_title` | متجري |  |
| `store_open` | مفتوح |  |
| `store_closed` | مغلق |  |
| `store_prep` | مدة التحضير الافتراضية |  |
| `store_prep_unit` | دقيقة |  |
| `store_hours` | ساعات العمل |  |
| `store_day_closed` | مغلق |  |
| `store_address_hint` | الشارع والعلامة التي يعرفها الناس |  |
| `store_point` | موقع المتجر على الخريطة |  |
| `store_point_none` | لم يُحدَّد بعد |  |
| `store_point_pick` | حدّد |  |
| `store_no_warnings` | لا إنذارات عليك |  |
| `store_emergency` | إغلاق طارئ |  |
| `store_emergency_do` | أغلق الآن |  |
| `store_emergency_hint` | يُقرأ فوراً من الإدارة، ولا يُحسب رفضاً عليك |  |
| `store_rename` | تعديل اسم المتجر |  |
| `store_sections` | أقسام السوق |  |
| `store_sections_hint` | أضف الأقسام التي ينزل فيها متجرك — وعندها لن تظهر لك إلا هي عند إضافة صنف |  |
| `store_sections_empty` | لا قسم بعد — متجرك لا يظهر في السوق حتى تختار |  |
| `store_sections_add` | أضف قسماً |  |
| `store_sections_pick` | اختر من الأقسام | **ميت** |
| `store_sections_all_in` | أضفت كل الأقسام |  |

## الإنذارات

| المفتاح | النصّ | |
|---|---|---|
| `warnings_hint` | ما على متجرك — يُقرأ قبل أن يُحظر |  |

## البلاغات

| المفتاح | النصّ | |
|---|---|---|
| `reports_list_hint` | ما أرسلتَه وما صار به |  |
| `reports_list_empty` | لم ترسل بلاغاً بعد |  |
| `reports_on_order` | على الطلب #%1$d |  |
| `reports_sales` | المبيعات |  |
| `reports_commission` | عمولة المنصّة |  |
| `reports_due` | مستحقك |  |
| `report_driver` | إبلاغ |  |
| `report_title` | إبلاغ عن سائق الطلب #%1$d |  |
| `report_hint` | يصل الإدارةَ ولا يصل السائق |  |
| `report_note` | ملاحظة (اختياريّة) |  |
| `reports_today_orders` | طلبات اليوم |  |
| `reports_delivered` | سُلّم |  |
| `reports_cancelled` | ملغي |  |
| `reports_due_today` | مستحق اليوم |  |
| `reports_mine` | شكاوى رفعتها |  |
| `reports_against` | شكاوى عليّ |  |
| `reports_against_empty` | لا شكاوى على متجرك — أحسنت |  |

## الإشعارات

| المفتاح | النصّ | |
|---|---|---|
| `push_ch_new_order` | طلبات جديدة |  |

## السجلّ

| المفتاح | النصّ | |
|---|---|---|
| `hist_money` | بِيع %1$s · عمولة المنصة %2$s (%3$d٪) · لك %4$s |  |

## رسائلُ النجاح

| المفتاح | النصّ | |
|---|---|---|
| `ok_address_saved` | حُفظ عنوانك |  |
| `ok_sections_saved` | حُفظت الأقسام | **ميت** |
| `ok_store_closed` | أُغلق متجرك — والإدارة تعلم |  |
| `ok_report_sent` | وصل بلاغُك — والإدارة تنظر فيه |  |

## حالاتُ الطلب

| المفتاح | النصّ | |
|---|---|---|
| `st_delivered` | سُلّم |  |
| `st_cancelled` | أُلغي |  |
| `st_rejected` | اعتُذر عنه |  |
| `st_failed` | تعذّر تسليمه |  |
| `st_refunded` | استُرجع |  |
| `os_dispatching` | بانتظار سائق |  |
| `os_assigned` | أُسند لسائق |  |
| `os_on_way` | في الطريق |  |
| `os_at_pickup` | السائق عندك |  |
| `os_picked_up` | استلمه السائق |  |
| `os_at_dropoff` | وصل الزبون |  |
| `os_rejected` | اعتذرت عنه |  |
| `os_cancelled` | أُلغي |  |
| `os_failed` | تعذّر تسليمه |  |
| `os_refunded` | استُرجع |  |
| `os_delivered` | سُلّم |  |

---

# ملحق د · فهرسُ الدوالّ

> **كلُّ فعلٍ يفعله صاحبُ المتجر أو تفعله الشاشةُ عنه.**

**`Backend.kt`** — `of`

**`drawer/DrawerScreens.kt`** — `load` · `WarningsScreen` · `switch` · `MyReportsScreen` · `pick` · `setCustom` · `SalesScreen` · `reasonAr` · `ticketStatusAr` · `day` · `pickRange` · `printReport`

**`MainActivity.kt`** — `MerchantApp` · `SignedIn` · `RowScope` · `NeedSections`

**`menu/MenuScreen.kt`** — `MenuScreen` · `Grid` · `ItemRow`

**`menu/MenuViewModel.kt`** — `openGroup` · `back` · `load` · `regroup` · `toggle` · `newItem` · `editItem` · `editDraft` · `cancelEdit` · `pickImage` · `clearImage` · `saveItem` · `deleteItem`

**`Menu.kt`** — `noStoreMsg`

**`orders/HistoryScreen.kt`** — `load` · `loadReasons` · `report` · `HistoryScreen` · `ReportSheet` · `statusAr`

**`orders/OrdersScreen.kt`** — `OrdersScreen` · `OrderCard` · `NextAction` · `Line` · `statusLabel`

**`orders/OrdersViewModel.kt`** — `busy` · `refresh` · `accept` · `startPreparing` · `markReady` · `reject` · `act` · `prefs` · `lastKnownSelfManage` · `rememberSelfManage`

**`store/StoreScreen.kt`** — `StoreScreen` · `SectionsEditor` · `HoursEditor` · `HoursRow` · `TimeField` · `AddressEditor` · `latinDigits`

**`store/StoreViewModel.kt`** — `setPoint` · `load` · `setOpen` · `setPrepMinutes` · `rename` · `saveAddress` · `saveSections` · `saveHours` · `emergency`

**المجموع: 71 دالّة.**

## دوالُّ النظام — تجاوزاتٌ لا يستدعيها المستخدم

**`MerchantApplication`** — `onCreate` · `onLowMemory` · `onTrimMemory`
  **وضيقُ الذاكرة يُفرغ ذاكرةَ الخريطة والصور** فلا يُقتل التطبيق.

**`MainActivity`** — `onCreate`

**`Backend`** — `of` · `home` · `appName` · `icon`
  **وهويّةُ التطبيق من المصدر لا من اسمٍ على الجهاز.**

**`push/PushService`** — `isUrgent` · `newsChannelName` · `urgentChannelName`
  **والعاجلُ محجوزٌ لطلبٍ جديد** — ومن جعل كلَّ شيءٍ عاجلاً أطفأ
  صاحبُه الإشعاراتِ كلَّها فخسر العاجلَ معها.

---

# ملحق هـ · فهرسُ نقاط المحرّك

> **ما يناديه التطبيقُ حرفاً** — ومن قرأ سطراً هنا عرف أين يفحصه.

| الفعل | الطريقة | النقطة |
|---|---|---|
| `stores` | GET | `/api/v1/merchant/stores` |
| `warnings` | GET | `/api/v1/merchant/warnings` |
| `orders` | GET | `/api/v1/merchant/stores/$storeId/orders` |
| `history` | GET | `/api/v1/merchant/stores/$storeId/orders?closed_only=true&page=$page` |
| `order` | GET | `/api/v1/merchant/orders/$orderId` |
| `transition` | POST | `/api/v1/merchant/orders/$orderId/transition` |
| `markReady` | POST | `/api/v1/merchant/orders/$orderId/ready` |
| `menu` | GET | `/api/v1/merchant/stores/$storeId/menu` |
| `setAvailability` | PATCH | `/api/v1/merchant/menu/items/$itemId/availability` |
| `platformSections` | GET | `/api/v1/merchant/platform-sections` |
| `storeSections` | GET | `/api/v1/merchant/stores/$storeId/sections` |
| `setStoreSections` | PUT | `/api/v1/merchant/stores/$storeId/sections` |
| `createItem` | POST | `/api/v1/merchant/stores/$storeId/menu/items` |
| `updateItem` | PATCH | `/api/v1/merchant/menu/items/$itemId` |
| `deleteItem` | DELETE | `/api/v1/merchant/menu/items/$itemId` |
| `createSection` | POST | `/api/v1/merchant/stores/$storeId/menu/sections` |
| `hours` | GET | `/api/v1/merchant/stores/$storeId/hours` |
| `setHours` | PUT | `/api/v1/merchant/stores/$storeId/hours` |
| `settings` | PATCH | `/api/v1/merchant/stores/$storeId/settings` |
| `setClosed` | POST | `/api/v1/merchant/stores/$storeId/emergency` |
| `wallet` | GET | `/api/v1/me/wallet` |
| `reportReasons` | GET | `/api/v1/merchant/report-reasons` |
| `reportDriver` | POST | `/api/v1/merchant/orders/$orderId/report` |
| `reportsAgainst` | GET | `/api/v1/merchant/stores/$storeId/reports-against` |
| `myReports` | GET | `/api/v1/merchant/my-reports` |

**المجموع: 25 نقطة.**

## ورفعُ الصورة خارجَ الفهرس

`POST /api/v1/merchant/media` — **ليست في الجدول أعلاه لأنّها لا تُنادى
بـ`api.call`**: ترفع ملفّاً (`multipart`) لا JSON، **فلها مسارُها.**

**وتردّ `{ id, thumb_url }` — والاثنان يُستعملان.** **والمصغّرةُ تُعاد
ولا تُرمى**: وإلّا رُسم الحرفُ الاحتياطيُّ بعد رفعٍ ناجح **فيُظنّ أنّ
الصورةَ لم تُحفظ** (بلاغُ المالك ٢٠٢٦-٠٨-٢٩).

---

# ملحق و · بنودٌ أُضيفت بعد الجولات الثلاث

**والعقدُ أعلاه كُتب قبل أن تُقرأ الشيفرةُ كلُّها** — وهذه بنودٌ لم
تكن معروفةً يومَها:

| # | يجب أن يحصل | الحال |
|---|---|---|
| ز-١ | **«متجرك لا يظهر في السوق حتى تختار»** يُقال لمن لا قسمَ له | ؟ |
| ز-٢ | **«أضفت كل الأقسام»** حين تنتهي | ؟ |
| ز-٣ | **الإغلاقُ الطارئ «لا يُحسب رفضاً عليك»** | ؟ |
| ز-٤ | **إحدى عشرةَ حالةً لرحلة الطلب** — كلٌّ باسمها | ؟ |
| ز-٥ | **«اعتذرت عنه» بصيغة الفاعل لا «رُفض»** | ؟ |
| ز-٦ | **حالٌ مجهولةٌ لا تُعرض «بانتظار السائق»** كذباً | ؟ |
| ز-٧ | **«شكاوى عليّ» و«شكاوى رفعتها» تبويبان لا قائمة** | ؟ |
| ز-٨ | **«لا شكاوى على متجرك — أحسنت»** | ؟ |
| ز-٩ | حالُ كلّ شكوى بأربع كلمات | ؟ |
| ز-١٠ | **أسبابُ البلاغ أربعةٌ من المحرّك** | ؟ |
| ز-١١ | **الإنذاراتُ تُقرأ قبل الحظر** | ؟ |
| ز-١٢ | **التقاريرُ بثلاثة مدىً وزرّ طباعة** | ؟ |
| ز-١٣ | **«مستحق اليوم» لا «مبيعات اليوم»** | ؟ |
| ز-١٤ | **العاجلُ محجوزٌ لطلبٍ جديدٍ وحدَه** | ؟ |
| ز-١٥ | **ساعاتُ الدوام لا تُغلق التطبيق** | ؟ |
| ز-١٦ | **نقطةٌ لم تُحدَّد تُقال صراحةً** | ؟ |
| ز-١٧ | **«اضغط الصنف لتبدّل توفره»** والضغطُ يقلبه | ؟ |
| ز-١٨ | **«تُضاف من لوحة الإدارة أو من مندوبك»** حين لا أصناف | ؟ |

**العقدُ صار ٥٩ بنداً · وكلُّها `؟` — لم يُنفَّذ الاختبارُ بعد.**
