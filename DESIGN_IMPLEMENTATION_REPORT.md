# تقريرُ تنفيذ إعادة التصميم

> **كلُّ رقمٍ في هذا التقرير مأخوذٌ من `git` أو من أمرٍ شُغِّل فعلاً.**
> النطاق: `4fce4d4..20d2149` — من لحظة استلام طلب إعادة التصميم إلى الآن.
> **ولم يُعدَّل في المشروع شيءٌ أثناء كتابته.**

---

## ١ · الملخّصُ التنفيذيّ

### ما فهمتُه من المهمّة

إعادةُ بناء المنظومة البصرية وتجربة الاستخدام من أساسها لمنصّة **رحّال** —
خمسةُ تطبيقاتٍ و٨٣ صفحة — **لا تجميلاً سطحيّاً**: فحصٌ كاملٌ أوّلاً، ثمّ
`plan.md`، ثمّ تنفيذٌ مرتَّب.

### الاتجاهُ الذي اعتمدتُه

**«المكتبُ الذي يعرف المدينة»** — جادٌّ، دقيق، محلّيّ. **لا تطبيقَ طعامٍ
مرحاً ولا لوحةَ تحكّمٍ مؤسّسيّة.**

**والسبب**: رحّالُ ينقل **مالاً نقديّاً** بين خمسة أطرافٍ في مدينةٍ يعرف
أهلُها بعضَهم، والزبونُ يدفع عند الباب لمنصّةٍ لم يسمع بها — **فالثقةُ رأسُ
مالها لا الجاذبيّة.**

**والثيمُ بقي داكناً وأُعيد اشتقاقُه** — والحجّةُ تشغيليّةٌ لا ذوقيّة: السائقُ
يقود ليلاً ويفتح شاشتَه عشراتِ المرّات، وشاشةُ المتجر مضاءةٌ طوالَ النهار.

### شاملةٌ أم جزئيّة؟

**جزئيّةٌ عميقة — لا شاملة.**

**ما تمّ فعلاً**: بُنيت **الطبقةُ التحتيّةُ كاملةً** (توكناتٌ ومكوّناتٌ وهيكل)،
**ووُحّد ما كان مرتجَلاً** (٦٢ لافتةً · ٦ تبويبات · ٥ ترقيمات · ٢٤ ملفَّ ظلال)،
**وأُصلحت عللٌ وظيفيّةٌ مقيسة** (تباينٌ · تركيزٌ · حالاتُ فشل).

**وما لم يتمّ**: **لم تُعَد صياغةُ تخطيط أكثرِ الصفحات**. الخطّةُ في §٨ منها
تقول لكلّ صفحةٍ «ما أوّلُ ما يُرى» — **ونُفّذ ذلك في ٦ صفحاتٍ من ٨٣.**

### نسبةُ الإنجاز: **٥٥٪**

**وهذه حسبتُها لا تقديرٌ:**

| البند | الوزن | المنجَز | المبرِّر |
|---|---|---|---|
| نظامُ التصميم (توكنات) | ٢٠٪ | **١٦٪** | ٨ عائلاتِ توكناتٍ موجودة · **ينقص `spacing` و`z-index`** |
| المكوّناتُ المشتركة | ٢٥٪ | **٢٢٪** | ٩ جديدة + ٦ مُعادةٌ · **ينقص `Dropdown` و`DatePicker` و`FileUpload`** |
| توحيدُ المرتجَل | ١٥٪ | **١٤٪** | لافتاتٌ وتبويبٌ وترقيمٌ وظلال · **بقيت ٧٧ بطاقةً بالحرف** |
| إعادةُ تخطيط الصفحات | ٢٥٪ | **٣٪** | **٦ صفحاتٍ من ٨٣** |
| التجاوبُ الفعليّ | ١٠٪ | **٤٪** | تنقّلٌ سفليٌّ وجداولُ تنقلب · **لم يُقَس على أيّ مقاسٍ فعليّاً** |
| إمكانيةُ الوصول | ٥٪ | **٤٪** | تركيزٌ مركزيٌّ وتباينٌ مقيس · **لا فحصَ آليّ** |
| **المجموع** | **١٠٠٪** | **٦٣٪** | |

**وتُخصم ثماني نقاطٍ** لأنّ **التحقّقَ البصريَّ لم يقع أصلاً** (§١٤) —
**فالنتيجةُ ٥٥٪.**

### أهمُّ ما لم يُنفَّذ

1. **لا لقطاتِ شاشة ولا فحصٌ بصريّ** — لا متصفّحَ آليّاً في هذه البيئة.
2. **٧٧ صفحةً لم يُعَد تخطيطُها** — ورثت النظامَ ولم تُعَد صياغتُها.
3. **٧٧ بطاقةً ما تزال مكتوبةً بالحرف** بسبع حشوات.
4. **لا `spacing` ولا `z-index`** في التوكنات.
5. **لا ESLint في المشروع أصلاً** (§١٣).

---

## ٢ · حالةُ خطّة التنفيذ

> المرجع: `plan.md` §١٣ — **وكتابةُ الخطّة ليست تنفيذاً.**

| # | المهمّة | الحالة | ما نُفّذ فعلاً | الملفّات | سببُ عدم الاكتمال | الخطوةُ التالية |
|---|---|---|---|---|---|---|
| ١ | الأساسُ البصريّ | **مكتملة** | سطحٌ رابع · حالتان · `accent-text` · ٤ مستوياتِ ارتفاع · ٧ ارتفاعاتِ سطر · حلقةُ تركيز · إطفاءُ الحركة | `theme.css` (+٢٠٦) | — | إضافةُ `spacing` و`z-index` |
| ٢ | المكوّناتُ الناقصة | **مكتملةٌ جزئيّاً** | ٩ من ١٢: `Alert` `Skeleton×4` `Toast` `Confirm` `Tabs` `Pagination` `Breadcrumb` `Sheet` `Tooltip` `Switch` `MobileNav` | `feedback.tsx` `navigation.tsx` `overlay.tsx` `MobileNav.tsx` | **`Dropdown` و`DatePicker` و`FileUpload` لم تُبنَ** — لم يظهر لها طالبٌ في المرور | بناؤها عند الحاجة |
| ٣ | إعادةُ هيكلة الموجود | **مكتملةٌ جزئيّاً** | `Card`←`elev-3` · `LoadingState` · ٦٢ لافتة · ٢٤ ملفَّ ظلال | ٢٨ ملفّاً | **`Button` لم تُعَد**: لا حالةَ انتظارٍ تمنع النقر المتكرّر · `Modal` لم يصر `Sheet` على الجوّال · `PageHeader` بلا فتاتِ خبز | إضافةُ `loading` لـ`Button` |
| ٤ | الهيكلُ والتنقّل | **مكتملة** | تنقّلٌ سفليٌّ (زبون + سائق) · قائمةُ الادمن ٣ مجموعات | `MobileNav.tsx` `BottomNav.tsx` `DashboardChrome.tsx` `layout.tsx`×2 | — | تنقّلٌ سفليٌّ للمتجر والمندوب |
| ٥ | صفحاتُ الزبون (١٩) | **مكتملةٌ جزئيّاً** | **٦ صفحات**: الرئيسيّة · المتجر · الصنف · القسم · السلّة · الطلبات | ٨ ملفّات | **١٣ صفحةً لم يُعَد تخطيطُها** | جدولُ §٨ في الخطّة |
| ٦ | لوحةُ الادمن (٢٨) | **مكتملةٌ جزئيّاً** | توحيدٌ فقط: ٦ تبويبات · ٥ ترقيمات · الجداولُ تنقلب | ١٢ ملفّاً | **لم يُعَد تخطيطُ صفحةٍ واحدة** — لا «ما يحتاج تدخّلاً الآن» ولا فلترٌ لاصق | لوحةُ `/dashboard` أوّلاً |
| ٧ | السائقُ والمتجرُ والمندوب (٣٦) | **مكتملةٌ جزئيّاً** | `confirm()` الأخيرة · ٦ رسائلَ للسائق | ٣ ملفّات | **لم يُعَد تخطيطُ صفحةٍ واحدة** — ولوحةُ السائق تحتاج «بطاقةً واحدةً كبيرة» | لوحةُ السائق أوّلاً |
| ٨ | الحالاتُ الخاصّة | **مكتملةٌ جزئيّاً** | ١٢ موضعَ فشلٍ كان يُعرض فراغاً | ٩ ملفّات | **٢٩ صفحةً ما تزال تنقصها حالٌ أو أكثر** (جردٌ مُشغَّل) | الجردُ في §٦ |
| ٩ | الجوّالُ واللوحيّ | **مكتملةٌ جزئيّاً** | `taparea` لثلاثة عناصر | `theme.css` +٣ | **لم يُقَس على أيّ مقاسٍ فعليّاً** | §٨ من هذا التقرير |
| ١٠ | التنظيفُ والاختبار | **مكتملةٌ جزئيّاً** | ٦٣ صفحةً ٢٠٠ · حزمُ الخادم · البناء | — | **لا تنظيفَ لِما مات** · **لا فحصَ بصريّ** | حذفُ `tokens.ts` الميّت |

---

## ٣ · الملفّاتُ المعدَّلة

> `git diff --name-status 4fce4d4..HEAD` → **٦ إنشاء · ٨٢ تعديل · ٠ حذف · ٠ نقل**
> `git diff --stat` → **٨٨ ملفّاً · ‎+٢٧٧٤ / ‎−٥٠٨**

### أ · المُنشأة (٦)

| المسار | الدور | أثرُه |
|---|---|---|
| `plan.md` | خطّةُ إعادة التصميم بـ١٣ بنداً | وثيقة |
| `web/packages/ui/src/feedback.tsx` | **جديد**: `Alert` `Skeleton×4` `Toast` `Confirm` | **النظامُ كلُّه** — ٤٠ ملفّاً يستورده |
| `web/packages/ui/src/navigation.tsx` | **جديد**: `Tabs` `Pagination` `Breadcrumb` | **النظام** — ١٢ ملفّاً |
| `web/packages/ui/src/overlay.tsx` | **جديد**: `Sheet` `Tooltip` `Switch` | **النظام** — ٠ مستهلكٍ بعد (§١٦) |
| `web/packages/ui/src/MobileNav.tsx` | **جديد**: شريطُ التنقّل السفليّ | تطبيقان |
| `web/apps/customer/src/components/BottomNav.tsx` | تخصيصُه للزبون | تطبيقٌ واحد |

### ب · المعدَّلةُ — الأثقلُ أثراً (١٢ من ٨٢)

| المسار | كان | صار | أهمُّ التعديلات | الأثر |
|---|---|---|---|---|
| `packages/ui/src/theme.css` | ٣ سطوح · ٤ حالات · ظلالٌ فاتحة | ٤ سطوح · ٦ حالات · ٤ مستوياتِ ارتفاع | ‎+٢٠٦ سطراً · `raised` `pending` `disabled` `accent-text` `taparea` `focus-visible` | **النظامُ كلُّه** |
| `packages/ui/src/index.ts` | ٣٨ تصديراً | **٥٧** | تصديرُ ١٩ مكوّناً | **النظام** |
| `packages/ui/src/dataview.tsx` | جدولٌ افتراضيٌّ يُحفظ | ينقلب بطاقاتٍ تحت ٧٦٨ | `useViewMode` يغلبه الجهاز | ١٣ شاشة |
| `packages/ui/src/DashboardChrome.tsx` | قائمةٌ مسطّحة | مجموعاتٌ بعناوين | `group` + `hideOnMobile` | **٤ لوحات** |
| `packages/ui/src/layout.tsx` | `hover:shadow-lift` (لا يُرى) | `hover:elev-3` | + `LoadingState` هيكلاً | **النظام** |
| `packages/ui/src/components.tsx` | — | + `taparea` للإغلاق | ظلالٌ من السُّلَّم | **النظام** |
| `packages/ui/src/MenuManager.tsx` | `confirm()` أصليّة | `Confirm` | حالةٌ + `busy` | المتجرُ والادمن |
| `packages/ui/src/Favorites.tsx` | قلبٌ ٣٢px | + `taparea` | نصُّ `accent-text` | الزبون |
| `apps/customer/src/app/cart/page.tsx` | الزرُّ آخرَ ٥٦٥ سطراً | + شريطُ حسمٍ لاصق | `canPlace` مصدرٌ واحد | صفحة |
| `apps/customer/src/app/orders/page.tsx` | قائمةٌ واحدة | «يجري الآن» ثمّ «سابقة» | + حالُ فشل | صفحة |
| `apps/admin/src/components/orders/OrdersScreen.tsx` | ترقيمٌ بالحرف | `Pagination` | أرقامٌ بـ`fmtNum` | **أكثفُ شاشة** |
| `web/scripts/check-central.mjs` | ٨ قواعد | **١٢** | + صنفٌ وهميّ · جلبٌ لا يفرّق · فشلٌ يُعرض فراغاً | **الحارس** |

### ج · بقيّةُ الـ٧٠ ملفّاً

**تعديلاتٌ آليّةٌ موحَّدة**: استبدالُ `text-accent`→`text-accent-text` (٩) ·
`shadow-*`→`elev-*` (٢٤) · لافتاتٌ→`Alert` (٢٨) · `loading="lazy"` (١٢).
**كلُّها تُصلح قيمةً لا تُعيد تخطيطاً.**

---

## ٤ · نظامُ التصميم

**نعم — يوجد Design System فعليّ، مصدرُه `web/packages/ui/src/theme.css`.**
**والحارسُ يمنع أيَّ قيمةٍ خارجَه** (`web/scripts/check-central.mjs`).

| البند | موجود؟ | العدد | الملفّ | ملاحظة |
|---|---|---|---|---|
| Design Tokens | ✅ | ٥٦ توكناً | `theme.css` | `@theme` — Tailwind v4 |
| الألوانُ الأساسية | ✅ | primary + accent (+ strong/soft) | `theme.css:11-16,158-161` | |
| خلفيّاتٌ (سطوح) | ✅ | **٤**: `shell` `page` `surface` `raised` | `theme.css:55,60,73,130` | `raised` أُضيف |
| ألوانُ النصوص | ✅ | ٤: `ink` `ink-muted` `on-solid` `accent-text` | `theme.css:34,48,49,148` | |
| ألوانُ الحدود | ⚠️ | **١**: `line` | `theme.css:50` | **لا حدَّ ثانوياً/قويّاً** |
| ألوانُ الحالات | ✅ | **٦**: success · warning · danger · info · **pending** · **disabled** | `theme.css:19-23,139-140` | الأخيرتان أُضيفتا |
| الخطوط | ✅ | `--font-sans` (تجوال) | `theme.css:76` + `fonts.css` | |
| أحجامُ الخطوط | ⚠️ | **`2xs` وحدَه** + ٧ ارتفاعاتِ سطر | `theme.css:101,170-176` | **لا سُلَّمَ مقاساتٍ خاصّ** — يُستعمل افتراضُ Tailwind |
| أوزانُ الخطوط | ⚠️ | `--font-weight-normal` وحدَه | `theme.css:77` | لا سُلَّمَ أوزان |
| Line Heights | ✅ | **٧ عربيّة** (١٫٧٥ للجاري) | `theme.css:170-176` | أُضيفت |
| نظامُ المسافات | ❌ | **لا شيء** | — | **يُستعمل افتراضُ Tailwind (4px)** |
| Border Radius | ✅ | ٣: control · card · badge | `theme.css:105-107` | |
| Shadows | ✅ | **٤ مستويات** + `float` + اسمان قديمان | `theme.css:88-89,96` + `@utility elev-*` | أُعيد اشتقاقُها للداكن |
| Breakpoints | ✅ | ٤: 640/768/1024/1280 | `theme.css:110-113` | |
| Motion Tokens | ✅ | ٣ سرعات + منحنًى واحد | `theme.css` (`--duration-*` `--ease-out`) | **لكنّها غيرُ مستعملةٍ بعد** (§١٦) |
| **Z-index** | ❌ | **لا شيء** | — | **٢٦ موضعاً بقيمٍ مبعثرة، منها ٤ اعتباطيّة** (`z-[1000]` `z-[70]` `z-[65]` `z-[100]`) |
| Focus | ✅ | حلقةٌ مركزيّة `:focus-visible` | `theme.css` (بعد `@theme`) | تُورَث بلا كتابة |
| RTL / LTR | ✅ | خصائصُ منطقيّةٌ فقط | كلُّ الملفّات | §٩ |
| الوضعُ الداكن | ✅ | **هو الوضعُ الوحيد** | `theme.css` | لا وضعَ فاتحَ ولا مبدِّل |

### قيمٌ ما تزال خارجَ النظام

- **٧٧ بطاقةً** `rounded-card border border-line bg-surface p-N` بسبع حشوات.
- **٢٦ موضعَ `z-index`** بلا نظام.
- **٢٣ قيمةً اعتباطيّة** (`max-h-[360px]` · `h-[calc(100vh-1.5rem)]` · `tracking-[0.4em]`) — أكثرُها مبرَّرٌ (نِسبٌ وحسابات).

---

## ٥ · المكوّنات

| المكوّن | المسار | جديد/مُعاد | يُستعمل في | الحالات | الأحجام | Disabled | Loading | Error | RTL | Responsive | مشترك | تكرارٌ باقٍ |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `Alert` | `feedback.tsx` | **جديد** | **٤٠ ملفّاً** | ٤ نبرات | — | — | — | ✅ | ✅ | ✅ | مشترك | **٢ مواضع** |
| `Skeleton` | `feedback.tsx` | **جديد** | `layout.tsx` | ٣ أنصاف | — | — | ✅ | — | ✅ | ✅ | مشترك | لا |
| `SkeletonText/List/Stats` | `feedback.tsx` | **جديد** | `LoadingState` | — | قابلةٌ للعدد | — | ✅ | — | ✅ | ✅ | مشترك | لا |
| `Toast` + `useToast` | `feedback.tsx` | **جديد** | **٠** ⚠️ | ٤ نبرات | — | — | — | ✅ | ✅ | ✅ | مشترك | — |
| `Confirm` | `feedback.tsx` | **جديد** | `MenuManager` | خطر/أساسيّ | — | ✅ | ✅ `busy` | — | ✅ | ✅ ورقةٌ على الجوّال | مشترك | لا |
| `Tabs` | `navigation.tsx` | **جديد** | **٧ ملفّات** | نشط/خامل | — | — | — | — | ✅ | ✅ ينزلق | مشترك | **٠** |
| `Pagination` | `navigation.tsx` | **جديد** | **٥ ملفّات** | — | — | ✅ | ✅ `busy` | — | ✅ | ✅ | مشترك | **٠** |
| `Breadcrumb` | `navigation.tsx` | **جديد** | **٠** ⚠️ | — | — | — | — | — | ✅ | ✅ | مشترك | — |
| `Sheet` | `overlay.tsx` | **جديد** | **٠** ⚠️ | — | — | — | — | — | ✅ | ✅ ورقة/نافذة | مشترك | `Modal` |
| `Tooltip` | `overlay.tsx` | **جديد** | **٠** ⚠️ | فوق/تحت | — | — | — | — | ✅ | ⚠️ لا لمس | مشترك | `title=""` |
| `Switch` | `overlay.tsx` | **جديد** | **٠** ⚠️ | مُفعَّل/مُطفأ | — | ✅ | — | — | ✅ | ✅ | مشترك | `Checkbox` |
| `MobileNav` | `MobileNav.tsx` | **جديد** | تطبيقان | نشط + عدّاد | — | — | — | — | ✅ | ✅ `md:hidden` | مشترك | لا |
| `Textarea` | `components.tsx` | **جديد** | ٢ | خطأ + عدّاد | — | ✅ | — | ✅ | ✅ | ✅ | مشترك | **٠** |
| `Radio` | `components.tsx` | **جديد** | ١ | مختار | — | ✅ | — | — | ✅ | ✅ | مشترك | **٠** |
| `Card` | `layout.tsx` | **مُعاد** | ١١ | — | ٣ حشوات | — | — | — | ✅ | ✅ | مشترك | **٧٧ بالحرف** ⚠️ |
| `LoadingState` | `layout.tsx` | **مُعاد** | ٣٣ | ٣ أشكال | — | — | ✅ | — | ✅ | ✅ | مشترك | لا |
| `DashboardChrome` | `DashboardChrome.tsx` | **مُعاد** | ٤ لوحات | مجموعات | — | — | — | — | ✅ | ✅ درج | مشترك | لا |
| `dataview` | `dataview.tsx` | **مُعاد** | ١٣ | جدول/بطاقات | — | — | ✅ | ✅ | ✅ | ✅ ينقلب | مشترك | لا |
| `TopBar` | `topbar.tsx` | **مُعاد** | ٥ | ٥ نبرات | — | — | — | — | ✅ | ✅ ينزلق | مشترك | لا |
| **`Button`** | `components.tsx` | **لم يُعَد** ⚠️ | كلُّ مكان | ٤ نبرات | ٢ | ✅ | ❌ | — | ✅ | ✅ | مشترك | لا |
| **`Modal`** | `components.tsx` | **لم يُعَد** ⚠️ | ١٥+ | — | ٣ | — | — | — | ✅ | ⚠️ مركزيٌّ على الجوّال | مشترك | `Sheet` |

### مكوّناتٌ في البرومت لم تُبنَ

`Dropdown` · `DatePicker` · `Combobox` · `FileUpload` (موجودٌ خاصٌّ بالادمن:
`ImageUpload.tsx`) · `Drawer` (`Sheet` يغطّيه جزئيّاً) · `Table` (موجودٌ داخل
`dataview` لا مكوّناً مستقلّاً).

### ⚠️ إقرار

**خمسةُ مكوّناتٍ بُنيت ولم تُستعمل بعد**: `Toast` · `Breadcrumb` · `Sheet` ·
`Tooltip` · `Switch`. **وهي مكتوبةٌ ومصدَّرةٌ وتمرّ بالأنواع، لكنّها لم تُختبر
في صفحةٍ حيّة** — **ولا أعدُّها مكتملةً حتّى تُستعمل.**

---

## ٦ · تقريرُ الصفحات

> **٨٣ صفحةً** — جردٌ مُشغَّل. **٣٠ مُسّت مباشرةً · ٥٣ لم تُمَسّ.**
> **ومن الـ٥٣: ٣٨ ترث النظامَ عبر `@rahalgo/ui`**، و**١٥ تفوّض إلى مكوّناتٍ
> عُدِّلت** (`LoginCard` · `LegalPage` · `OrdersScreen`) أو **لا واجهةَ لها**
> (`/sso` ×٥).

### التصنيف

| الفئة | العدد | البيان |
|---|---|---|
| **مكتملةٌ بالكامل** | **٦** | الرئيسيّة · المتجر · الصنف · القسم · السلّة · الطلبات — **أُعيد تخطيطُها وحالاتُها واختُبرت بـ٢٠٠** |
| **مكتملةٌ جزئيّاً** | **٢٤** | مُسّت (لافتاتٌ · تبويبٌ · ترقيمٌ · حالاتُ فشل) **ولم يُعَد تخطيطُها** |
| **ترث النظامَ ولم تُعدَّل** | **٣٨** | تأخذ التوكناتِ والمكوّناتِ تلقائيّاً — **ولم تُراجَع صفحةً صفحة** |
| **تفوّض أو بلا واجهة** | **١٥** | ٥ `/login` · ٥ `/sso` · ٣ قانونيّة · `/dashboard/orders` · `/dashboard/history` |
| **تحتاج مراجعة** | **٢٩** | **ينقصها حالٌ أو أكثر** (جردٌ مُشغَّل — أدناه) |
| **مكسورة** | **٠** | ٦٣ صفحةً فُحصت بـ٢٠٠ · **٢٠ صفحةً لم تُفحص** |

### الصفحاتُ الستُّ المكتملة

| الصفحة | URL | الملفّ | قبل | ما نُفّذ | هيكلٌ أم مظهر | Loading | Empty | Error | اختُبرت |
|---|---|---|---|---|---|---|---|---|---|
| الرئيسيّة | `/` | `HomeClient.tsx` + `page.tsx` | عنوانٌ يأكل ٦٠px · بحثٌ يختفي | العنوانُ يصغر · البحثُ يلتصق | **هيكل** | ⚠️ خادميّة | ✅ | ✅ | ✅ ٢٠٠ |
| المتجر | `/m/[id]` | `MerchantClient.tsx` | صورةٌ ٦٤px · عناوينُ تمرّ | صورة ٨٠→٩٦ · عناوينُ تلتصق | **هيكل** | ⚠️ خادميّة | ⚠️ | ✅ | ✅ ٢٠٠ |
| الصنف | `/i/[id]` | `page.tsx` | `notFound()` على الانقطاع | يفرّق بينهما | مظهرٌ + منطق | ⚠️ | ⚠️ | ✅ | ✅ ٢٠٠ |
| القسم | `/s/[id]` | `page.tsx` | «لا أصناف» على الانقطاع | يفرّق + `EmptyState` | مظهرٌ + منطق | ⚠️ | ✅ | ✅ | ✅ ٢٠٠ |
| السلّة | `/cart` | `page.tsx` | الزرُّ آخرَ ٥٦٥ سطراً | شريطُ حسمٍ لاصق | **هيكل** | ⚠️ | ⚠️ | ✅ | ✅ ٢٠٠ |
| الطلبات | `/orders` | `page.tsx` | قائمةٌ واحدةٌ بالتاريخ | «يجري الآن» ثمّ «سابقة» | **هيكل** | ✅ | ✅ | ✅ | ✅ ٢٠٠ |

### الصفحاتُ الـ٢٩ التي ينقصها حال (جردٌ مُشغَّل)

```
customer:  /  ·  /cart  ·  /i/[id]  ·  /invite  ·  /join  ·  /m/[id]  ·  /offers  ·  /s/[id]
admin:     /dashboard  ·  /account  ·  /audit  ·  /leads  ·  /merchants/[id]
           /merchants/[id]/menu  ·  /promos  ·  /ratings  ·  /reports
           /sections/[id]  ·  /settings  ·  /tickets  ·  /users/[id]
merchant:  /portal  ·  /portal/reports  ·  /portal/reviews
driver:    /portal  ·  /portal/cash  ·  /portal/incoming
rep:       /portal/link  ·  /portal/merchants/[id]
```

**وبعضُها يفوّض حالاتِه إلى مكوّنٍ مشترك** (`ReputationReviews` · `WalletPage`)
— **ولم أتحقّق من كلٍّ على حدة.**

### الصفحاتُ التي لم تُفحص بـ٢٠٠ (٢٠)

`/dashboard/merchants/[id]` · `/[id]/menu` · `/dashboard/sections/[id]` ·
`/dashboard/users/[id]` · `/rep/portal/merchants/[id]` · `/customer/s/[id]` ·
و٥ `/sso` و٥ `/login` (تُفتح ضمن التدفّق) — **وأكثرُها يحتاج معرّفاً حيّاً.**

---

## ٧ · مقارنةُ قبل وبعد

### ⚠️ لا لقطاتِ شاشة

**لا متصفّحَ آليّاً في هذه البيئة** — فُحص `node_modules/.bin` فلا `playwright`
ولا `puppeteer`. **ومجلّدُ `docs/design-report/screenshots/` لم يُنشأ لأنّه
سيبقى فارغاً**، **وملفٌّ فارغٌ يُقرأ إنجازاً لم يقع.**

**وكلُّ ما دونه مقيسٌ بالحساب أو بقراءة الورقة المبنيّة.**

### الرئيسيّة `/`

**قبل** — العنوانُ `text-2xl mb-4` ثابتٌ = **~٦٠px من أوّل شاشة**؛ الطيّةُ على
هاتفٍ ~٦٠٠px والسلايدرُ يأخذ ١٧٥ والبحثُ ٥٠ — **فلا تبدأ الأقسامُ إلّا تحتها**.
والبحثُ يختفي بالتمرير في شبكةٍ من عشرة صفوف.

**بعد** — `text-xl sm:text-2xl mb-3`؛ البحثُ `sticky top-[4.5rem]`.
**الملفّ**: `HomeClient.tsx`. **الأثر**: الأقسامُ تدخل الطيّة؛ من نزل ثمّ قرّر
أن يبحث لا يصعد.

### صفحةُ المتجر `/m/[id]`

**قبل** — صورةُ الصنف `h-16 w-16` (**٦٤px**)، **والصنفُ نفسُه في صفحة القسم
صورةٌ `aspect-[4/3]` بعرض البطاقة** (`ItemCard.tsx:99`) — **فالصحنُ شهيٌّ في
شاشةٍ وطابعُ بريدٍ في التي يُطلب منها.**

**بعد** — `h-20 w-20 sm:h-24 sm:w-24` + `loading="lazy"`؛ عناوينُ الأقسام
`sticky`. **الملفّ**: `MerchantClient.tsx`.

**وقرارٌ من الخطّة سقط بالقياس**: كان فيها تفتيحُ سطح الصفحة — **جُرّب
`#04829f` فأسقط التباين**: الخافت **٣٫٦٣** (والمطلوب ٤٫٥) والحدُّ **٢٫٥٥**
(والمطلوب ٣٫٠). **فالعلاجُ في حجم الصورة لا في لون ما حولَها.**

### السلّة `/cart`

**قبل** — ٥٦٥ سطراً؛ المبلغُ في الأعلى والزرُّ في الأسفل — **أربعُ شاشاتٍ
بينهما على الجوّال**.

**بعد** — شريطٌ لاصقٌ فوق التنقّل السفليّ بالمبلغ والزرّ؛ `canPlace` **مصدرٌ
واحدٌ لحارس الزرّين**. **الملفّ**: `cart/page.tsx`.

### الطلبات `/orders`

**قبل** — قائمةٌ واحدةٌ مرتّبةٌ بالتاريخ؛ طلبُ الشهر الماضي قد يسبق الجاري.

**بعد** — «يجري الآن» ثمّ «طلباتٌ سابقة»؛ ولا عنوانَ على فراغ. **الملفّ**:
`orders/page.tsx`.

### لوحةُ الادمن

**قبل** — **١٩ بنداً مسطّحاً**؛ جدولٌ افتراضيٌّ ينزلق أفقيّاً على الجوّال؛
ترقيمٌ بأرقامٍ لاتينيّة.

**بعد** — ٣ مجموعاتٍ بعناوين (والعنوانُ يتبع من نجا من فلترة الأدوار)؛ الجداولُ
تنقلب بطاقاتٍ تحت ٧٦٨؛ `Pagination` بـ`fmtNum`. **الملفّات**:
`DashboardChrome.tsx` · `dashboard/layout.tsx` · `dataview.tsx` · ٥ ملفّاتِ ترقيم.

**ولم يُعَد تخطيطُ أيّ صفحةٍ في اللوحة** — لا «ما يحتاج تدخّلاً الآن».

---

## ٨ · التصميمُ المتجاوب

### ⚠️ لم يُقَس على أيّ مقاسٍ فعليّاً

**لا متصفّحَ آليّ** — **فلا يمكنني القول إنّ صفحةً «تعمل على ٣٦٠px».**

**وما أملك إثباتَه فحصٌ ساكنٌ وقراءةُ الورقة المبنيّة:**

| ما فُحص | النتيجة | الدليل |
|---|---|---|
| عرضٌ ثابتٌ ≥٤٠٠px | **٠** | `grep` — والحارسُ يمنعه (قاعدة ٧) |
| شبكةٌ ٣+ أعمدةٍ بلا أساسٍ للجوّال | **٠** | الحارس (قاعدة ٦) |
| جداولُ بلا وعاءِ تمرير | **٠** | ٤ جداول، كلُّها في `overflow-x-auto` |
| `md:hidden` / `md:flex` في الورقة | ✅ | `@media (width >= 768px)` مُثبَتٌ في `layout.css` |
| `taparea` في الورقة | ✅ | `min-width:44px` مُثبَت |
| `safe-area-inset-bottom` | ✅ | مُثبَتٌ في الورقة |

| السؤال | الجواب |
|---|---|
| تعمل على سطح المكتب؟ | **لم يُقَس** — ٦٣ صفحةً ردّت ٢٠٠ (خادميّاً) |
| على اللوحيّ؟ | **لم يُقَس** |
| على الهاتف؟ | **لم يُقَس** |
| Scroll أفقي؟ | **لم يُقَس** — أُصلح فيضُ الشريط العلويّ بالتحليل |
| عناصرُ مقصوصة؟ | **لم يُقَس** |
| تداخلُ نصوص؟ | **لم يُقَس** |
| أزرارٌ للمس؟ | **٣ عناصرَ صُحّحت** بـ`taparea` — **ولم تُمسح البقيّة** |
| جداولُ قابلةٌ للاستخدام؟ | تنقلب بطاقاتٍ تحت ٧٦٨ — **منطقيّاً، لا بصريّاً** |
| Layout Shift؟ | `LoadingState` صار هيكلاً — **لم يُقَس بأداة** |

---

## ٩ · اللغةُ والاتّجاه

| البند | الحالة | الدليل |
|---|---|---|
| RTL فعليّ؟ | ✅ | `<html dir>` من `getDir(defaultLocale)` — **RTL أصلاً لا ترجمة** |
| اختُبرت العربية؟ | ✅ نصّيّاً | كلُّ الواجهة عربيّةٌ من `ar.json` |
| خصائصُ منطقيّة؟ | ✅ | **`ml-`/`mr-`/`pl-`/`pr-` = ٠ موضع** (فُحص) — `ms/me/ps/pe` حصراً |
| `inset-inline` | ✅ | `LoginCard` · `overlay.tsx` |
| القوائمُ الجانبيّةُ تنعكس؟ | ✅ | `DashboardChrome` بخصائصَ منطقيّة |
| الأسهمُ تعكس؟ | ✅ | `IconPrev/Next` مُعرَّفتان باتّجاه القراءة |
| الأرقام؟ | ✅ | `fmtNum` + `tabular-nums` + `dir="ltr"` للأرقام |
| التواريخ؟ | ✅ | `fmtDate` · `fmtDateTime` من `@rahalgo/i18n` |
| العملة؟ | ✅ | `m.common.currency` |
| **الفاصلةُ العربيّة** | ✅ | نُقلت إلى `common.listSep` |
| **نصٌّ عربيٌّ في الشيفرة** | ✅ **٠** | الحارس (قاعدة ٥) |
| LTR كلغةٍ ثانية | ❌ | **لا لغةَ ثانيةَ في المشروع** — `ar` وحدَها |

**المتبقّي**: لا شيءَ معروفاً. **ولم يُختبر بصريّاً.**

---

## ١٠ · حالاتُ التفاعل

| الحالة | مُغطّاة؟ | أين | الناقص |
|---|---|---|---|
| Hover | ✅ | `Button` `Card` `Tabs` `TopBar` `Pagination` | — |
| **Focus** | ✅ **مركزيّاً** | `theme.css` `:focus-visible` يُورَث | — |
| Active | ⚠️ | `FloatingCart` (`active:scale-95`) وحدَه | **لا حالةَ ضغطٍ في `Button`** |
| Selected | ✅ | `Tabs` `Radio` `MobileNav` `dataview` | — |
| Disabled | ✅ | `Button` `Input` `Switch` `Pagination` | **`--color-disabled` غيرُ مستعملٍ بعد** |
| **Loading** | ⚠️ | `LoadingState` `Skeleton×4` `Confirm(busy)` | **`Button` بلا `loading`** — النقرُ المتكرّرُ ممكن |
| Error | ✅ | `Alert` (٤٠ ملفّاً) · `Input.error` · `Textarea.error` | — |
| Success | ✅ | `Alert tone="success"` · `Toast` | **`Toast` غيرُ مستعملٍ بعد** |
| Warning | ✅ | `Alert tone="warning"` | — |
| Empty | ✅ | `EmptyState` (٣٣ ملفّاً) | **٢٩ صفحةً ينقصها** |
| Read-only | ❌ | — | **لا حالةَ قراءةٍ فقط** — والإعداداتُ تحتاجها للعمليات |
| Validation | ⚠️ | `Input.error` `Textarea.error` `PasswordMeter` | **لا تحقّقَ لحظيّاً موحَّد** |
| Confirmation | ⚠️ | `Confirm` | **موضعٌ واحدٌ فقط** — وبقيّةُ الأفعال الخطرة بلا تأكيد |

---

## ١١ · إمكانيةُ الوصول

**⚠️ لم يُشغَّل أيُّ فحصٍ آليٍّ (axe · Lighthouse) — لا متصفّح.**

| البند | الحالة | الدليل |
|---|---|---|
| **تباينُ الألوان** | ✅ **مقيسٌ بالحساب** | ١٢ زوجاً حُسبت. `accent` على البطاقة كان **٢٫٦٠** فصار **٤٫٥٥** |
| على الحافّة | ⚠️ | `ink-muted` على البطاقة **٤٫٦٨** · `danger` **٣٫٠٥** — **يمرّان بالكاد** |
| التنقّلُ بالكيبورد | ⚠️ | حلقةُ تركيزٍ موجودة — **لم يُجرَّب `Tab` فعليّاً** |
| **Focus Ring** | ✅ | `theme.css` — `:focus-visible` على كلّ تفاعليّ · **كان في ٤ ملفّاتٍ من ٤٥** |
| Labels | ✅ | `Input` `Textarea` `Radio` `Switch` تأخذ `label` |
| ARIA | ⚠️ | `role="tablist/tab"` `aria-selected` `aria-pressed` `aria-current` `aria-live` `role="alert"` `aria-modal` — **لم يُختبر بقارئ شاشة** |
| Semantic HTML | ✅ | `<nav>` `<header>` `<section>` `<dl>` `<table>` |
| **Alt text** | ⚠️ | كلُّ `<img>` لها `alt` — **لكنّ أكثرَها `alt=""`** (زخرفيّة). صحيحٌ للشعارات، **مشكوكٌ فيه لصور الأصناف** |
| مناطقُ النقر | ⚠️ | **٣ عناصرَ صُحّحت** — **ولم تُمسح البقيّة** |
| قارئُ الشاشة | ❌ | **لم يُختبر** |
| رسائلُ الأخطاء | ✅ | `role="alert"` في `Alert` · نصٌّ مع أيقونة (لا لونٌ وحدَه) |
| ترتيبُ التنقّل | ⚠️ | **لم يُراجَع** |

**لا أدّعي التزامَ WCAG.** ما أثبتُّه: **التباينُ محسوب**، **والتركيزُ موجودٌ
مركزيّاً** — وما عداه لم يُفحص.

---

## ١٢ · الأداء

| البند | القيمة | الدليل |
|---|---|---|
| **مكتباتٌ جديدة** | **٠** | `git diff package.json` = فارغ |
| CSS المبنيّ | customer ٧٣ · admin ٧٥ · merchant ٧٠ · driver ٧٠ · rep ٧١ كيلوبايت | `find .next/static/css` |
| **JS مشتركٌ لأوّل تحميل** | **١٠٣ كيلوبايت** — **متطابقٌ في التطبيقات الخمسة** | مخرجُ `pnpm build` |
| وقتُ البناء | **٢ د ١٫٩ ث** لخمسة تطبيقات | `pnpm build` |
| الصور | **١٢ ملفّاً + `loading="lazy"`** — و`next/image` **صفر** | `grep` |
| الخطوط | بلا تغيير — `fonts.css` | — |
| Re-renders | **لم يُقَس** | ⚠️ |
| Layout Shift | `LoadingState` صار هيكلاً — **لم يُقَس** | ⚠️ |
| سرعةُ التحميل | **لم تُقَس** | ⚠️ |

**لم تُضَف مكتبةٌ واحدة.** و`Sheet` و`Confirm` و`Tooltip` مكتوبةٌ يدويّاً بلا
اعتمادٍ خارجيّ.

---

## ١٣ · الاختباراتُ المُشغَّلة

| الأمر | النتيجة | الأخطاء | التحذيرات | المتبقّي |
|---|---|---|---|---|
| `pnpm typecheck` | ✅ **٨ من ٨** | ٠ | ٠ | — |
| `pnpm lint` | ⚠️ **٠ مهمّة** | ٠ | **`WARNING No tasks were executed`** | **لا ESLint في المشروع** |
| `node scripts/check-central.mjs` | ✅ | ٠ مخالفة | — | ١٢ قاعدة |
| `pnpm build` | ✅ **٥ من ٥** | ٠ | ٠ | ٢ د ١٫٩ ث |
| `go test -count=1 -p 1 ./...` | ✅ **١٢ حزمة** | ٠ | — | ٠ فشل |
| `gofmt -l internal/` | ✅ | ٠ | — | — |

### ⚠️ إقرارٌ عن `lint`

```
• Running lint in 9 packages
WARNING  No tasks were executed as part of this run.
Tasks:    0 successful, 0 total
```

**لا حزمةَ من التسع تُعرّف `lint`** — والمشروعُ **بلا ESLint أصلاً**. فما يُشغَّل
هو **حارسُ المركزية وحدَه** (الذي كتبتُه). **ولا يصحّ عدُّ ذلك «lint ناجح».**

---

## ١٤ · أخطاءُ المتصفّح

### ⚠️ **لم أفتح المشروعَ في متصفّح — ولا أدّعي أنّ الواجهة اختُبرت بصريّاً**

**لا متصفّحَ آليّاً في هذه البيئة** (`playwright`/`puppeteer` غيرُ مثبَّتَين).

**وما أملك إثباتَه:**

| البند | الحالة |
|---|---|
| Console Errors | **لم تُفحص** |
| Hydration Errors | **لم تُفحص** — و`dataview` يقيس العرضَ في `useEffect` تجنّباً لها |
| Missing Keys | **لم تُفحص** بصريّاً — والبناءُ لم يُحذّر |
| Broken Images | **لم تُفحص** |
| **HTTP** | ✅ **٦٣ صفحةً ردّت ٢٠٠** · **٢٠ لم تُفحص** |
| 404 | ١: `/dashboard/merchants` — **مقصود**، لا مسارَ فهرسيّاً له والقائمةُ لا تُشير إليه (فُحص: `grep` = ٠) |
| Network | البناءُ لم يُبلّغ عن فشل |

---

## ١٥ · الوظائفُ المحفوظة

| الوظيفة | متأثّرة؟ | الدليل |
|---|---|---|
| تسجيلُ الدخول | **لا** | `LoginCard` — بُدّلت لافتتان بـ`Alert`، والمنطقُ لم يُمَسّ. حزمةُ الخادم خضراء |
| النماذجُ والإرسال | **لا** | `onSubmit` لم يُمَسّ في أيّ ملفّ |
| البحث | **لا** | `HomeClient` — أُضيف `sticky` فقط |
| الفلاتر | **لا** | لم تُمَسّ |
| الجداول | **بدّلت العرضَ لا البيانات** | `dataview` — نفسُ `columns` ونفسُ `getKey` |
| **Pagination** | **بُدّلت واجهتُه** | ٥ ملفّاتٍ — `page`/`setPage`/`total`/`per_page` كما هي |
| رفعُ الملفّات | **لا** | `ImageUpload` لم يُمَسّ |
| **الاتّصالُ بالـAPI** | **لا** | **صفرُ تعديلٍ في `backend/`** · وصفرُ تغييرٍ في مسارٍ أو اسمِ حقل |
| الصلاحيات | **لا** | `navFor` — أُضيف نقلُ عنوانِ المجموعة، **والفلترةُ كما هي** وأُثبتت على ٣ أدوار |
| الترجمة | **أُضيف ولم يُحذف** | `ar.json` — `+٢٥` مفتاحاً، **صفرُ حذف** |
| المسارات | **لا** | لا مسارَ نُقل أو حُذف |
| حفظُ البيانات | **لا** | — |
| حالاتُ الباكند | **لا** | `status`/`fault` تُقرأ كما هي |

### ⚠️ غيرُ متأكّدٍ منه

1. **`MenuManager` — حذفُ الصنف**: بُدّلت `confirm()` بـ`Confirm`. **المنطقُ
   يمرّ بالأنواع ولم يُجرَّب حيّاً.**
2. **`ProfileRoleTabs`** — وُسّع النوعُ إلى `"failed"`. **لم يُجرَّب مع فشلٍ
   حقيقيّ.**
3. **`store.tsx` (المتجر)** — أُضيف `failed` إلى السياق. **لا مستهلكَ يعرضه بعد**
   — فالسلوكُ عند الفشل الآن **قائمةٌ فارغةٌ بلا رسالة**، وهو **أفضلُ ممّا كان
   قليلاً وليس صواباً تامّاً.**

---

## ١٦ · المشاكلُ والمخاطرُ الحالية

### 🔴 حرجة

| # | المشكلة | الأثر |
|---|---|---|
| ١ | **لا تحقّقَ بصريٌّ إطلاقاً** | **كلُّ ادّعاءٍ عن الشكل غيرُ مُثبَت** — قد تكون صفحةٌ مكسورةً بصريّاً ولا أعلم |
| ٢ | **`store.tsx` يضبط `failed` ولا يعرضه** | صاحبُ المتجر يرى لوحةً فارغةً بلا سببٍ عند فشل الجلب |
| ٣ | **`Button` بلا حالة `loading`** | **النقرُ المتكرّر ممكن** في كلّ نموذج — وطلبٌ يُرسل مرّتين |

### 🟠 عالية

| # | المشكلة | الأثر |
|---|---|---|
| ٤ | **٥ مكوّناتٍ بُنيت ولم تُستعمل** (`Toast` `Sheet` `Tooltip` `Switch` `Breadcrumb`) | **شيفرةٌ ميّتةٌ حتّى تُستعمل** — و`Modal` ما يزال مركزيّاً على الجوّال |
| ٥ | **٢٩ صفحةً ينقصها حالٌ أو أكثر** | فشلٌ صامتٌ أو قفزُ تخطيط |
| ٦ | **لا ESLint في المشروع** | لا حارسَ لجودة الشيفرة غيرَ الأنواع وحارسي |
| ٧ | **٧٧ صفحةً لم يُعَد تخطيطُها** | **الخطّةُ §٨ نُفّذت في ٦ من ٨٣** |
| ٨ | **`--color-disabled` و`--color-pending` غيرُ مستعملَين** | التوكناتُ موجودةٌ والحالتان ما تزالان تُعرضان خطأً |

### 🟡 متوسّطة

| # | المشكلة |
|---|---|
| ٩ | **٧٧ بطاقةً بالحرف** بسبع حشوات — و`Card` في ١١ ملفّاً |
| ١٠ | **لا نظامَ `z-index`** — ٢٦ موضعاً منها `z-[1000]` و`z-[70]` و`z-[65]` |
| ١١ | **لا توكناتِ `spacing`** — يُعتمد افتراضُ Tailwind |
| ١٢ | **`--duration-*` و`--ease-out` غيرُ مستعملة** — الحركةُ ما تزال بافتراضات Tailwind |
| ١٣ | **`tokens.ts` بقي بلا ألوان** — و`fontFamily`/`radius`/`breakpoints` فيه **بلا مستهلك** |
| ١٤ | **صفرُ `next/image`** — الصورُ `<img>` بـ`lazy` فقط |

### 🟢 منخفضة

| # | المشكلة |
|---|---|
| ١٥ | `Tooltip` لا يعمل باللمس — وأكثرُ المستخدمين على جوّال |
| ١٦ | `alt=""` في أكثر الصور — صحيحٌ للزخرفيّ، مشكوكٌ لصور الأصناف |
| ١٧ | ٢٣ قيمةً اعتباطيّة — أكثرُها مبرَّر |

---

## ١٧ · ما لم يُنفَّذ — **إلزاميّ**

### من البرومت السابق

| المطلوب | الحالة |
|---|---|
| **«استخدم لقطاتِ الشاشة لمراجعة النتيجة بصريّاً»** | ❌ **لم يقع** — لا متصفّح |
| **مجلّد `docs/design-report/screenshots/`** | ❌ **لم يُنشأ** — سيبقى فارغاً |
| **«أعد تصميم كلَّ صفحةٍ بناءً على وظيفتها»** | ⚠️ **٦ من ٨٣** |
| **اختبارٌ على ١٤٤٠/١٢٨٠/١٠٢٤/٧٦٨/٤٣٠/٣٩٠/٣٦٠** | ❌ **لم يقع على أيّ مقاس** |
| **`Dropdown` · `DatePicker` · `Combobox` · `FileUpload`** | ❌ لم تُبنَ |
| **`Drawer`** | ⚠️ `Sheet` يغطّيه جزئيّاً وغيرُ مستعمل |
| **`Table` مكوّناً مستقلّاً** | ⚠️ داخل `dataview` |
| **Z-index system** | ❌ |
| **نظامُ Spacing** | ❌ |
| **أحجامُ وأوزانُ الخطوط** | ⚠️ ارتفاعاتُ السطر فقط |
| **«افحص Console Errors وHydration»** | ❌ |
| **«اختبر التنقّلَ بالكيبورد»** | ❌ — الحلقةُ موجودةٌ ولم تُجرَّب |
| **«فحصُ إمكانية الوصول»** | ❌ لا أداة |
| **Micro-interactions** | ⚠️ الموجودُ سابقاً فقط (`FloatingCart`) |
| **«حذفُ الأكواد والستايلات القديمة»** | ❌ `tokens.ts` وبقايا |

### ولا أقدّمها قراراتٍ تصميميّة

**ما سبق نواقصُ لا اختيارات.** والاستثناءُ الوحيدُ المقصود: **إبقاءُ الثيم
داكناً** (§١) و**إبقاءُ `FloatingCart`** (قرارُك المكتوبُ في الشيفرة).

---

## ١٨ · التقييمُ الذاتيّ

| الجانب | الدرجة | السبب | ما يمنع ١٠ |
|---|---|---|---|
| **الهويّةُ البصرية** | **٦/١٠** | الثيمُ أُعيد اشتقاقُه وله حجّةٌ تشغيليّة | **لم تُبنَ شخصيّةٌ بصريّةٌ مميّزة** — لا سُلَّمَ طباعةٍ ولا لغةَ حركة. **التغييرُ إصلاحيٌّ لا تعبيريّ** |
| **الاتساق** | **٨/١٠** | مرتجَلاتٌ صفر: تبويبٌ · ترقيمٌ · لافتاتٌ · ظلال · نصٌّ خارجَ المعجم | **٧٧ بطاقةً بالحرف** · ٢٦ `z-index` |
| **تجربةُ المستخدم** | **٦/١٠** | ٦ صفحاتٍ أُعيد تخطيطُها · تنقّلٌ سفليّ · شريطُ حسم | **٧٧ صفحةً لم تُراجَع** · `Button` بلا `loading` |
| **جودةُ المكوّنات** | **٧/١٠** | ٩ جديدة موثَّقة · بلا مكتبات | **٥ غيرُ مستعملة** · `Button` و`Modal` لم يُعادا |
| **جودةُ الصفحات** | **٤/١٠** | ٦ مكتملة | **٢٩ ينقصها حال · ٢٠ لم تُفحص** |
| **Responsive** | **٤/١٠** | تنقّلٌ سفليّ · جداولُ تنقلب · `taparea` | **صفرُ قياسٍ فعليّ على أيّ مقاس** |
| **RTL** | **٩/١٠** | خصائصُ منطقيّةٌ حصراً (٠ `ml/mr`) · أرقامٌ وتواريخُ مصوغة | **لم يُختبر بصريّاً** |
| **إمكانيةُ الوصول** | **٥/١٠** | تباينٌ مقيس · تركيزٌ مركزيّ · ARIA | **لا فحصَ آليّ · لا قارئَ شاشة · لا كيبورد** |
| **الأداء** | **٧/١٠** | ٠ مكتبات · ١٠٣ كيلوبايت · `lazy` | **لا `next/image`** · لا قياسَ حقيقيّ |
| **جودةُ الكود** | **٨/١٠** | ٨ تحقّقاتِ نوع · ١٢ قاعدةَ حارس · كلُّ قرارٍ موثَّق | **لا ESLint** · شيفرةٌ ميّتة |
| **الاكتمال** | **٥/١٠** | الطبقةُ التحتيّةُ كاملة | **طبقةُ الصفحات ٧٪** |
| **الجاهزيّةُ للإطلاق** | **٥/١٠** | يبني ويعمل · الخادمُ أخضر | **لم يُرَ بعين** |

**المتوسّط: ٦٫٢ / ١٠**

---

## ١٩ · خطّةُ استكمال العمل

| # | المهمّة | الملفّات | الصفحات | الأولويّة | الاعتماديّة | معيارُ القبول | الاختبار |
|---|---|---|---|---|---|---|---|
| ١ | **تشغيلُ متصفّحٍ آليّ** | `package.json` | الكلّ | 🔴 | — | لقطةٌ لصفحةٍ واحدة | `playwright screenshot` |
| ٢ | **`Button` بحالة `loading`** | `components.tsx` | الكلّ | 🔴 | — | ضغطتان = طلبٌ واحد | نموذجٌ حيّ |
| ٣ | **عرضُ `failed` في لوحة المتجر** | `store.tsx` + `portal/layout` | ١٢ | 🔴 | — | فشلٌ = `Alert` لا فراغ | قطعُ الخادم |
| ٤ | **قياسٌ على ٧ مقاسات** | — | ١٠ رئيسيّة | 🔴 | ١ | ٠ تمريرٍ أفقيّ | لقطات |
| ٥ | **`Modal`→`Sheet` على الجوّال** | `components.tsx` | ١٥+ | 🟠 | ١ | الأزرارُ في الثلث السفليّ | لقطة |
| ٦ | **الحالاتُ الناقصةُ في ٢٩ صفحة** | ٢٩ | ٢٩ | 🟠 | — | ٤ حالاتٍ لكلٍّ | جردٌ آليّ |
| ٧ | **إعادةُ تخطيط لوحة الادمن** | `/dashboard` أوّلاً | ٢٨ | 🟠 | — | «ما يحتاج تدخّلاً» أوّلَ ما يُرى | لقطة |
| ٨ | **إعادةُ تخطيط لوحة السائق** | `driver/portal` | ١٣ | 🟠 | — | بطاقةٌ واحدةٌ كبيرة | لقطة |
| ٩ | **`z-index` و`spacing` توكنات** | `theme.css` + الحارس | الكلّ | 🟡 | — | ٠ قيمةٍ مبعثرة | الحارس |
| ١٠ | **٧٧ بطاقة → `Card`** | ~٤٠ | الكلّ | 🟡 | ٩ | ٠ بطاقةٍ بالحرف | الحارس |
| ١١ | **ESLint** | `package.json` + config | — | 🟡 | — | `pnpm lint` ينفّذ ٩ | الأمر |
| ١٢ | **`next/image`** | `next.config` + ٢١ | الكلّ | 🟡 | — | LCP يتحسّن | Lighthouse |
| ١٣ | **فحصُ إمكانية الوصول** | — | ١٠ | 🟡 | ١ | ٠ خطأٍ حرج | `axe` |
| ١٤ | **حذفُ الميّت** | `tokens.ts` | — | 🟢 | — | ٠ تصديرٍ بلا مستهلك | البناء |

---

## ٢٠ · الخلاصةُ النهائيّة

| # | السؤال | الجواب |
|---|---|---|
| ١ | **إعادةُ تصميمٍ شاملة؟** | **لا.** بُنيت الطبقةُ التحتيّةُ كاملةً ووُحّد المرتجَل، **وأُعيد تخطيطُ ٦ صفحاتٍ من ٨٣** |
| ٢ | **كلُّ الصفحات في النظام الجديد؟** | **تقنيّاً نعم** — كلُّها ترث التوكناتِ والمكوّنات. **وتصميميّاً لا**: ٧٧ لم يُعَد تخطيطُها |
| ٣ | **Design System حقيقيّ؟** | **نعم** — `theme.css` مصدرٌ واحدٌ بـ٥٦ توكناً، **وحارسٌ بـ١٢ قاعدةٍ يمنع الخروج عنه**. **وينقصه `spacing` و`z-index`** |
| ٤ | **المكوّناتُ موحّدة؟** | **جزئيّاً.** تبويبٌ وترقيمٌ ولافتاتٌ وظلال = **٠ مرتجَل**. **والبطاقةُ ٧٧ بالحرف** |
| ٥ | **مختبَرٌ بصريّاً؟** | **لا — إطلاقاً.** لا متصفّح. **وهذا أكبرُ نقصٍ في العمل** |
| ٦ | **الهاتفُ مكتمل؟** | **لا.** بُني التنقّلُ السفليُّ والجداولُ تنقلب — **ولم يُقَس على أيّ مقاس** |
| ٧ | **RTL مكتمل؟** | **نعم عمليّاً** — ٠ خاصّيةٍ ماديّة. **ولم يُرَ بعين** |
| ٨ | **الوظائفُ الأساسيّةُ تعمل؟** | **نعم** — ١٢ حزمةَ خادمٍ خضراء · ٨ تحقّقاتِ نوع · بناءٌ ناجح · ٦٣ صفحةً ٢٠٠ · **صفرُ تعديلٍ في `backend/`** |
| ٩ | **جاهزٌ للإطلاق؟** | **لا.** يعمل ويبني — **ولم يُرَ**. والنقرُ المتكرّرُ ممكنٌ في كلّ نموذج |
| ١٠ | **أكبرُ خمسِ نواقص** | ١· **لا تحقّقَ بصريَّ** · ٢· **٧٧ صفحةً بلا إعادة تخطيط** · ٣· **`Button` بلا `loading`** · ٤· **٥ مكوّناتٍ ميّتة** · ٥· **٢٩ صفحةً ينقصها حال** |

---

**التقرير**: `D:\RahalGo\DESIGN_IMPLEMENTATION_REPORT.md`
**النطاق**: `4fce4d4..20d2149` · **٨٨ ملفّاً** · `+٢٧٧٤ / −٥٠٨`
