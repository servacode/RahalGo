# **سكربتاتُ الصدفة بنهاياتِ لينكس دائماً.**
#
# (المرحلة ٦أ ٢٠٢٦-٠٨-٢١.)
#
# **وCRLF تكسر `dash` في الحاوية**: يقرأ `set -eu\r` فيردّ
# «Illegal option -». **وهو عطبٌ يبدو في السطر الأوّل ومصدرُه المحرّر.**
*.sh text eol=lf
*.env text eol=lf
Dockerfile* text eol=lf
