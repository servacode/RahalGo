# خطّةُ إعادة تصميم واجهة رحّال

> **كُتبت بعد فحصٍ كامل، وكلُّ رقمٍ فيها مقيسٌ لا مقدَّر.** ولم تُعدَّل سطراً
> من التصميم قبل كتابتها.

---

## ١ · فهمُ المشروع

**رحّال منصّةُ توصيلٍ متعدّدةُ الأصناف في الرقّة** — سوريا. ليست تطبيقَ مطاعم
وحدَه: مطاعمُ وبقالةٌ وصيدليّاتٌ ولحّامون وحلويّاتٌ وورودٌ وإلكترونيّات (١٦ تصنيفاً
في `CategoryIcon`).

**وخمسةُ تطبيقاتٍ لا واحد:**

| التطبيق | المنفذ | الصفحات | من يستعمله وأين |
|---|---|---|---|
| الزبون | ٣٠٠٣ | ١٩ | جوّالٌ في يده، شبكةٌ ضعيفة، يتصفّح ويطلب |
| الادمن | ٣٠٠١ | ٢٨ | مكتبٌ على لابتوب، ساعاتٌ متّصلة، كثافةُ بيانات |
| المتجر | ٣٠٠٢ | ١٢ | جهازٌ خلف الكاشير، **يُنظر إليه لا يُقرأ** |
| السائق | ٣٠٠٥ | ١٣ | **جوّالٌ بيدٍ واحدةٍ وهو واقفٌ في الشارع** |
| المندوب | ٣٠٠٤ | ١١ | جوّالٌ في الطريق، يزور المتاجر |

**والسياقُ التجاريّ يحكم**: منصّةٌ جديدةٌ في مدينةٍ تُبنى، **والثقةُ رأسُ مالها**
— لا الجاذبيّة. زبونٌ يدفع نقداً عند الباب لمنصّةٍ لم يسمع بها **يحتاج أن تبدو
جادّةً لا مرحة.**

**والتقنية**: Next.js 15 · React 19 · Tailwind v4 · pnpm workspaces · حزمةُ
`@rahalgo/ui` مشتركة · معجمٌ عربيٌّ واحد (`ar.json`) · RTL أصلاً لا ترجمةً.

---

## ٢ · الصفحاتُ والمسارات

**الزبون (١٩):** `/` · `/m/[id]` · `/i/[id]` · `/s/[id]` · `/cart` · `/orders` ·
`/wallet` · `/notifications` · `/account` · `/favorites` · `/offers` · `/invite` ·
`/complaints` · `/login` · `/join` · `/sso` · `/terms` · `/privacy` · `/help`

**الادمن (٢٨):** `/dashboard` · `orders` · `users` + `[id]` · `merchants` + `[id]`
+ `[id]/menu` · `sections` + `[id]` · `wallet` · `cash` · `payouts` · `claims` ·
`losses` · `tickets` · `ratings` · `incentives` · `promos` · `leads` ·
`emergencies` · `history` · `audit` · `reports` · `notifications` · `settings` ·
`account` · `login` · `sso`

**المتجر (١٢):** `portal` · `menu` · `reports` · `reviews` · `wallet` ·
`complaints` · `notifications` · `settings` · `account` · `login`

**السائق (١٣):** `portal` · `incoming` · `history` · `cash` · `incentives` ·
`reviews` · `complaints` · `notifications` · `account` · `login`

**المندوب (١١):** `portal` · `merchants` + `[id]` · `link` · `incentives` ·
`wallet` · `notifications` · `account` · `login` · `sso`

**المجموع: ٨٣ صفحة.**

---

## ٣ · جردُ المكوّنات الحالية

**٣٨ تصديراً من `@rahalgo/ui`** — وهي جيّدةٌ في أساسها:

`Button` `Input` `Textarea` `Radio` `Checkbox` `Select` `Badge` `Modal`
`FormSection` `OtpInput` `PasswordMeter` · `PageContainer` `PageHeader` `Card`
`EntityCard` `ListRow` `StatGrid` `StatCard` `EmptyState` `LoadingState`
`TabCards` `Stars` · `TopBar` `TopBarLink` `TopBarChip` `TopBarActions`
`WalletPill` `Avatar` `CountBadge` · `DashboardChrome` `CategoryIcon`
`BannerSlider` `FavoritesPage` `FavoriteButton` · `NotificationBell`
`NotificationToast` `LiveNotifications` · و٧٩ أيقونة.

**وما ينقص — وهو أهمّ من الموجود:**

| ناقص | ماذا يقع بدلاً منه اليوم |
|---|---|
| **Alert** | **٨٩ موضعاً بثمانِ صياغاتٍ مختلفة** لرسالة الخطأ نفسِها |
| **Skeleton** | `LoadingState` سطرُ نصٍّ «جارٍ التحميل» — **والصفحةُ تقفز حين تصل البيانات** |
| **Tabs** | مرتجَلٌ في **٦ ملفّات**، كلٌّ بأصنافه |
| **Pagination** | ٥٦ موضعَ ترقيمٍ بلا مكوّن |
| **Tooltip** | لا شيء — **والنصُّ المقصوصُ يُقصّ بلا بديل** |
| **Toast** | `NotificationToast` للإشعارات فقط، **ولا شيءَ لنجاح الحفظ** |
| **Switch** | `Checkbox` مكانه في الإعدادات |
| **Drawer / BottomSheet** | لا شيء — **والنوافذُ على الجوّال مركزيّةٌ ضيّقة** |
| **ConfirmDialog** | `confirm()` الأصليّ أو نوافذُ مرتجَلة |
| **Breadcrumb** | لا شيء في لوحةٍ بعمق ثلاثة مستويات |

---

## ٤ · المشكلاتُ المكتشَفة — **بالقياس لا بالرأي**

### ٤٫١ · البطاقةُ مكتوبةٌ بالحرف ٧٧ مرّة

```
26 × rounded-card border border-line bg-surface p-4
14 × … p-6      12 × … p-3      5 × … p-5
 5 × … p-10      1 × … p-8       1 × … p-7      1 × … p-1
```

**ومكوّنُ `Card` موجودٌ ويُستعمل في ١١ ملفّاً فقط.** فسبعُ حشواتٍ مختلفةٍ لبطاقةٍ
واحدة — **ولا أحدَ قرّر ذلك، إنّما تراكم.**

### ٤٫٢ · رسالةُ الخطأ ثمانيةُ أشكال

```
20 × "text-sm text-danger"
20 × "rounded-control bg-danger/10 px-3 py-2 text-sm text-danger"
13 × "text-danger"
13 × "mb-4 rounded-control bg-danger/10 …"
 8 × "text-xs text-danger"          6 × "py-10 text-center text-danger"
 5 × "mt-3 rounded-control …"       4 × "flex items-start gap-2 border …"
```

**والزبونُ يرى شكلاً في شاشةٍ وشكلاً في التالية** — فيقرأ الثانيةَ من جديد.

### ٤٫٣ · التركيزُ غائبٌ عمليّاً

**٤٥ ملفّاً فيها أزرار · وأربعةُ ملفّاتٍ فقط تُعرّف حالةَ تركيز.**

**ومن ينتقل بالكيبورد لا يرى أين هو.** وهذا شرطٌ في معايير القبول التي وضعتَها.

### ٤٫٤ · الظلالُ من لوحةٍ فاتحةٍ ماتت

```css
--shadow-card: 0 1px 3px rgb(21 43 51 / 0.04), 0 24px 64px -32px rgb(21 43 51 / 0.28);
```

**الثيمُ داكن** (`--color-shell: #00141c`) **والظلُّ أسودُ بأربعة بالمئة** —
**لا يُرى أصلاً.** بقيَ من يوم كانت اللوحةُ فاتحةً، **ولم يُعَد اشتقاقُه.**

**فلا ارتفاعَ في المنصة كلِّها**: كلُّ شيءٍ يقف على مستوًى واحد، **والبطاقةُ
تُميَّز بحدٍّ سميكٍ لا بظلّ** — ومن هنا الطابعُ الصندوقيّ.

### ٤٫٥ · البرتقاليُّ يسقط على البطاقة

| | التباين | المطلوب | |
|---|---|---|---|
| البرتقاليُّ على البطاقة | **٢٫٦٠** | ٣٫٠ | **ساقط** |
| الأزرقُ على البطاقة | ٣٫٥٢ | ٣٫٠ | سليم |
| النصُّ الخافت | ٤٫٦٨ | ٤٫٥ | **على الحافّة** |
| الخطر | ٣٫٠٥ | ٣٫٠ | **على الحافّة** |

**و`text-accent` يُستعمل في ٢٠ ملفّاً** — أسعارٌ وروابطُ وأرقام.

### ٤٫٦ · أسماءُ التوكنز تكذب

```css
--color-primary:       #6fd8f5   /* سماويٌّ فاتح */
--color-primary-dark:  #dff2fb   /* ← أفتحُ منه! */
--color-primary-light: #084e66   /* ← أدكنُ منه! */
```

**من كتب `text-primary-dark` يتوقّع داكناً فيأتيه شبهُ أبيض.** وهو انقلابٌ
مقصودٌ في ثيمٍ داكن، **لكنّ الأسماءَ لم تتبع المعنى** — فكلُّ من يقرؤها يُخطئ.

### ٤٫٧ · لا سُلَّمَ نصّيّاً للمشروع

**توكنُ مقاسٍ واحدٌ في الثيم كلِّه** (`--text-2xs`)، وما عداه من افتراضات
Tailwind. **فلا وزنَ ولا ارتفاعَ سطرٍ مقرَّرٌ للعربية** — وهي تحتاج ارتفاعَ سطرٍ
أوسعَ من اللاتينية لأنّ حروفَها تنزل وتصعد.

### ٤٫٨ · حالةُ التحميل تُقفز الصفحة

```tsx
export function LoadingState() { return <p className="py-10 text-center">جارٍ التحميل</p>; }
```

**سطرٌ واحدٌ محلَّ صفحةٍ كاملة** — فحين تصل البيانات **يقفز كلُّ شيء**
(Layout Shift). وهو ممنوعٌ صراحةً في معايير القبول.

### ٤٫٩ · لا تنقّلَ سفليّاً على الجوّال

**الزبونُ يصل أقسامَه من شريطٍ علويٍّ ينزلق أفقيّاً** (أصلحتُ فيضَه أمس).
**والإبهامُ يصل أسفلَ الشاشة لا أعلاها** — وهي قاعدةٌ فيزيائيّةٌ لا ذوق.

### ٤٫١٠ · التبويبُ مرتجَلٌ ستَّ مرّات

`merchants/[id]` · `promos` · `settings` · `users` · `users/[id]` ·
`WalletPage` — **كلٌّ بأصنافه ومقاساته.**

### ٤٫١١ · لا صورةَ محسَّنة

**١٨ ملفّاً فيها `<img>` خام · وصفرُ استعمالٍ لـ`next/image`.** وصورُ الأصناف
هي أثقلُ ما في صفحة المتجر، **وشبكةُ الرقّة ليست ليفاً.**

### ٤٫١٢ · قائمةُ الادمن ١٩ بنداً مسطَّحة

**بلا تجميع** — والمكتبُ يبحث بالعين في كلّ مرّة.

---

## ٥ · الرؤيةُ التصميميّة

### شخصيّةُ المنتج: **«المكتبُ الذي يعرف المدينة»**

**لا «تطبيقُ طعامٍ مرح» ولا «لوحةُ تحكّمٍ مؤسّسيّة».** رحّالُ منصّةٌ تنقل مالاً
حقيقيّاً بين خمسة أطراف في مدينةٍ صغيرةٍ يعرف أهلُها بعضَهم. **فالانطباعُ
المطلوب: جادٌّ، دقيق، سريع، ومحلّيّ.**

**والقرارُ الحاكم**: كلُّ عنصرٍ يقول ما هو، **ولا زخرفةَ تُفسَّر على أنّها معنًى.**

### القرارُ الأوّل والأصعب: **يبقى الثيمُ داكناً — ويُعاد اشتقاقُه**

**والحجّةُ ليست الذوق:**

- **السائقُ يقود ليلاً** ويفتح الشاشةَ عشراتِ المرّات — والأبيضُ الساطعُ يعمي.
- **المتجرُ خلف الكاشير** وشاشتُه مضاءةٌ طوالَ النهار.
- **وهويّةُ العلامة أزرقُ بتروليّ** (`docs/BRAND.md`) — **والداكنُ يخدمها.**

**لكنّه اليوم داكنٌ بالاسم فقط**: ظلالُه من لوحةٍ فاتحة، **وحدودُه سميكةٌ تعوّض
غيابَ الظلّ**، فتبدو الشاشةُ شبكةَ صناديقَ لا سطوحاً متراكبة.

**فالعملُ إعادةُ اشتقاقٍ لا تبديلُ ألوان**: سطوحٌ بأربع درجاتِ ارتفاع، وظلالٌ
تُرى على الداكن، **وحدودٌ تخفت لأنّ الارتفاع يحلّ محلَّها.**

**واستثناءٌ واحدٌ مدروس**: **صفحةُ المتجر وبطاقاتُ الأصناف تُفتَّح درجةً** —
**الطعامُ يُشترى بالعين**، وصورةٌ على أزرقَ غامقٍ تفقد شهيّتَها. سطحٌ أفتحُ خلف
الصور وحدَها، **بالهويّة نفسِها لا بخروجٍ عنها.**

### الألوان

```
الأساسُ    سماويٌّ بترولـيّ   — ما ثبت: الشريطُ والسطوحُ والحالة
البارزُ    برتقاليُّ اللوغو   — ما يتحرّك: الفعلُ الرئيسيّ وحدَه
المحايدُ   مصبوغٌ نحو الأزرق  — ورماديٌّ خالصٌ بجانب بترولـيٍّ يبدو غريباً عنه
```

**والحالاتُ ستٌّ لا أربع**: نجاح · تحذير · خطر · معلومة · **انتظار** ·
**معطَّل** — والأخيرتان ناقصتان اليوم، **وحالةُ «بانتظار المتجر» تُعرض تحذيراً
وهي ليست تحذيراً.**

**والبرتقاليُّ يُصحَّح**: يبقى تعبئةً بنصٍّ داكن (٨٫٤٩)، **وله درجةٌ نصّيّةٌ
أفتحُ للنصّ على البطاقة** — لأنّ ٢٫٦٠ لا تُقرأ.

### الخطوط

خطُّ «تجوال» يبقى — **عربيٌّ أصلاً لا لاتينيٌّ مُعرَّب.** ويُبنى له سُلَّمٌ من
ستّ درجاتٍ **بارتفاعات سطرٍ عربيّة** (١٫٧ للنصّ الجاري لا ١٫٥).

**والأرقامُ `tabular-nums` أينما اصطفّت** — المالُ يُقرأ عموداً.

### المسافات والهندسة

سُلَّمُ ٤ بكسل. **والحشوةُ تُشتقّ من دور البطاقة لا من ذوق كاتبها**: ثلاثُ
كثافاتٍ مقرَّرة (مضغوطة/عاديّة/فسيحة)، **بدل سبع.**

**ونصفُ القطر يبقى ١٦/٨** — مألوفٌ وغيرُ مبالَغ.

### الحركة

`--motion-fast: 120ms` للتفاعل · `--motion-base: 200ms` للانتقال ·
`--motion-slow: 320ms` لما يدخل ويخرج. **ومنحنًى واحدٌ** لا خمسة.

**ويحترم `prefers-reduced-motion` مركزيّاً** لا في كلّ مكوّن.

### الجوّال

**تنقّلٌ سفليٌّ للزبون والسائق** — حيث الإبهام. **والنوافذُ تصير أوراقاً تصعد
من الأسفل** (BottomSheet) لأنّ نافذةً مركزيّةً على شاشةٍ طويلةٍ تترك يدَ
المستخدم بعيدةً عن أزرارها.

---

## ٦ · هيكلُ نظام التصميم

**كلُّه في `packages/ui/src/theme.css`** — والحارسُ (`scripts/check-central.mjs`)
يمنع أيَّ قيمةٍ خارجَه.

```
اللون    ٤ سطوح (shell · page · surface · raised) + ٦ حالات + ٣ نصوص + ٢ حدود
النصّ    ٦ مقاسات × ٣ أوزان + ارتفاعاتُ سطرٍ عربيّة
المسافة  سُلَّمُ ٤px + ٣ كثافاتٍ للحشوة
الشكل    ٣ أنصاف أقطار + ٢ سماكةِ حدّ
العمق    ٤ مستوياتِ ارتفاعٍ مشتقّةٍ للداكن
الحركة   ٣ سرعات + منحنًى واحد
التركيز  حلقةٌ واحدةٌ مركزيّة تُورَث
المقاسات ٤ عتباتٍ + مقاسُ لمسٍ أدنى ٤٤px
```

**والتوكناتُ الجديدةُ تُضاف ولا تُبدَّل أسماءُ القديمة** — لأنّ ٨٣ صفحةً تقرؤها،
**وتبديلُ اسمٍ يكسر ما لا يُحصى.** ويُستثنى `primary-dark/light` **لأنّ اسمَيهما
يكذبان**، فيُضاف لهما بديلان صادقان ويُهجَر القديمُ تدريجاً.

---

## ٧ · المكوّناتُ التي تُبنى أو تُعاد هيكلتُها

**تُبنى (ناقصة):** `Alert` · `Skeleton` · `Tabs` · `Pagination` · `Tooltip` ·
`Toast` · `Switch` · `Sheet` (BottomSheet/Drawer) · `Confirm` · `Breadcrumb` ·
`MobileNav` · `Field` (غلافُ الحقل الموحَّد)

**تُعاد هيكلتُها:** `Card` (ثلاثُ كثافات) · `Button` (٣ مقاسات + حالةُ انتظار
تمنع النقر المتكرّر) · `Modal` (يصير `Sheet` على الجوّال) · `LoadingState` (يصير
هيكلاً عظميّاً) · `EmptyState` (نصٌّ + سبب + فعل) · `PageHeader` (فتاتُ خبزٍ +
أفعال) · `DashboardChrome` (قائمةٌ مجمَّعة) · `topbar` (يخفّ بعد التنقّل السفليّ)

**تبقى كما هي:** الأيقوناتُ · `CategoryIcon` · `Stars` · `OtpInput` ·
`PasswordMeter` · `BannerSlider` · `map` · `MenuManager` — **جيّدةٌ ومدروسة.**

---

## ٨ · خطّةُ تعديل الصفحات

**كلُّ صفحةٍ تُقاس بسؤالٍ واحد: ما أوّلُ ما يجب أن تراه العين؟**

| الصفحة | أوّلُ ما يُرى | الفعلُ الرئيسيّ | التخطيط |
|---|---|---|---|
| رئيسيّةُ الزبون | **المتاجرُ المفتوحةُ الآن** | ادخل متجراً | شبكةٌ + تصنيفاتٌ لاصقة |
| صفحةُ المتجر | **الطعامُ نفسُه** | أضِف للسلّة | **سطحٌ أفتح** + قائمةٌ لاصقة |
| السلّة | **المبلغُ النهائيّ** | أكّد الطلب | ملخّصٌ ثابتٌ أسفلَ الجوّال |
| طلباتي | **الطلبُ الجاري** | تتبّعْه | بطاقةٌ حيّةٌ ثمّ سجلّ |
| لوحةُ الادمن | **ما يحتاج تدخّلاً الآن** | افتح الطلب | صفٌّ من مؤشّراتٍ ثمّ طابور |
| طلباتُ الادمن | **الطابورُ نفسُه** | أسنِد/حوِّل | جدولٌ كثيفٌ + فلترٌ لاصق |
| لوحةُ السائق | **مهمّتُه الحاليّة** | الخطوةُ التالية | بطاقةٌ واحدةٌ كبيرةٌ تُضغط بإبهام |
| لوحةُ المتجر | **طلبٌ ينتظر قَبولاً** | اقبل | **أكبرُ ما في الشاشة** |
| لوحةُ المندوب | **هدفُه هذا الشهر** | ادعُ متجراً | مؤشّرٌ ثمّ قائمة |
| المحفظة | **الرصيد** | اسحب | رقمٌ كبيرٌ ثمّ كشف |
| الإعدادات | **ما بحثتَ عنه** | بدِّل | بحثٌ + مجموعاتٌ مطويّة |

**وأربعُ حالاتٍ لكلّ صفحةٍ لا واحدة**: تحميلٌ (هيكل) · فراغٌ (سبب + فعل) ·
خطأٌ (سبب + إعادة) · بيانات.

---

## ٩ · مخاطرُ التنفيذ

| الخطر | الاحتمال | الوقاية |
|---|---|---|
| **كسرُ صفحةٍ بتبديل توكن** | عالٍ | **تُضاف التوكناتُ ولا تُبدَّل** · والحارسُ يمسك الأسماءَ الميّتة |
| **٨٣ صفحةً — نفادُ العناية** | عالٍ | **تنفيذٌ على موجاتٍ يُختبر بعد كلٍّ منها** |
| كسرُ عقد الباكند | متوسّط | **لا يُمَسّ ملفٌّ في `backend/`** · وحزمةُ الاختبار بعد كلّ موجة |
| فقدُ ميزةٍ في إعادة البناء | متوسّط | **جردُ الأفعال قبل كلّ صفحةٍ وبعدَها** |
| تراجعُ الأداء | متوسّط | لا مكتبةَ جديدة · و`next/image` يخفّف |
| انقلابُ RTL | منخفض | خصائصُ منطقيّةٌ فقط (`ms/me/ps/pe`) — **قائمةٌ أصلاً** |

---

## ١٠ · خطّةُ الحفاظ على الوظائف

1. **لا تُمَسّ `backend/`** — ولا اسمُ حقلٍ ولا مسارُ نقطة.
2. **لا يُحذف نصٌّ من `ar.json`** — يُضاف إليه.
3. **قبل كلّ صفحة**: تُجرَد أفعالُها (كلُّ `onClick` و`onSubmit` ونداءِ `api`)
   **وتُقارَن بعدَها.**
4. **`pnpm typecheck` بعد كلّ موجة** — ثمانيةُ تحقّقات.
5. **حزمةُ الخادم كاملةً** بعد كلّ موجة — **لأنّ الحقيقةَ في الدفتر.**
6. **حارسُ المركزية** بعد كلّ موجة.
7. **فتحُ الصفحات الثلاثِ والثمانين حيّاً** والتأكّد من ٢٠٠ وصفرِ خطأ.

---

## ١١ · خطّةُ التجاوب

| المقاس | العرض | القرار |
|---|---|---|
| جوّال | ‎< 640 | **تنقّلٌ سفليّ** · بطاقاتٌ لا جداول · أوراقٌ تصعد · لمسٌ ≥ ٤٤px |
| لوحيّ | 640–1024 | عمودان · الشريطُ الجانبيُّ درجاً · الجداولُ تنزلق |
| لابتوب | 1024–1536 | شريطٌ جانبيٌّ ثابت · ثلاثةُ أعمدة · جداولُ كاملة |
| مكتبٌ واسع | ‎> 1536 | **سقفٌ للعرض** — والسطرُ الطويلُ لا يُقرأ |

**والجداولُ لا تُضغط على الجوّال**: تنقلب إلى بطاقاتٍ بالحقول نفسِها.

---

## ١٢ · خطّةُ الاختبار

**بعد كلّ موجة:**

```
pnpm typecheck              ٨ تحقّقات
node scripts/check-central  حارسُ المركزية
go test -count=1 -p 1 ./... حزمةُ الخادم
٦ منافذَ ٢٠٠
```

**وفي النهاية:** الصفحاتُ الثلاثُ والثمانون تُفتح · وتُقاس أوزانُ الأوراق
المبنيّة · **ويُفحص التركيزُ والتباينُ بالحساب لا بالعين.**

**ولا لقطاتِ شاشة** — لا متصفّحَ آليّاً في هذه البيئة، **والتحقّقُ نصّيٌّ كما
اتّفقنا.**

---

## ١٣ · ترتيبُ التنفيذ

| الموجة | ماذا | لماذا أوّلاً |
|---|---|---|
| **١** | **الأساسُ البصريّ** — سطوحٌ وارتفاعٌ وسُلَّمٌ نصّيٌّ وتركيزٌ وحركة | كلُّ ما بعده يقرأ منه |
| **٢** | **المكوّناتُ الناقصة** — Alert · Skeleton · Tabs · Pagination · Tooltip · Toast · Switch · Sheet · Confirm | **٨٩ موضعَ خطأٍ ينتظرها** |
| **٣** | **إعادةُ هيكلة الموجود** — Card · Button · Modal · Loading · Empty · Header | تُورَث في ٨٣ صفحة |
| **٤** | **الهيكلُ والتنقّل** — Chrome · topbar · **تنقّلٌ سفليٌّ للجوّال** | الإطارُ قبل ما فيه |
| **٥** | **صفحاتُ الزبون** (١٩) | **وجهُ المنصة** |
| **٦** | **لوحةُ الادمن** (٢٨) | أكثفُها بياناتٍ |
| **٧** | **السائقُ والمتجرُ والمندوب** (٣٦) | تشترك في الهيكل |
| **٨** | **الحالاتُ الخاصّة** — كلُّ فراغٍ وخطأٍ وتحميل | ما يُنسى دائماً |
| **٩** | **الجوّالُ واللوحيّ** — مراجعةُ كلّ مقاس | بعد استقرار المحتوى |
| **١٠** | **التنظيفُ والاختبار** — حذفُ الميّت · قياسُ الأداء | آخرُ ما يُفعل |

---

## ما نُفّذ — الموجاتُ العشرُ كلُّها

| الموجة | ما تمّ | ما أُثبت |
|---|---|---|
| **١ · الأساس** | سطحٌ رابع · حالتان ناقصتان · `accent-text` (٢٫٦٠ ← ٤٫٥٥) · شبكةُ ارتفاعٍ للداكن · سُلَّمُ سطرٍ عربيّ · حلقةُ تركيزٍ مركزيّة · احترامُ إطفاء الحركة | التوكناتُ في الورقة المبنيّة |
| **٢ · المكوّنات** | `Alert` `Skeleton×4` `Toast` `Confirm` `Tabs` `Pagination` `Sheet` `Tooltip` `Switch` `Breadcrumb` `MobileNav` | ٨ تحقّقاتِ نوع |
| **٣ · إعادةُ الهيكلة** | ٥٨ لافتةً وُحّدت · ٢٤ ملفَّ ظلالٍ من السُّلَّم · التوكنان الميّتان صارا اسمين للحيّ | المرتجَلُ الباقي: ٢ |
| **٤ · الهيكل** | تنقّلٌ سفليٌّ للزبون والسائق · قائمةُ الادمن ٣ مجموعات | `md:hidden` في الورقة · المنطقُ على ٣ أدوار |
| **٥ · الزبون** | الطيّةُ · البحثُ اللاصق · عناوينُ الأقسام · الصورةُ ٦٤←٩٦ · شريطُ حسمِ السلّة · «يجري الآن» | ١٧ صفحةً ٢٠٠ |
| **٦ · الادمن** | ٦ تبويباتٍ ← مكوّن · ٥ ترقيماتٍ ← مكوّن · الجداولُ تنقلب بطاقات | ٢١ صفحةً ٢٠٠ |
| **٧ · اللوحاتُ الثلاث** | آخرُ `confirm()` أصليّة · ٦ رسائلَ للسائق | ٢٥ صفحةً ٢٠٠ |
| **٨ · الحالات** | ١٢ موضعاً كان يعرض الفشلَ فراغاً — منها صندوقُ السائق وطلباتُ الزبون وسجلُّ التدقيق | الحارس |
| **٩ · التجاوب** | `taparea` — ٣ عناصرَ تحت ٤٤ بكسلاً | في الورقة المبنيّة |
| **١٠ · الاختبار** | ٦٣ صفحةً · ١٢ حزمةَ خادم · ٨ تحقّقاتِ نوع · الحارس | كلُّها خضراء |

## وقرارٌ من هذه الخطّة سقط بالقياس

كُتب فيها **«صفحةُ المتجر تُفتَّح درجةً — الطعامُ يُشترى بالعين»**. وجُرّب
`#04829f` **فأسقط التباين**: النصُّ الخافت ٣٫٦٣ (والمطلوب ٤٫٥) والحدُّ ٢٫٥٥
(والمطلوب ٣٫٠).

**فالعلاجُ في حجم الصورة لا في لون ما حولَها** — وهو ما نُفّذ.

## والحارسُ صار اثنتَي عشرةَ قاعدة

**وستُّ عائلاتِ خللٍ صامتٍ محروسةٌ الآن:**

	لونٌ خارجَ التوكنز · أيقونةٌ مرسومةٌ باليد · نصٌّ خارجَ المعجم
	صنفٌ يسمّي توكناً لا وجودَ له · جلبٌ لا يفرّق بين الانقطاع والفراغ
	فشلُ جلبٍ يُعرض فراغاً

**وفيه استثناءٌ يُكتب سببُه** (`@empty-ok`) لا يُطفأ الحارسُ من أجله —
**وسكوتٌ بلا سببٍ يُقرأ سهواً.**
