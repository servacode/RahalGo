# **انسخه إلى `.env` واملأه** — ولا يُلتزَم `.env` أبداً.
#
# ولا يُطبع المفتاحُ في طرفيّةٍ ولا سجلٍّ ولا تقرير.
ELEVENLABS_API_KEY=
# **وصوتُ المشروع** — واسمُه عند المالك: rahalgo2
ELEVENLABS_VOICE_ID=
# **والنموذجُ قابلٌ للتبديل** — والافتراضُ في `config.ts`.
ELEVENLABS_MODEL_ID=
